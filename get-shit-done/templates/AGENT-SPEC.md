---
phase: {N}
slug: {phase-slug}
status: draft
topology_pattern: {chain | routing | parallel | orchestrator-workers | evaluator-optimizer | autonomous | mixed}
agent_count: {N}
created: {date}
---

# Phase {N} -- Agent System Design Contract

> Architecture contract for agentic system phases. Generated by gsd-agent-researcher, verified by gsd-agent-checker. Each section captures WHAT was decided and WHY -- so failures trace back to design intent.

---

<!-- Design Decisions: researcher asks, educates on tradeoffs, user decides -->

## Agent Roster

| Agent Name | Role | Model Profile | Tools Allowed |
|------------|------|---------------|---------------|
| {agent-1}  | {one-line role description} | {quality / balanced / budget} | {comma-separated tool list} |
| {agent-2}  | {one-line role description} | {quality / balanced / budget} | {comma-separated tool list} |

### Rationale

{Why this decomposition? Why these agents and not fewer/more? What would break if you merged agents X and Y? Researcher asks: could one agent do this? What forces the split? Answer must reference concrete failure mode that decomposition prevents.}

---

## Orchestration Pattern

| Field | Value |
|-------|-------|
| Selected pattern | {chain / routing / parallel / orchestrator-workers / evaluator-optimizer / autonomous / mixed} |
| Reference | `~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md` |
| Sub-patterns used | {e.g. "evaluator-optimizer inside one orchestrator worker" or "none"} |

**Why this pattern fits:** {Map phase goal to pattern. Cite the When-to-Use row from AGENTIC-PATTERNS.md. If "mixed", name the primary pattern and which sub-patterns appear inside it.}

**What would break if a simpler pattern were used:** {Specific failure mode the simpler pattern cannot handle. If you cannot name one, the simpler pattern wins.}

### Rationale

{Researcher's challenge log: what simpler pattern was considered and rejected, and why. If the user proposed a graph for a linear pipeline, capture that conversation here so the rejection is documented.}

---

## Communication Contracts

For each agent-to-agent boundary, define the message shape and malformed-message handling.

### Contract: {sender-agent} -> {receiver-agent}

**Sender:** {agent name from roster}
**Receiver:** {agent name from roster}
**Message shape:**

```json
{
  "field_1": "string",
  "field_2": "number",
  "metadata": {
    "trace_id": "string",
    "source_step": "string"
  }
}
```

**Malformed message handling:** {What does the receiver do? Reject with error? Drop? Retry? Escalate to HITL gate? Specific behavior, not "handle gracefully".}

### Rationale

{Why this shape? Why these fields and not others? If a field is optional, when is it absent? Researcher asks: what happens if an upstream agent drops a required field -- does the downstream agent fail loud, fail silent, or recover?}

---

## Human-in-the-Loop Boundaries

| Decision Point | Gate Type (approval/escalation/notification) | Trigger Condition | Timeout Behavior |
|----------------|----------------------------------------------|-------------------|------------------|
| {what's being decided} | approval | {what causes the gate to fire} | {what happens if no human responds in time -- block, default-deny, default-approve} |
| {decision point 2} | escalation | {trigger} | {timeout behavior} |

### Rationale

{Why is HITL required at these specific points and not others? What's the cost of removing each gate -- catastrophic action with no rollback, irreversible spend, reputational risk? Researcher pushes back on gates that exist "just in case" -- each gate must justify its latency cost.}

---

## Memory Strategy

| Memory Type (short-term/persistent/retrieval) | Scope (per-agent/shared) | Storage Mechanism | Eviction Policy |
|-----------------------------------------------|---------------------------|-------------------|-----------------|
| short-term | per-agent | {in-context window / scratchpad file} | {turn count / context size limit} |
| persistent | shared | {DB table / KV store / file} | {TTL / manual / never} |
| retrieval | shared | {vector store / search index} | {reindex cadence} |

### Rationale

{Why these memory types? What state actually needs to persist across runs vs. just within a single agent's turn? Researcher asks: if agent A writes to shared memory and agent B reads it, who owns the schema? What happens when the schema changes mid-run?}

---

<!-- Cross-Cutting Concerns: researcher forces a decision, explains consequences of skipping -->

## Observability & Evaluation

### Tracing

{Tool/approach named -- e.g. "OpenTelemetry with Langfuse exporter", "Inline JSON log lines parsed by ELK", "LangSmith built-in tracing". What spans are captured: per-agent invocation, per-tool-call, per-LLM-call, per-state-transition.}

### Boundary Logging

For each agent boundary listed in Communication Contracts:

| Boundary | What gets logged | What state is tracked | Format |
|----------|------------------|------------------------|--------|
| {sender -> receiver} | {input payload, output payload, latency, token count} | {trace_id, parent_span_id, agent state snapshot} | {JSON / structured log / OTLP span} |

### Failure Diagnosis

For each agent in the roster: how to determine this agent failed, and what to look at first.

| Agent | Failure detection signal | Where to look | Common root causes |
|-------|--------------------------|---------------|-------------------|
| {agent-1} | {explicit error / unexpected output shape / timeout / silent wrong answer} | {trace span name / log file / metric} | {tool permission denied, malformed input, model rate limit, prompt drift} |

### Rationale

{Why these specific observability decisions? What pain point in past systems forced these choices? Researcher captures the user's debugging story: "last time I built X, debugging took N hours because Y was not logged" -- and ensures Y is logged here. If observability section says "standard logging", checker MUST flag it.}

---

## Reflection & Self-Correction

| Field | Value |
|-------|-------|
| What triggers reflection | {agent output below threshold / explicit user feedback / scheduled review / never} |
| Metrics checked | {output schema validity / answer quality score / tool-use correctness} |
| What happens on failure | {retry with modified prompt / escalate to HITL / fall back to simpler agent / abort} |
| Iteration limit | {max N retries before escalation -- prevents infinite loops} |
| Tied to evaluation criteria | {reference Test Contracts that define quality thresholds} |

### Rationale

{Why does the system need (or not need) self-correction here? Researcher asks: is the failure mode "wrong but plausible" (needs reflection) or "wrong and obvious" (needs guardrails, not reflection)? If reflection is enabled, what stops the loop from spinning forever?}

---

## Security & Permission Boundaries

| Agent | Allowed Tools | Denied Tools | Scope Constraints |
|-------|---------------|--------------|-------------------|
| {agent-1} | {explicit tool list} | {tools agent must NEVER call -- e.g. "shell exec", "DB write", "external API"} | {file paths, API endpoints, data classes the agent may touch} |

**Prompt injection defense approach:** {Input sanitization layer / instruction hierarchy enforcement / sandboxed tool execution / isolation by separate session / "we trust the input" -- and why that's safe.}

**Input validation strategy:** {Where are inputs validated -- agent boundary, tool boundary, both? What gets rejected vs. quarantined vs. logged-only?}

### Rationale

{Why these tool restrictions per agent? Principle of least privilege -- each agent gets only what it needs. Researcher asks: if this agent were prompt-injected by a malicious input, what's the worst it could do? The deny list and scope constraints exist to bound that worst case.}

---

## Error Handling & Failure Modes

| Failure Scenario | Affected Agent(s) | Detection Method | Recovery Strategy | Fallback Behavior |
|------------------|--------------------|------------------|-------------------|-------------------|
| {LLM API rate limit} | {all calling agents} | {429 response / latency spike} | {exponential backoff with jitter} | {queue and retry / fail open / fail closed} |
| {malformed agent output} | {downstream consumers} | {schema validation} | {one retry with corrective prompt} | {escalate to HITL gate} |
| {tool unavailable / permission denied} | {agent attempting call} | {tool returns error} | {try alternate tool if listed / log and continue / abort} | {return partial result with degraded notice} |

### Rationale

{Why these specific failure scenarios and not others? Researcher walks through "what's the worst that can happen at each agent boundary" and forces a recovery decision. Untreated failure modes are technical debt -- if you skip a row here, document why the failure cannot occur or is acceptable.}

---

<!-- Lightweight Capture: researcher asks briefly, planner has implementation flexibility -->

## Reasoning & Planning Approach

- {How agents decompose tasks: pre-planned chain / dynamic plan-then-act / ReAct loop / chain-of-thought inline / structured output schema}
- {How sequencing decisions are made: hardcoded order / orchestrator decides / each agent decides next step}
- {Whether intermediate plans are exposed for inspection / overridable by HITL}
- {Any explicit prompting techniques relied on: few-shot examples, system prompt structure, tool-use format}
- {Anything the planner should know but that does not require strong upfront commitment}

---

## Test Contracts

Test contracts use the same scenario/observable/pass-criteria structure as TEST-SPEC.md, scoped to agent boundaries. Each contract verifies what one agent does given a specific input.

### Contract: {AgentName} -- {behavior description}

| Field | Value |
|-------|-------|
| Agent | {agent name from roster} |
| Input | {message type / tool call / state shape} |
| Preconditions | {system state before agent runs -- memory contents, prior agent outputs, permission flags set} |

**Action:** {exact input payload or trigger -- the literal message or function call that drives the agent}

**Observables:**
- Output matches shape: {schema or example}
- Tool {X} is NOT called unless permission {Y} is present
- State field {Z} is updated with value matching {pattern}
- Boundary log entry is written to {trace store} with fields {list}
- Latency under {N}ms / token count under {N}

**Pass criteria:** all observables true
**Failure mode covered:** {reference to row in Error Handling section, or "no -- add row to Error Handling"}

---

## Checker Sign-Off

| # | Dimension | Status | Notes |
|---|-----------|--------|-------|
| 1 | Agent Roster | {PASS/FLAG/FAIL} | {what specifically was checked} |
| 2 | Orchestration Pattern | {PASS/FLAG/FAIL} | {pattern named, simpler-pattern challenge documented} |
| 3 | Communication Contracts | {PASS/FLAG/FAIL} | {every roster agent has at least one contract, malformed-message behavior defined} |
| 4 | Human-in-the-Loop | {PASS/FLAG/FAIL} | {gates have triggers and timeout behavior, or HITL explicitly N/A} |
| 5 | Memory Strategy | {PASS/FLAG/FAIL} | {memory types named, scope and eviction defined} |
| 6 | Observability | {PASS/FLAG/FAIL} | {tracing tool named, boundary logs defined, failure diagnosis path stated -- not "TBD" or "standard logging"} |
| 7 | Reflection | {PASS/FLAG/FAIL} | {triggers and iteration limit defined, or reflection explicitly N/A with reason} |
| 8 | Security/Permissions | {PASS/FLAG/FAIL} | {per-agent tool allow/deny lists, prompt-injection defense named} |
| 9 | Error Handling | {PASS/FLAG/FAIL} | {at least one row per agent boundary with detection + recovery} |
| 10 | Reasoning Approach | {PASS/FLAG/FAIL} | {brief notes present -- lightweight capture is acceptable here} |

**Approval:** {approved YYYY-MM-DD / blocked -- see issues above}
