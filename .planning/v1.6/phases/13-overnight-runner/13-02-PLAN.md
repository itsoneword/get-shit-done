---
phase: 13-overnight-runner
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/autonomous.md
  - commands/gsd2/autonomous.md
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "Skill(gsd2:autonomous, '--phase N') runs exactly one phase and ends its final message with a greppable 'PHASE RESULT: <completed|parked|failed> phase=N ...' line — the runner's outcome contract"
    - "Under GSD_RUN_ID, the discuss step delegates to Skill(gsd2:discuss-phase, 'N --auto') so the Phase 11 evaluator and Phase 12 park branch actually fire during overnight runs; a PHASE PARKED return bubbles up as PHASE RESULT: parked"
    - "Under GSD_RUN_ID, no AskUserQuestion pause survives: human_needed routes to a mailbox question and continues; unresolved gaps and blockers route to PHASE RESULT: failed — never a hang, never a silent success"
    - "Interactive sessions (no GSD_RUN_ID, no --phase) behave exactly as today — smart_discuss, AskUserQuestion routing, lifecycle all unchanged"
  artifacts:
    - "get-shit-done/workflows/autonomous.md (--phase selector, harness-mode branches, PHASE RESULT contract, adaptive-depth prompt alignment at :255/:276)"
    - "commands/gsd2/autonomous.md (argument-hint covering --phase)"
  key_links:
    - "autonomous.md harness discuss branch -> Skill(gsd2:discuss-phase, '--auto') (broken = evaluator/park machinery never fires overnight, ESC-02/PARK-01 dead in harness runs)"
    - "autonomous.md PHASE RESULT line -> overnight.md outcome detection (broken = runner records every phase as ambiguous/failed)"
    - "autonomous.md harness mailbox appends -> gsd-tools mailbox append (broken = deferred verifications invisible in the morning inbox)"
---

<objective>
Make `autonomous.md` drivable by the overnight runner: a `--phase N` single-phase selector with a machine-greppable outcome line, harness-mode (GSD_RUN_ID set) delegation of the discuss step to `discuss-phase --auto` so the shipped Phase 11/12 evaluator+park machinery fires, and harness-mode replacements for every AskUserQuestion pause (park-don't-block applied to autonomous's own asks). Also lands the locked Phase 10 carry-over: align lines ~255/~276 with the adaptive triage-first depth style.

Why: today autonomous.md's smart_discuss is an inline batch-table flow with no evaluator and no park branch — under an overnight run, parking would never fire and AskUserQuestion would stall an unattended session. RUN-01 ("runs remaining phases unattended") is impossible without these changes. The AGENT-SPEC contract locks the boundary: overnight wraps autonomous via Skill() per phase; the phase loop is NOT rewritten inline — these are surgical additions gated on GSD_RUN_ID / --phase, leaving interactive behavior byte-equivalent.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6/phases/13-overnight-runner/13-CONTEXT.md
@.planning/v1.6/phases/13-overnight-runner/13-AGENT-SPEC.md
@get-shit-done/workflows/autonomous.md
@commands/gsd2/autonomous.md

<interfaces>
Facts verified by direct read — the executor builds against these, not assumptions:

- autonomous.md structure: steps `initialize` (parses --from), `discover_phases` (roadmap analyze, filter incomplete), `execute_phase` (3a Smart Discuss / 3b Plan / 3c Execute / 3d Post-Execution Routing), `smart_discuss` (inline sub-steps 1-5), `iterate`, `lifecycle`, `handle_blocker` (AskUserQuestion with Fix/Skip/Stop).
- Line ~255 (sub-step 3 of smart_discuss): "Skip grey areas already decided in prior phases. Generate 3-4 grey areas with ~4 questions each." Line ~276: "**\"Discuss deeper\"**: ... After 4 questions, offer \"More questions\" / \"Next area\"."
- discuss-phase.md `--auto` mode (shipped Phase 11/12): auto-selects choices, runs the escalation evaluator when GSD_RUN_ID is set, and on a park-and-ask verdict under `--auto` + GSD_RUN_ID writes mailbox (pending) + park snapshot and returns a `## PHASE PARKED` block. It also AUTO-ADVANCES: on success it invokes `Skill(skill="gsd2:plan-phase", args="${PHASE} --auto")` which chains onward; for detected_domain ∈ {UI, Agentic, UI+Agentic} it PAUSES the chain with a message containing "design contract is required before planning".
- Mailbox CLI (run-id from GSD_RUN_ID env): `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" mailbox append --data '{"question":"...","phase":N,"context":"...","status":"pending"}'` — prints q-NNN; exits 1 on bad input or uninitialized run.
- Outcome detection contract (AGENT-SPEC): runner derives parked from the PHASE PARKED sentinel, failed from failure signals, completed otherwise; ambiguous → failed. The PHASE RESULT line this plan adds makes that detection exact instead of heuristic.
- commands/gsd2/autonomous.md frontmatter: `argument-hint: "[--from N]"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: --phase selector + harness-mode discuss delegation + PHASE RESULT contract</name>
  <files>get-shit-done/workflows/autonomous.md</files>
  <read_first>
    - get-shit-done/workflows/autonomous.md (full file — the steps being modified and the exact bash idioms used for flag parsing)
    - get-shit-done/workflows/discuss-phase.md lines 690-740 (auto-advance chain behavior and the design-contract pause message — what the harness branch must detect)
    - .planning/v1.6/phases/13-overnight-runner/13-AGENT-SPEC.md "Communication Contracts" (overnight-runner -> autonomous-loop env/args contract; outcome record statuses completed|parked|failed)
  </read_first>
  <action>
Edit get-shit-done/workflows/autonomous.md. All additions are gated — interactive multi-phase behavior stays byte-equivalent. Three changes:

**1. `initialize` step — parse `--phase N` and detect harness mode.** After the existing `--from` parsing block, add:

```bash
SINGLE_PHASE=""
if echo "$ARGUMENTS" | grep -qE '\-\-phase\s+[0-9]'; then
  SINGLE_PHASE=$(echo "$ARGUMENTS" | grep -oE '\-\-phase\s+[0-9]+\.?[0-9]*' | awk '{print $2}')
fi
HARNESS_MODE=""
if [ -n "$GSD_RUN_ID" ]; then HARNESS_MODE="true"; fi
```

Add prose after the banner: "If `SINGLE_PHASE` is set, display `Single-phase mode: phase ${SINGLE_PHASE}`. If `HARNESS_MODE` is true, display `Harness mode: run ${GSD_RUN_ID} — non-interactive routing active`."

**2. `discover_phases` step — single-phase filter.** After the existing incomplete-phase filtering prose, add:

"**Single-phase mode (`SINGLE_PHASE` set):** restrict the phase list to exactly that phase number. If the phase is not in ROADMAP.md: end immediately with final line `PHASE RESULT: failed phase=${SINGLE_PHASE} reason=not-found`. If the phase is already complete (disk_status complete AND roadmap_complete): end immediately with final line `PHASE RESULT: completed phase=${SINGLE_PHASE} reason=already-complete`. In single-phase mode the lifecycle step (audit/complete/cleanup) is NEVER entered — after the one phase finishes, emit the PHASE RESULT line (see change 3) and stop."

**3. `execute_phase` step 3a — harness-mode discuss branch.** Replace the current 3a body ("If `has_context` true ... If false: Execute the smart_discuss step ...") with a mode split:

- "If `has_context` true: Display `Phase ${PHASE_NUM}: Context exists — skipping discuss.` Proceed to 3b."
- "If false AND `HARNESS_MODE` is not true: Execute the smart_discuss step (unchanged interactive path). After completion, re-fetch `PHASE_STATE` and verify `has_context`. If still false: handle_blocker."
- "If false AND `HARNESS_MODE` is true: do NOT run smart_discuss. Invoke the real discuss skill so the escalation evaluator (Phase 11) and park branch (Phase 12) fire:

  ```
  Skill(skill="gsd2:discuss-phase", args="${PHASE_NUM} --auto")
  ```

  Route on the skill's return:
  - Output contains `PHASE PARKED` → the phase is parked (mailbox question + snapshot were already written by discuss-phase). Skip 3b/3c/3d entirely; the phase outcome is `parked` — capture the q-NNN id from the PHASE PARKED block for the PHASE RESULT line.
  - Output contains `design contract is required` (UI/Agentic auto-chain pause) → append a mailbox question so it surfaces in the morning inbox:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" mailbox append --data '{"question":"Phase ${PHASE_NUM} needs an interactive design contract (ui-phase/agent-spec-phase) before planning — run it, then rerun the phase","phase":${PHASE_NUM},"context":"autonomous harness mode: discuss-phase --auto paused its chain at the design-contract gate","status":"pending"}'
    ```

    The phase outcome is `failed` with reason `needs-design-contract`. Skip 3b/3c/3d.
  - Otherwise → discuss succeeded. Note: discuss-phase --auto auto-advances (it may have already chained plan-phase and even execute-phase). Re-fetch `PHASE_STATE` via `init phase-op` before each subsequent sub-step and SKIP what the chain already did: run 3b only if `has_plans` is false; run 3c only if no `*-VERIFICATION.md` exists in `${PHASE_DIR}`; always run 3d."

**4. End-of-phase outcome contract.** Add to the end of the `execute_phase` step (after 3d) and to the `iterate` step:

"**Single-phase mode:** after the phase resolves (via 3d routing, a parked discuss, or a failure), do not iterate. End the response with the outcome line as the FINAL line of output, exactly one of:

```
PHASE RESULT: completed phase=${PHASE_NUM}
PHASE RESULT: completed phase=${PHASE_NUM} deferred_verification=q-NNN
PHASE RESULT: parked phase=${PHASE_NUM} q=q-NNN
PHASE RESULT: failed phase=${PHASE_NUM} reason=<short-kebab-reason>
```

This line is the runner's outcome contract (AGENT-SPEC: ambiguous outcomes are treated as failed by the caller) — it must be machine-greppable: `^PHASE RESULT: (completed|parked|failed) phase=`. Multi-phase mode (no `--phase`) does not emit it."

Add to `<success_criteria>` of the workflow: "- Single-phase mode (--phase N) runs exactly one phase, never enters lifecycle, and ends with exactly one PHASE RESULT line" and "- Harness mode (GSD_RUN_ID set) delegates discuss to discuss-phase --auto and never calls smart_discuss".
  </action>
  <verify>grep -n "SINGLE_PHASE\|HARNESS_MODE\|PHASE RESULT:" get-shit-done/workflows/autonomous.md | head -20 && grep -c "PHASE RESULT:" get-shit-done/workflows/autonomous.md && grep -n 'discuss-phase", args="${PHASE_NUM} --auto"' get-shit-done/workflows/autonomous.md</verify>
  <acceptance_criteria>
    - autonomous.md contains the `SINGLE_PHASE` and `HARNESS_MODE` parsing blocks (grep matches both variable names)
    - autonomous.md contains `Skill(skill="gsd2:discuss-phase", args="${PHASE_NUM} --auto")` inside a HARNESS_MODE-gated branch of 3a, and the interactive path still contains "Execute the smart_discuss step"
    - autonomous.md contains all four PHASE RESULT line variants: `PHASE RESULT: completed`, `PHASE RESULT: parked`, `PHASE RESULT: failed`, and the regex/greppable contract statement `^PHASE RESULT: (completed|parked|failed) phase=`
    - autonomous.md contains the design-contract mailbox append (`needs an interactive design contract` and `mailbox append`)
    - Single-phase lifecycle exclusion stated: grep -q "lifecycle step (audit/complete/cleanup) is NEVER entered" get-shit-done/workflows/autonomous.md (or equivalent exact sentence as written above)
    - The smart_discuss step body itself is unmodified by this task (git diff shows no hunk inside the smart_discuss step for this task's commit)
  </acceptance_criteria>
  <done>The runner can invoke one phase at a time and read an exact outcome; overnight runs go through discuss-phase --auto so parking and the ledger evaluator actually fire.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: harness-mode non-interactive routing for 3d and handle_blocker</name>
  <files>get-shit-done/workflows/autonomous.md</files>
  <read_first>
    - get-shit-done/workflows/autonomous.md (post-Task-1 state — step 3d routing and handle_blocker as they now read)
    - .planning/v1.6/phases/13-overnight-runner/13-CONTEXT.md decisions section (skip-to-independent posture; fail-safe direction "treat as failed, never assume completed")
  </read_first>
  <action>
Edit get-shit-done/workflows/autonomous.md — add HARNESS_MODE branches to the two places that currently AskUserQuestion mid-run. Interactive behavior (no GSD_RUN_ID) unchanged.

**1. Step 3d Post-Execution Routing.** For each status, prepend a harness branch before the existing interactive prose:

- **Empty** (no VERIFICATION.md/status): "HARNESS_MODE: phase outcome is `failed` with reason `no-verification` — do not AskUserQuestion. Interactive: handle_blocker as today."
- **`passed`**: unchanged (no ask exists today).
- **`human_needed`**: "HARNESS_MODE: do not ask. Append the deferred verification to the mailbox so it surfaces in the morning inbox:

  ```bash
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" mailbox append --data '{"question":"Phase ${PHASE_NUM} completed but needs human verification: <one-line summary of the human_verification items from VERIFICATION.md>","phase":${PHASE_NUM},"context":"autonomous harness mode: verification status human_needed — items listed in ${PHASE_DIR}/<file>-VERIFICATION.md","status":"pending"}'
  ```

  Capture the printed q-NNN. Phase outcome is `completed` with `deferred_verification=q-NNN` on the PHASE RESULT line. Display `Phase ${PHASE_NUM} > Human validation deferred to inbox (q-NNN)`. Interactive: existing AskUserQuestion flow unchanged."
- **`gaps_found`**: "HARNESS_MODE: do not ask. Run gap closure exactly once automatically (the existing 1-attempt limit applies):

  ```
  Skill(skill="gsd2:plan-phase", args="${PHASE_NUM} --gaps")
  Skill(skill="gsd2:execute-phase", args="${PHASE_NUM} --no-transition")
  ```

  Re-read VERIFY_STATUS. `passed` → outcome `completed`. `human_needed` → route per the harness human_needed branch above. Still `gaps_found` (or empty) → outcome `failed` with reason `gaps_found` (fail-safe direction: a wrongly-failed phase is reviewable in the morning; a wrongly-completed phase poisons downstream merges). Interactive: existing AskUserQuestion flow unchanged."

**2. handle_blocker step.** Prepend: "**HARNESS_MODE: never AskUserQuestion.** Log the blocker description in the response output. In single-phase mode: the phase outcome is `failed` with a short kebab-case reason derived from the blocker description (e.g. `reason=no-plans-produced`) — emit the PHASE RESULT line and stop. In multi-phase harness mode: treat as the existing 'Skip this phase' branch (log `Phase {N} > {Name} — Skipped (harness): {description}`) and proceed to iterate. Interactive sessions: the existing 3-option AskUserQuestion, unchanged."

Add to `<success_criteria>`: "- Harness mode never invokes AskUserQuestion anywhere in the workflow — every former pause routes to mailbox + continue or to a failed outcome".
  </action>
  <verify>grep -n "HARNESS_MODE" get-shit-done/workflows/autonomous.md | wc -l && grep -n "deferred_verification\|reason=gaps_found\|never AskUserQuestion" get-shit-done/workflows/autonomous.md</verify>
  <acceptance_criteria>
    - Step 3d contains harness branches for empty/human_needed/gaps_found: grep matches `reason=no-verification` (or `no-verification`), `needs human verification` + `mailbox append`, and `reason=gaps_found` in autonomous.md
    - handle_blocker contains the sentence fragment `never AskUserQuestion` inside a HARNESS_MODE-gated paragraph
    - The interactive AskUserQuestion options ("Validate now", "Run gap closure", "Fix and retry") all still present in the file (grep each string) — interactive flow preserved
    - Gap closure in harness mode is bounded to one attempt (the prose says "exactly once" and reuses the existing 1-attempt limit)
  </acceptance_criteria>
  <done>An unattended run can traverse every autonomous.md decision point without a human: verifications defer to the inbox, unresolved gaps fail loudly, blockers never hang.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: adaptive-depth prompt alignment (:255/:276) + command stub argument-hint</name>
  <files>get-shit-done/workflows/autonomous.md, commands/gsd2/autonomous.md</files>
  <read_first>
    - get-shit-done/workflows/autonomous.md smart_discuss sub-steps 3 and 4 (the two fixed-count sentences being replaced — locate by content, line numbers have shifted after Tasks 1-2)
    - commands/gsd2/autonomous.md (frontmatter shape)
  </read_first>
  <action>
**1. get-shit-done/workflows/autonomous.md — two surgical sentence replacements** (the locked Phase 10 carry-over: remove the fixed-question-count style; commit 4b21af1 removed it from discuss-phase):

Replace (sub-step 3, currently "Generate 3-4 grey areas with ~4 questions each."):

OLD: `Skip grey areas already decided in prior phases. Generate 3-4 grey areas with ~4 questions each. For each question: pre-select recommended answer (based on prior decisions, codebase patterns, domain conventions, ROADMAP criteria), generate 1-2 alternatives, annotate with prior decision/code context where relevant.`

NEW: `Skip grey areas already decided in prior phases. Triage first, then go only as deep as the phase demands: surface only the genuinely undecided areas (no fixed area count) and, within each, only the questions whose answer actually changes implementation (no fixed question count). A clear-cut phase may yield one area with one question; an ambiguous one may yield several. For each question: pre-select recommended answer (based on prior decisions, codebase patterns, domain conventions, ROADMAP criteria), generate 1-2 alternatives, annotate with prior decision/code context where relevant.`

Replace (sub-step 4, "Discuss deeper" bullet):

OLD: `**"Discuss deeper"**: Switch to interactive mode for this area -- ask questions one-at-a-time with 2-3 options + "You decide". After 4 questions, offer "More questions" / "Next area". On "Next area", show final summary.`

NEW: `**"Discuss deeper"**: Switch to interactive mode for this area -- ask questions one-at-a-time with 2-3 options + "You decide". Continue while genuinely undecided, implementation-changing questions remain; once what's left is low-stakes, offer "More questions" / "Next area". On "Next area", show final summary.`

Zero other changes to smart_discuss.

**2. commands/gsd2/autonomous.md — frontmatter only:**

Change `argument-hint: "[--from N]"` to `argument-hint: "[--from N] [--phase N]"`.

In the `<context>` block, after the existing `--from N` line, add: `Optional flag: \`--phase N\` — run exactly one phase and end with a \`PHASE RESULT:\` outcome line (used by the overnight runner; usable manually too).`
  </action>
  <verify>! grep -q "3-4 grey areas" get-shit-done/workflows/autonomous.md && ! grep -q "After 4 questions" get-shit-done/workflows/autonomous.md && grep -q "Triage first" get-shit-done/workflows/autonomous.md && grep -q -- "--phase N" commands/gsd2/autonomous.md</verify>
  <acceptance_criteria>
    - get-shit-done/workflows/autonomous.md contains `Triage first, then go only as deep as the phase demands` and `Continue while genuinely undecided`
    - Neither `3-4 grey areas` nor `After 4 questions` appears anywhere in get-shit-done/workflows/autonomous.md
    - commands/gsd2/autonomous.md frontmatter argument-hint is `"[--from N] [--phase N]"` and the context block mentions `PHASE RESULT:`
  </acceptance_criteria>
  <done>The fixed-question-count style is gone from autonomous.md (Phase 10 locked carry-over closed) and /gsd2:autonomous advertises the single-phase selector.</done>
</task>

</tasks>

<verification>
- Interactive invariant: with no GSD_RUN_ID and no --phase, every original step (smart_discuss, 3d AskUserQuestion routing, handle_blocker options, lifecycle) is reachable and textually intact.
- Harness invariant: with GSD_RUN_ID set, the file contains zero paths that reach AskUserQuestion — verify by reading each HARNESS_MODE branch end-to-end.
- Outcome contract: all four PHASE RESULT variants documented; single-phase mode excludes lifecycle.
- `npm test` green (prose-only change — sanity check that no test greps autonomous.md's old text; if one does, update it to the new adaptive-depth text in the same commit).
</verification>

<success_criteria>
- RUN-01 (autonomous half): autonomous.md is unattended-capable under GSD_RUN_ID — discuss goes through the evaluator/park machinery, every human pause is replaced by mailbox routing or a failed outcome, and the runner gets an exact per-phase outcome via the PHASE RESULT contract.
- Phase 10 locked carry-over (prompt alignment at :255/:276) shipped.
</success_criteria>

<output>
After completion, create `.planning/v1.6/phases/13-overnight-runner/13-02-SUMMARY.md`
</output>
