---
phase: 12-park-don-t-block-mailbox
plan: "02"
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - tests/mailbox.test.cjs
  - get-shit-done/bin/lib/mailbox.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements: [PARK-02]
must_haves:
  truths:
    - "gsd-tools mailbox answer <run-id> --id q-NNN --answer <text> sets status=answered, answer text, and an ISO answered_ts on exactly that record, leaving all others byte-identical"
    - "gsd-tools mailbox review <run-id> walks every unanswered question in one session, printing question + context + options + evidence, reading the answer from stdin"
    - "Answering a question whose phase has a parked/phase-{N}.json snapshot prints a resume handoff with the resume instruction and staleness state"
    - "The mailbox append path remains appendFileSync-only — only the answer/review path does a full-file rewrite"
  artifacts:
    - "get-shit-done/bin/lib/mailbox.cjs (writeMailbox + cmdMailboxAnswer + cmdMailboxReview + printResumeHandoff)"
    - "tests/mailbox.test.cjs (extended with answer/review cases)"
    - "get-shit-done/bin/gsd-tools.cjs (mailbox answer/review dispatch)"
  key_links:
    - "gsd-tools.cjs mailbox dispatch -> cmdMailboxAnswer/cmdMailboxReview (broken = inbox CLI unreachable)"
    - "cmdMailboxReview/cmdMailboxAnswer -> park.cjs checkStaleness via printResumeHandoff (broken = handoff printed without staleness, PARK-03 visibility lost)"
    - "answer path -> writeMailbox full rewrite, append path -> appendFileSync (inverted = concurrent-park corruption, Pitfall 3)"
---

<objective>
Ship the human-facing inbox CLI: `gsd-tools mailbox answer` (targeted record update) and `gsd-tools mailbox review` (interactive loop over all pending questions), plus a resume-handoff printer that surfaces the parked snapshot's resume instruction and staleness state the moment a question is answered.

Why: PARK-02 requires all parked questions to be resolvable in one inbox session without switching tabs. Phase 10 deliberately deferred the answer mechanism to this phase (mailbox.cjs header line 7: "Phase 12 fills answer via separate mechanism") and the schema already carries `status`/`answer`/`answered_ts` — this is a planned extension, not a redesign.

Depends on 12-01: `printResumeHandoff` imports `park.cjs` (snapshotPath + checkStaleness), and both plans edit the gsd-tools.cjs dispatch table.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6/phases/12-park-don-t-block-mailbox/12-CONTEXT.md
@.planning/v1.6/phases/12-park-don-t-block-mailbox/12-RESEARCH.md
@get-shit-done/bin/lib/mailbox.cjs
@get-shit-done/bin/lib/park.cjs
@tests/mailbox.test.cjs
@tests/helpers.cjs

<interfaces>
From 12-01 (must exist before this plan runs — verify with grep before starting):

- `park.cjs` exports `snapshotPath(cwd, runId, phase)` and `checkStaleness(cwd, snapshot)` → `{changed, unchanged, missing, git_range}`.
- gsd-tools.cjs has a `case 'park'` block; this plan only extends the existing `case 'mailbox'` switch (sub-cases `append`/`list` exist at lines ~948-976).

Existing mailbox.cjs facts (verified by direct read):
- `readMailbox(cwd, runId)` → array, skips malformed lines. `mailboxPath(cwd, runId)`, `runDir(cwd, runId)` helpers exist.
- `cmdMailboxAppend` defaults `status: 'open'`; park-written entries (Phase 12-03) will pass `status: 'pending'` explicitly. DECISION (resolves RESEARCH Open Question 1): "unanswered" means `status !== 'answered'` — review and the pending count treat 'open' and 'pending' identically; no schema migration.
- Test helper `runGsdToolsWithInput(argsArray, cwd, stdinInput)` exists in tests/helpers.cjs (spawnSync with `input`) — purpose-built for testing the interactive review loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for mailbox answer/review (RED)</name>
  <files>tests/mailbox.test.cjs</files>
  <read_first>
    - tests/mailbox.test.cjs (existing fixtures: initRunDir, readMailboxRaw, seeding via runGsdTools mailbox append — extend, do not duplicate)
    - tests/helpers.cjs (runGsdToolsWithInput signature for stdin-driven review tests)
    - get-shit-done/bin/lib/park.cjs (snapshot field names for the handoff test fixture: phase, question_id, resume_instruction, content_hashes, context_path, git_head)
  </read_first>
  <action>
Append new describe blocks to `tests/mailbox.test.cjs` (do not modify existing tests — they pin the append/list contract). Test against this exact contract:

**`mailbox answer` (runGsdTools):**
1. Seed a run with two appends (`{question: 'Q one?', phase: 3}`, `{question: 'Q two?', phase: 4}`). `['mailbox', 'answer', runId, '--id', 'q-001', '--answer', 'Use Redis']` → exit 0, stdout `q-001`; readMailboxRaw shows q-001 with `status: 'answered'`, `answer: 'Use Redis'`, `answered_ts` matching `/^\d{4}-\d{2}-\d{2}T/`; q-002 unchanged (`status: 'open'`, `answer: null`); file still has exactly 2 lines in original order (q-001 first).
2. Answer a nonexistent id (`--id q-999`) → exit 1, stderr contains `not found`.
3. Missing `--id` or `--answer` → exit 1, stderr contains usage text (`--id` and `--answer`).
4. No run-id arg + env without GSD_RUN_ID → exit 1, stderr contains `no run context`.
5. Env fallback: no run-id arg, `GSD_RUN_ID` set in opts.env → exit 0.
6. Re-answering an already-answered question → exit 1, stderr contains `already answered` (answers are terminal state — single mutation).

**`mailbox review` (runGsdToolsWithInput):**
7. Seed 3 unanswered (statuses: two `'open'` via plain append, one `'pending'` via append with `"status":"pending"` in --data) + 1 pre-answered (seed by writing the JSONL line directly with status `'answered'`). `runGsdToolsWithInput(['mailbox', 'review', runId], tmp, 'first answer\nsecond answer\n\n')` → exit 0; stdout contains all 3 unanswered question texts and does NOT contain the answered one's text; after the run, records 1 and 2 are `status: 'answered'` with the matching answer text, record 3 (empty stdin line = skip) keeps its prior status; stdout contains a summary line with `answered: 2` and `skipped: 1`.
8. Review presents context fields: seed one entry with `context`, `options` (array), `evidence` set → review stdout contains the context text, each option string, and the evidence text (single-sitting property: everything needed to answer is inline).
9. Review with zero unanswered questions → exit 0, stdout contains `no pending questions`.
10. Both `'open'` and `'pending'` statuses are presented (covered by test 7 — assert explicitly that the pending-status question text appears).

**Resume handoff:**
11. Seed an unanswered question with `phase: 5`, and write `.planning/run/{runId}/parked/phase-5.json` directly with `{phase: 5, blocked_at: 'x', question_id: 'q-001', phase_dir: null, context_path: null, resume_instruction: 'Resume discuss-phase --auto for phase 5', content_hashes: {'STATE.md': null, 'ROADMAP.md': null, 'cross-phase-notes.md': null, 'CONTEXT.md': null}, git_head: null, ts: new Date().toISOString()}`. Answer it via `mailbox answer` → stdout contains `Resume handoff` and `Resume discuss-phase --auto for phase 5`.
12. Same via `mailbox review` (stdin answer) → stdout contains `Resume handoff` and the resume_instruction text.
13. Answering a question whose phase has NO snapshot → stdout does NOT contain `Resume handoff` (no crash, no noise).

**Append-path invariant (Pitfall 3 regression guard):**
14. After an answer-induced rewrite, two further `mailbox append` calls each add exactly one line (total line count = previous + 2) and the answered record's fields survive intact.

Run `node --test tests/mailbox.test.cjs` and confirm the new tests FAIL (cmdMailboxAnswer/cmdMailboxReview do not exist; dispatch rejects the subcommands) while the pre-existing append/list tests still PASS. Commit the red tests.
  </action>
  <verify>node --test tests/mailbox.test.cjs 2>&1 | tail -5 — exits non-zero with failures only in the new describe blocks</verify>
  <acceptance_criteria>
    - tests/mailbox.test.cjs contains the strings `'mailbox', 'answer'`, `'mailbox', 'review'`, `runGsdToolsWithInput`, `Resume handoff`, `already answered`, `no pending questions`
    - grep -c "test(" tests/mailbox.test.cjs increased by >= 12 relative to git HEAD (git diff --stat shows additions only, no deletions of existing tests)
    - node --test tests/mailbox.test.cjs exits non-zero (RED)
  </acceptance_criteria>
  <done>Failing tests pin the answer/review contract: terminal-state answers, full-rewrite update semantics, stdin-driven review loop covering 'open' + 'pending', inline context presentation, resume handoff with and without snapshot, and append-path integrity after rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement writeMailbox, cmdMailboxAnswer, cmdMailboxReview, printResumeHandoff + dispatch (GREEN)</name>
  <files>get-shit-done/bin/lib/mailbox.cjs, get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - tests/mailbox.test.cjs (the contract just written)
    - get-shit-done/bin/lib/mailbox.cjs (current header comment — must be updated since "NO writeMailbox" is no longer true for the answer path)
    - get-shit-done/bin/lib/park.cjs (snapshotPath + checkStaleness signatures for printResumeHandoff)
    - get-shit-done/bin/gsd-tools.cjs case 'mailbox' (lines ~943-982 — add review/answer sub-cases here)
  </read_first>
  <action>
**1. Extend `get-shit-done/bin/lib/mailbox.cjs`:**

Update the module header: replace deviation note 1-2 with the Phase 12 posture — "MAILBOX.jsonl appends are appendFileSync-only (cmdMailboxAppend). The ONLY full-file rewrite is the answer path (writeMailbox), because answers are terminal-state mutations of existing records — a question is answered exactly once. Never use writeMailbox to add records (concurrent parks would be lost — Pitfall 3)." Keep the existing comment that 'open' and 'pending' both count as unanswered.

Add (top-level require: `const park = require('./park.cjs');` — no cycle: park.cjs does not require mailbox.cjs):

- `writeMailbox(cwd, runId, records)` — `fs.writeFileSync(mailboxPath(cwd, runId), records.map(r => JSON.stringify(r)).join('\n') + '\n', 'utf8')`; for `records.length === 0` write `''`. JSDoc: "Full-file rewrite. ONLY for terminal-state answer updates — never for appends."
- `answerRecord(records, qId, answerText)` — pure: find index by `r.id === qId`; return `{error: 'not found'}` if absent; return `{error: 'already answered'}` if `records[idx].status === 'answered'`; else return `{records: <copy with records[idx] replaced by Object.assign({}, rec, {status: 'answered', answer: answerText, answered_ts: new Date().toISOString()})>, record: <updated record>}`. Export for tests.
- `printResumeHandoff(cwd, runId, phase, out)` — `out` defaults to `process.stdout`; if `phase == null` return false; if `!fs.existsSync(park.snapshotPath(cwd, runId, phase))` return false; parse the snapshot, run `park.checkStaleness(cwd, snapshot)`, then write:
  ```
  ── Resume handoff: phase {N} ──
  resume: {resume_instruction}
  staleness: {changed.join(', ') or 'nothing changed since park'}
  git range: {git_range or 'n/a'}
  detail: gsd-tools park staleness {runId} --phase {N}
  ```
  Return true. Wrap snapshot parse + staleness in try/catch — a corrupt snapshot prints `(snapshot unreadable: {path})` and returns false; never throws.
- `cmdMailboxAnswer(cwd, runId, qId, answerText)` — gate effectiveRunId (`runId || process.env.GSD_RUN_ID`; stderr `mailbox answer: no run context — set GSD_RUN_ID or pass run-id arg`, exit 1); if `!qId || !answerText` → stderr `mailbox answer <run-id> --id <q-NNN> --answer <text>`, exit 1; gate run dir exists (same message pattern as append); `readMailbox` → `answerRecord` → on error stderr `mailbox answer: question {qId} {error}` exit 1; `writeMailbox`; `printResumeHandoff(cwd, effectiveRunId, record.phase)`; stdout `qId + '\n'`.
- `cmdMailboxReview(cwd, runId)` — gate effectiveRunId (stderr `mailbox review: no run context`, exit 1) + run dir. `const pending = readMailbox(...).filter(r => r.status !== 'answered')`. If empty → stdout `no pending questions for run {id}\n`, return. Otherwise use `node:readline` (`readline.createInterface({input: process.stdin, output: process.stdout})` with `rl.question` wrapped in a promise, async loop — works with piped stdin from spawnSync). For each pending record print:
  ```
  ── {id} (phase {phase or '-'}) [{status}] ──
  question: {question}
  context: {context or '-'}
  options: {options joined with ' | ' or '-'}
  evidence: {evidence or '-'}
  ```
  then prompt `answer (empty = skip)> `. Empty/whitespace line → count as skipped, leave record untouched. Non-empty → apply `answerRecord` against the CURRENT in-memory records array, print `recorded: {id}\n`, `printResumeHandoff` for its phase. After the loop: if any answered, `writeMailbox` ONCE with the final records array; print summary `answered: {n}  skipped: {m}\n`; `rl.close()`. Make the cmd handler async and have the dispatch call it without awaiting issues (top-level: `mailbox.cmdMailboxReview(cwd, runId)` returning a promise is fine — node keeps the process alive until readline closes).

Extend `module.exports` with `writeMailbox, answerRecord, printResumeHandoff, cmdMailboxAnswer, cmdMailboxReview`.

**2. Dispatch in `get-shit-done/bin/gsd-tools.cjs`** — extend the existing `case 'mailbox'` inner switch (after `case 'list'`):
```javascript
case 'answer': {
  // mailbox answer [run-id] --id q-NNN --answer "text"
  let effectiveRunId = runId;
  if (runId === '--id' || runId === '--answer') effectiveRunId = undefined;
  const idIdx = args.indexOf('--id');
  const ansIdx = args.indexOf('--answer');
  mailbox.cmdMailboxAnswer(cwd, effectiveRunId,
    idIdx !== -1 ? args[idIdx + 1] : null,
    ansIdx !== -1 ? args[ansIdx + 1] : null);
  break;
}
case 'review': {
  // mailbox review [run-id]
  let effectiveRunId = runId;
  if (effectiveRunId && effectiveRunId.startsWith('--')) effectiveRunId = undefined;
  mailbox.cmdMailboxReview(cwd, effectiveRunId);
  break;
}
```
(`runId` is already `args[2]` in this case block.)

Run `node --test tests/mailbox.test.cjs` until green, then full `npm test`.
  </action>
  <verify>npm test 2>&1 | tail -3 — full suite green including the extended tests/mailbox.test.cjs and tests/park.test.cjs from 12-01</verify>
  <acceptance_criteria>
    - get-shit-done/bin/lib/mailbox.cjs contains `function writeMailbox(`, `function answerRecord(`, `function printResumeHandoff(`, `function cmdMailboxAnswer(`, `function cmdMailboxReview(`, and `require('./park.cjs')`
    - cmdMailboxAppend still uses appendFileSync (grep "appendFileSync" get-shit-done/bin/lib/mailbox.cjs matches in cmdMailboxAppend; the append path contains no writeMailbox call)
    - grep "case 'answer'" and grep "case 'review'" both match inside the mailbox case of get-shit-done/bin/gsd-tools.cjs
    - npm test exits 0
  </acceptance_criteria>
  <done>All mailbox answer/review tests pass; full suite green; the inbox CLI primitive is complete: every unanswered question reviewable and answerable in one stdin session, with resume handoffs printed for parked phases.</done>
</task>

</tasks>

<verification>
- `npm test` fully green (mailbox.test.cjs extended, no existing test modified or deleted — `git diff tests/mailbox.test.cjs` shows additions only).
- Append-only invariant intact: park/append paths use appendFileSync; only answer/review rewrite the file.
- `gsd-tools mailbox review` with piped stdin answers multiple questions in one invocation (proven by test 7).
</verification>

<success_criteria>
- PARK-02 (CLI half): `gsd-tools mailbox review <run-id>` presents each parked question with context/options/evidence inline, accepts answers from stdin, records status=answered + answer + answered_ts — all questions resolvable in a single session.
- `gsd-tools mailbox answer` provides the targeted single-question path the /gsd2:inbox skill (12-03) will call.
- Resume handoff (PARK-03 visibility): answering a parked question immediately prints the resume instruction + staleness state + git range.
</success_criteria>

<output>
After completion, create `.planning/v1.6/phases/12-park-don-t-block-mailbox/12-02-SUMMARY.md`
</output>
