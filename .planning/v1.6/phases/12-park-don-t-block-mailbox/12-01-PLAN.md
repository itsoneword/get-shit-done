---
phase: 12-park-don-t-block-mailbox
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/park.test.cjs
  - get-shit-done/bin/lib/park.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/ledger.cjs
autonomous: true
requirements: [PARK-01, PARK-03, PARK-04]
must_haves:
  truths:
    - "A park context snapshot can be written to .planning/run/{run-id}/parked/phase-{N}.json with one CLI call (gsd-tools park create)"
    - "Running gsd-tools park staleness after a planning file changed lists exactly the changed files and the git range since park"
    - "Two consecutive phase-boundary snapshots over an unchanged DECISIONS.jsonl flag the run as stuck — visible in gsd-tools ledger list output and run.log"
    - "Interactive sessions without GSD_RUN_ID see zero behavior change — park commands without run context exit 1, ledger list output is unchanged when no stuck flag is set"
  artifacts:
    - "get-shit-done/bin/lib/park.cjs"
    - "tests/park.test.cjs"
    - "get-shit-done/bin/gsd-tools.cjs (park create/staleness + run snapshot dispatch)"
    - "get-shit-done/bin/lib/ledger.cjs (stuck header in cmdLedgerList)"
  key_links:
    - "gsd-tools.cjs dispatch -> park.cjs cmd handlers (broken = commands unreachable from CLI)"
    - "cmdRunSnapshot -> RUN-META.json phase_snapshots + run.log (broken = stuck detection never persists)"
    - "cmdLedgerList -> RUN-META.json stuck flag (broken = stuck flag invisible to human)"
---

<objective>
Ship the parking/staleness/stuck primitives as a new `lib/park.cjs` module with CLI surface, following the Phase 10 ledger/mailbox module pattern (pure functions exported for tests, `cmd*` handlers for process I/O, thin dispatch in gsd-tools.cjs).

Why: PARK-01 needs a machine-facing resume snapshot, PARK-03 needs a staleness substrate (content hashes at park time, re-hash at resume), and PARK-04 needs stuck detection (identical DECISIONS.jsonl hash across two consecutive phase boundaries). All downstream Phase 12 work (mailbox review handoff, discuss-phase park branch, inbox skill) calls these primitives, so they go first.

All design decisions below are locked by 12-CONTEXT.md or resolved within Claude's discretion (command names, snapshot field names, sha256, RUN-META.json storage for boundary hashes, header-warning-line stuck surface).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.6/phases/12-park-don-t-block-mailbox/12-CONTEXT.md
@.planning/v1.6/phases/12-park-don-t-block-mailbox/12-RESEARCH.md
@get-shit-done/bin/lib/ledger.cjs
@get-shit-done/bin/lib/mailbox.cjs
@tests/ledger.test.cjs
@tests/helpers.cjs

<interfaces>
Existing primitives this plan builds on (verified by direct read):

- `ledger.cjs cmdRunInit(cwd, runId)` already creates `.planning/run/{runId}/parked/` (line 161), empty DECISIONS.jsonl + MAILBOX.jsonl, and RUN-META.json with `{run_id, started_ts, phases, status}`. No mkdir needed in park.cjs.
- `ledger.cjs cmdLedgerList(cwd, runId, opts, raw)` resolves `effectiveRunId = runId || process.env.GSD_RUN_ID`, then prints either raw JSONL lines or a `formatTable(...)` table. The stuck header goes in the non-raw branch ONLY (raw output stays machine-parseable JSONL).
- Run-context gate pattern (copy from `cmdMailboxAppend`): resolve effectiveRunId from arg or `GSD_RUN_ID`; exit 1 with stderr message if absent; exit 1 if `runDir(cwd, effectiveRunId)` does not exist.
- Test helpers: `runGsdTools(argsArray, cwd, {env})` (execFileSync wrapper, returns {success, output, error}), `createTempProject()` (non-git temp dir), `createTempGitProject()` (git repo with one commit), `cleanup(tmp)`. Tests seed run dirs with a local `initRunDir(tmp, runId)` fixture (see tests/mailbox.test.cjs lines 31-41 for the exact fixture to copy).
- `gsd-tools.cjs` already parses a global `raw` boolean from `--raw` and has `case 'run'` (line ~984, currently only `init` sub) and lib requires at the top of the file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for park.cjs primitives and CLI (RED)</name>
  <files>tests/park.test.cjs</files>
  <read_first>
    - tests/mailbox.test.cjs (fixture pattern: initRunDir, readMailboxRaw, describe/beforeEach/afterEach structure, runGsdTools array-args usage)
    - tests/helpers.cjs (runGsdTools opts.env override for GSD_RUN_ID-absent tests; createTempProject vs createTempGitProject)
    - get-shit-done/bin/lib/ledger.cjs (RUN-META.json shape written by cmdRunInit; formatTable columns for the ledger list test)
  </read_first>
  <action>
Create `tests/park.test.cjs` using `node:test` + `node:assert`, mirroring the mailbox.test.cjs structure (header comment naming requirements PARK-01/PARK-03/PARK-04, local `initRunDir(tmp, runId)` fixture copied from mailbox.test.cjs lines 31-41, `createTempProject`/`createTempGitProject` + `cleanup` per describe block).

The module under test is `get-shit-done/bin/lib/park.cjs` — require it directly for pure functions (`const park = require('../get-shit-done/bin/lib/park.cjs')`) and use `runGsdTools` for CLI behavior. Test against this exact contract:

**Pure functions (direct require):**
1. `hashContent(str)` — returns 64-char lowercase hex; same input twice gives identical output; `hashContent('a')` !== `hashContent('b')`.
2. `hashFile(filePath)` — returns the same hex as `hashContent` of the file body; returns `null` for a nonexistent path.
3. `buildContentHashes(cwd, contextPath)` — returns an object with exactly the keys `'STATE.md'`, `'ROADMAP.md'`, `'cross-phase-notes.md'`, `'CONTEXT.md'`; hashes `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/cross-phase-notes.md` under cwd and `contextPath` for `'CONTEXT.md'`; missing files map to `null`; `contextPath` of `null` maps `'CONTEXT.md'` to `null`.
4. `buildParkSnapshot(opts)` with `opts = {phase, blockedAt, questionId, phaseDir, contextPath, resumeInstruction, contentHashes, gitHead}` — returns `{phase, blocked_at, question_id, phase_dir, context_path, resume_instruction, content_hashes, git_head, ts}` where `ts` matches `/^\d{4}-\d{2}-\d{2}T/` (ISO) and all other fields are copied verbatim.
5. `checkStaleness(cwd, snapshot)` — re-hashes the four tracked files (same paths as buildContentHashes, with `'CONTEXT.md'` from `snapshot.context_path`) and returns `{changed, unchanged, missing, git_range}`:
   - nothing touched since snapshot → all four keys in `unchanged`, `changed: []`, `missing: []`
   - modify `.planning/STATE.md` content → `changed` contains `'STATE.md'`, `unchanged` does not
   - delete a file that hashed non-null at park time → its key appears in `missing`
   - file that was `null` at park and exists now → its key appears in `changed`
   - `git_range` is `` `${snapshot.git_head}..HEAD` `` when `snapshot.git_head` is non-null, `null` otherwise (the pure function never runs git)
6. `decisionsHash(cwd, runId)` — sha256 hex of DECISIONS.jsonl content; `null` when the file is absent; changes when a line is appended.
7. `isStuck(meta)` — `false` when `meta.phase_snapshots` is missing or has < 2 entries; `true` when the last two entries have equal non-null `decisions_hash`; `false` when they differ; `false` when the last hash is `null`.
8. `appendPhaseSnapshot(cwd, runId, snap)` with `snap = {phase, ts, decisions_hash}` — appends to `RUN-META.json` `phase_snapshots` array (creating it), sets `meta.stuck` to `isStuck(meta)` (boolean, present after every call), writes the file back pretty-printed, returns the updated meta object.
9. `resolveGitHead(cwd)` — returns a 40-char hex sha in a `createTempGitProject()` dir; returns `null` in a plain `createTempProject()` dir (no throw).

**CLI — `park create` (runGsdTools):**
10. `['park', 'create', runId, '--phase', '3', '--question', 'q-001', '--blocked-at', 'discuss-phase --auto: question_triage', '--resume', 'Resume discuss-phase --auto for phase 3 with answer to q-001 injected']` on an initRunDir'd `createTempGitProject()` → exit 0; file `.planning/run/{runId}/parked/phase-3.json` exists and parses; parsed snapshot has `phase: 3` (number), `question_id: 'q-001'`, `blocked_at` and `resume_instruction` matching the flags, `content_hashes` with the four keys, `git_head` matching `/^[0-9a-f]{40}$/`, ISO `ts`; stdout contains `parked/phase-3.json`.
11. Same command in a non-git `createTempProject()` → exit 0 and `git_head: null` (Pitfall 4: git failure must not crash snapshot creation).
12. `park create` against an uninitialized run-id → exit 1, stderr contains `not initialized`.
13. `park create` with no run-id arg and env lacking `GSD_RUN_ID` (pass `{env: {...process.env, GSD_RUN_ID: undefined}}` — build an env object WITHOUT the key, e.g. destructure it out) → exit 1, stderr contains `no run context`.
14. `park create` with no run-id arg but `GSD_RUN_ID` set in opts.env to an initialized run → exit 0 (env fallback works).
15. `park create` missing `--phase` or `--question` → exit 1, stderr contains usage text.

**CLI — `park staleness`:**
16. After a `park create` for phase 3 with nothing changed: `['park', 'staleness', runId, '--phase', '3']` → exit 0, stdout contains `unchanged` and does not list any file under a `changed` label.
17. Modify `.planning/ROADMAP.md` then rerun → stdout `changed` section contains `ROADMAP.md`; stdout contains the resume instruction text; in a git project stdout contains `..HEAD`.
18. `--raw` variant → stdout parses as JSON with keys `changed`, `unchanged`, `missing`, `git_range`, `resume_instruction`, `question_id`, `phase`.
19. `park staleness` for a phase with no snapshot → exit 1, stderr contains `no snapshot`.

**CLI — `run snapshot` (stuck detection):**
20. Seed DECISIONS.jsonl with one line; `['run', 'snapshot', runId, '--phase', '1']` → exit 0; RUN-META.json `phase_snapshots` has 1 entry `{phase: 1, ts: <ISO>, decisions_hash: <64-hex>}`; `stuck` is `false`; stdout contains `snapshot recorded`.
21. Without touching DECISIONS.jsonl, run `['run', 'snapshot', runId, '--phase', '2']` → RUN-META.json `stuck` is `true`; stdout contains `STUCK`; file `.planning/run/{runId}/run.log` exists and contains `STUCK`.
22. Append another DECISIONS.jsonl line, then `['run', 'snapshot', runId, '--phase', '3']` → `stuck` back to `false` (recovery), stdout does NOT contain `STUCK`.
23. `run snapshot` with no run context (env without GSD_RUN_ID, no arg) → exit 1.

**CLI — ledger list stuck surface:**
24. Seed RUN-META.json with `stuck: true` (direct fs write) and one ledger record; `['ledger', 'list', runId]` → stdout contains `STUCK FLAG` before the table header line.
25. Same but `stuck: false` or key absent → stdout does NOT contain `STUCK FLAG` (zero behavior change).
26. `['ledger', 'list', runId, '--raw']` with `stuck: true` → every stdout line parses as JSON (no header pollution in raw mode).

Run `node --test tests/park.test.cjs` and confirm the suite FAILS (park.cjs does not exist yet — require throws). Commit the red tests.
  </action>
  <verify>node --test tests/park.test.cjs 2>&1 | tail -5 — exits non-zero (RED: module not found / failing tests). Also: existing suite still green for everything else: npm test 2>&1 | tail -3 shows only park.test.cjs failing.</verify>
  <acceptance_criteria>
    - tests/park.test.cjs exists and contains the strings `hashContent`, `buildParkSnapshot`, `checkStaleness`, `decisionsHash`, `isStuck`, `appendPhaseSnapshot`, `resolveGitHead`, `'park', 'create'`, `'park', 'staleness'`, `'run', 'snapshot'`, `STUCK FLAG`
    - grep -c "test(" tests/park.test.cjs returns >= 20
    - node --test tests/park.test.cjs exits non-zero (RED)
    - tests/park.test.cjs contains `createTempGitProject` (git_head sha case) and a no-GSD_RUN_ID env-override case
  </acceptance_criteria>
  <done>A comprehensive failing test file pins the park.cjs contract: snapshot fields, staleness diff semantics, stuck threshold of exactly 2 consecutive identical hashes, run-context gating, and the ledger list stuck header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement park.cjs, CLI dispatch, and ledger list stuck header (GREEN)</name>
  <files>get-shit-done/bin/lib/park.cjs, get-shit-done/bin/gsd-tools.cjs, get-shit-done/bin/lib/ledger.cjs</files>
  <read_first>
    - tests/park.test.cjs (the contract just written — implement exactly this)
    - get-shit-done/bin/lib/mailbox.cjs (module header style, run-context gate in cmdMailboxAppend lines 157-212, exports shape)
    - get-shit-done/bin/lib/ledger.cjs (cmdLedgerList lines 254-271 — where the stuck header goes; runDir/ledgerPath helpers to mirror)
    - get-shit-done/bin/gsd-tools.cjs lines 900-1000 (ledger/mailbox/run dispatch cases — extension pattern, how `raw` and run-id args are parsed)
  </read_first>
  <action>
**1. Create `get-shit-done/bin/lib/park.cjs`** ('use strict', requires: `fs`, `path`, `node:crypto`, `child_process` execSync). Module header comment documenting three invariants:
- "Park path never rewrites MAILBOX.jsonl — mailbox append stays appendFileSync-only (see mailbox.cjs); park.cjs only writes parked/phase-{N}.json and RUN-META.json"
- "Boundary hashes are recorded at phase COMPLETION, not phase start — two consecutive identical hashes mean a phase completed without growing the ledger" (Pitfall 5)
- "Pure functions never run git — resolveGitHead is called only from cmd* handlers and try/catches to null" (Pitfall 4)

Implement (signatures pinned by tests):
- `runDir(cwd, runId)`, `snapshotPath(cwd, runId, phase)` → `.planning/run/{runId}/parked/phase-{phase}.json`, `runLogPath(cwd, runId)` → `.planning/run/{runId}/run.log`, `metaPath(cwd, runId)` → RUN-META.json path
- `hashContent(str)` → `crypto.createHash('sha256').update(str).digest('hex')`
- `hashFile(filePath)` → null if !existsSync, else hashContent(readFileSync utf8)
- `buildContentHashes(cwd, contextPath)` → `{'STATE.md': hashFile(.planning/STATE.md), 'ROADMAP.md': hashFile(.planning/ROADMAP.md), 'cross-phase-notes.md': hashFile(.planning/cross-phase-notes.md), 'CONTEXT.md': contextPath ? hashFile(contextPath) : null}`
- `buildParkSnapshot({phase, blockedAt, questionId, phaseDir, contextPath, resumeInstruction, contentHashes, gitHead})` → `{phase, blocked_at: blockedAt, question_id: questionId, phase_dir: phaseDir || null, context_path: contextPath || null, resume_instruction: resumeInstruction, content_hashes: contentHashes, git_head: gitHead != null ? gitHead : null, ts: new Date().toISOString()}`
- `checkStaleness(cwd, snapshot)` → for each of the four keys, compute current hash (CONTEXT.md from `snapshot.context_path`, resolved relative to cwd if not absolute); classify: old === new (including both null) → unchanged; old non-null && new null → missing; otherwise → changed. Return `{changed, unchanged, missing, git_range: snapshot.git_head ? snapshot.git_head + '..HEAD' : null}`
- `decisionsHash(cwd, runId)` → null if DECISIONS.jsonl absent, else hashContent of file body
- `isStuck(meta)` → last two of `meta.phase_snapshots || []`: `snaps.length >= 2 && last.decisions_hash !== null && last.decisions_hash === prev.decisions_hash`
- `appendPhaseSnapshot(cwd, runId, snap)` → read RUN-META.json, push snap onto `meta.phase_snapshots` (init []), set `meta.stuck = isStuck(meta)`, writeFileSync pretty-printed + '\n', return meta
- `resolveGitHead(cwd)` → try `execSync('git rev-parse HEAD', {cwd, stdio: ['pipe','pipe','pipe']}).toString().trim()` catch → null
- `cmdParkCreate(cwd, runId, opts)` where opts = `{phase, question, blockedAt, resume, phaseDir, contextPath}`: gate effectiveRunId (`runId || process.env.GSD_RUN_ID`, else stderr `park create: no run context — set GSD_RUN_ID or pass run-id arg`, exit 1); gate run dir exists (stderr `park create: run not initialized: {id} (run: gsd-tools run init {id})`, exit 1); require numeric `opts.phase` and non-empty `opts.question` (stderr `park create: --phase <N> and --question <q-id> are required`, exit 1); build snapshot via `buildContentHashes` + `resolveGitHead(cwd)` + `buildParkSnapshot`; writeFileSync snapshotPath pretty-printed + '\n'; stdout the cwd-relative path `.planning/run/{id}/parked/phase-{N}.json`
- `cmdParkStaleness(cwd, runId, opts, raw)`: gate effectiveRunId; if snapshot file absent → stderr `park staleness: no snapshot for phase {N}`, exit 1; parse snapshot, run `checkStaleness`; raw → `process.stdout.write(JSON.stringify({phase: snapshot.phase, question_id: snapshot.question_id, resume_instruction: snapshot.resume_instruction, changed, unchanged, missing, git_range}) + '\n')`; non-raw → print lines: `phase: {N}`, `question: {question_id}`, `changed: {list or 'none'}`, `unchanged: {list or 'none'}`, `missing: {list or 'none'}`, `git range: {git_range or 'n/a'}`, `resume: {resume_instruction}`
- `cmdRunSnapshot(cwd, runId, opts)`: gate effectiveRunId + run dir; compute `decisionsHash`; `appendPhaseSnapshot` with `{phase: opts.phase, ts: ISO, decisions_hash}`; appendFileSync run.log line `[{ISO}] snapshot phase={N} decisions_hash={first 12 chars or 'null'}\n`; if `meta.stuck` → appendFileSync run.log `[{ISO}] STUCK: ledger unchanged across last 2 phase boundaries (phase {N})\n` and stdout `STUCK: ledger unchanged across last 2 phase boundaries\n`; always stdout `snapshot recorded: phase={N} decisions_hash={12-char prefix or 'null'}\n`

Export everything listed in the test contract: `{runDir, snapshotPath, runLogPath, metaPath, hashContent, hashFile, buildContentHashes, buildParkSnapshot, checkStaleness, decisionsHash, isStuck, appendPhaseSnapshot, resolveGitHead, cmdParkCreate, cmdParkStaleness, cmdRunSnapshot}`.

**2. Wire dispatch in `get-shit-done/bin/gsd-tools.cjs`:**
- Add `const park = require('./lib/park.cjs');` next to the existing ledger/mailbox requires (find them with grep `require('./lib/ledger.cjs')`).
- New `case 'park':` block (place after `case 'mailbox'`): `const sub = args[1]; let runId = args[2]; if (runId && runId.startsWith('--')) runId = undefined;` then flag parsing via indexOf for `--phase` (parseInt), `--question`, `--blocked-at`, `--resume`, `--phase-dir`, `--context-path`; sub `create` → `park.cmdParkCreate(cwd, runId, {phase, question, blockedAt, resume, phaseDir, contextPath})`; sub `staleness` → `park.cmdParkStaleness(cwd, runId, {phase}, raw)`; default → stderr `park: unknown subcommand: {sub}`, exit 1.
- Extend the existing `case 'run'` switch with `case 'snapshot':` → same runId/--phase parsing → `park.cmdRunSnapshot(cwd, runId, {phase})`.

**3. Stuck header in `get-shit-done/bin/lib/ledger.cjs` `cmdLedgerList`:** after resolving `effectiveRunId` and BEFORE printing, in the non-raw branch only: try-read `.planning/run/{effectiveRunId}/RUN-META.json`; if it parses and `meta.stuck === true`, `process.stdout.write('STUCK FLAG: run ' + effectiveRunId + ' — ledger unchanged across last 2 phase boundaries\n')`. Wrap in try/catch so a missing/corrupt RUN-META.json never breaks ledger list. Raw branch untouched.

Run `node --test tests/park.test.cjs` until green, then full `npm test` (983+ existing tests must stay green — especially tests/ledger.test.cjs, which pins cmdLedgerList output).
  </action>
  <verify>npm test 2>&1 | tail -3 — full suite green including tests/park.test.cjs</verify>
  <acceptance_criteria>
    - get-shit-done/bin/lib/park.cjs exists; grep "module.exports" -A 20 lists cmdParkCreate, cmdParkStaleness, cmdRunSnapshot, checkStaleness, isStuck, decisionsHash
    - grep "case 'park'" get-shit-done/bin/gsd-tools.cjs returns a match; grep "'snapshot'" get-shit-done/bin/gsd-tools.cjs returns a match inside the run case
    - grep "STUCK FLAG" get-shit-done/bin/lib/ledger.cjs returns a match inside cmdLedgerList non-raw branch
    - grep -c "appendFileSync" get-shit-done/bin/lib/park.cjs >= 1 (run.log) and grep -c "writeFileSync.*MAILBOX" get-shit-done/bin/lib/park.cjs == 0 (park never touches MAILBOX.jsonl)
    - node --test tests/park.test.cjs exits 0; npm test exits 0
  </acceptance_criteria>
  <done>All park.test.cjs tests pass; full suite green; `park create`, `park staleness`, `run snapshot` reachable from the CLI; `ledger list` shows the STUCK FLAG header when RUN-META.json says stuck.</done>
</task>

</tasks>

<verification>
- `npm test` fully green (park.test.cjs added, ledger.test.cjs unaffected).
- Manual smoke (optional, in a temp dir): `gsd-tools run init t1 && gsd-tools park create t1 --phase 1 --question q-001 --blocked-at x --resume y && gsd-tools park staleness t1 --phase 1` round-trips.
- No behavior change without run context: every new cmd handler exits 1 with a clear stderr message when neither arg nor GSD_RUN_ID provides a run-id.
</verification>

<success_criteria>
- PARK-01 (snapshot half): one CLI call writes parked/phase-{N}.json with question_id, blocked_at, resume_instruction, content hashes of STATE.md/ROADMAP.md/cross-phase-notes.md/CONTEXT.md, git HEAD, ts.
- PARK-03 (substrate): `park staleness` re-hashes the same files and reports changed/unchanged/missing + git range — visible, never auto-blocking.
- PARK-04: identical DECISIONS.jsonl hash across exactly 2 consecutive `run snapshot` calls sets stuck=true, appends a STUCK line to run.log, and surfaces a STUCK FLAG header in `ledger list`.
</success_criteria>

<output>
After completion, create `.planning/v1.6/phases/12-park-don-t-block-mailbox/12-01-SUMMARY.md`
</output>
