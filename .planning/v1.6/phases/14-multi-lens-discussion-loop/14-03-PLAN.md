---
phase: 14-multi-lens-discussion-loop
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - get-shit-done/workflows/discuss-loop.md
autonomous: false
requirements: [LOOP-01, LOOP-02]
sidecar_impact: [ARCHITECTURE]
must_haves:
  truths:
    - "Running /gsd2:discuss-loop <path> spawns three parallel fresh-context lenses per round and produces distinct positions grounded in artifact text (anchors are verbatim substrings)"
    - "Convergence (no blocking lens AND zero new constraints) exits the loop early; the transcript's round_delta records show the verifiable round-over-round constraint delta"
    - "At round 3 without convergence in autonomous context (--auto AND GSD_RUN_ID), a mailbox entry with status pending appears whose options map 1:1 to labeled lens positions — never a blended average"
    - "Interactive runs (no --auto or no GSD_RUN_ID) never write MAILBOX.jsonl; divergent positions are presented in-session"
    - "Every loop writes .planning/discuss-loop/<loop_id>/transcript.jsonl with loop_start, position, round_delta, loop_end records; transcript or gated-write failure aborts the loop loudly"
  artifacts:
    - "get-shit-done/workflows/discuss-loop.md"
  key_links:
    - "workflow → gsd-tools discuss-loop validate/delta/survivors/transcript CLI (broken = orchestrator improvises deterministic steps, unauditable)"
    - "workflow → gsd-tools mailbox append / ledger append with exit-code checks (broken = silent drop, violates 'proposes, never disposes')"
    - "workflow → Task(subagent_type=gsd-lens-*) spawns (broken = no judgment happens)"
    - "workflow → get-shit-done/references/escalation-contract.md membership check (broken = converged modifications apply ungated)"
---

<objective>
Write the discuss-loop orchestrator workflow — the heart of Phase 14. It resolves the artifact, runs up to 3 rounds of three parallel lens Task() spawns, validates every position block and computes every round delta through the `gsd-tools discuss-loop` primitives from plan 14-01, then either converges (verdict; ledger record in run context; escalation-contract-gated application when the verdict modifies a committed artifact) or escalates the divergent positions (mailbox in autonomous context, in-session presentation otherwise). Then install to runtime and verify end-to-end with a live smoke run.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6/phases/14-multi-lens-discussion-loop/14-CONTEXT.md
@.planning/v1.6/phases/14-multi-lens-discussion-loop/14-AGENT-SPEC.md
@get-shit-done/workflows/discuss-phase.md
@get-shit-done/workflows/inbox.md
@get-shit-done/references/escalation-contract.md
@get-shit-done/bin/lib/discuss-loop.cjs
@agents/gsd-lens-skeptic.md
@commands/gsd2/discuss-loop.md

<interfaces>
**CLI primitives from plan 14-01 (the workflow calls these; never reimplement inline):**

| Command | Purpose | Output |
|---|---|---|
| `gsd-tools discuss-loop loop-id <ref>` | generate loop id (suffixes `--$GSD_RUN_ID` if set) | id on stdout |
| `gsd-tools discuss-loop validate --round N --data '<block>' [--prior '<id array>'] [--artifact <path>]` | position-block validation incl. anchor substring check | exit 0 `valid` / exit 1 + one error per line |
| `gsd-tools discuss-loop delta --round N --data '<block array>' [--degraded]` | round_delta JSON incl. `converged` flag | round_delta JSON on stdout |
| `gsd-tools discuss-loop survivors --data '<array of rounds>'` | surviving final-round positions ordered by divergence weight | `[{lens, weight, block}]` JSON |
| `gsd-tools discuss-loop transcript <loop-id> --data '<record>'` | append one transcript record (injects ts + loop_id) | transcript path; exit 1 on failure |

**Gated writes (Phases 10/12 — exit codes MUST be checked):**
- `gsd-tools mailbox append --data '<json>'` → prints `q-NNN`; exits non-zero on missing run context / missing `question`
- `gsd-tools ledger append --data '<json>'` → prints `dec-NNN`; requires `decision`, `alternatives`, `evidence`, `confidence`, `escalated`

**Transcript record shapes (LOCKED — AGENT-SPEC):** every record carries `loop_id`, `ts` (injected by the CLI), `type`:
- `loop_start` — `{artifact:{kind,ref}, question, run_id|null, interactive: bool}`
- `position` — the validated lens position block verbatim
- `lens_failure` — `{lens, round, attempt, errors:[...], raw_output}`
- `round_delta` — `{round, blocking_lenses, new_constraint_ids, carried_count, converged, degraded}` (produced by the delta CLI)
- `loop_end` — `{outcome:"converged"|"escalated"|"aborted", rounds_run, verdict|null, mailbox_id|null, ledger_id|null}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write get-shit-done/workflows/discuss-loop.md (orchestrator prose)</name>
  <files>get-shit-done/workflows/discuss-loop.md</files>
  <read_first>
    - get-shit-done/workflows/discuss-phase.md lines 360-437 (the inline escalation evaluator + mailbox/park pattern to adapt for converged-modification gating)
    - get-shit-done/workflows/inbox.md (workflow file structure: purpose + process + named steps; run-resolution prose style)
    - get-shit-done/bin/lib/discuss-loop.cjs (the exact CLI semantics the prose drives)
    - agents/gsd-lens-skeptic.md (what the lenses expect in the envelope — the workflow constructs it)
    - get-shit-done/references/escalation-contract.md (the 4 criteria / conditions / tie-break rules applied inline in the converged path)
  </read_first>
  <action>
Create `get-shit-done/workflows/discuss-loop.md` structured like inbox.md (`<purpose>` then `<process>` with named `<step>` blocks). All gsd-tools invocations use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" ...` (the runtime path convention used by discuss-phase.md and inbox.md).

**`<purpose>`** — "Multi-lens judgment loop for a concrete artifact. Three independent fresh-context lenses (Skeptic, User-Advocate, Architect) judge per round, up to a hard cap of 3 rounds. Convergence is a deterministic flag check, never sentence similarity. Non-convergence escalates the labeled divergent positions — never a synthesized average (LOOP-02). THIN constraint: this workflow judges and routes; it never resumes runs or schedules work (runner territory, Phases 13/15)."

**`<step name="parse_arguments">`** — Parse `$ARGUMENTS`:
- Positional non-flag token → artifact file path.
- `--decision dec-NNN` → ledger-record artifact selector.
- `--question "<text>"` → the open question to judge.
- `--run-id <id>` → explicit run for `--decision` resolution.
- `--auto` → autonomous context marker.
- Neither a path nor `--decision` given → print usage (`/gsd2:discuss-loop <artifact-path | --decision dec-NNN> [--question <text>] [--run-id <id>] [--auto]`) and stop.
Define the two mode flags used everywhere below: `run_context` = `GSD_RUN_ID` env var set OR `--run-id` passed; `autonomous` = `--auto` present AND `GSD_RUN_ID` set. (Mirrors discuss-phase.md: interactive calibration with GSD_RUN_ID still asks the human; only `--auto` + run id writes the mailbox.)

**`<step name="resolve_artifact">`** — fail fast, zero spawns, zero writes on failure:
- File path: must exist (`test -f`) else usage error and stop. Read full content. `artifact = {kind:"file", ref:"<path>", content}`.
- `--decision dec-NNN`: if `GSD_RUN_ID` or `--run-id` set → `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" ledger list <run-id> --raw`, find the line whose `"id"` equals the dec-NNN; not found → error and stop. If NO run context → scan: `grep -l '"id":"dec-NNN"' .planning/run/*/DECISIONS.jsonl 2>/dev/null`; zero matches → error "dec-NNN not found in any run"; multiple matching runs → error "dec-NNN exists in multiple runs — pass --run-id"; exactly one → use it. Render the record as pretty-printed JSON. `artifact = {kind:"decision", ref:"dec-NNN", content}`.
- Oversized guard: if content exceeds ~50,000 characters, stop with "artifact too large for lens context — pass a section or a smaller file" (AGENT-SPEC input-validation row).
- Question: from `--question`; if absent and interactive → ask the user "What open question should the lenses judge this artifact against?"; if absent and autonomous → default to "Should this artifact be accepted as-is, modified, or rejected — and what downstream constraints does it force or break?"

**`<step name="init_loop">`** —
1. `loop_id=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" discuss-loop loop-id "<ref>")`
2. Materialize the artifact content to `.planning/discuss-loop/<loop_id>/artifact.txt` via the Write tool (this file is what `validate --artifact` substring-checks anchors against, and makes every spawn reconstructible from the loop directory).
3. Append `loop_start`: `discuss-loop transcript <loop_id> --data '{"type":"loop_start","artifact":{"kind":"<kind>","ref":"<ref>"},"question":"<q>","run_id":<"id"|null>,"interactive":<bool>}'`.
4. **Every** `discuss-loop transcript` call in this workflow must be exit-code checked: non-zero → print "transcript write failed — aborting (unauditable loop violates the trust constraint)" and stop with non-zero outcome. State this rule once here and reference it.

**`<step name="round_loop">`** — for round N = 1, 2, 3:
1. Build the round envelope ONCE (identical for all three lenses except the `lens` field):
```json
{
  "loop_id": "<loop_id>", "lens": "<lens>", "round": N,
  "question": "<question>",
  "artifact": {"kind": "<kind>", "ref": "<ref>", "content": "<<<ARTIFACT\n<content>\nARTIFACT>>>"},
  "prior_positions": [<ALL validated blocks from ALL prior rounds; [] in round 1>],
  "output_schema": "<the position-block schema verbatim + carried/new tagging rule — copy it from the lens agent output_contract>"
}
```
The artifact content is wrapped in the `<<<ARTIFACT` / `ARTIFACT>>>` data markers the lens agents are trained on.
2. Spawn all three lenses IN PARALLEL — three Task() calls in a single message: `Task(subagent_type="gsd-lens-skeptic", description="Skeptic round N", prompt=<envelope with lens=skeptic>)`, same for `gsd-lens-user-advocate` and `gsd-lens-architect`. Wait for all three before any validation (Pitfall 5: never diff partial results).
3. For each lens response: extract the FINAL fenced ```json block. Validate via `discuss-loop validate --round N --data '<block>' --prior '<JSON array of every constraint id from all prior rounds>' --artifact .planning/discuss-loop/<loop_id>/artifact.txt`.
   - Exit 0 → append the block to the transcript as a `position` record (`{"type":"position", ...block}`).
   - Exit 1 → ONE corrective re-spawn of that lens for this round: same envelope plus "Your previous output failed validation: <error lines>. Emit only the corrected JSON block." Validate the retry.
   - Retry also fails → append `lens_failure` record `{"type":"lens_failure","lens":"<lens>","round":N,"attempt":2,"errors":[...],"raw_output":"<verbatim>"}`. Then the degraded ladder: 2 valid lenses remain → continue the round degraded; ≤1 valid lens → append `loop_end` `{"type":"loop_end","outcome":"aborted","rounds_run":N,"verdict":null,"mailbox_id":null,"ledger_id":null}`, report the infrastructure failure explicitly (it must NOT masquerade as divergence), and stop non-zero.
4. Compute the delta: `delta=$(discuss-loop delta --round N --data '<JSON array of this round's validated blocks>' [--degraded if a lens failed this round])`. Append the delta JSON verbatim as the `round_delta` transcript record.
5. Branch on `converged` in the delta: `true` → go to converged_path. `false` and N < 3 → next round (prior_positions grows by this round's validated blocks). `false` and N == 3 → go to escalation_path.
6. Interactive sessions: print a one-line round summary after each delta (e.g. "Round N: blocking=[skeptic], new constraints=3 — not converged").

**`<step name="converged_path">`** —
1. Build the verdict from the final round: consensus position (all `accept` → "accept"; any `modify` → "modify" with the union of `modification` texts as the concrete change list), plus the final constraint set (the carried constraints — these are the agreed downstream constraints).
2. **If the verdict is `modify` and the artifact is an existing committed file** (kind `file`, tracked in git): apply the escalation contract INLINE (adapt discuss-phase.md question_triage step 1-3 verbatim): read `$HOME/.claude/get-shit-done/references/escalation-contract.md` once; apply the four criteria (irreversibility, security boundary, scope change, spec ambiguity) as membership checks against the proposed modification; compute the verdict BEFORE any write. Borderline → `proceed-and-log`, EXCEPT borderline irreversibility/security → `park-and-ask`.
   - `proceed-and-log` (reversible, no criterion fires): apply the modification to the artifact via Edit, note "applied" in the loop_end verdict.
   - `park-and-ask`: do NOT apply. Autonomous → mailbox confirm entry (`mailbox append` with `question` = "Confirm modification to <ref>: <modification summary>", `options` = ["apply as proposed","reject modification"], `evidence` = final constraint set summary, `context` = "discuss-loop <loop_id>: converged modify verdict, <criterion+condition> fired", `status`:"pending"`); exit-code check; capture q-NNN. Interactive → ask the human directly (AskUserQuestion); apply only on yes.
   - Judgment-only outcomes (verdict `accept`/`reject`) and net-new content: logged, never gated (locked CONTEXT decision).
3. **If `run_context`**: append the verdict to the ledger:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" ledger append --data '{
  "decision": "<accept | modify: <summary>>",
  "alternatives": ["<non-prevailing lens positions, labeled>"],
  "evidence": "<final constraint set + loop_id>",
  "confidence": "HIGH",
  "escalated": <true if park-and-ask fired, else false>,
  "escalation_verdict": "<proceed-and-log | park-and-ask | proceed>",
  "escalation_reason": "<criterion + condition, or: all criteria negative>",
  "phase": null,
  "context": "discuss-loop: <loop_id>",
  "question": "<question>"
}'
```
Exit-code check: non-zero → `loop_end` outcome `aborted`, stop non-zero. Capture dec-NNN. Interactive sessions WITHOUT run context: present the verdict in-session, skip the ledger (write-once ledger is run-gated — Phase 10).
4. Append `loop_end`: `{"type":"loop_end","outcome":"converged","rounds_run":N,"verdict":"<summary>","mailbox_id":<q-NNN|null>,"ledger_id":<dec-NNN|null>}`. Print a CONVERGED report: verdict, rounds run, transcript path, ledger/mailbox ids.

**`<step name="escalation_path">`** — bifurcation is the FIRST branch (Pitfall 6):
1. `survivors=$(discuss-loop survivors --data '<JSON array of all rounds' validated blocks>')`.
2. **Autonomous (`--auto` AND `GSD_RUN_ID`)**: build the mailbox entry from survivors — options map 1:1 to lens blocks, ordered as returned (divergence weight already orders the top two divergent positions first):
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" mailbox append --data '{
  "question": "<question>",
  "options": ["Skeptic: <block.summary> [blocking: <blocking constraint statements>]", "Architect: <...>", ...],
  "evidence": "<constraint-delta summary: which constraints stayed new/blocking across rounds, per lens — from the round_delta records>",
  "context": "<ref> | rounds: 3 | loop: <loop_id><; degraded: <lens> if any>",
  "status": "pending"
}'
```
Each option string is built ONLY from that one lens's block (summary + its blocking constraints) — never combine text from two lenses into one option (LOOP-02; the no-synthesis rule). `status` MUST be `"pending"` explicitly (Pitfall 1 — the CLI default is `"open"`). Exit-code check: non-zero → `loop_end` `aborted` + stop non-zero (never silently drop an escalation). Capture q-NNN.
3. **Interactive (anything else)**: present the surviving positions in-session, labeled per lens with summary, blocking constraints, and weight. Do NOT write the mailbox. MAILBOX.jsonl and DECISIONS.jsonl remain untouched.
4. Append `loop_end`: `{"type":"loop_end","outcome":"escalated","rounds_run":3,"verdict":null,"mailbox_id":<q-NNN|null>,"ledger_id":null}`. Print an ESCALATED report: positions, mailbox id (or "presented in-session"), transcript path, and (autonomous) "answer via /gsd2:inbox".

End the file with a `<guardrails>` section restating the absolutes: hard cap 3 rounds; never synthesize a blended option; never write MAILBOX.jsonl/DECISIONS.jsonl directly (CLIs only); every gated write and transcript write is exit-code checked; transcript failure aborts; interactive sessions never mailbox-write.
  </action>
  <verify>grep -c "discuss-loop transcript" get-shit-done/workflows/discuss-loop.md ≥ 4; grep -q '"status": "pending"' get-shit-done/workflows/discuss-loop.md</verify>
  <acceptance_criteria>
    - get-shit-done/workflows/discuss-loop.md exists with `<purpose>` and `<process>` containing steps named parse_arguments, resolve_artifact, init_loop, round_loop, converged_path, escalation_path
    - Contains `gsd-lens-skeptic`, `gsd-lens-user-advocate`, `gsd-lens-architect` as Task subagent_type values
    - Contains `discuss-loop validate`, `discuss-loop delta`, `discuss-loop survivors`, `discuss-loop transcript`, `discuss-loop loop-id` invocations
    - Contains `"status": "pending"` in the mailbox append payload and the string `escalation-contract.md`
    - Contains `<<<ARTIFACT` data markers in the envelope construction
    - Contains the literal strings `loop_start`, `position`, `lens_failure`, `round_delta`, `loop_end` (all five record types)
    - Contains an explicit `--auto` AND `GSD_RUN_ID` conjunction for the mailbox branch, and text forbidding mailbox writes in interactive sessions
    - Contains the one-corrective-re-spawn rule and the 2-valid-continue / ≤1-abort degraded ladder
    - grep -i "synthes" output includes a "never" rule (no-synthesis guardrail present)
  </acceptance_criteria>
  <done>The orchestrator workflow exists, drives every deterministic step through the 14-01 CLI primitives, and encodes all locked branch semantics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install to runtime and verify structural integrity</name>
  <files>get-shit-done/workflows/discuss-loop.md</files>
  <read_first>
    - package.json (the dev script: `npm run dev` = build:hooks + `node bin/install.js --local`)
  </read_first>
  <action>
1. Run `npm run dev` (source → runtime sync; Pitfall 7 — command stubs, workflows, and agents are not live until installed). Must exit 0.
2. Verify the runtime copies exist: `.claude/commands/gsd2/discuss-loop.md`, `.claude/get-shit-done/workflows/discuss-loop.md`, `.claude/agents/gsd-lens-skeptic.md`, `.claude/agents/gsd-lens-user-advocate.md`, `.claude/agents/gsd-lens-architect.md` (project-local runtime), and that the installed workflow still contains `discuss-loop transcript` (path transform did not mangle the CLI calls).
3. Run the full test suite: `node scripts/run-tests.cjs` — must exit 0.
4. Update `.planning/v1.6/phases/14-multi-lens-discussion-loop/14-VALIDATION.md`: flip the Status column to ✅ green for every 14-01 row (the unit contracts now pass) and set `wave_0_complete: true` in its frontmatter.
  </action>
  <verify>ls .claude/agents/gsd-lens-*.md | wc -l → 3; node scripts/run-tests.cjs exits 0</verify>
  <acceptance_criteria>
    - `npm run dev` exits 0
    - All five runtime files listed above exist
    - `grep -c "discuss-loop transcript" .claude/get-shit-done/workflows/discuss-loop.md` ≥ 4
    - `node scripts/run-tests.cjs` exits 0
    - 14-VALIDATION.md frontmatter contains `wave_0_complete: true`
  </acceptance_criteria>
  <done>Source and runtime are in sync; full suite green; validation doc reflects green unit contracts.</done>
</task>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 3: Live smoke run — converge-or-escalate end to end</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/v1.6/phases/14-multi-lens-discussion-loop/14-AGENT-SPEC.md (Test Contracts TC-ORCH-interactive — the observables being confirmed)
  </read_first>
  <action>
Before pausing, prepare the smoke fixture so the human only has to run one command: create `$TMPDIR/discuss-loop-fixture.md` (or `.planning/tmp/discuss-loop-fixture.md`) — a short CONTEXT.md-style document (~40 lines) containing one deliberate hidden assumption (e.g. "all users have GSD_RUN_ID set in every session") and one user-workflow regression (e.g. "every question is re-asked at each phase boundary"). Record a `wc -c` checksum of any existing MAILBOX.jsonl files under `.planning/run/*/`.

Then present to the user:

**Run (in a fresh Claude Code session in this repo, NO GSD_RUN_ID set):**
```
/gsd2:discuss-loop <fixture-path> --question "Is this context document sound as a basis for planning?"
```

**Confirm these observables (AGENT-SPEC TC-ORCH-interactive + ROADMAP success criteria):**
1. Three lens spawns per round, run in parallel; each produces a DISTINCT position (skeptic flags the hidden assumption; user-advocate flags the workflow regression) — positions quote the fixture verbatim in their anchors.
2. `.planning/discuss-loop/loop-*/transcript.jsonl` exists with `loop_start`, ≥3 `position` records, a `round_delta` per round, and a terminal `loop_end` — and the round_delta records show the constraint delta (new ids in round 1; carried/converged movement in later rounds).
3. The loop either converges early (verdict presented in-session, NO ledger write) or runs 3 rounds and presents labeled divergent positions in-session.
4. No MAILBOX.jsonl changed (compare checksums) — interactive runs never touch the mailbox.
</action>
  <verify>Human confirms all four observables; transcript file inspected</verify>
  <acceptance_criteria>
    - Human reports the loop ran end-to-end without manual intervention
    - transcript.jsonl contains all record types listed in observable 2 (checkable by reading the file)
    - MAILBOX.jsonl checksums unchanged
  </acceptance_criteria>
  <done>A live interactive run satisfies the LOOP-01 grounding and LOOP-02 brake/bifurcation observables.</done>
</task>

</tasks>

<verification>
- Workflow file passes all Task 1 grep criteria; runtime copies exist and match
- Full test suite green (`node scripts/run-tests.cjs`)
- Live smoke run confirmed by human (Task 3)
- Autonomous-path mechanics (mailbox `pending` entry, ledger record) are covered by the 14-01 unit contracts for the primitives plus the existing mailbox/ledger gate tests; full autonomous end-to-end is exercised when the Phase 13/15 runner wires discuss-loop in (out of scope here, per CONTEXT Deferred)
</verification>

<success_criteria>
- `/gsd2:discuss-loop <artifact-path | --decision dec-NNN>` runs up to 3 rounds of three-lens judgment with fresh-context parallel spawns
- Convergence yields a structured verdict with a transcript-verifiable round-over-round constraint delta; converged modifications route through the escalation contract
- Non-convergence escalates labeled, never-synthesized lens positions — mailbox (`status: pending`) only when `--auto` + `GSD_RUN_ID`, in-session otherwise
</success_criteria>

<output>
After completion, create `.planning/v1.6/phases/14-multi-lens-discussion-loop/14-03-SUMMARY.md`
</output>
