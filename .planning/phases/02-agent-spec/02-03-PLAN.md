---
phase: 02-agent-spec
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - get-shit-done/workflows/agent-spec-phase.md
  - commands/gsd2/agent-spec-phase.md
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements:
  - SPEC-02
  - SPEC-03
user_setup: []
must_haves:
  truths:
    - agent-spec-phase command exists and loads agent-spec-phase.md workflow
    - agent-spec-phase.md spawns gsd-agent-researcher with 10-dimension consultant character
    - agent-spec-phase.md spawns gsd-agent-checker that validates 10 dimensions
    - Revision loop runs max 2 iterations when checker returns ISSUES FOUND
    - discuss-phase auto_advance step shows agent-spec-phase as next step for agentic domains
    - Researcher prompt references AGENTIC-PATTERNS.md for topology education
    - Checker validates that every roster agent has at least one communication contract
    - Checker validates that observability section names a tracing tool and describes boundary logging
  artifacts:
    - get-shit-done/workflows/agent-spec-phase.md
    - commands/gsd2/agent-spec-phase.md
    - get-shit-done/workflows/discuss-phase.md
  key_links:
    - commands/gsd2/agent-spec-phase.md loads workflows/agent-spec-phase.md via @reference
    - agent-spec-phase.md spawns gsd-agent-researcher registered in model-profiles.cjs by Plan 02
    - agent-spec-phase.md spawns gsd-agent-checker registered in model-profiles.cjs by Plan 02
    - agent-spec-phase.md writes AGENT-SPEC.md using template from Plan 01
    - Researcher references references/AGENTIC-PATTERNS.md created by Plan 01
    - discuss-phase.md auto_advance step conditionally shows agent-spec-phase
---

<objective>
Create the agent-spec-phase workflow, its slash command entry point, and the discuss-phase trigger. The workflow orchestrates gsd-agent-researcher (consultant character) and gsd-agent-checker (10-dimension validator) with a revision loop, following the exact ui-phase.md pattern. The discuss-phase update surfaces `/gsd2:agent-spec-phase` as a next step when the domain is Agentic.
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-spec/02-CONTEXT.md
@.planning/phases/02-agent-spec/02-RESEARCH.md

<interfaces>
From Plan 01:
  - get-shit-done/templates/AGENT-SPEC.md exists with 10 dimension sections + test contracts + checker sign-off
  - get-shit-done/references/AGENTIC-PATTERNS.md exists with 6 topology patterns

From Plan 02:
  - model-profiles.cjs has gsd-agent-researcher (quality opus, balanced sonnet, budget haiku)
  - model-profiles.cjs has gsd-agent-checker (quality sonnet, balanced sonnet, budget haiku)
  - config.cjs has workflow.agent_spec and workflow.agent_spec_gate in VALID_CONFIG_KEYS
  - init plan-phase returns has_agent_spec and agent_spec_path

ui-phase.md workflow structure (12 steps):
  1. Initialize (init plan-phase, resolve-model, config-get)
  2. Parse and Validate Phase
  3. Check Prerequisites (CONTEXT.md, RESEARCH.md)
  4. Check Existing Spec
  5. Spawn Researcher
  6. Handle Researcher Return
  7. Spawn Checker
  8. Handle Checker Return
  9. Revision Loop (max 2)
  10. Present Final Status
  11. Commit
  12. Update State

commands/gsd2/ui-phase.md structure:
  frontmatter with name, description, argument-hint, allowed-tools
  body with objective, execution_context (@workflow + @reference), context, process
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create agent-spec-phase.md workflow</name>
  <files>get-shit-done/workflows/agent-spec-phase.md</files>
  <read_first>
    - get-shit-done/workflows/ui-phase.md (exact structural pattern to follow -- all 12 steps)
    - .planning/phases/02-agent-spec/02-CONTEXT.md (researcher character as technical consultant, 10 dimensions, 3 groupings)
    - .planning/phases/02-agent-spec/02-RESEARCH.md (researcher character definition, checker dimensions, anti-patterns)
    - get-shit-done/templates/AGENT-SPEC.md (template the researcher fills -- created by Plan 01)
    - get-shit-done/references/AGENTIC-PATTERNS.md (topology reference the researcher draws from -- created by Plan 01)
  </read_first>
  <action>
Create `get-shit-done/workflows/agent-spec-phase.md` following the exact 12-step structure of ui-phase.md. Below are the step-by-step contents. Replicate the markdown structure, bash code blocks, and Task() call format from ui-phase.md exactly.

PURPOSE BLOCK: Same XML tags as ui-phase.md. Purpose text: "Generate an agent system design contract (AGENT-SPEC.md) for agentic phases. Orchestrates gsd-agent-researcher and gsd-agent-checker with a revision loop. Sits between discuss-phase and plan-phase; locks agent architecture, communication contracts, topology, observability, and security decisions before the planner creates tasks."

REQUIRED_READING: `@~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md`

STEP 1 INITIALIZE: Same as ui-phase.md but resolve `gsd-agent-researcher` and `gsd-agent-checker`, and check `workflow.agent_spec` config key:
  AGENT_RESEARCHER_MODEL via resolve-model gsd-agent-researcher
  AGENT_CHECKER_MODEL via resolve-model gsd-agent-checker
  AGENT_SPEC_ENABLED via config-get workflow.agent_spec (default true)
  If AGENT_SPEC_ENABLED is false: display "Agent spec phase is disabled in config." Exit.
  If planning_exists is false: Error -- run /gsd2:new-project first.

STEP 2 PARSE AND VALIDATE PHASE: Identical to ui-phase step 2.

STEP 3 CHECK PREREQUISITES: Same warnings as ui-phase step 3.

STEP 4 CHECK EXISTING AGENT-SPEC: Same as ui-phase step 4 but ls for *-AGENT-SPEC.md. Same 3 options: Update / View / Skip.

STEP 5 SPAWN GSD-AGENT-RESEARCHER: This is the KEY differentiation from ui-phase. The researcher has a consultant character, not a form-filler.

Display banner with "GSD > AGENT SYSTEM DESIGN CONTRACT -- PHASE {N}" and "Spawning agent system researcher..."

Build the researcher prompt INLINE (not a separate agent .md file). The prompt must include this persona definition:

"You are a senior agentic systems consultant. Your job is to have a genuine conversation with the user about their agentic system architecture, educate them on tradeoffs, and fill out an AGENT-SPEC.md design contract.

Your character:
- You are NOT a form-filler. You are an expert who challenges overcomplicated approaches.
- If the user proposes a complex framework (like LangGraph) for a linear pipeline, push back and explain why a simpler chain approach would work better.
- Explain consequences of design choices. Example: If you choose X, debugging will look like Y. If you choose Z instead, you get W for free.
- At every agent boundary, specifically ask: what gets logged, what state is tracked, how to diagnose which agent failed.
- The agentic domain is newer than UI -- your educational role is higher priority here. Users often do not know what they do not know.

Your approach:
1. Open by showing what you detected from the codebase: existing agent files, framework imports, deployment patterns.
2. Map the phase goal to the closest topology pattern from the reference doc. Challenge the match if simpler would work.
3. Cover all 10 design dimensions through natural conversation, not a numbered list. Order follows what the user opens with.
4. For Design Decisions (dimensions 1-5): ask, educate on tradeoffs, let user decide.
5. For Cross-Cutting Concerns (dimensions 6-9): force a decision. Explain what happens if they skip it. Do not accept TBD or standard logging for observability.
6. For Lightweight Capture (dimension 10): ask briefly, note the answer, move on.
7. At each agent-to-agent boundary: define the message shape, what happens when it is malformed, and what gets logged.

Reference document: Read ~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md for topology patterns, tradeoffs, failure modes, and observability requirements. Draw from this when educating the user."

The prompt objective: "Create agent system design contract for Phase {phase_number}: {phase_name}. Answer: What architecture, communication, and observability contracts does this agentic system need?"

Files_to_read: state_path, roadmap_path, requirements_path, context_path, research_path, ~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md

Output: Write to {phase_dir}/{padded_phase}-AGENT-SPEC.md using template ~/.claude/get-shit-done/templates/AGENT-SPEC.md

Rules (include these verbatim in the prompt):
- Every section in the template must have content. No section left as placeholder.
- The Observability section MUST name at least one tracing tool, describe boundary-level logging for each agent pair, and include a failure diagnosis path.
- Every agent in the roster MUST have at least one communication contract defined.
- The Rationale subsection in each dimension MUST explain WHY, not just repeat WHAT.
- Test contracts MUST use the scenario/observable/pass-criteria format from TEST-SPEC.md.
- Include at least one test contract per agent in the roster.

Task() call with subagent_type="gsd-agent-researcher", model=AGENT_RESEARCHER_MODEL.

STEP 6 HANDLE RESEARCHER RETURN: Same pattern as ui-phase step 6. Check for "## AGENT-SPEC COMPLETE" or "## AGENT-SPEC BLOCKED".

STEP 7 SPAWN GSD-AGENT-CHECKER: Display banner "GSD > VERIFYING AGENT-SPEC" and "Spawning agent spec checker..."

Build checker prompt INLINE with this persona:

"You are an agent system architecture reviewer. Validate the AGENT-SPEC.md against 10 dimensions. Return APPROVED or BLOCKED.

Validation dimensions -- each is binary against concrete criteria:

1. Agent Roster: PASS if every agent has name, role description, model profile, tools list. FAIL if any agent is listed without all four fields.
2. Orchestration Pattern: PASS if pattern is named, rationale explains why simpler will not work, reference to AGENTIC-PATTERNS.md is present. FAIL if pattern is unnamed or rationale is missing.
3. Communication Contracts: PASS if every agent pair that communicates has message shape (as code block), malformed-message handling, and the number of contracts >= number of agents minus 1. FAIL if any communicating pair lacks a contract.
4. Human-in-the-Loop: PASS if at least one decision point is defined with gate type and trigger condition. FLAG if section says none needed (acceptable but noteworthy). FAIL if section is empty.
5. Memory Strategy: PASS if at least one memory type is described with scope and mechanism. FLAG if no persistent memory needed is stated with rationale. FAIL if section is empty.
6. Observability: PASS if and only if (a) at least one tracing tool is named, (b) at least one boundary-level log statement is described per agent pair, (c) at least one failure diagnosis path is stated. FAIL if any of a, b, c is missing. TBD or standard logging is an automatic FAIL.
7. Reflection: PASS if trigger condition, metrics, and failure response are defined. FLAG if no reflection needed with rationale. FAIL if empty.
8. Security/Permissions: PASS if every agent has allowed/denied tools listed and scope constraints defined. FAIL if any agent lacks permission boundaries.
9. Error Handling: PASS if at least one failure scenario per agent is defined with detection + recovery. FAIL if section is empty or has fewer scenarios than agents.
10. Reasoning Approach: PASS if 3+ bullet points describe task decomposition approach. FLAG if fewer than 3. FAIL if empty.

Blocking criteria: Any dimension at FAIL = BLOCKED. All dimensions PASS or FLAG = APPROVED.

Additional cross-checks:
- Number of communication contracts should be >= (number of roster agents - 1). If not, FLAG with explanation.
- Test contracts section should have >= 1 contract per roster agent. If not, FLAG.
- Every Rationale subsection should contain because or equivalent causal reasoning. If any is purely descriptive (restates WHAT without WHY), FLAG that dimension."

Checker files_to_read: AGENT-SPEC.md (primary), context_path, research_path, AGENTIC-PATTERNS.md.

Checker return format -- approved uses heading "## AGENT-SPEC VERIFIED" with 10 dimension checkboxes and "Approval: approved {date}". Blocked uses heading "## ISSUES FOUND" with Critical (must fix) and Recommended (non-blocking) subsections.

Task() call with subagent_type="gsd-agent-checker", model=AGENT_CHECKER_MODEL.

STEP 8 HANDLE CHECKER RETURN: Same as ui-phase step 8. "## AGENT-SPEC VERIFIED" proceeds to step 10. "## ISSUES FOUND" proceeds to step 9.

STEP 9 REVISION LOOP (MAX 2 ITERATIONS): Same structure as ui-phase step 9. If revision_count < 2, re-spawn researcher with revision prompt (paste blocking issues, fix ONLY listed issues, do NOT re-ask answered questions). After max iterations, AskUserQuestion with 3 options: Force approve / Edit manually / Abandon.

STEP 10 PRESENT FINAL STATUS: Banner "GSD > AGENT-SPEC READY". Show phase name, dimensions passed count, any FLAG recommendations. Next up: /gsd2:plan-phase {N} with /clear reminder.

STEP 11 COMMIT: Same pattern as ui-phase. Commit message: "docs(${padded_phase}): agent system design contract"

STEP 12 UPDATE STATE: Same pattern. Stopped at "Phase ${PHASE} AGENT-SPEC approved".

CLOSING: success_criteria checklist (12 items matching the 12 steps).
  </action>
  <verify>
Run: `grep -c "^## " get-shit-done/workflows/agent-spec-phase.md` -- expect 12 (steps 1-12).
Run: `grep "gsd-agent-researcher" get-shit-done/workflows/agent-spec-phase.md` -- at least 3 matches (resolve-model, Task call, revision).
Run: `grep "gsd-agent-checker" get-shit-done/workflows/agent-spec-phase.md` -- at least 2 matches (resolve-model, Task call).
Run: `grep "AGENTIC-PATTERNS" get-shit-done/workflows/agent-spec-phase.md` -- at least 2 matches (required_reading, researcher files_to_read).
Run: `grep "workflow.agent_spec" get-shit-done/workflows/agent-spec-phase.md` -- at least 1 match.
Run: `grep "Dimension 6 Observability" get-shit-done/workflows/agent-spec-phase.md` -- at least 1 match (checker criteria).
Run: `grep "revision_count" get-shit-done/workflows/agent-spec-phase.md` -- at least 1 match.
Run: `grep "AGENT-SPEC VERIFIED" get-shit-done/workflows/agent-spec-phase.md` -- at least 1 match.
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/agent-spec-phase.md exists
    - File has 12 numbered step headings (## 1 through ## 12)
    - Step 1 resolves gsd-agent-researcher and gsd-agent-checker models and checks workflow.agent_spec config
    - Step 5 researcher prompt contains senior agentic systems consultant character definition
    - Step 5 researcher prompt includes AGENTIC-PATTERNS.md in files_to_read
    - Step 5 researcher rules require no empty sections, observability must name tracing tool, every roster agent needs communication contract
    - Step 7 checker prompt defines 10 binary dimensions with concrete pass/fail criteria
    - Step 7 checker Dimension 6 Observability requires tracing tool named AND boundary logging described AND failure diagnosis path stated
    - Step 9 revision loop has max 2 iterations with force-approve/edit/abandon options
    - Step 11 commits AGENT-SPEC.md using gsd-tools commit
    - Step 12 updates state with AGENT-SPEC approved
    - purpose block references AGENTIC-PATTERNS.md in required_reading
  </acceptance_criteria>
  <done>agent-spec-phase.md workflow exists with 12 steps, consultant-character researcher, 10-dimension checker, and revision loop</done>
</task>

<task type="auto">
  <name>Task 2: Create command entry point and update discuss-phase</name>
  <files>
    commands/gsd2/agent-spec-phase.md
    get-shit-done/workflows/discuss-phase.md
  </files>
  <read_first>
    - commands/gsd2/ui-phase.md (command entry point pattern -- frontmatter + objective + execution_context + context + process)
    - get-shit-done/workflows/discuss-phase.md (auto_advance step at the end -- where next steps are displayed, currently shows /gsd2:ui-phase for frontend work)
  </read_first>
  <action>
**commands/gsd2/agent-spec-phase.md** -- Create new file following the exact structure of `commands/gsd2/ui-phase.md`. Use these exact values:

FRONTMATTER (between triple-dash delimiters, same as ui-phase.md):
  name: gsd2:agent-spec-phase
  description: Generate agent system design contract (AGENT-SPEC.md) for agentic phases
  argument-hint: "[phase]"
  allowed-tools: Read, Write, Bash, Glob, Grep, Task, WebFetch, AskUserQuestion, mcp__context7__*

BODY (same XML tag structure as ui-phase.md):
  objective: "Create an agent system design contract (AGENT-SPEC.md) for an agentic phase. Orchestrates gsd-agent-researcher and gsd-agent-checker. Flow: Validate -> Research Architecture -> Verify AGENT-SPEC -> Done"
  execution_context: two @references:
    @~/.claude/get-shit-done/workflows/agent-spec-phase.md
    @~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md
  context: "Phase number: $ARGUMENTS -- optional, auto-detects next unplanned phase if omitted."
  process: "Execute @~/.claude/get-shit-done/workflows/agent-spec-phase.md end-to-end. Preserve all workflow gates."

**discuss-phase.md** -- Find the `auto_advance` step (near end of file, around line 483) where the "Also available" section shows:
  `/gsd2:ui-phase ${PHASE}` -- generate UI design contract (if frontend work)

Add a new line immediately after it:
  `/gsd2:agent-spec-phase ${PHASE}` -- generate agent system design contract (if agentic work)

This is a minimal change -- one line added to the next-steps display. The domain router already classifies the domain; this just surfaces the command.
  </action>
  <verify>
Run: `head -5 commands/gsd2/agent-spec-phase.md` -- should show frontmatter starting with name: gsd2:agent-spec-phase.
Run: `grep "agent-spec-phase.md" commands/gsd2/agent-spec-phase.md` -- at least 1 match (workflow reference).
Run: `grep "AGENTIC-PATTERNS" commands/gsd2/agent-spec-phase.md` -- at least 1 match.
Run: `grep "agent-spec-phase" get-shit-done/workflows/discuss-phase.md` -- at least 1 match.
Run: `grep -A1 "ui-phase" get-shit-done/workflows/discuss-phase.md | grep "agent-spec-phase"` -- confirms the line is right after ui-phase line.
  </verify>
  <acceptance_criteria>
    - commands/gsd2/agent-spec-phase.md exists
    - Command frontmatter has name: gsd2:agent-spec-phase
    - Command frontmatter has allowed-tools including Task and AskUserQuestion
    - Command execution_context references @~/.claude/get-shit-done/workflows/agent-spec-phase.md
    - Command execution_context references @~/.claude/get-shit-done/references/AGENTIC-PATTERNS.md
    - discuss-phase.md contains /gsd2:agent-spec-phase in the auto_advance next-steps section
    - discuss-phase.md still contains /gsd2:ui-phase (not replaced, added alongside)
  </acceptance_criteria>
  <done>Command entry point exists and discuss-phase surfaces agent-spec-phase as next step for agentic domains</done>
</task>
</tasks>

<verification>
- `ls commands/gsd2/agent-spec-phase.md get-shit-done/workflows/agent-spec-phase.md` -- both exist
- `grep -c "^## " get-shit-done/workflows/agent-spec-phase.md` -- returns 12
- `grep "gsd-agent-researcher" get-shit-done/workflows/agent-spec-phase.md` -- matches
- `grep "gsd-agent-checker" get-shit-done/workflows/agent-spec-phase.md` -- matches
- `grep "senior agentic systems consultant" get-shit-done/workflows/agent-spec-phase.md` -- matches
- `grep "Dimension 6 Observability" get-shit-done/workflows/agent-spec-phase.md` -- matches
- `grep "agent-spec-phase" get-shit-done/workflows/discuss-phase.md` -- matches
- `grep "agent-spec-phase" commands/gsd2/agent-spec-phase.md` -- matches
</verification>

<success_criteria>
- /gsd2:agent-spec-phase command is a valid Claude slash command that loads the workflow
- Workflow follows the exact 12-step ui-phase pattern with agent-spec-specific content
- Researcher has consultant character that educates and challenges, not a form-filler
- Checker has concrete binary criteria for all 10 dimensions, with observability requiring specific evidence
- Revision loop caps at 2 iterations with user escape hatch
- discuss-phase shows agent-spec-phase as an option for agentic domains
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-spec/02-03-SUMMARY.md`
</output>
