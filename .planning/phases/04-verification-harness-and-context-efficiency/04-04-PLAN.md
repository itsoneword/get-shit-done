---
phase: 04-verification-harness-and-context-efficiency
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - .claude/get-shit-done/workflows/execute-phase.md
  - .claude/get-shit-done/templates/phase-prompt.md
  - .claude/get-shit-done/bin/lib/init.cjs
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - truth: "execute-phase.md fires the verifier loop after a task with verify_after: true completes, before advancing to the next wave"
      artifacts:
        - path: .claude/get-shit-done/workflows/execute-phase.md
          provides: <verify_loop> sub-flow block + post-spot-check splice
          contains: "verify_after"
      verify:
        - cmd: "grep -q 'verify_after' .claude/get-shit-done/workflows/execute-phase.md && grep -qE '<verify_loop>|verify loop sub-flow|## Verify Loop' .claude/get-shit-done/workflows/execute-phase.md"
          expect: ""
          type: unit
    - truth: "Loop respects the iteration ceiling of 3 and surfaces ceiling-reached as a CHECKPOINT REACHED block"
      artifacts:
        - path: .claude/get-shit-done/workflows/execute-phase.md
          provides: ceiling logic + CHECKPOINT REACHED handoff
          contains: "ceiling-reached"
      verify:
        - cmd: "grep -q 'ceiling-reached' .claude/get-shit-done/workflows/execute-phase.md && grep -q 'iteration_count\\|iterations_attempted\\|max_iterations' .claude/get-shit-done/workflows/execute-phase.md"
          expect: ""
          type: unit
    - truth: "Each loop agent is spawned in a fresh context — orchestrator constructs files_to_read explicitly per role and never passes executor work logs to verifier or investigator"
      artifacts:
        - path: .claude/get-shit-done/workflows/execute-phase.md
          provides: per-role files_to_read construction rule
          contains: "fresh context"
      verify:
        - cmd: "grep -qE 'fresh context|fresh-context' .claude/get-shit-done/workflows/execute-phase.md"
          expect: ""
          type: unit
    - truth: "Loop debug file path follows .planning/debug/{plan-slug}-verify-loop.md convention"
      artifacts:
        - path: .claude/get-shit-done/workflows/execute-phase.md
        - path: .claude/get-shit-done/templates/phase-prompt.md
      verify:
        - cmd: "grep -q 'verify-loop.md' .claude/get-shit-done/workflows/execute-phase.md"
          expect: ""
          type: unit
    - truth: "verify_after: true task attribute is documented in phase-prompt.md template alongside the must_haves verify: schema"
      artifacts:
        - path: .claude/get-shit-done/templates/phase-prompt.md
          provides: verify_after task attribute documentation
          contains: "verify_after"
      verify:
        - cmd: "grep -cE 'verify_after.*true|verify_after:\\s+true' .claude/get-shit-done/templates/phase-prompt.md"
          expect: "^[1-9][0-9]*$"
          type: unit
    - truth: "init execute-phase surfaces verify config (default-on flag, opt-out flag) in its return JSON"
      artifacts:
        - path: .claude/get-shit-done/bin/lib/init.cjs
          provides: cmdInitExecutePhase with verify config keys
          contains: "auto_verify"
      verify:
        - cmd: "grep -qE 'auto_verify|verify_loop|verify_config' .claude/get-shit-done/bin/lib/init.cjs"
          expect: ""
          type: unit
        - cmd: "node .claude/get-shit-done/bin/gsd-tools.cjs init execute-phase 04-verification-harness-and-context-efficiency --raw 2>&1 | jq -e 'has(\"auto_verify\") or has(\"verify_loop\") or has(\"verify_config\")' 2>&1"
          expect: "true"
          type: integration
  artifacts:
    - path: .claude/get-shit-done/workflows/execute-phase.md
      provides: verify-loop integration
      contains: "verify_after"
    - path: .claude/get-shit-done/templates/phase-prompt.md
      provides: verify_after task attribute docs
      contains: "verify_after"
    - path: .claude/get-shit-done/bin/lib/init.cjs
      provides: verify config in init execute-phase output
      contains: "auto_verify"
  key_links:
    - from: .claude/get-shit-done/workflows/execute-phase.md
      to: .claude/agents/gsd-verifier.md
      via: Task() spawn with loop_input.json or LOOP MODE prompt block (Plan 04-03 contract)
      pattern: "LOOP MODE|loop_input"
    - from: .claude/get-shit-done/workflows/execute-phase.md
      to: .claude/agents/gsd-debugger.md
      via: Task() spawn with goal: find_root_cause_only and symptoms_prefilled: true
      pattern: "find_root_cause_only"
    - from: .claude/get-shit-done/workflows/execute-phase.md
      to: .claude/agents/gsd-fixer.md
      via: Task() spawn with ROOT CAUSE FOUND block input + loop_iteration field
      pattern: "ROOT CAUSE FOUND|loop_iteration"
    - from: .claude/get-shit-done/workflows/execute-phase.md
      to: .planning/debug/{plan-slug}-verify-loop.md
      via: orchestrator writes structured tagged log lines per AGENT-SPEC §Observability
      pattern: "verify-loop.md"
---

<objective>
Splice the verifier loop into `execute-phase.md` so that completing a task marked `verify_after: true` automatically fires verifier → investigator → fixer with fresh contexts and a 3-iteration ceiling. Document the `verify_after` task attribute in the plan template. Surface verify config (default-on, opt-out) from `init execute-phase`. The integration uses the loop-mode primitives delivered by Plan 04-03; this plan does not redefine the contracts — it consumes them.

Architecture truth: `.planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md`. Specifically:
- §"Communication Contracts" — all 7 message shapes (orchestrator↔verifier, ↔investigator, ↔fixer, ceiling-reached handoff)
- §"Human-in-the-Loop Boundaries" — ceiling-reached is a `block` escalation
- §"Memory Strategy" — debug file at `.planning/debug/{plan-slug}-verify-loop.md`
- §"Observability" — structured tagged log lines per boundary
- §"Reasoning & Planning Approach" — orchestrator owns sequencing, agents are stateless

This plan has `autonomous: false` because the very feature it ships (CHECKPOINT REACHED ceiling-reached) is a checkpoint the executor will encounter while dogfooding it. Mark `verify_after: true` on tasks here so the harness validates itself on the next phase that runs after Plan 04-04 lands.
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-01-PLAN.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-03-PLAN.md
</context>

<interfaces>
**Splice location in execute-phase.md (RESEARCH §2):** between current step 5 (spot-check SUMMARY.md, lines 258–278 — ends "If all pass, report what was built") and current step 6 (handle failures, lines 284–288). Concretely: after "If all pass, report what was built" ends, insert the verify-loop sub-flow.

**Existing structure to preserve:** `verify_phase_goal` step at lines 473–499 fires once at end of all waves and uses gsd-verifier in standalone mode. Do NOT remove or alter this — the per-task loop is a complement, not a replacement.

**Per-role files_to_read construction (AGENT-SPEC §Reasoning + §Security):**
- **verifier (loop):** `[plan_path, must_haves_slice as inline JSON or scoped file]` — explicitly NOT executor SUMMARY.md, not task work logs.
- **investigator:** `[verifier_failure_report (file or inline JSON), files_implicated from prior fixer attempts only, plan_path]` — explicitly NOT executor SUMMARY.md, NOT current iteration's fixer output (only prior_fix_attempts).
- **fixer:** `[investigator_hypothesis (## ROOT CAUSE FOUND block), plan_slice, recent_diff via 'git diff HEAD~1' or similar]`.

**Loop debug file (AGENT-SPEC §Memory):** `.planning/debug/{plan-slug}-verify-loop.md`. Created on loop entry; structured frontmatter with `status`, `iteration`, `max_iterations: 3`, `trace_id`. Append-only Evidence and Eliminated sections survive iteration boundaries. Directory `.planning/debug/` does NOT exist today (RESEARCH §6) — orchestrator creates it on demand via `mkdir -p` before first write.

**CHECKPOINT REACHED ceiling-reached block shape (AGENT-SPEC §"orchestrator → user"):**

```
## CHECKPOINT REACHED
type: ceiling-reached
task_id: <id>
plan_path: <path>
iterations_attempted: 3
debug_file_path: .planning/debug/{plan-slug}-verify-loop.md
chronological_narrative:
  - iteration: 1
    verifier_result: <summary>
    investigator_hypothesis: <summary>
    fixer_commit: <sha or null>
    re_verify_result: <summary>
  - iteration: 2
    ...
  - iteration: 3
    ...
final_gaps:
  - truth: <text>
    reason: <text>
```

**Iteration ceiling: 3** (hardcoded, not configurable per AGENT-SPEC §HITL). Early exit on classification = `not-yet-built` or `unrelated` (regardless of iteration count).

**`auto_verify: false` opt-out at plan level** (CONTEXT §Default Behavior): if a PLAN.md frontmatter contains `auto_verify: false`, the loop does NOT fire for any task in that plan. Default is ON.
</interfaces>

<tasks>

<task type="auto" verify_after="true">
  <name>Task 1: Document verify_after task attribute and auto_verify plan flag in phase-prompt.md template</name>
  <files>.claude/get-shit-done/templates/phase-prompt.md</files>
  <read_first>
    - .claude/get-shit-done/templates/phase-prompt.md (current task attribute documentation; the verify: block additions from Plan 04-03 Task 2)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Reasoning & Planning Approach" → "Planner note"
    - .planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md §"Default Behavior" and §"Verification Trigger"
  </read_first>
  <action>
1. Locate the task attribute documentation section (where existing attributes like `tdd="true"` are documented). If no such section exists yet, add one titled `### Task Attributes` immediately after the task example block.

2. Add a `verify_after` attribute entry:

```markdown
**`verify_after="true"`** — when this task completes successfully, fire the verify-loop (verifier → investigator → fixer, max 3 iterations) before advancing. The loop runs the `verify:` commands authored under each `must_haves.truths[].verify[]` entry. If all pass, execution continues silently. If any fail and the loop cannot resolve within 3 iterations, execution pauses with a `## CHECKPOINT REACHED` block of `type: ceiling-reached`. Default: not set (no automatic verification). Author this attribute on tasks whose completion can be checked by an automated command — e.g. an API responds, a CLI subcommand returns expected output, a file contains an expected string.
```

3. Add a frontmatter-level documentation entry for `auto_verify`:

```markdown
**`auto_verify: false`** (plan frontmatter, optional) — opt out of the verify-loop for the entire plan. When absent (default behavior), the loop fires for any task with `verify_after="true"`. Use sparingly — silent skips are harder to spot than visible loop runs.
```

4. Cross-link to the verify schema added in Plan 04-03 Task 2 — single sentence: "See §The verify: block (executable assertions) for authoring `verify:` commands consumed by this loop."

5. Do not modify the existing must_haves verify schema docs (Plan 04-03 owns them).
  </action>
  <verify>
grep -cE 'verify_after.*true|verify_after:\s+true' .claude/get-shit-done/templates/phase-prompt.md && grep -c 'auto_verify' .claude/get-shit-done/templates/phase-prompt.md && grep -c 'ceiling-reached' .claude/get-shit-done/templates/phase-prompt.md
  </verify>
  <acceptance_criteria>
    - `grep -cE 'verify_after' .claude/get-shit-done/templates/phase-prompt.md` returns ≥ 2 (attribute name + at least one example)
    - `grep -q 'auto_verify' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - `grep -q 'ceiling-reached' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - The verify_after entry appears in a `### Task Attributes` section: `grep -q '### Task Attributes' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - Cross-link to the verify: block exists: `grep -q 'verify: block' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - Existing `tdd` docs (if present) still appear: `grep -c 'tdd' .claude/get-shit-done/templates/phase-prompt.md` is unchanged or greater than baseline
  </acceptance_criteria>
  <done>Template documents verify_after task attribute, auto_verify plan flag, and cross-links to the verify: schema.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 2: Add verify config keys to init execute-phase return JSON</name>
  <files>.claude/get-shit-done/bin/lib/init.cjs</files>
  <read_first>
    - .claude/get-shit-done/bin/lib/init.cjs (locate `cmdInitExecutePhase` — typically returns plan metadata for the phase)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Memory Strategy" (debug file path convention)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md §"Default Behavior" (default ON, opt-out per plan)
  </read_first>
  <action>
In `.claude/get-shit-done/bin/lib/init.cjs::cmdInitExecutePhase`, extend the returned JSON with a `verify_loop` block (top-level key) computed per-plan:

```js
verify_loop: {
  default_enabled: true,
  max_iterations: 3,
  debug_dir: ".planning/debug",
  per_plan: {
    "<plan-id>": {
      auto_verify: <true unless PLAN.md frontmatter sets auto_verify: false>,
      verify_after_tasks: [<list of task names or indices that have verify_after: true>]
    }
  }
}
```

Implementation:

1. After the existing plan enumeration that builds the plans array, iterate the same plan files. For each, parse the frontmatter via the existing `extractFrontmatter` helper.
2. `auto_verify`: read `frontmatter.auto_verify`. If set to `false`, mark `auto_verify: false`. Otherwise default to `true`.
3. `verify_after_tasks`: scan the PLAN.md body for `<task ...>` blocks containing `verify_after="true"` (or `verify_after: true` in YAML-style attributes). Record the task `<name>` value or its 1-based index. Use a regex like `/<task[^>]*verify_after="true"[^>]*>([\s\S]*?)<\/task>/g` and within each match extract the `<name>...</name>` content.
4. Add the assembled `verify_loop` object to the existing return value before the JSON output step.

Do NOT change any existing return keys. The new key is additive.
  </action>
  <verify>
node .claude/get-shit-done/bin/gsd-tools.cjs init execute-phase 04-verification-harness-and-context-efficiency --raw 2>&1 | jq -e '.verify_loop != null and .verify_loop.max_iterations == 3 and (.verify_loop.per_plan | type == "object")'
  </verify>
  <acceptance_criteria>
    - `grep -qE 'verify_loop|auto_verify' .claude/get-shit-done/bin/lib/init.cjs` returns 0
    - `node .claude/get-shit-done/bin/gsd-tools.cjs init execute-phase 04-verification-harness-and-context-efficiency --raw | jq -e '.verify_loop'` exits 0
    - `jq '.verify_loop.max_iterations'` returns `3`
    - `jq '.verify_loop.default_enabled'` returns `true`
    - `jq '.verify_loop.debug_dir'` returns `".planning/debug"`
    - `jq '.verify_loop.per_plan | keys | length'` returns ≥ 1 for this phase (which has 4 plans)
    - `jq '.verify_loop.per_plan["04-04"].verify_after_tasks | length'` returns ≥ 1 (this plan has verify_after tasks)
    - Calling `init execute-phase` for an older phase WITHOUT verify_after tasks still produces valid output (regression — `verify_after_tasks: []` is acceptable, missing key is not)
    - No existing keys removed: `jq 'has("plans") and has("phase")'` still returns true
  </acceptance_criteria>
  <done>cmdInitExecutePhase returns a verify_loop block with default_enabled, max_iterations, debug_dir, and per-plan auto_verify + verify_after_tasks.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 3: Splice verify-loop sub-flow into execute-phase.md</name>
  <files>.claude/get-shit-done/workflows/execute-phase.md</files>
  <read_first>
    - .claude/get-shit-done/workflows/execute-phase.md (lines 158-316 task loop; specifically lines 258-278 spot-check; lines 284-288 failure handling; lines 473-499 verify_phase_goal — preserve, do not alter)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md (ALL of §Communication Contracts and §Observability)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §2 (exact splice location, parallel-wave serialization)
    - .claude/agents/gsd-verifier.md (loop-mode contract — Plan 04-03 Task 4)
    - .claude/agents/gsd-fixer.md (loop-mode contract — Plan 04-03 Task 5)
    - .claude/agents/gsd-debugger.md (find_root_cause_only mode — pre-existing)
  </read_first>
  <action>
1. Add a new `<verify_loop>` sub-flow block to `execute-phase.md`, AFTER the existing `<step name="execute_waves">` block and BEFORE `<step name="verify_phase_goal">`. Block contents:

```markdown
<sub_flow name="verify_loop">

**Trigger:** any task with `verify_after="true"` in a PLAN.md whose plan frontmatter does NOT set `auto_verify: false`.

**Source of truth for contracts:** `.planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md` §Communication Contracts. Every JSON message shape below is reproduced from that spec.

**Pre-condition:** spot-check for the plan passed (existing step 5 in execute_waves).

**Procedure:**

1. **Setup:** ensure `.planning/debug/` exists (`mkdir -p .planning/debug`). Compute `plan_slug = <phase>-<plan>` (e.g. `04-04`). Compute `debug_file = .planning/debug/{plan_slug}-verify-loop.md`. Initialize debug file with frontmatter `status: verifying`, `iteration: 1`, `max_iterations: 3`, `trace_id: <uuid or timestamp+random>`, `created: <ISO ts>`.

2. **Iteration loop, max 3:**

   a. **Spawn loop-verifier (fresh context):** Task() with `subagent_type=gsd-verifier`. `<files_to_read>` MUST contain ONLY: `[PLAN.md path, debug_file path if iteration > 1]`. Append a `## LOOP MODE` block to the prompt with the JSON input shape:
      ```json
      {"plan_path": "<>", "task_id": "<>", "verify_commands": [<from must_haves>], "must_haves_slice": [<>], "iteration": <N>, "trace_id": "<>", "debug_file_path": "<>"}
      ```
      Append `boundary.validate` log line to debug file.

   b. **Parse `## LOOP VERIFY RESULT` block** from verifier output. Append `agent.return agent=verifier status=<> iter=<N> trace_id=<>` log line to debug file.

   c. If `status == "pass"`: append `loop.end status=pass iterations=<N>` log line. Continue executor to next task. **Do NOT emit any user-visible block.** Skip to step 3 (cleanup).

   d. If `status == "fail"` AND `iteration < 3`:

      i. **Spawn loop-investigator (fresh context):** Task() with `subagent_type=gsd-debugger`. Pass `goal: find_root_cause_only` and `symptoms_prefilled: true` flags. `<files_to_read>` MUST contain ONLY: `[verifier_report (extract gaps), files_implicated from prior_fix_attempts if any, PLAN.md path, debug_file path]`. Explicitly DO NOT pass executor SUMMARY.md, executor work logs, or the just-written code's diff in iteration 1. Construct the input message per §"loop-orchestrator → loop-investigator" contract including `prior_fix_attempts` array (empty on iteration 1).

      ii. **Parse `## ROOT CAUSE FOUND` block** from investigator output. Append `agent.return agent=investigator classification=<> iter=<N>` log line.

      iii. **If classification ∈ {`not-yet-built`, `unrelated`}:** fire ceiling-reached IMMEDIATELY (skip remaining iterations). Use the `chronological_narrative` shape from step 4 below with only the iterations actually run.

      iv. **Spawn loop-fixer (fresh context):** Task() with `subagent_type=gsd-fixer`. `<files_to_read>` MUST contain ONLY: `[investigator hypothesis ROOT CAUSE FOUND block, PLAN.md path, recent_diff via 'git diff HEAD~1 -- <files_implicated>', debug_file path]`. Construct input per §"loop-orchestrator → loop-fixer" contract including `loop_iteration: <N>`.

      v. **Parse `## FIXES COMPLETE (loop)` block** from fixer output. Append `agent.return agent=fixer status=<> commit=<> iter=<N>` log line. Capture `commit_hash` for `prior_fix_attempts` on the next iteration.

      vi. Increment `iteration`. Update debug file frontmatter `iteration: <new value>`. Loop back to step 2a.

   e. If `status == "fail"` AND `iteration == 3`: fall through to step 4 (ceiling reached).

3. **Cleanup on pass:** update debug file `status: resolved`. Append final log line `loop.end status=pass`. (Optionally archive — not required for v1.)

4. **Ceiling-reached handoff:** emit a `## CHECKPOINT REACHED` block to the user with the shape per §"orchestrator → user (ceiling-reached)". Update debug file `status: ceiling_reached`. Pause executor. Block indefinitely — do NOT default-approve, do NOT default-deny, do NOT advance to the next wave.

   ```
   ## CHECKPOINT REACHED
   type: ceiling-reached
   task_id: <id>
   plan_path: <path>
   iterations_attempted: <1, 2, or 3>
   debug_file_path: <path>
   chronological_narrative:
     - iteration: 1
       verifier_result: <one-line summary>
       investigator_hypothesis: <one-line summary>
       fixer_commit: <sha or null>
       re_verify_result: <one-line summary or "(not run — ceiling)">
     - iteration: 2
       ...
   final_gaps:
     - truth: <text>
       reason: <text>
   ```

**Parallel-wave serialization (RESEARCH §2):** when multiple plans in a wave each have `verify_after` triggers, run their loop sub-flows SEQUENTIALLY after all spot-checks pass for the wave. Concurrent loops would race on the loop debug file and confuse iteration counting.

**Permission scoping (AGENT-SPEC §Security):**
- Verifier runs ONLY `verify_commands[*].cmd` from the plan — orchestrator validates this list before spawning.
- Investigator is denied Bash via the agent definition itself; orchestrator must NOT attempt to override.
- Fixer's only allowed Bash is `gsd-tools.cjs commit ...`; the agent definition enforces this.
- `actual_output` from verifier is truncated to 1024 chars before being passed in any subsequent message (`cmdVerifyCommands` already truncates — orchestrator does not re-expand).

**Opt-out:** if `init execute-phase` returned `verify_loop.per_plan["<plan-id>"].auto_verify == false`, do NOT execute this sub-flow for any task in that plan. Log `loop.skipped reason=auto_verify_false plan=<id>` to stdout (not the debug file — file is never created when skipped).

</sub_flow>
```

2. **Splice the trigger** into `<step name="execute_waves">`. After the existing post-spot-check "If all pass, report what was built" line (around line 278), insert:

```markdown
**Verify-loop trigger (Phase 4):**

After spot-checks pass for each plan in the wave:
- Read `verify_loop.per_plan["<plan-id>"]` from the init execute-phase JSON (already loaded into context in earlier steps).
- For each task name in `verify_after_tasks` that was just completed (cross-reference SUMMARY.md task list):
  - Execute the `<sub_flow name="verify_loop">` defined below.
- If any verify_loop emitted `## CHECKPOINT REACHED type: ceiling-reached`: pause execution, do not advance to the next wave, surface the block to the user.
- If all verify_loops pass: continue to step 6 (handle failures, then next wave).

Loops within a single wave run sequentially (parallel-wave serialization rule).
```

3. Do NOT remove or edit the existing `<step name="verify_phase_goal">` (lines 473-499). The two are complementary.

4. Add a one-line note at the top of the existing task loop block: "Phase 4 adds an inline verify-loop sub-flow per task — see `<sub_flow name=\"verify_loop\">` below."

5. Reference AGENT-SPEC.md by path in BOTH the new sub_flow block AND the trigger splice (orchestrator must remind itself contract source is the spec, not folklore).
  </action>
  <verify>
grep -q 'verify_after' .claude/get-shit-done/workflows/execute-phase.md && grep -qE '<sub_flow name="verify_loop">|## Verify Loop' .claude/get-shit-done/workflows/execute-phase.md && grep -q 'ceiling-reached' .claude/get-shit-done/workflows/execute-phase.md && grep -q 'AGENT-SPEC.md' .claude/get-shit-done/workflows/execute-phase.md && grep -q 'verify_phase_goal' .claude/get-shit-done/workflows/execute-phase.md
  </verify>
  <acceptance_criteria>
    - `grep -q 'verify_after' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -qE '<sub_flow name="verify_loop">|## Verify Loop' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q 'ceiling-reached' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q 'max_iterations' .claude/get-shit-done/workflows/execute-phase.md` returns 0 OR `grep -q 'iterations_attempted' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -qE 'fresh context|fresh-context' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q '04-AGENT-SPEC.md' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q 'verify-loop.md' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q 'find_root_cause_only' .claude/get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q 'auto_verify' .claude/get-shit-done/workflows/execute-phase.md` returns 0 (opt-out path documented)
    - `grep -q '<step name="verify_phase_goal">' .claude/get-shit-done/workflows/execute-phase.md` returns 0 (existing step preserved)
    - `grep -q 'parallel.*serial\|sequentially' .claude/get-shit-done/workflows/execute-phase.md` returns 0 (parallel-wave serialization rule documented)
    - File line count increased substantially (e.g., > 800 lines, baseline was 703)
  </acceptance_criteria>
  <done>execute-phase.md has the verify_loop sub-flow block with all required structural elements, the trigger splice in execute_waves, AGENT-SPEC reference, and verify_phase_goal preserved.</done>
</task>

<task type="checkpoint:human-verify" verify_after="false">
  <name>Task 4: Human verification of end-to-end harness on a synthetic plan</name>
  <files>(no source modification — verification only)</files>
  <read_first>
    - .planning/phases/04-verification-harness-and-context-efficiency/04-04-SUMMARY.md (the SUMMARY produced after Task 3)
    - .claude/get-shit-done/workflows/execute-phase.md (the spliced sub_flow)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §Test Contracts (TC-LOOP-PASS, TC-LOOP-RECOVER, TC-LOOP-CEILING)
  </read_first>
  <action>
Human-driven verification. The user (or a subsequent dogfood phase) creates a temporary throwaway plan in a sandbox containing one task with `verify_after="true"` and a deliberately failing `verify:` command (e.g., `cmd: "false"`, `expect: "true"`). They run `/gsd2:execute-phase` against this synthetic plan and confirm:

**Pass scenario** (TC-LOOP-PASS):
- Add a task with `verify_after="true"` and a passing verify entry (`cmd: "echo ok"`, `expect: "ok"`).
- Confirm: no `## CHECKPOINT REACHED` block appears between tasks.
- Confirm: a debug file is created at `.planning/debug/<slug>-verify-loop.md` with `status: resolved`.
- Confirm: the loop runs in <30 seconds for a trivial verify command.

**Ceiling scenario** (TC-LOOP-CEILING):
- Add a task with `verify_after="true"` and a deliberately broken verify entry (`cmd: "false"`, `expect: "ok"`).
- Confirm: 3 verifier invocations happen (debug file shows 3 `agent.return agent=verifier` log lines).
- Confirm: 2 investigator invocations happen (iterations 1 and 2 only).
- Confirm: 2 fixer invocations happen.
- Confirm: a `## CHECKPOINT REACHED type: ceiling-reached` block appears with `iterations_attempted: 3`.
- Confirm: executor pauses (does not proceed to next wave).

**Non-recoverable scenario** (partial TC-LOOP-RECOVER):
- Force the investigator to return `classification: not-yet-built` (e.g., the failing verify command depends on a future-phase feature).
- Confirm: the loop exits after 1 investigator return without spawning a fixer; `iterations_attempted: 1` in the ceiling-reached block.

This is a manual verification because it requires authoring throwaway test plans and observing real subagent behavior — not something the executor can automate inline. If any scenario fails, file a bug report against the plan that introduced the regression and re-run the failing scenario after the fix.
  </action>
  <verify>
ls .planning/debug/ 2>&1 | head -5 || echo "(debug dir not yet created — run synthetic test first)"
  </verify>
  <acceptance_criteria>
    - User confirms TC-LOOP-PASS scenario behaves as documented (no checkpoint, debug file created, loop runs)
    - User confirms TC-LOOP-CEILING scenario behaves as documented (3 verifier invocations, 2 investigator/fixer invocations, ceiling-reached block with iterations_attempted: 3, executor pauses)
    - User confirms non-recoverable classification scenario exits early
    - Debug file under `.planning/debug/` contains structured tagged log lines for each boundary (`agent.spawn`, `agent.return`, `boundary.validate`, `loop.start`, `loop.end`)
    - User explicitly approves moving to verify_phase_goal (no checkpoint deferred)
  </acceptance_criteria>
  <done>All three test scenarios verified by user against the live harness. SUMMARY.md notes any deviations and remediation.</done>
</task>

</tasks>

<verification>
1. `verify_after` task attribute and `auto_verify` plan flag are documented in `phase-prompt.md`.
2. `init execute-phase` returns a `verify_loop` config block with per-plan auto_verify and verify_after_tasks.
3. `execute-phase.md` contains the `verify_loop` sub_flow with fresh-context invocation rules, ceiling logic, AGENT-SPEC reference, debug file path convention, parallel serialization rule, and opt-out path.
4. `verify_phase_goal` step (end-of-waves verification) is preserved unchanged.
5. Three test scenarios verified end-to-end by human checkpoint.
</verification>

<success_criteria>
- All acceptance criteria pass for tasks 1–3 (automated).
- Task 4 (checkpoint:human-verify) confirms TC-LOOP-PASS, TC-LOOP-CEILING, and the non-recoverable early-exit scenario behave per AGENT-SPEC §Test Contracts.
- The phase's "Success signal" from CONTEXT §Expected Outcome is achievable: a real future phase where the executor claims done, the verifier catches a real failure, the loop autonomously diagnoses and fixes within ≤3 iterations.
- No existing standalone path of gsd-verifier, gsd-debugger, or gsd-fixer is broken (verified against Plan 04-02's dependency graph).
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-and-context-efficiency/04-04-SUMMARY.md` covering:
- Per-task confirmation of acceptance criteria
- Splice location (file:line where verify_loop sub_flow was inserted)
- Confirmation that verify_phase_goal step at lines 473-499 is preserved unchanged
- Sample debug file contents from a TC-LOOP-PASS dry run (truncated)
- Sample CHECKPOINT REACHED block from a TC-LOOP-CEILING dry run
- Any deviations from AGENT-SPEC contracts (must be zero — flag if not)
- Note that Phase 5+ phases will dogfood the harness automatically once they include `verify_after="true"` tasks
</output>
