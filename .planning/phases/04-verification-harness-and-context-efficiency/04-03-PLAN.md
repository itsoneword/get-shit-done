---
phase: 04-verification-harness-and-context-efficiency
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - .claude/agents/gsd-verifier.md
  - .claude/agents/gsd-fixer.md
  - .claude/get-shit-done/templates/phase-prompt.md
  - .claude/get-shit-done/bin/lib/verify.cjs
  - .claude/get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - truth: "gsd-verifier supports a loop invocation mode that consumes verify_commands and emits a structured LOOP VERIFY RESULT block"
      artifacts:
        - path: .claude/agents/gsd-verifier.md
          provides: loop-mode invocation contract
          contains: "verify_commands"
      verify:
        - cmd: "grep -q 'verify_commands' .claude/agents/gsd-verifier.md && grep -q 'LOOP VERIFY RESULT' .claude/agents/gsd-verifier.md"
          expect: ""
          type: unit
    - truth: "gsd-fixer supports a loop invocation mode keyed off investigator hypothesis input and emits structured FIXES COMPLETE with loop_iteration"
      artifacts:
        - path: .claude/agents/gsd-fixer.md
          provides: loop-mode invocation contract
          contains: "loop_iteration"
      verify:
        - cmd: "grep -q 'loop_iteration' .claude/agents/gsd-fixer.md && grep -qE 'hypothesis|ROOT CAUSE FOUND' .claude/agents/gsd-fixer.md"
          expect: ""
          type: unit
    - truth: "gsd-debugger already supports goal: find_root_cause_only — investigator role reuses this mode without source change"
      artifacts:
        - path: .claude/agents/gsd-debugger.md
          provides: existing find_root_cause_only mode (no edit needed)
          contains: "find_root_cause_only"
      verify:
        - cmd: "grep -q 'find_root_cause_only' .claude/agents/gsd-debugger.md"
          expect: ""
          type: unit
    - truth: "must_haves schema in plan template is extended with a verify: block under each truth entry"
      artifacts:
        - path: .claude/get-shit-done/templates/phase-prompt.md
          provides: verify schema documentation
          contains: "verify:"
      verify:
        - cmd: "awk '/must_haves:/,/^---/' .claude/get-shit-done/templates/phase-prompt.md | grep -c 'verify:'"
          expect: "^[1-9][0-9]*$"
          type: unit
    - truth: "gsd-tools.cjs exposes a 'verify commands' subcommand that parses verify: entries from a PLAN.md and runs cmd assertions"
      artifacts:
        - path: .claude/get-shit-done/bin/lib/verify.cjs
          provides: cmdVerifyCommands export
          contains: "cmdVerifyCommands"
      verify:
        - cmd: "grep -q 'cmdVerifyCommands' .claude/get-shit-done/bin/lib/verify.cjs"
          expect: ""
          type: unit
        - cmd: "node .claude/get-shit-done/bin/gsd-tools.cjs verify commands --help 2>&1 | grep -q 'verify' || node .claude/get-shit-done/bin/gsd-tools.cjs verify commands .claude/get-shit-done/templates/phase-prompt.md --raw 2>&1 | head -1"
          expect: ".*"
          type: integration
  artifacts:
    - path: .claude/agents/gsd-verifier.md
      provides: loop-mode aware verifier
      contains: "verify_commands"
    - path: .claude/agents/gsd-fixer.md
      provides: loop-mode aware fixer
      contains: "loop_iteration"
    - path: .claude/get-shit-done/templates/phase-prompt.md
      provides: must_haves schema with verify: block
      contains: "verify:"
    - path: .claude/get-shit-done/bin/lib/verify.cjs
      provides: cmdVerifyCommands subcommand
      contains: "cmdVerifyCommands"
    - path: .claude/get-shit-done/bin/gsd-tools.cjs
      provides: argv routing for `verify commands` subcommand
  key_links:
    - from: .claude/agents/gsd-verifier.md
      to: .claude/get-shit-done/bin/lib/verify.cjs
      via: verifier in loop mode invokes `gsd-tools.cjs verify commands` to execute verify: assertions
      pattern: "verify commands"
    - from: .claude/get-shit-done/templates/phase-prompt.md
      to: .claude/agents/gsd-verifier.md
      via: must_haves.verify: block authored at plan time becomes the verifier's input contract
      pattern: "verify:"
    - from: .claude/agents/gsd-fixer.md
      to: AGENT-SPEC contract loop-fixer→loop-orchestrator
      via: structured FIXES COMPLETE block with status, commit_hash, files_changed, iteration, trace_id
      pattern: "loop_iteration|trace_id"
---

<objective>
Build the loop primitives: in-place adaptations to gsd-verifier and gsd-fixer that add a "loop" invocation mode without breaking standalone usage; verify the existing `find_root_cause_only` mode of gsd-debugger covers the investigator role; extend the PLAN.md `must_haves:` schema with a `verify:` block; add a `verify commands` subcommand to gsd-tools that runs those assertions.

Architecture truth: `.planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md` — the JSON contracts in §Communication Contracts are the input/output shapes the verifier/investigator/fixer must produce or accept. Quote them verbatim where applicable.

This plan does NOT splice the loop into execute-phase.md — that is Plan 04-04's job. This plan only delivers the primitives 04-04 will wire together.
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
</context>

<interfaces>
**Verifier loop-mode return contract (AGENT-SPEC §"loop-verifier → loop-orchestrator"):**

```json
{
  "status": "pass | fail",
  "iteration": 1,
  "trace_id": "string",
  "score": 1,
  "total": 1,
  "gaps": [
    {
      "truth": "string",
      "status": "failed",
      "reason": "string",
      "cmd": "string",
      "actual_output": "string",
      "expected": "string"
    }
  ]
}
```

**Verifier loop-mode INPUT shape (from AGENT-SPEC §"loop-orchestrator → loop-verifier"):**

```json
{
  "plan_path": "string",
  "task_id": "string",
  "verify_commands": [{ "cmd": "string", "expect": "string", "type": "unit | integration | e2e" }],
  "must_haves_slice": [...],
  "iteration": 1,
  "trace_id": "string",
  "debug_file_path": "string | null"
}
```

**Fixer loop-mode INPUT shape (from AGENT-SPEC §"loop-orchestrator → loop-fixer"):** `hypothesis`, `classification`, `files_implicated`, `suggested_fix_direction`, `plan_slice`, `recent_diff`, `iteration`, `trace_id`, `debug_file_path`.

**Fixer loop-mode OUTPUT shape (AGENT-SPEC §"loop-fixer → loop-orchestrator"):**

```json
{
  "status": "fixed | partial | skipped",
  "commit_hash": "string | null",
  "files_changed": ["string"],
  "fix_summary": "string",
  "iteration": 1,
  "trace_id": "string"
}
```

**must_haves verify schema (RESEARCH §3, also reproduced in AGENT-SPEC):**

```yaml
must_haves:
  truths:
    - truth: "API returns user data for valid ID"
      artifacts: [src/api/users.ts]
      verify:
        - cmd: "curl -s http://localhost:3000/api/users/1 | jq '.id'"
          expect: "1"
          type: integration   # unit | integration | e2e | ui
```

**verify.cjs current exports** (`.claude/get-shit-done/bin/lib/verify.cjs`):
- `cmdVerifySummary`, `cmdVerifyPlanStructure`, `cmdVerifyPhaseCompleteness`, `cmdVerifyReferences`, `cmdVerifyCommits`, `cmdVerifyArtifacts`, `cmdVerifyKeyLinks`
- New export to add: `cmdVerifyCommands(cwd, planFilePath, raw)` — same shape as the existing exports.

**Standalone behavior to preserve (callers identified by Plan 04-02):** existing `verify-work` workflow, `/gsd2:verify-work` command, `/gsd2:fix` command, `/gsd2:debug` command, post-wave verify-phase-goal step in `execute-phase.md`. Verifier and fixer must continue to work in their pre-existing call paths when no loop-mode signal is present.
</interfaces>

<tasks>

<task type="auto" verify_after="true">
  <name>Task 1: Confirm gsd-debugger find_root_cause_only mode is loop-investigator-ready (no source edit)</name>
  <files>.claude/agents/gsd-debugger.md</files>
  <read_first>
    - .claude/agents/gsd-debugger.md (full file — focus on lines 220-260 around `goal: find_root_cause_only` and lines 380-420 around `## ROOT CAUSE FOUND` block)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Communication Contracts" → "loop-investigator → loop-orchestrator"
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §1 (debugger adaptation)
  </read_first>
  <action>
This is a verification-only task. RESEARCH §1 claims gsd-debugger already supports the investigator role via `goal: find_root_cause_only` + `symptoms_prefilled: true`. Confirm by:

1. Confirming the file already declares both `goal: find_root_cause_only` and `symptoms_prefilled: true` as documented invocation flags.
2. Confirming the `## ROOT CAUSE FOUND (goal: find_root_cause_only)` return block exists and contains at least the fields `root_cause`, `files_involved`, and `confidence` (or close synonyms).
3. Confirming the `## INVESTIGATION INCONCLUSIVE` return path exists for the non-recoverable case.

If all three are present: do NOT edit the file. Record the confirmation in this task's commit message ("Plan 04-03 Task 1: gsd-debugger investigator-ready, no source change").

If any is missing: STOP, do not improvise — surface to the user as a CHECKPOINT REACHED note. Append a "Blocking issue: gsd-debugger missing {marker_name}" entry to this plan's SUMMARY.md and do NOT proceed to Task 2. Plan 04-03 cannot continue until gsd-debugger is confirmed investigator-ready (which is a prerequisite for the loop's investigator role). The reuse strategy depends on these existing.
  </action>
  <verify>
grep -n 'find_root_cause_only' .claude/agents/gsd-debugger.md && grep -n 'symptoms_prefilled' .claude/agents/gsd-debugger.md && grep -n '## ROOT CAUSE FOUND' .claude/agents/gsd-debugger.md && grep -n '## INVESTIGATION INCONCLUSIVE\|INVESTIGATION INCONCLUSIVE' .claude/agents/gsd-debugger.md
  </verify>
  <acceptance_criteria>
    - `grep -q 'find_root_cause_only' .claude/agents/gsd-debugger.md` returns 0
    - `grep -q 'symptoms_prefilled' .claude/agents/gsd-debugger.md` returns 0
    - `grep -q '## ROOT CAUSE FOUND' .claude/agents/gsd-debugger.md` returns 0
    - `grep -qE 'INVESTIGATION INCONCLUSIVE|investigation_inconclusive' .claude/agents/gsd-debugger.md` returns 0
    - `git diff --stat .claude/agents/gsd-debugger.md` is empty (no edits made)
    - The 04-03 SUMMARY.md (output of this plan) explicitly notes "gsd-debugger requires no source change for investigator role"
  </acceptance_criteria>
  <done>All four greps succeed; no source edit made; finding noted in summary.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 2: Extend must_haves schema in phase-prompt.md template with verify: block</name>
  <files>.claude/get-shit-done/templates/phase-prompt.md</files>
  <read_first>
    - .claude/get-shit-done/templates/phase-prompt.md (lines 27-31 — top frontmatter snippet; lines 547-608 — must_haves detailed section)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §3 (verify schema spec)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Reasoning & Planning Approach" → "Planner note"
  </read_first>
  <action>
Edit `.claude/get-shit-done/templates/phase-prompt.md`:

1. In the top "File Template" frontmatter snippet (around lines 27-31), expand the `must_haves:` block to show:

```yaml
must_haves:
  truths:                   # Observable behaviors that must be true for goal achievement
    - truth: "..."
      artifacts: [path/to/file]
      verify:               # Executable assertions — consumed by verify-loop primitives (Phase 4)
        - cmd: "shell command"
          expect: "expected stdout match (string or regex)"
          type: unit          # unit | integration | e2e | ui (ui deferred to v2)
  artifacts: []             # Files that must exist with real implementation
  key_links: []             # Critical connections between artifacts
```

2. In the detailed must_haves section (lines 547-608), insert a new subsection titled `### The verify: block (executable assertions)` documenting:
   - When to author `verify:` entries (any truth that can be checked by a shell command)
   - Field semantics: `cmd` (required, shell-executable), `expect` (required, string or regex), `type` (required enum)
   - Backward compatibility: plans without `verify:` continue to work — verifier falls back to its existing 3-level artifact check
   - Companion task attribute: `verify_after: true` on a task triggers the loop after that task completes (documented in Plan 04-04)

3. Add a one-line note at the top of the must_haves section: "Phase 4 introduces the optional `verify:` sub-block under each truth — see §The verify: block below."

Do NOT remove or restructure any existing must_haves documentation. The extension is additive.
  </action>
  <verify>
awk '/must_haves:/,/^---/' .claude/get-shit-done/templates/phase-prompt.md | grep -c 'verify:' && grep -c '### The verify: block' .claude/get-shit-done/templates/phase-prompt.md && grep -c 'verify_after' .claude/get-shit-done/templates/phase-prompt.md
  </verify>
  <acceptance_criteria>
    - `grep -q '### The verify: block' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - `grep -q 'verify_after' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - `grep -q 'unit | integration | e2e' .claude/get-shit-done/templates/phase-prompt.md` returns 0
    - `awk '/must_haves:/,/^---/' .claude/get-shit-done/templates/phase-prompt.md | grep -c 'verify:'` returns ≥ 1
    - `grep -c 'truths:' .claude/get-shit-done/templates/phase-prompt.md` is unchanged from before this task (existing docs preserved — count from original file)
    - Total file size increased (additive change): `wc -l` is greater than baseline
  </acceptance_criteria>
  <done>Template documents the verify: block, types enum, backward-compat note, and verify_after task attribute reference.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 3: Add cmdVerifyCommands subcommand to verify.cjs</name>
  <files>.claude/get-shit-done/bin/lib/verify.cjs, .claude/get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - .claude/get-shit-done/bin/lib/verify.cjs (full file — model the new export after `cmdVerifyArtifacts` and `cmdVerifyKeyLinks` which already parse must_haves YAML)
    - .claude/get-shit-done/bin/gsd-tools.cjs (argv routing for `verify` subcommand — find where `verify artifacts` and `verify key-links` are dispatched)
    - .claude/get-shit-done/bin/lib/frontmatter.cjs (parsing helper used by other verify commands)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §3 (return shape `{ all_passed, results: [...] }`)
  </read_first>
  <action>
1. In `.claude/get-shit-done/bin/lib/verify.cjs`, add a new exported function `cmdVerifyCommands(cwd, planFilePath, raw)`:

```js
function cmdVerifyCommands(cwd, planFilePath, raw) {
  if (!planFilePath) error('plan file path required');
  const fullPath = path.resolve(cwd, planFilePath);
  const content = safeReadFile(fullPath);
  // Parse frontmatter, extract must_haves.truths[].verify[]
  // For each verify entry: spawn cmd via child_process.execSync (or spawnSync), capture stdout
  // Compare actual stdout (trimmed) against expect using either:
  //   - string equality, OR
  //   - regex match if expect starts and ends with '/' (e.g. "/^[0-9]+$/")
  // Aggregate: { all_passed: bool, results: [{cmd, expect, actual, passed, type, truth}] }
  // Output via output(...) helper used by sibling commands.
}
```

2. Implementation rules:
   - Use the same frontmatter extraction approach as `cmdVerifyArtifacts` (or import `extractFrontmatter` from frontmatter.cjs).
   - Each `verify` entry is associated with its parent `truth` (carry the truth string into the result row for traceability).
   - Validate `type` is one of `unit | integration | e2e | ui`. Unknown types → result row has `passed: false, reason: "unknown type"`.
   - Capture stdout with a 30-second timeout per command. On timeout: `passed: false, reason: "timeout"`.
   - Non-zero exit code: still capture stdout; `passed: false, reason: "exit code N"` if expect didn't match. (Some verify commands exit 0 on success and still emit data — exit code alone is not the pass/fail signal; the `expect` match is.)
   - Truncate captured `actual` to 1024 chars (prompt-injection mitigation per AGENT-SPEC §Security).

3. Append `cmdVerifyCommands` to the module exports object at the bottom of `verify.cjs`.

4. In `.claude/get-shit-done/bin/gsd-tools.cjs`, route the `verify commands <plan-path>` subcommand to `cmdVerifyCommands(cwd, planPath, raw)`. Mirror the dispatch pattern used for `verify artifacts` and `verify key-links`.

5. Do NOT change the behavior of any existing `cmdVerify*` function.
  </action>
  <verify>
node .claude/get-shit-done/bin/gsd-tools.cjs verify commands .planning/phases/04-verification-harness-and-context-efficiency/04-01-PLAN.md --raw 2>&1 | jq -e '.all_passed != null and (.results | type == "array")' 2>&1
  </verify>
  <acceptance_criteria>
    - `grep -q 'cmdVerifyCommands' .claude/get-shit-done/bin/lib/verify.cjs` returns 0
    - `grep -q 'cmdVerifyCommands' .claude/get-shit-done/bin/gsd-tools.cjs` returns 0
    - `node .claude/get-shit-done/bin/gsd-tools.cjs verify commands .planning/phases/04-verification-harness-and-context-efficiency/04-01-PLAN.md --raw` produces parseable JSON with keys `all_passed` and `results`
    - The returned `results` array has `length > 0` for 04-01-PLAN.md (which contains verify: blocks authored by this plan)
    - Each result row contains keys: `cmd`, `expect`, `actual`, `passed`, `truth`
    - `actual` field is ≤ 1024 chars on every row (`jq '[.results[].actual | length] | max'` returns ≤ 1024)
    - Existing `verify artifacts` and `verify key-links` subcommands still work unchanged: `node .claude/get-shit-done/bin/gsd-tools.cjs verify artifacts .planning/phases/04-verification-harness-and-context-efficiency/04-01-PLAN.md --raw` exits 0
  </acceptance_criteria>
  <done>New subcommand parses verify: blocks, runs cmds, compares against expect, returns structured aggregate. Existing verify subcommands unaffected.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 4: Adapt gsd-verifier with loop invocation mode</name>
  <files>.claude/agents/gsd-verifier.md</files>
  <read_first>
    - .claude/agents/gsd-verifier.md (full file — frontmatter lines 1-11; Step 0 re-verification mode; Step 2 truth derivation; lines 319-350 `## Return to Orchestrator` block)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Communication Contracts" → both verifier contracts (input + output)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §1 (verifier adaptation surface)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md (verifier callers — preserve their input/output expectations)
  </read_first>
  <action>
Edit `.claude/agents/gsd-verifier.md` to add a loop-mode invocation path. Standalone behavior must remain default and unchanged.

1. **Frontmatter:** add `invocation_mode: standalone | loop` documentation. Keep current frontmatter fields. Insert a new line in the body (not frontmatter — frontmatter remains as-is for tool compatibility) explicitly stating: "Loop mode is signaled by the presence of `loop_input.json` path or a `## LOOP MODE` block in the prompt provided by `<files_to_read>`."

2. **Process body:** add a new Step 0.5 (between current Step 0 re-verification check and Step 1 context load):

```markdown
### Step 0.5 — Detect invocation mode

If the prompt or `<files_to_read>` includes a `loop_input.json` path or a structured block matching:

\```json
{
  "plan_path": "...",
  "task_id": "...",
  "verify_commands": [...],
  "iteration": N,
  "trace_id": "..."
}
\```

then enter **LOOP MODE** and skip to Step LOOP-1 (below). Otherwise continue with Step 1 (standalone).
```

3. Add a new `## Loop Mode Process` section (after the standalone process, before `## Return to Orchestrator`):

```markdown
## Loop Mode Process

### Step LOOP-1 — Run verify commands
Invoke `node .claude/get-shit-done/bin/gsd-tools.cjs verify commands <plan_path> --raw`. Parse the returned `{all_passed, results: [...]}`.

### Step LOOP-2 — Build gaps array
For every `result` with `passed: false`, construct a gap entry:
\```json
{ "truth": "<from result.truth>", "status": "failed", "reason": "<from result.reason or 'output mismatch'>", "cmd": "<result.cmd>", "actual_output": "<result.actual>", "expected": "<result.expect>" }
\```

### Step LOOP-3 — Emit LOOP VERIFY RESULT block
Output exactly one fenced JSON block:

\```
## LOOP VERIFY RESULT
\`\`\`json
{
  "status": "pass | fail",
  "iteration": <iteration from input>,
  "trace_id": "<trace_id from input>",
  "score": <count of passed results>,
  "total": <total result count>,
  "gaps": [...]
}
\`\`\`
\```

Do NOT write a VERIFICATION.md file in loop mode (that is the standalone artifact).
Do NOT commit anything in loop mode.
```

4. Update the `## Return to Orchestrator` section to reference both modes — standalone returns the existing `## Verification Complete` block; loop returns the `## LOOP VERIFY RESULT` block.

5. Reference AGENT-SPEC.md verbatim: at the top of the new `## Loop Mode Process` section, include a line: `Contract source of truth: .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §Communication Contracts.`

6. Do NOT alter Steps 1-10 of the standalone flow. Do NOT change the existing `tools:` field in frontmatter.
  </action>
  <verify>
grep -q 'LOOP VERIFY RESULT' .claude/agents/gsd-verifier.md && grep -q 'verify_commands' .claude/agents/gsd-verifier.md && grep -q 'Step LOOP-1' .claude/agents/gsd-verifier.md && grep -q 'AGENT-SPEC.md' .claude/agents/gsd-verifier.md
  </verify>
  <acceptance_criteria>
    - `grep -q 'LOOP VERIFY RESULT' .claude/agents/gsd-verifier.md` returns 0
    - `grep -q 'verify_commands' .claude/agents/gsd-verifier.md` returns 0
    - `grep -qE '(Step LOOP-1|## Loop Mode Process)' .claude/agents/gsd-verifier.md` returns 0
    - `grep -q 'AGENT-SPEC.md' .claude/agents/gsd-verifier.md` returns 0
    - `grep -q '## Return to Orchestrator' .claude/agents/gsd-verifier.md` returns 0 (standalone return preserved)
    - `grep -q '## Verification Complete' .claude/agents/gsd-verifier.md` returns 0 (standalone block name preserved)
    - File starts with `---` and existing frontmatter fields (`name`, `description`, `tools`, `color`) are unchanged: `head -11 .claude/agents/gsd-verifier.md | grep -c 'name:\|description:\|tools:\|color:'` returns ≥ 4
    - Line count increased compared to baseline (additive change)
  </acceptance_criteria>
  <done>Verifier supports loop mode via Step 0.5 detection and Step LOOP-1/2/3 processing, emits LOOP VERIFY RESULT block, references AGENT-SPEC.md, preserves standalone path.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 5: Adapt gsd-fixer with loop invocation mode</name>
  <files>.claude/agents/gsd-fixer.md</files>
  <read_first>
    - .claude/agents/gsd-fixer.md (full file — frontmatter, classification logic, `## FIXES COMPLETE` and `## FIXES PARTIAL` return blocks)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §"Communication Contracts" → both fixer contracts (input + output)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md §1 (fixer adaptation surface)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md (fixer callers — preserve standalone use)
  </read_first>
  <action>
Edit `.claude/agents/gsd-fixer.md` to add loop-mode support. Standalone behavior remains default.

1. Add a `## Loop Mode Process` section after the existing standalone process. Section contents:

```markdown
## Loop Mode Process

Triggered when input includes a `## ROOT CAUSE FOUND` block from the loop-investigator (gsd-debugger in `goal: find_root_cause_only` mode) and a `loop_iteration` field.

Contract source of truth: .planning/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md §Communication Contracts.

### Step LOOP-1 — Parse hypothesis
Extract: `hypothesis`, `classification`, `files_implicated`, `suggested_fix_direction`, `plan_slice`, `iteration`, `trace_id`.

### Step LOOP-2 — Validate classification
If `classification` is `not-yet-built` or `unrelated`: do NOT attempt a fix. Emit `## FIXES SKIPPED` with `reason: "classification: <value>"` and return immediately.

If `classification` is `current-phase` or `regression`: proceed.

### Step LOOP-3 — Apply minimal fix
Edit ONLY files in `files_implicated` (plus direct dependencies found by grep — same scoping rule as standalone mode). Do not refactor unrelated code.

### Step LOOP-4 — Commit via gsd-tools
Use `node .claude/get-shit-done/bin/gsd-tools.cjs commit "verify-loop/fix/attempt-<iteration>: <one-line summary>" --files <files>`. Capture commit hash from stdout.

### Step LOOP-5 — Emit FIXES COMPLETE block
Output exactly one fenced JSON block:

\```
## FIXES COMPLETE (loop)
\`\`\`json
{
  "status": "fixed | partial | skipped",
  "commit_hash": "<sha or null>",
  "files_changed": ["..."],
  "fix_summary": "...",
  "loop_iteration": <iteration>,
  "iteration": <iteration>,
  "trace_id": "<trace_id>"
}
\`\`\`
\```

`loop_iteration` and `iteration` are intentional duplicates: `iteration` matches the AGENT-SPEC contract field; `loop_iteration` is the legacy/auditability field that distinguishes loop commits from standalone in commit message search.
```

2. Update frontmatter `tools:` field commentary (in the body, not the YAML) to note: "In loop mode, Bash usage is restricted to `gsd-tools.cjs commit` — no direct git or shell-out for verification."

3. Preserve the existing standalone classification logic and the `## FIXES COMPLETE` (without `(loop)`) and `## FIXES PARTIAL` blocks for non-loop callers.

4. Do NOT change the YAML frontmatter (`name`, `description`, `tools`, `color`).
  </action>
  <verify>
grep -q 'loop_iteration' .claude/agents/gsd-fixer.md && grep -q 'FIXES COMPLETE (loop)' .claude/agents/gsd-fixer.md && grep -q 'AGENT-SPEC.md' .claude/agents/gsd-fixer.md && grep -q 'verify-loop/fix' .claude/agents/gsd-fixer.md
  </verify>
  <acceptance_criteria>
    - `grep -q 'loop_iteration' .claude/agents/gsd-fixer.md` returns 0
    - `grep -q 'FIXES COMPLETE (loop)' .claude/agents/gsd-fixer.md` returns 0
    - `grep -q 'verify-loop/fix' .claude/agents/gsd-fixer.md` returns 0
    - `grep -q 'AGENT-SPEC.md' .claude/agents/gsd-fixer.md` returns 0
    - `grep -qE '## Loop Mode Process|Step LOOP-1' .claude/agents/gsd-fixer.md` returns 0
    - `grep -q 'classification' .claude/agents/gsd-fixer.md` returns 0 (loop step references existing classification)
    - Standalone `## FIXES COMPLETE` block (without "(loop)") still present: `grep -E '^## FIXES COMPLETE$' .claude/agents/gsd-fixer.md` returns 0 OR equivalent line is preserved
    - Frontmatter unchanged: `head -11 .claude/agents/gsd-fixer.md | grep -c 'name:\|description:\|tools:\|color:'` returns ≥ 4
    - Line count increased (additive change)
  </acceptance_criteria>
  <done>Fixer supports loop mode with hypothesis-driven fix path, classification short-circuit, commit prefix convention, and FIXES COMPLETE (loop) block. Standalone path preserved.</done>
</task>

</tasks>

<verification>
1. Schema in template documents the new `verify:` block plus `verify_after` attribute.
2. New `verify commands` CLI subcommand parses verify entries and runs them — verifiable end-to-end against 04-01-PLAN.md (which contains verify: blocks).
3. Verifier source contains the LOOP VERIFY RESULT block, Step LOOP-1/2/3, and AGENT-SPEC.md reference.
4. Fixer source contains FIXES COMPLETE (loop) block, classification short-circuit, commit prefix convention.
5. Debugger source unchanged — find_root_cause_only mode pre-existed (Task 1 confirms by grep).
6. Standalone callers of verifier and fixer (per Plan 04-02 dependency graph) still see their original return blocks.
</verification>

<success_criteria>
- All five tasks pass acceptance criteria.
- A test invocation: `node .claude/get-shit-done/bin/gsd-tools.cjs verify commands .planning/phases/04-verification-harness-and-context-efficiency/04-01-PLAN.md --raw` returns valid JSON with `all_passed` and `results` keys, with at least one result row matching a 04-01 verify entry.
- The four edited files (`gsd-verifier.md`, `gsd-fixer.md`, `phase-prompt.md`, `verify.cjs`) plus `gsd-tools.cjs` show in `git diff --name-only`; `gsd-debugger.md` does NOT.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-and-context-efficiency/04-03-SUMMARY.md` covering:
- Per-task confirmation of acceptance criteria
- Note that gsd-debugger required no source change (Task 1 finding)
- Sample output of `verify commands --raw` against 04-01-PLAN.md (truncated to 50 lines)
- Any deviations from AGENT-SPEC contracts (must be zero — flag if not)
</output>
