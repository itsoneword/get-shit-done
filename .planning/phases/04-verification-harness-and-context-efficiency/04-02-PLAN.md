---
phase: 04-verification-harness-and-context-efficiency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
  - .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - truth: "A machine-readable dependency graph exists for agents, workflows, and gsd-tools subcommands under .claude/get-shit-done/"
      artifacts:
        - path: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
          provides: structured caller/callee map
          contains: "agents"
      verify:
        - cmd: "jq 'has(\"agents\") and has(\"workflows\") and has(\"tools\")' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json"
          expect: "true"
          type: unit
    - truth: "Graph captures every loop-relevant agent (gsd-verifier, gsd-debugger, gsd-fixer) with a callers list"
      artifacts:
        - path: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
      verify:
        - cmd: "jq '[.agents[\"gsd-verifier\"].callers, .agents[\"gsd-debugger\"].callers, .agents[\"gsd-fixer\"].callers] | map(length > 0) | all' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json"
          expect: "true"
          type: unit
    - truth: "Human-readable companion summary lists per-agent callers and per-tool subcommand callers"
      artifacts:
        - path: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
          provides: narrative summary of the JSON graph
          contains: "gsd-verifier"
      verify:
        - cmd: "grep -c '^### gsd-' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md"
          expect: "^[1-9][0-9]*$"
          type: unit
  artifacts:
    - path: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
      provides: caller/callee map for agents, workflows, tools
    - path: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
      provides: human-readable summary of the JSON graph
  key_links:
    - from: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
      to: .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
      via: shared schema and caller/callee data
      pattern: "agents|workflows|tools"
    - from: Plan 04-03 implementer
      to: 04-dependency-graph.json
      via: read-before-modify dependency on caller list for verifier/debugger/fixer
      pattern: "gsd-verifier|gsd-debugger|gsd-fixer"
---

<objective>
Discovery-only plan. Produce a structured map of every caller/callee relationship inside `.claude/get-shit-done/` so that Plan 04-03 (which adapts gsd-verifier/gsd-debugger/gsd-fixer in-place) knows the full call surface and does not break standalone invocation paths. Output is two artifacts: a JSON graph and a markdown summary. No source changes.

This plan exists because the user explicitly requested "build a graph" before rewriting the loop primitives (CONTEXT §Specifics).
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md
</context>

<interfaces>
**Output schema (RESEARCH §4):**

```json
{
  "agents": {
    "<agent-name>": {
      "callers": ["<file path of caller>", ...],
      "spawned_by": ["<workflow file>", ...]
    }
  },
  "workflows": {
    "<workflow-name>.md": {
      "callers": ["<file path of caller>", ...],
      "includes": ["<workflow file referenced via @-include>", ...]
    }
  },
  "tools": {
    "<subcommand>": {
      "callers": ["<file path>", ...]
    }
  }
}
```

Granularity: file-level only (NOT line-level). Scope: only `.claude/get-shit-done/` and `.claude/commands/` and `.claude/agents/` trees — exclude `.planning/`, `worktrees/`, and `node_modules/`.

**Required grep recipes (RESEARCH §4):**
- Agent spawns via Task tool: `grep -rn "subagent_type=\"gsd-" .claude/get-shit-done/workflows/` (also try `subagent_type='gsd-` and bare `@gsd-` references for Copilot-style invocations)
- Agent name references: `grep -rln "gsd-verifier\|gsd-debugger\|gsd-fixer\|gsd-executor\|gsd-planner\|gsd-codebase-mapper\|gsd-document-mapper\|gsd-document-updater\|gsd-test-designer\|gsd-plan-checker\|gsd-nyquist-auditor\|gsd-roadmapper\|gsd-research-synthesizer\|gsd-phase-researcher\|gsd-project-researcher\|gsd-user-profiler\|gsd-ui-researcher\|gsd-ui-checker\|gsd-ui-auditor\|gsd-integration-checker\|gsd-agent-researcher\|gsd-agent-checker" .claude/`
- Workflow @-includes: `grep -rln "@.*workflows/" .claude/`
- Tool subcommand calls: `grep -rE 'gsd-tools\.cjs (init|roadmap|phase|state|commit|verify|frontmatter|history-digest)' .claude/ -l` and per-subcommand `grep -rn "gsd-tools.cjs <subcmd>" .claude/`
</interfaces>

<tasks>

<task type="auto" verify_after="true">
  <name>Task 1: Build the JSON dependency graph</name>
  <files>.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json</files>
  <read_first>
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md (§4 — schema, grep recipes, scope)
    - .claude/agents/ (all `.md` files — these are the canonical agent names)
    - .claude/get-shit-done/workflows/ (all `.md` files — workflow inventory)
    - .claude/get-shit-done/bin/lib/ (all `.cjs` files — find exported subcommand names)
    - .claude/commands/gsd2/ (command files that often @-include workflows)
  </read_first>
  <action>
1. Enumerate the universe to map:
   - **Agents:** every `*.md` filename (without extension) in `.claude/agents/`. Strip the `gsd-` prefix is NOT needed — keep as-is (e.g., `gsd-verifier`).
   - **Workflows:** every `*.md` filename in `.claude/get-shit-done/workflows/`.
   - **Tools (subcommands):** parse `.claude/get-shit-done/bin/lib/*.cjs` for exported `cmd*` functions and translate to subcommand names (e.g., `cmdInitProgress` → `init progress`). Cross-check against actual `gsd-tools.cjs` argv routing if exports don't map cleanly.
2. For each agent, run `grep -rln "<agent-name>"` across `.claude/get-shit-done/`, `.claude/commands/`, `.claude/agents/` and record:
   - `callers`: deduplicated list of file paths that mention the agent (any reference, not just spawn).
   - `spawned_by`: subset of callers that contain `subagent_type` or `Task(` patterns within 5 lines of the agent name (best-effort — if unsure, copy entry into `callers` only).
3. For each workflow, record:
   - `callers`: any file that references the workflow path (`grep -rln "workflows/<name>" .claude/`)
   - `includes`: any `@-include` lines INSIDE the workflow itself that reference other workflows (`grep -nE "@.*workflows/" .claude/get-shit-done/workflows/<name>`).
4. For each subcommand, record `callers`: files containing `gsd-tools.cjs <subcommand>` strings.
5. Exclude paths under `.planning/`, `worktrees/`, `node_modules/`, `.git/`.
6. Write the assembled object to `.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json` as pretty-printed JSON (`JSON.stringify(obj, null, 2)` shape — 2-space indent).
7. The graph must include at minimum entries for `gsd-verifier`, `gsd-debugger`, `gsd-fixer`, `gsd-executor`, `gsd-planner`, the workflows `execute-phase.md`, `execute-plan.md`, `progress.md`, `verify-work.md`, and the tool subcommands `init progress`, `init execute-phase`, `roadmap analyze`, `phase add`, `verify artifacts`, `verify key-links`, `commit`.
  </action>
  <verify>
jq '{agents_count: (.agents|length), workflows_count: (.workflows|length), tools_count: (.tools|length), verifier_callers: .agents["gsd-verifier"].callers | length, debugger_callers: .agents["gsd-debugger"].callers | length, fixer_callers: .agents["gsd-fixer"].callers | length}' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json` exists
    - `jq -e '.' <file>` exits 0 (valid JSON)
    - `jq -e '.agents["gsd-verifier"].callers | length > 0'` exits 0
    - `jq -e '.agents["gsd-debugger"].callers | length > 0'` exits 0
    - `jq -e '.agents["gsd-fixer"].callers | length > 0'` exits 0
    - `jq -e '.workflows["execute-phase.md"].callers | length > 0'` exits 0
    - `jq -e '.tools["init progress"].callers | length > 0'` exits 0
    - `jq '.agents | length'` returns ≥ 15 (most agents in `.claude/agents/` should appear)
    - No paths in any `callers` array begin with `.planning/`, `worktrees/`, or `node_modules/` (verify with `jq '[.. | strings? | select(startswith(".planning/") or startswith("worktrees/") or startswith("node_modules/"))] | length'` returns `0`)
    - No source files under `.claude/` are modified (`git diff --stat .claude/` is empty)
  </acceptance_criteria>
  <done>JSON graph file exists, parses, contains all required agents/workflows/tools with non-empty caller lists.</done>
</task>

<task type="auto">
  <name>Task 2: Generate human-readable graph summary</name>
  <files>.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md</files>
  <read_first>
    - .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.json (output of Task 1)
  </read_first>
  <action>
Generate `.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md` containing three sections:

```markdown
# Phase 4 — Agent / Workflow / Tool Dependency Graph

**Generated:** {ISO date}
**Scope:** .claude/agents/, .claude/get-shit-done/, .claude/commands/
**Source:** 04-dependency-graph.json

## Agents

### gsd-<agent>
- **Callers:** {bullet list of file paths}
- **Spawned by:** {workflow paths or "(none detected)"}

(repeat for every agent in the JSON)

## Workflows

### <workflow>.md
- **Callers:** {bullet list}
- **Includes:** {workflow paths or "(none)"}

## Tools (gsd-tools subcommands)

### <subcommand>
- **Callers:** {bullet list}

## Risk Surface for Plan 04-03

Highlights from this graph that Plan 04-03 must respect:
- gsd-verifier callers: {paths}  → loop-mode adaptation must preserve standalone return shape used by these callers.
- gsd-debugger callers: {paths}  → the `goal: find_root_cause_only` flag must remain compatible.
- gsd-fixer callers: {paths}  → loop-mode `## FIXES COMPLETE` extension must not break existing parsers.
```

Each agent/workflow/tool gets its own H3 section. Bullet lists are populated from the JSON. The "Risk Surface" section is hand-curated commentary — at minimum identify which non-loop callers exist for each of the three loop primitives.
  </action>
  <verify>
grep -c '^### gsd-' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md && grep -c '^## Risk Surface' .planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-verification-harness-and-context-efficiency/04-dependency-graph.md` exists
    - `grep -c '^### gsd-' <file>` returns ≥ 15 (one per agent in JSON)
    - `grep -q '^## Workflows' <file>` returns 0
    - `grep -q '^## Tools' <file>` returns 0
    - `grep -q '^## Risk Surface for Plan 04-03' <file>` returns 0
    - File mentions `gsd-verifier`, `gsd-debugger`, `gsd-fixer` in the Risk Surface section (`grep -A 20 '^## Risk Surface' <file> | grep -c 'gsd-verifier\|gsd-debugger\|gsd-fixer'` returns ≥ 3)
    - No source files under `.claude/` are modified (`git diff --stat .claude/` is empty)
  </acceptance_criteria>
  <done>Markdown summary exists with per-agent/per-workflow/per-tool sections plus a hand-curated "Risk Surface" callout for the loop primitives.</done>
</task>

</tasks>

<verification>
1. JSON validity: `jq -e '.' 04-dependency-graph.json` exits 0.
2. Coverage: at least 15 agents, the canonical workflows, and the canonical tool subcommands all appear with non-empty caller lists.
3. Companion markdown: at least one H3 heading per agent and a hand-curated Risk Surface block.
4. No source under `.claude/` modified — this is a documentation deliverable only.
</verification>

<success_criteria>
- Plan 04-03 implementer can answer "who else calls gsd-verifier?" by reading the JSON, without re-running greps.
- The Risk Surface section of the markdown lists every standalone caller of the three loop primitives so the adaptation work can preserve them.
- Graph regenerates deterministically — re-running the task produces a JSON with the same agent/workflow/tool keys (caller order may vary).
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-and-context-efficiency/04-02-SUMMARY.md` describing:
- Counts (agents/workflows/tools indexed)
- Top 3 highest-callsite agents
- Top 3 highest-callsite tool subcommands
- Risk callouts for Plan 04-03 (any non-loop caller of the trio that needs preserving)
</output>
