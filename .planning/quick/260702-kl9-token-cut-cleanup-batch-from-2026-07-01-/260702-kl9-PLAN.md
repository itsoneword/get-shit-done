---
phase: quick-260702-kl9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/references/project-context.md
  - get-shit-done/references/ui-brand.md
  - get-shit-done/references/ui-brand-guide.md
  - agents/gsd-executor.md
  - agents/gsd-ui-auditor.md
  - agents/gsd-ui-researcher.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-planner.md
  - agents/gsd-test-designer.md
  - agents/gsd-ui-checker.md
  - get-shit-done/templates/phase-prompt.md
  - get-shit-done/bin/lib/template.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - tests/template.test.cjs
  - docs/SYSTEM-MAP.md
  - docs/system/_gaps.md
  - docs/system/templates-references.md
  - docs/system/tool-cli.md
  - .planning/reference/COMPARISON.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "execute-phase.md contains exactly ONE executor prompt template; the worktree vs in-place difference is a single conditional insert block"
    - "All 7 agent files reference the shared project-context.md instead of carrying inline discovery boilerplate"
    - "ui-brand.md is under 60 lines and every existing @-include of it still resolves"
    - "node .claude/get-shit-done/bin/gsd-tools.cjs template select summary exits with an unknown-command error"
    - "npm test passes with template.test.cjs removed"
  artifacts:
    - "get-shit-done/references/project-context.md"
    - "get-shit-done/references/ui-brand-guide.md"
  key_links:
    - "commands/gsd2/*.md → @~/.claude/get-shit-done/references/ui-brand.md (10 stubs; path must not change)"
    - "agents/*.md <project_context> → @~/.claude/get-shit-done/references/project-context.md (new)"
    - "get-shit-done/bin/gsd-tools.cjs must NOT require ./lib/template.cjs after deletion (broken = CLI crashes at startup for every command)"
---

<objective>
Token-cut cleanup batch from the 2026-07-01 self-audit (uncontested items only, validated by discuss-loop). Five sub-items: (1) merge the two near-duplicate executor prompt blocks in execute-phase.md, (2) extract shared `<project_context>` boilerplate from 7 agent files into one reference, (3) trim ui-brand.md 199 → <60 lines, (4) delete orphaned phase-prompt.md, (5) remove dead template.cjs and its CLI/doc traces.

Behavior-preserving only: deduplication, trimming, removal. No workflow semantics change.

CRITICAL repo convention: edit SOURCE files (`get-shit-done/`, `agents/`, `commands/`), never the project-local `.claude/` runtime copies. Runtime sync happens once at the end via `npm run dev`.

Out of scope (lens-loop blocked — do NOT touch): checkpoints.md/tdd.md frontmatter gating; gsd-phase-researcher micro-research persona split.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/references/ui-brand.md

Pre-verified facts (planner already grepped — trust these, re-verify only where a task says to):
- The two executor prompts live at execute-phase.md lines ~261-370. They are byte-identical except: worktree variant has a `<worktree_isolation>` block, in-place variant has a `<parallel_execution>` block. `<objective>`, `<execution_context>`, `<files_to_read>`, `<mcp_tools>`, `<success_criteria>` are identical.
- All 7 agents' `<project_context>` blocks are semantically identical (read ./CLAUDE.md; check .claude/skills/ or .agents/skills/; read SKILL.md ~130 lines each; load rules/*.md as needed; never load full AGENTS.md). No agent-specific deltas exist.
- `phase-prompt.md`: zero references anywhere in get-shit-done/, agents/, commands/, hooks/, bin/.
- `template.cjs`: sole runtime consumer is gsd-tools.cjs (require at line ~176, dispatch `case 'template':` at ~line 358, help-text "Template Fill" section at ~lines 127-135, "template" token in the usage-error string at ~line 225). Test runner globs tests/*.test.cjs, so deleting the test file needs no runner change.
- Doc files still referencing `template select`/`template fill`/`template.cjs`: docs/SYSTEM-MAP.md, docs/system/_gaps.md, docs/system/templates-references.md, docs/system/tool-cli.md, .planning/reference/COMPARISON.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md. Do NOT edit .planning/v1.4/** (historical archive) or .planning/reference/2026-07-01-self-revision.md (the audit source document).
- ui-brand.md consumers: 10 commands/gsd2/*.md stubs + 6 get-shit-done/workflows/*.md all reference the path `references/ui-brand.md` — the file must stay at that path.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deduplicate executor prompt, extract project-context reference, trim ui-brand</name>
  <files>get-shit-done/workflows/execute-phase.md, get-shit-done/references/project-context.md, agents/gsd-executor.md, agents/gsd-ui-auditor.md, agents/gsd-ui-researcher.md, agents/gsd-phase-researcher.md, agents/gsd-planner.md, agents/gsd-test-designer.md, agents/gsd-ui-checker.md, get-shit-done/references/ui-brand.md, get-shit-done/references/ui-brand-guide.md</files>
  <action>
Three prompt-file edits, all in SOURCE tree:

**1a. Merge executor prompts in get-shit-done/workflows/execute-phase.md (lines ~261-370).**
Replace the two full `Task(...)` code fences ("Worktree mode prompt (WORKTREE_MODE=true):" and "In-place fallback prompt (WORKTREE_MODE=false):") with ONE templated block:
- A single heading like "**Executor prompt (both modes):**" followed by ONE `Task(...)` code fence containing the shared prompt with a `{MODE_BLOCK}` placeholder positioned between `</objective>` and `<execution_context>`.
- Immediately after the fence, define the two mode inserts once each: "If WORKTREE_MODE=true, `{MODE_BLOCK}` is:" followed by the existing `<worktree_isolation>` block verbatim; "If WORKTREE_MODE=false, `{MODE_BLOCK}` is:" followed by the existing `<parallel_execution>` block verbatim.
- Preserve every line of the shared sections byte-for-byte (`<objective>`, `<execution_context>` with its 4 @-refs, `<files_to_read>`, `<mcp_tools>`, `<success_criteria>`, and the `model="{executor_model}"` comment). Do not touch anything before line ~246 or after the second fence's close (step 3 "Wait for all agents..." onward stays intact).

**1b. Extract shared project-context boilerplate.**
Create `get-shit-done/references/project-context.md` with this canonical content (superset of all 7 variants, ~8 lines):

```
Before starting work, discover project context:

1. Read `./CLAUDE.md` if it exists — follow all project-specific guidelines, security requirements, and coding conventions.
2. Check `.claude/skills/` or `.agents/skills/` — if either exists:
   - List available skills and read each `SKILL.md` (lightweight, ~130 lines)
   - Load specific `rules/*.md` only as needed during the work
   - Never load full `AGENTS.md` files (100KB+ — too large for context)
```

In each of the 7 agent files (gsd-executor, gsd-ui-auditor, gsd-ui-researcher, gsd-phase-researcher, gsd-planner, gsd-test-designer, gsd-ui-checker), replace the entire body of the `<project_context>...</project_context>` block with the single line `@~/.claude/get-shit-done/references/project-context.md`, keeping the surrounding tags. Read each block before replacing to confirm no agent-specific line exists (planner pre-verified none do; if you find one, keep that line inline after the @-ref).

**1c. Trim get-shit-done/references/ui-brand.md to <60 lines.**
Keep at the SAME path (10 command stubs + 6 workflows @-include it): the `<ui_patterns>` wrapper, stage banner template + stage-name list (compressed to one line of names), checkpoint box template + the 3 type/prompt pairs, status symbol legend, progress/tasks/plans one-liners, Next Up block template, error box template, table example, and a terse bullet form of the Communication Standard + Anti-Patterns (evidence-first, no reflexive praise, confidence tied to basis verified/inferred/unknown, don't capitulate under pushback, don't over-correct into contrarianism; visual anti-patterns as one compact list). Total must be <60 lines.
Create `get-shit-done/references/ui-brand-guide.md` holding the moved illustrative prose: the ✗/✓ example pairs, the explanatory paragraphs from "Communication Standard", and any banner/box usage commentary. Start it with a one-line note: "Extended guide for ui-brand.md — NOT @-included by any command; reference reading only." Do not add any @-include of the guide anywhere.

Commit this task: `refactor(token-cut): dedupe executor prompt, shared project-context ref, trim ui-brand`
  </action>
  <verify>
`wc -l get-shit-done/references/ui-brand.md` → under 60.
`grep -c "Task(" get-shit-done/workflows/execute-phase.md` shows the executor-spawn section has one Task fence where there were two (worktree merge step 3.5 unaffected).
`for f in agents/gsd-executor.md agents/gsd-ui-auditor.md agents/gsd-ui-researcher.md agents/gsd-phase-researcher.md agents/gsd-planner.md agents/gsd-test-designer.md agents/gsd-ui-checker.md; do grep -A2 "<project_context>" "$f" | grep -q "references/project-context.md" || echo "MISSING: $f"; done` → no output.
`grep -rn "ui-brand-guide" commands/ get-shit-done/workflows/` → no @-includes of the guide.
  </verify>
  <done>One executor prompt template with conditional mode insert; 7 agents share one project-context reference; ui-brand.md <60 lines at unchanged path; guide file exists and is not @-included; committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete phase-prompt.md and remove dead template.cjs (code + docs)</name>
  <files>get-shit-done/templates/phase-prompt.md, get-shit-done/bin/lib/template.cjs, tests/template.test.cjs, get-shit-done/bin/gsd-tools.cjs, docs/SYSTEM-MAP.md, docs/system/_gaps.md, docs/system/templates-references.md, docs/system/tool-cli.md, .planning/reference/COMPARISON.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md</files>
  <action>
**2a. Delete orphaned phase-prompt.md.** First re-verify zero references: `grep -rn "phase-prompt" get-shit-done/ agents/ commands/ hooks/ bin/ scripts/ docs/ --include="*.md" --include="*.cjs" --include="*.js"` must return nothing (hits inside .planning/ history are acceptable and ignored). Then `git rm get-shit-done/templates/phase-prompt.md`.

**2b. Remove template.cjs and its CLI wiring** in get-shit-done/bin/gsd-tools.cjs:
- `git rm get-shit-done/bin/lib/template.cjs tests/template.test.cjs`
- Delete the require line: `const template = require('./lib/template.cjs');` (~line 176)
- Delete the entire `case 'template': { ... }` dispatch block (~lines 358-382)
- Delete the "Template Fill:" help-text section from the header comment (~lines 126-135: the three `template fill summary|plan|verification` entries and their option lines)
- Remove the `template, ` token from the usage-error commands list (~line 225)

**2c. Update the 8 doc files** that reference `template select`/`template fill`/`template.cjs`: docs/SYSTEM-MAP.md, docs/system/_gaps.md, docs/system/templates-references.md, docs/system/tool-cli.md, .planning/reference/COMPARISON.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md. For each: remove the template.cjs module/command entries (rows, bullets, file listings). Where prose describes the command as an available capability, delete or reword; where a doc is comparative/historical narrative (COMPARISON.md), a brief "(removed 2026-07)" note is acceptable instead of rewriting. Do NOT edit `.planning/v1.4/**` or `.planning/reference/2026-07-01-self-revision.md`.

Commit this task: `refactor(token-cut): remove dead template.cjs + orphaned phase-prompt.md`
  </action>
  <verify>
`grep -rn "template" get-shit-done/bin/gsd-tools.cjs | grep -vi "scaffold\|templates/"` → no hits referencing lib/template or template subcommands.
`node get-shit-done/bin/gsd-tools.cjs state get --raw 2>&1 | head -1` → runs without a module-not-found crash (proves the require removal is clean; run from repo root).
`grep -rln "template select\|template fill\|template\.cjs" docs/ .planning/codebase/ .planning/reference/COMPARISON.md` → no hits.
`ls get-shit-done/templates/phase-prompt.md get-shit-done/bin/lib/template.cjs tests/template.test.cjs 2>&1` → all three "No such file".
  </verify>
  <done>Three files deleted; gsd-tools.cjs boots and no longer advertises or dispatches `template`; all 8 docs updated; committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync runtime and run full test suite</name>
  <files>.claude/get-shit-done/ (runtime, via installer), hooks/dist (via build)</files>
  <action>
Run `npm run dev` (builds hooks, installs source into project-local `.claude/`). Then check for stale runtime copies of deleted files — the installer copies but may not prune: if `.claude/get-shit-done/templates/phase-prompt.md` or `.claude/get-shit-done/bin/lib/template.cjs` still exist, delete them directly (runtime pruning of removed source files is the one permitted direct `.claude/` edit).

Confirm new runtime files landed: `.claude/get-shit-done/references/project-context.md` and `.claude/get-shit-done/references/ui-brand.md` (<60 lines).

Run `npm test` — full suite must pass with tests/template.test.cjs gone.

Run the runtime CLI sanity check per repo convention (project-local path, not $HOME): `node .claude/get-shit-done/bin/gsd-tools.cjs template select summary 2>&1` → must print the unknown-command usage error, not execute.

If `npm run dev` output shows tracked-file changes worth keeping (e.g. hooks/dist stamps), commit them scoped to the files this work changed: `chore(token-cut): sync runtime after cleanup batch`. Never `git add -A` (parallel sessions share this tree).
  </action>
  <verify>
`npm test` exits 0. `node .claude/get-shit-done/bin/gsd-tools.cjs template select summary` errors with unknown command. `wc -l .claude/get-shit-done/references/ui-brand.md` < 60. `ls .claude/get-shit-done/bin/lib/template.cjs .claude/get-shit-done/templates/phase-prompt.md 2>&1` → both missing.
  </verify>
  <done>Runtime mirrors source (new refs present, deleted files pruned), full test suite green, CLI rejects `template` subcommand.</done>
</task>

</tasks>

<verification>
- `npm test` passes (template.test.cjs removed, nothing else broken).
- `grep -rn "template select\|template fill\|template\.cjs" get-shit-done/ agents/ commands/ docs/ .planning/codebase/` → zero hits.
- `grep -rn "phase-prompt" get-shit-done/ agents/ commands/ docs/` → zero hits.
- execute-phase.md has one executor prompt template; both mode inserts defined exactly once.
- All 7 agent `<project_context>` blocks contain only the shared @-reference.
- ui-brand.md <60 lines; all 16 existing @-include/reference sites unchanged (path stable); ui-brand-guide.md not @-included anywhere.
- No behavior change: worktree/in-place spawn semantics, agent discovery behavior, and command stub loading are identical — only token count differs.
</verification>

<success_criteria>
All five sub-items landed as three atomic commits; source and runtime in sync via `npm run dev`; `npm test` green; every removal verified reference-free by grep; out-of-scope items (checkpoints.md/tdd.md gating, researcher persona split) untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-kl9-token-cut-cleanup-batch-from-2026-07-01-/260702-kl9-SUMMARY.md`
</output>
