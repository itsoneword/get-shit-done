---
phase: quick-260704-m9p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/roadmap.cjs
  - get-shit-done/bin/lib/parallel-gate.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/config.cjs
  - tests/roadmap.test.cjs
  - tests/config.test.cjs
  - get-shit-done/workflows/autonomous.md
  - .planning/reference/2026-07-04-parallel-phase-execution.md
autonomous: true
requirements: []
sidecar_impact: [ARCHITECTURE]
must_haves:
  truths:
    - "`gsd-tools roadmap frontier` returns the set of incomplete phases whose hard `depends_on` phases are all complete, ignoring `sequence_after` entirely"
    - "Frontier phases that share files_modified are split into a co-schedulable subset plus a serialized remainder (axis-A guard reused from parallel-gate)"
    - "Autonomous mode, after each completion, launches every co-schedulable frontier phase as a per-worktree headless `claude -p` process, merges each on exit, and caps concurrency at max_parallel_phases"
    - "When parallelization=false OR the frontier has one phase, autonomous falls back to today's serial inline execute_phase path"
    - "Every parallel launch and merge writes a ledger record (auditable-from-ledger)"
    - "`config-get parallelization` and `config-get max_parallel_phases` return their loadConfig defaults (true, 4) even when the on-disk config.json omits those keys"
  artifacts:
    - get-shit-done/bin/lib/roadmap.cjs
    - get-shit-done/bin/lib/config.cjs
    - tests/roadmap.test.cjs
    - tests/config.test.cjs
    - get-shit-done/bin/gsd-tools.cjs
    - get-shit-done/bin/lib/core.cjs
    - get-shit-done/workflows/autonomous.md
  key_links:
    - "roadmap frontier -> parallel-gate getPhaseFiles (broken = no axis-A co-schedule guard, file-overlapping phases co-run and merge dirty)"
    - "autonomous.md iterate -> `roadmap frontier` JSON (broken = no cross-phase parallelism, silent regression to serial)"
    - "autonomous.md iterate -> `config-get parallelization`/`config-get max_parallel_phases` (broken = scheduler reads error strings on default configs, mis-branches to serial or crashes)"
    - "autonomous launcher -> worktree add/merge/remove (broken = writes escape to main tree, isolation lost)"
    - "each launch/merge -> ledger append (broken = unauditable autonomous decision, violates trust constraint)"
---

<objective>
Build the parallel-phase executor. Split the scheduling brain (testable lib) from the orchestration (prose):

- **A (testable):** a new `gsd-tools roadmap frontier` command that computes the runnable frontier — incomplete phases whose HARD `depends_on` are all satisfied (ignoring soft `sequence_after`), optionally filtered to a co-schedulable subset with no axis-A file overlap. This is the scheduler brain; it gets node:test coverage.
- **B (prose):** rewrite `autonomous.md` `<step name="iterate">` (and the execute_phase invocation) into a frontier scheduler that launches each co-schedulable frontier phase as a per-worktree headless `claude -p` process, merges on exit, caps concurrency, and falls back to serial when parallelization is off or the frontier is singular.
- **C (config/docs):** add the concurrency-cap config key, make `config-get` fall back to loadConfig defaults for keys the on-disk config omits, and document the new parallel behavior.

The P3 spike already PROVED the isolation mechanism (background `claude -p --dangerously-skip-permissions` with cwd=worktree keeps writes in-worktree; `worktree merge` returns clean). Do NOT re-litigate feasibility — build on it.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reference/2026-07-04-parallel-phase-execution.md
@get-shit-done/bin/lib/roadmap.cjs
@get-shit-done/bin/lib/parallel-gate.cjs
@get-shit-done/bin/lib/worktree.cjs
@get-shit-done/bin/lib/phase.cjs
@get-shit-done/bin/lib/config.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/workflows/autonomous.md
@CLAUDE.md

<interfaces>
Existing, reuse — do NOT reimplement:

- `roadmap.cjs cmdRoadmapAnalyze(cwd, raw, opts)` — parses ROADMAP.md into `phases[]`, each `{ number, name, depends_on, sequence_after, disk_status, roadmap_complete, ... }`. `depends_on`/`sequence_after` are raw strings like "Phase 4, Phase 11" or null. `disk_status === 'complete'` already folds in `roadmap_complete`. NOTE: this fn calls `output()` which exits — factor a non-exiting internal (see Task 1) so `frontier` can consume `phases[]` in-process.
- `parallel-gate.cjs getPhaseFiles(cwd, phaseNum)` — returns `Set<string>` of files_modified across a phase's plans. Currently an internal, NOT exported. Task 1 exports it and imports it into roadmap.cjs for the axis-A co-schedule filter. This is the "reuse parallel-gate logic" the design mandates.
- `worktree.cjs` — `cmdWorktreeAdd(cwd,dir,branch,opts)`, `cmdWorktreeMerge(cwd,branch)` (returns `{clean, conflict_files}`; NEVER aborts on conflict), `cmdWorktreeRemove(cwd,dir,opts)`. All emit JSON, exit 0 on detected-conflict state. Router: `gsd-tools worktree add|merge|remove|prune`.
- `core.cjs loadConfig(cwd)` — returns config incl. `parallelization` (boolean). Its `defaults` object supplies values when config.json omits a key; the on-disk config.json in real projects (including THIS repo) usually lacks `parallelization`. Add `max_parallel_phases` to both `defaults` and the returned object (Task 1).
- `config.cjs cmdConfigGet(cwd, keyPath, raw)` (~line 203) — reads `.planning/config.json` off disk ONLY and `error()`s "Key not found" when the key is absent. This is the blocker: `config-get parallelization` errors on default configs. Task 1 adds a defaults fallback.
- `config.cjs VALID_CONFIG_KEYS` — set of settable keys; `max_parallel_phases` must be added.
- Router: the real CLI verb is `config-get <key>` (hyphenated, top-level `case 'config-get'` in gsd-tools.cjs) — there is NO `config get` subcommand. Every invocation must use `config-get`.
- Ledger: `gsd-tools ledger append --data '{...}'` (append-only JSONL, requires GSD_RUN_ID or explicit arg).

Axis-B is moot WITHIN a frontier: if phase A hard-depends on B and A is in the frontier, B must already be complete (else A is excluded), so B is not in the frontier. Two frontier phases can never have a hard-dep edge — only axis-A (file overlap) can couple them. The co-schedule filter therefore checks axis-A only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `roadmap frontier` scheduler command + config cap knob + config-get defaults fallback + node:test coverage</name>
  <files>get-shit-done/bin/lib/roadmap.cjs, get-shit-done/bin/lib/parallel-gate.cjs, get-shit-done/bin/gsd-tools.cjs, get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/config.cjs, tests/roadmap.test.cjs, tests/config.test.cjs</files>
  <action>
1. **Factor a non-exiting analyzer** in `roadmap.cjs`: extract the body of `cmdRoadmapAnalyze` that builds the result object into `analyzeRoadmapData(cwd, opts)` returning `{ phases, ... }` WITHOUT calling `output()`. Rewrite `cmdRoadmapAnalyze` to `output(analyzeRoadmapData(cwd, opts), raw)`. Behavior must be byte-identical (existing roadmap tests still pass). Use the FULL (non-scoped) `phases` list inside frontier — never the scoped-trimmed slice.

2. **Export `getPhaseFiles`** from `parallel-gate.cjs` (add to `module.exports`; it already exists as an internal). Do not change its logic.

3. **Add `cmdRoadmapFrontier(cwd, raw)`** to `roadmap.cjs`:
   - `const { phases } = analyzeRoadmapData(cwd, { scoped: false })`.
   - `const isComplete = p => p.disk_status === 'complete'`.
   - Helper `extractDepPhaseNums(str)`: from a `depends_on` string, extract phase numbers via `/Phase\s+(\d+[A-Z]?(?:\.\d+)*)/gi` (NOT `.includes()` — avoids "Phase 1" matching "Phase 10"). Returns `string[]`.
   - **frontier** = incomplete phases (`!isComplete`) whose EVERY hard-dep number resolves to a complete phase. A phase with null/empty `depends_on` is eligible. `sequence_after` is NEVER read (that is the whole point of P1 — reading it resurrects the false-serial-edge bug).
   - **co-schedule filter (axis-A):** iterate frontier in roadmap order; keep a running `coschedulable[]`. For each candidate compute `getPhaseFiles(cwd, candidate.number)`; if it intersects the file set of any already-selected member, push `{ phase, conflicts_with, overlap_files }` to `serialized[]`; else add to `coschedulable`. (Two frontier phases cannot be axis-B coupled — see interfaces note — so only axis-A is checked.)
   - Emit JSON: `{ frontier: string[], coschedulable: string[], serialized: [{phase, conflicts_with, overlap_files}] }` via `output(result, raw)`. Import `getPhaseFiles` from `./parallel-gate.cjs` at top of roadmap.cjs.
   - Export `cmdRoadmapFrontier` (and keep the internal analyzer un-exported unless a test needs it — export `analyzeRoadmapData` too if convenient for testing).

4. **Router:** in `gsd-tools.cjs` `case 'roadmap'` add `else if (subcommand === 'frontier') { roadmap.cmdRoadmapFrontier(cwd, raw); }` and update the "Unknown roadmap subcommand" list to include `frontier`. Also add a one-line usage comment near the other `roadmap` doc lines (~line 44-46).

5. **Config cap knob:** in `core.cjs loadConfig` add `max_parallel_phases: 4` to the `defaults` object AND to the returned object (`max_parallel_phases: get('max_parallel_phases') ?? defaults.max_parallel_phases`). In `config.cjs` add `'max_parallel_phases'` to `VALID_CONFIG_KEYS`.

6. **`config-get` defaults fallback (BLOCKER FIX):** change `cmdConfigGet` (config.cjs ~line 203) so that when a requested key is ABSENT from the on-disk `.planning/config.json`, it merges in the `loadConfig(cwd)` defaults and returns the default value instead of erroring — while PRESERVING every current behavior when the key IS present on disk, and STILL erroring (`Key not found`) for keys that are neither on disk nor in defaults (e.g. `nonexistent_key`, `workflow.nonexistent`), and still erroring `No config.json` when the file is absent. Implementation: after reading the on-disk `config` object, resolve the dot-path against it; if the resolved value is `undefined`, require `./core.cjs` `loadConfig`, call `loadConfig(cwd)`, resolve the SAME dot-path against the defaults-merged result, and return that if defined; only `error('Key not found: ...')` when it is still undefined. Keep the existing `No config.json` guard (do NOT let the fallback mask a missing config file — that path must still error as today). Watch for a require cycle: `config.cjs` may `require('./core.cjs')` lazily inside the function if a top-level cycle appears.

7. **Roadmap frontier tests** — append a `describe('roadmap frontier', ...)` block to `tests/roadmap.test.cjs` (reuse existing temp-project + writeRoadmap-style helpers; mirror `parallel-gate.test.cjs` fixture style for writing PLAN files with `files_modified`). Cover at minimum:
   - Two incomplete phases, no deps → both appear in `frontier` AND `coschedulable`.
   - Phase A `depends_on` Phase B, B incomplete → A absent from `frontier`.
   - Phase A `depends_on` Phase B, B complete (roadmap checkbox `[x]` or summary present) → A present in `frontier`.
   - Phase A `sequence_after` Phase B (no `depends_on`), B incomplete → A PRESENT in `frontier` (soft edge ignored).
   - Two frontier phases sharing a `files_modified` entry → one in `coschedulable`, the other in `serialized` with `conflicts_with` set.

8. **`config-get` fallback tests** — append to the existing `describe('config-get command', ...)` block in `tests/config.test.cjs` (reuse `createTempProject`, `runGsdTools`, and write a config.json that OMITS `parallelization` and `max_parallel_phases`, mirroring this repo's real config). Cover:
   - `config-get parallelization` on a config.json lacking the key → success, returns `true` (the loadConfig default).
   - `config-get max_parallel_phases` on a config.json lacking the key → success, returns `4` (the new default).
   - `config-get parallelization` on a config.json that SETS `parallelization: false` → success, returns `false` (on-disk value wins over default).
   - Regression: the existing `errors for nonexistent key` and `errors for deeply nested nonexistent key` cases still error `Key not found` (defaults fallback must NOT rescue keys absent from defaults), and `errors when config.json does not exist` still errors `No config.json`.
  </action>
  <verify>node --test tests/roadmap.test.cjs tests/config.test.cjs && node get-shit-done/bin/gsd-tools.cjs config-get parallelization && node get-shit-done/bin/gsd-tools.cjs config-get max_parallel_phases</verify>
  <done>New `roadmap frontier` command returns `{frontier, coschedulable, serialized}`; all listed roadmap + config-get test cases pass; existing roadmap and config tests still green; `getPhaseFiles` exported from parallel-gate; `max_parallel_phases` defaults to 4 and is a recognized config key; `config-get parallelization` and `config-get max_parallel_phases` run clean (return true / 4) against a config.json that omits both keys, while unknown keys still error.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite autonomous.md iterate/execute into a frontier scheduler with per-worktree headless runner</name>
  <files>get-shit-done/workflows/autonomous.md</files>
  <action>
Replace the serial `<step name="iterate">` (and adjust how execute_phase is invoked) so that, after each phase completion, the loop schedules the frontier instead of picking the next roadmap-ordered phase. Keep all existing per-phase behavior (discuss → plan → execute → 3d verify/gap-closure/human_needed routing, ledger, mailbox, PHASE RESULT contract) intact — you are changing HOW MANY phases run and their isolation, not the per-phase lifecycle.

Write the rewritten iterate step as explicit prose + shell:

1. **Compute frontier + read config (use the real `config-get` verb):**
   ```bash
   FRONTIER=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap frontier)
   MAX_PAR=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get max_parallel_phases)
   PAR=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get parallelization)
   ```
   `config-get` now returns the loadConfig defaults (parallelization=true, max_parallel_phases=4) even when config.json omits the keys, so these reads never emit error strings on a default project. Parse `coschedulable[]` (the phases to run this round) and `serialized[]` (deferred to a later round — surface them, do not drop).

2. **Establish an UNCONDITIONAL run-scoped log dir (stdout capture fix):** synthesize a run id when `$GSD_RUN_ID` is unset and always create the log dir — do NOT gate any of this on harness mode:
   ```bash
   RUN_ID="${GSD_RUN_ID:-$(date +%Y%m%d-%H%M%S)-$$}"
   LOG_DIR=".planning/run/$RUN_ID"
   mkdir -p "$LOG_DIR"
   ```

3. **Serial fallback (unchanged path):** if `PAR` is `false` OR `coschedulable` has ≤1 phase → run the single next phase via today's INLINE path (loop back to `execute_phase` in-process, no worktree). This preserves current behavior exactly for the common case. Document that this is the fallback.

4. **Parallel path (≥2 co-schedulable phases, parallelization on):** launch up to `MAX_PAR` phases concurrently. For each phase N in `coschedulable` (respecting the cap):
   - `worktree add .worktrees/phase-N gsd/parallel-phase-N` (via `gsd-tools worktree add`).
   - Launch a BACKGROUND headless process from the ORCHESTRATOR SHELL (NOT a Task/Skill subagent — the CLAUDE.md orchestrator-level constraint): `claude -p "run gsd autonomous single phase N" --dangerously-skip-permissions` with cwd set to the worktree, invoking `/gsd2:autonomous --phase N` (single-phase mode, which already emits the machine-greppable `PHASE RESULT:` contract line). **Redirect stdout+stderr UNCONDITIONALLY** to a per-phase log in the run-scoped dir: append `> "$LOG_DIR/phase-N.log" 2>&1` to every launch (this is the ONLY stdout source the merge decision reads — it must exist in and out of harness mode).
   - Ledger the launch: `gsd-tools ledger append --data '{"decision":"launched parallel phase N in worktree","evidence":"headless claude -p --dangerously-skip-permissions cwd=.worktrees/phase-N log=$LOG_DIR/phase-N.log","confidence":"HIGH","phase":"N",...}'`.
   - **On process exit:** grep the phase's log (`$LOG_DIR/phase-N.log`) for the final `PHASE RESULT:` line for the outcome — the log always exists (step 2/4 capture is unconditional), so this decision is never harness-gated. If `completed` → `gsd-tools worktree merge gsd/parallel-phase-N`; inspect `{clean}` — if `clean:false`, SURFACE the `conflict_files` (append a mailbox question in harness mode; do NOT abort the merge — worktree.cjs already leaves it reviewable), and do NOT remove the worktree. If clean → `worktree remove .worktrees/phase-N --branch gsd/parallel-phase-N`. If `failed`/`parked` (or no `PHASE RESULT:` line found) → leave the worktree for review, ledger the outcome, do NOT merge. Ledger every merge and removal.
   - **Failure isolation:** one phase failing/parking must not abort its siblings — continue draining the other launched processes.

5. **Loop:** after all launched processes of this round drain (and merges resolve), re-run `roadmap frontier` and repeat until frontier is empty; then proceed to `lifecycle` (unchanged). Single-phase mode (`SINGLE_PHASE` set) still never enters iterate.

6. Add an in-prose note (near the parallel path) that parallel phases run headless with `--dangerously-skip-permissions` BY CONSTRUCTION (isolation via worktree, unattended autonomous run) and that this is acceptable per the P3 spike + logged to the ledger.

Do NOT touch the resume-route (3a.0), smart_discuss, or 3d routing internals beyond what is needed to invoke them per-worktree.
  </action>
  <verify>grep -q "roadmap frontier" get-shit-done/workflows/autonomous.md && grep -q "config-get max_parallel_phases" get-shit-done/workflows/autonomous.md && grep -q "config-get parallelization" get-shit-done/workflows/autonomous.md && ! grep -q "config get " get-shit-done/workflows/autonomous.md && grep -q "worktree add" get-shit-done/workflows/autonomous.md && grep -q "dangerously-skip-permissions" get-shit-done/workflows/autonomous.md && grep -q "worktree merge" get-shit-done/workflows/autonomous.md && grep -q "LOG_DIR" get-shit-done/workflows/autonomous.md && grep -q "ledger append" get-shit-done/workflows/autonomous.md && grep -qi "serial" get-shit-done/workflows/autonomous.md</verify>
  <done>iterate step computes the frontier, reads config via `config-get` (never `config get`), unconditionally captures each headless launch's stdout to `$LOG_DIR/phase-N.log` under a synthesized-or-inherited run id, launches ≥2 co-schedulable phases as per-worktree headless `claude -p` processes (orchestrator shell, not subagents) capped at max_parallel_phases, reads that log to decide merge-vs-leave, merges clean phases and surfaces conflicts without aborting, ledgers each launch+merge, and falls back to the inline serial path when parallelization=false or the frontier is singular. Single-phase mode and lifecycle untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Document the concurrency cap and parallel behavior</name>
  <files>get-shit-done/workflows/autonomous.md, .planning/reference/2026-07-04-parallel-phase-execution.md</files>
  <action>
1. In `autonomous.md` `<purpose>` (and/or `<success_criteria>`), add a short line stating the loop now runs independent frontier phases in parallel (per-worktree headless), capped by the `max_parallel_phases` config key (default 4), and falls back to serial when `parallelization=false` or only one phase is runnable.
2. In the reference doc `2026-07-04-parallel-phase-execution.md`, append a short "P4 — IMPLEMENTED" note under the P3 GO section: record the new `roadmap frontier` command, the `max_parallel_phases` config key + default, the `config-get` defaults-fallback fix (so default projects read the knobs cleanly), that parallel phases run headless/skip-permissions by construction with unconditional per-phase stdout capture, and that every launch/merge is ledgered. Keep it to a few bullets — this is a marker, not a redesign.
  </action>
  <verify>grep -q "max_parallel_phases" get-shit-done/workflows/autonomous.md && grep -qi "roadmap frontier\|P4" .planning/reference/2026-07-04-parallel-phase-execution.md</verify>
  <done>Config key `max_parallel_phases` (default 4) and the parallel/serial-fallback behavior are documented in autonomous.md; the reference doc records P4 as implemented with the frontier command, config key, config-get fallback, headless/skip-permissions + unconditional-capture note, and ledger auditability.</done>
</task>

</tasks>

<verification>
- `node --test tests/roadmap.test.cjs tests/config.test.cjs` passes (frontier cases + existing analyze cases + config-get defaults-fallback cases + existing config cases).
- `node --test tests/parallel-gate.test.cjs` still passes (getPhaseFiles export is additive).
- `node get-shit-done/bin/gsd-tools.cjs roadmap frontier --cwd .` runs without error against this repo and returns `{frontier, coschedulable, serialized}`.
- `node get-shit-done/bin/gsd-tools.cjs config-get max_parallel_phases` returns 4 and `node get-shit-done/bin/gsd-tools.cjs config-get parallelization` returns true — BOTH clean against this repo's config.json, which omits both keys.
- autonomous.md contains the frontier scheduler, per-worktree headless launch, unconditional `$LOG_DIR` stdout capture, merge, cap, serial fallback, `config-get` reads (no `config get`), and ledger calls (grep checks in Task 2/3 verify).
</verification>

<success_criteria>
- The scheduling brain is testable lib code: `roadmap frontier` surfaces incomplete phases with satisfied hard deps, ignores `sequence_after`, and separates file-overlapping frontier phases into a serialized remainder — all covered by node:test.
- `config-get` returns loadConfig defaults for keys the on-disk config omits (so `parallelization` and `max_parallel_phases` read cleanly on every project), while still erroring for truly-unknown keys and a missing config file — covered by node:test.
- The orchestration is prose: autonomous.md launches co-schedulable frontier phases as per-worktree headless `claude -p` processes from the orchestrator shell, unconditionally captures each launch's stdout to a run-scoped per-phase log used for the merge decision, caps concurrency at `max_parallel_phases`, merges clean phases (surfacing conflicts without aborting), and falls back to the serial inline path when parallelization is off or a single phase is runnable.
- Every parallel launch and merge is written to the ledger (auditable-from-ledger).
- Zero new npm dependencies; existing per-phase lifecycle, single-phase mode, and lifecycle step are preserved.
</success_criteria>

<output>
After completion, create `.planning/quick/260704-m9p-parallel-phase-frontier-scheduler-per-wo/260704-m9p-SUMMARY.md`.
</output>
</content>
</invoke>
