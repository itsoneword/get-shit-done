---
milestone: v1.6
milestone_name: Autonomous Supervision Harness
audited: 2026-06-18
status: tech_debt
scores:
  requirements: 18/18
  phases: 6/6
  integration: 5/5 flows complete
  flows: 5/5
gaps:
  requirements: []   # no unsatisfied or orphaned requirements — all 18 wired and verified
  integration:
    - id: "I-1"
      severity: warning
      title: "triage.cjs cmdMailboxAppend calls process.exit(1) — uncatchable by triage.cjs try/catch"
      file: "get-shit-done/bin/lib/triage.cjs:234-240"
      detail: "cmdMailboxAppend (mailbox.cjs ~273-297) calls process.exit on validation/run-dir failure; the surrounding try/catch in cmdTriageRun cannot catch it, so the 'count per-item failures and continue' contract is bypassed in the failure path. Normal path (run dir exists + internally-built proposals) never triggers it; observable overnight outcome stays correct (non-zero exit -> PHASE_FAILURE phase=triage). Latent robustness bug, not an E2E break."
      affected_reqs: ["TRIAGE-01"]
    - id: "I-2"
      severity: warning
      title: "Skill not listed in allowed-tools for overnight.md / autonomous.md command stubs"
      file: "commands/gsd2/overnight.md:5-12"
      detail: "Both stubs call Skill(...) but omit Skill from allowed-tools. Works at orchestrator level per design (CLAUDE.md constraint: subagents lack Skill grants), but the orchestrator-level-only requirement is implicit rather than documented in the stub."
      affected_reqs: ["RUN-01"]
    - id: "I-3"
      severity: info
      title: "Programmatic `triage run` always emits needs-input verdict"
      file: "get-shit-done/bin/lib/triage.cjs:224-229; get-shit-done/workflows/overnight.md:347-348"
      detail: "Overnight's step 6.5 emits needs-input placeholders by design (LLM verdict reasoning lives in triage.md, run via standalone /gsd2:triage). Documented and accepted; inbox renders needs-input correctly."
      affected_reqs: ["TRIAGE-01", "TRIAGE-02"]
    - id: "I-4"
      severity: info
      title: "supersedingRecordExists exported but unused in workflow prose"
      file: "get-shit-done/bin/lib/triage.cjs:145,253"
      detail: "autonomous.md step R3 implements the idempotency check inline over `ledger list --raw` rather than calling the export. Functionally correct (no behavior gap); dead export surface."
      affected_reqs: ["PARK-03"]
  flows: []   # no broken end-to-end flows
deferred_human_uat:
  - phase: "11-escalation-contract-discuss-phase-wiring"
    file: ".planning/v1.6/phases/11-escalation-contract-discuss-phase-wiring/11-HUMAN-UAT.md"
    item: "ESC-03 calibration sign-off — 11-CALIBRATION.md ships PENDING; the overnight health check fails closed (greps uppercase PASS) until a human runs a live calibration and records the outcome. This blocks the FIRST real overnight run — the milestone's headline capability."
    blocks: "first /gsd2:overnight run (operational gate, not a code gap)"
  - phase: "14-multi-lens-discussion-loop"
    file: ".planning/v1.6/phases/14-multi-lens-discussion-loop/14-HUMAN-UAT.md"
    item: "4 live-smoke UAT items remain [pending] — deferred per explicit user decision until all v1.6 phases complete (now reached). An orchestrator smoke run was performed during 14-03 (loop-2026-06-12T14-33-21-305Z) and documented; full human UAT still open."
    blocks: "nothing operationally — verification-debt only"
nyquist:
  compliant_phases: ["10", "13", "14"]
  partial_phases: ["11", "12", "15"]   # VALIDATION.md exists, nyquist_compliant:false
  missing_phases: []
  overall: "partial — all phases have VALIDATION.md; 3 marked nyquist_compliant:false (prose/CLI-wiring phases). Run /gsd2:validate-phase 11|12|15 to close if formal Nyquist coverage is wanted."
tech_debt:
  - phase: 14-multi-lens-discussion-loop
    items:
      - "Hard-wrapped artifacts burn lens-agent retries on verbatim-anchor validation (5/9 in smoke run) — consider whitespace-normalized anchor matching."
      - "`discuss-loop survivors --data` requires a NESTED per-round array; workflow prose reads as flat — clarify prose."
  - phase: 15-resume-logic-backlog-triage-worker
    items:
      - "Harden triage.cjs failure path (I-1): make cmdMailboxAppend raise instead of process.exit, or pre-validate in cmdTriageRun, so per-item failure counting actually works."
      - "supersedingRecordExists (I-4): either wire autonomous.md R3 to the export, or drop the unused export."
  - phase: cross-cutting
    items:
      - "Document Skill dependency / orchestrator-level-only in overnight.md + autonomous.md allowed-tools (I-2)."
carry_over_out_of_scope:
  - "v1.5 human UAT still open for phases 02/06/09 (separate milestone)."
  - "v1.5 archival via /gsd2:complete-milestone still pending."
---

# v1.6 Milestone Audit — Autonomous Supervision Harness

**Status:** `tech_debt` — all requirements satisfied and all E2E flows wired; no critical blockers. Deferred human-UAT (phases 11, 14) and a small set of robustness/doc warnings need conscious review before archival, and the ESC-03 calibration gate must be completed before the first real overnight run.

**Goal (from ROADMAP):** Delegate the human's job of monitoring 5–10 parallel GSD sessions to an agentic supervision loop with a predictable, auditable escalation mechanism — the human reviews one inbox of logged decisions and parked questions instead of babysitting tabs.

## Requirements Coverage — 18/18 satisfied

| REQ | Phase | Traceability | VERIFICATION | Final |
|-----|-------|--------------|--------------|-------|
| LEDGER-01/02/03 | 10 | `[x]` | passed | satisfied |
| ESC-01/02/03 | 11 | `[x]` | human_needed* | satisfied (ESC-03 calibration deferred) |
| PARK-01/02/04 | 12 | `[x]` | passed | satisfied |
| PARK-03 | 12→15 | `[x]` | passed (15) | satisfied (snapshot/staleness in 12, replay in 15 step 3a.0) |
| RUN-01/02/03/04 | 13 | `[x]` | passed | satisfied |
| LOOP-01/02 | 14 | `[x]` | human_needed* | satisfied (4 smoke items deferred) |
| TRIAGE-01/02 | 15 | `[x]` | passed | satisfied |

\* `human_needed` = automated verification passed; only human-UAT items remain (deferred, see below). No `gaps_found` among the six phases. No orphaned requirements (all 18 appear in phase VERIFICATION tables).

**PARK-03 traceability note:** REQUIREMENTS.md maps PARK-03 to Phase 12, but the replay implementation landed in Phase 15 (step 3a.0) by an explicit design deferral recorded in all three Phase-12 plans. Both phases verified; the requirement is fully satisfied across the two.

## Phase Verification Summary

| Phase | VERIFICATION | Nyquist (VALIDATION.md) |
|-------|--------------|--------------------------|
| 10 Decision Ledger + CLI | passed | compliant |
| 11 Escalation Contract | human_needed (ESC-03 gate) | partial |
| 12 Park-Don't-Block Mailbox | passed | partial |
| 13 Overnight Runner | passed | compliant (wave_0 done) |
| 14 Multi-Lens Discussion Loop | human_needed (4 smoke items) | compliant (wave_0 done) |
| 15 Resume Logic + Triage Worker | passed | partial |

## Cross-Phase Integration — 5/5 flows complete

The integration checker verified the milestone's layered substrate (shared `.planning/run/<run-id>/` layout + DECISIONS.jsonl + MAILBOX.jsonl, GSD_RUN_ID gate) is consistently wired end-to-end:

1. **Overnight unattended run** (RUN-01/02/03/04) — overnight.md → run init → ESC-03 health check → autonomous.md per phase → discuss-phase `--auto` → escalation evaluator → ledger/park/mailbox → PHASE RESULT → run record-phase/snapshot → run report. **Complete.**
2. **PARK-03 E2E** — park (12) → mailbox answer (12) → resume replay (15 step 3a.0). All field names match (`question_id`, `resume_instruction`, `changed`/`unchanged`/`missing`/`git_range`). **Complete.**
3. **Escalation evaluator → ledger** (ESC-01/02) — fires only under GSD_RUN_ID; `--data` dispatch falls back to env run-id. **Complete.**
4. **Triage → inbox** (TRIAGE-01/02) — `triage-verdict:` prefix contract consistent across triage.cjs / triage.md / inbox.md; `status:"pending"` overrides the CLI `open` default; accept prints routing, never executes. **Complete.**
5. **Morning report** (RUN-04) — reads exactly RUN-META.json + DECISIONS.jsonl + MAILBOX.jsonl, never run.log. **Complete.**

GSD_RUN_ID gate is byte-equivalent for interactive sessions across all phases. 22 cross-phase exports connected, 0 orphaned routes, 0 unprotected run-context operations.

### Integration findings (non-blocking)
- **I-1 (warning)** — triage.cjs failure path relies on a try/catch that can't catch `cmdMailboxAppend`'s `process.exit`. Normal path unaffected; harden for robustness.
- **I-2 (warning)** — `Skill` omitted from overnight.md/autonomous.md `allowed-tools` (works at orchestrator level; document the dependency).
- **I-3 (info)** — programmatic `triage run` emits `needs-input` placeholders by design.
- **I-4 (info)** — `supersedingRecordExists` exported but unused (autonomous.md implements inline).

## Definition of Done — assessment

The milestone goal is **structurally achieved**: every harness component (ledger, escalation contract, park/mailbox, overnight runner, discussion loop, triage worker) is built, unit-tested, and wired into the shared run substrate, with the propose-never-dispose and audit-from-ledger-alone invariants enforced in code. The single thing standing between "built" and "operational" is the **ESC-03 trust-ladder calibration** — a deliberate human gate, not a code gap. The overnight runner correctly fails closed until it is completed.

## Recommended actions before archival

1. **Complete the ESC-03 calibration** (run a live interactive phase under a run-id, score escalation precision against `11-GOLDEN-SET.md`, record the outcome in `11-CALIBRATION.md`). This unlocks the first overnight run — the milestone's headline capability. → `/gsd2:verify-work 11`
2. **Close or consciously defer the Phase 14 human-UAT** (4 live-smoke items). → `/gsd2:verify-work 14`
3. **Optionally** address the two warnings (I-1 triage robustness, I-2 allowed-tools doc) and tech-debt items — small, non-blocking. → `/gsd2:plan-milestone-gaps` if you want them as a cleanup phase, or track in backlog.
4. Then `/gsd2:complete-milestone v1.6` to archive (accepting any remaining tech debt into the backlog).
