---
phase: 06-skill-self-sufficiency-audit-and-port-superpo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/tdd.md
  - .claude/get-shit-done/references/tdd.md
  - agents/gsd-executor.md
  - .claude/agents/gsd-executor.md
  - get-shit-done/workflows/execute-plan.md
  - .claude/get-shit-done/workflows/execute-plan.md
  - agents/gsd-planner.md
  - .claude/agents/gsd-planner.md
  - .claude/gsd-local-patches/agents/gsd-planner.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "tdd.md states the Iron Law verbatim and enforces watch-it-fail as mandatory"
    - "The planner exempts agent/prompt/workflow/reference edits from tdd=true tagging"
    - "When a task is tdd=true, the executor is told to verify the test visibly fails before writing code"
  artifacts:
    - get-shit-done/references/tdd.md
    - agents/gsd-executor.md
    - get-shit-done/workflows/execute-plan.md
    - agents/gsd-planner.md
  key_links:
    - "gsd-executor <tdd_execution> RED step -> tdd.md Iron Law (broken = executor skips watch-it-fail)"
    - "gsd-planner TDD Detection exemption -> prevents force-tagging GSD's own prompt-edit work as tdd=true"
---

<objective>
Port superpowers execution-time TDD discipline (Gap 1) into GSD. Today `tdd.md` and the executor define RED-GREEN-REFACTOR but carry no enforcement: no Iron Law, no mandatory watch-it-fail, no rationalization/red-flag self-check, and no exemption for GSD's own prompt/agent/workflow edits. This plan (a) extends `tdd.md` with the Iron Law + watch-it-fail + rationalization table + red flags + the agent-change exemption, (b) hardens the executor's `<tdd_execution>` block and the `execute-plan.md` `<tdd_plan_execution>` block to stop and verify the test fails, and (c) adds an explicit exemption clause to the planner's TDD-tagging heuristic so agent/prompt/workflow/reference edits are not force-tagged `tdd=true`.

This is a faithful port. The exact text to write is in this plan's task actions, drawn verbatim from RESEARCH.md's extracted content. No new commands. Per CONTEXT.md these are prompt/reference edits — do NOT tag any task here `tdd=true`; validation is structural (grep/file-exists).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-CONTEXT.md
@.planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-RESEARCH.md
@.planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-VALIDATION.md

## Source↔Runtime Mirror Rule (MANDATORY)
Every edit has two homes. Source is committed; runtime (`.claude/`) is gitignored.
- **Reference files** (`get-shit-done/references/tdd.md`) carry NO `~/.claude/` path tokens. After editing source, `cp` source → runtime for byte-identity. Do NOT hand-retype.
- **Agent/workflow files** (`agents/gsd-executor.md`, `get-shit-done/workflows/execute-plan.md`, `agents/gsd-planner.md`) DO carry path tokens: source uses `~/.claude/`, runtime uses the absolute `/home/cleversol/gsd2/mine/.claude/`. Apply the SAME textual edit to both; do NOT `cp` (it would corrupt the path tokens). Only the path token may differ between twins.
- **gsd-planner.md has THREE copies**: `agents/gsd-planner.md` (source), `.claude/agents/gsd-planner.md` (runtime), `.claude/gsd-local-patches/agents/gsd-planner.md` (a diverged copy ~2 lines behind). Apply ONLY the narrow exemption-clause edit to the local-patches copy — do NOT reconcile its other divergence.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tdd.md with Iron Law, watch-it-fail, rationalization table, red flags, agent-change exemption</name>
  <files>get-shit-done/references/tdd.md, .claude/get-shit-done/references/tdd.md</files>
  <read_first>
    - get-shit-done/references/tdd.md (current state — see what RED-GREEN-REFACTOR content already exists; append, don't duplicate)
    - ~/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0/skills/test-driven-development/SKILL.md (the source skill being ported — port faithfully, not from memory)
    - .planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-RESEARCH.md (Gap 1 Edit 1 — carries the exact extracted content to write)
  </read_first>
  <action>
Append a new section to `get-shit-done/references/tdd.md` titled `## Execution-Time Discipline (Iron Law)`. Write the following content faithfully (this is the verbatim port from the superpowers source skill, extracted in RESEARCH.md Gap 1 Edit 1):

1. **The Iron Law** — a boxed/bolded rule near the top of the new section, verbatim:
   `NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST`

2. **Watch-it-fail (MANDATORY):** Write the rule that after writing a test you MUST run it and explicitly confirm it fails. Never skip. A test that passes immediately proves nothing — it may be testing the wrong thing, or the code may already exist. Include the sentence: "Violating the letter of the rules is violating the spirit of the rules."

3. **Agent/prompt/workflow/reference exemption:** A clearly labeled paragraph stating that edits which change agent behavior via prompt, tools, or reference files are exempt from the Iron Law when the behavior is not unit-testable. Use wording that contains the literal sequence `agent`, `prompt`, `workflow`, and `reference` close together (e.g. "Changes to agent behavior via prompt edits, tool edits, workflow-only modifications, and reference files are exempt from tdd=true when behavior is not unit-testable."). Tell the planner to mark such tasks `tdd="false"` explicitly so they aren't force-tagged.

4. **Rationalization table** — a two-column markdown table titled "Rationalizations and rebuttals" with these 11 rows verbatim:
   | Rationalization | Reality |
   |---|---|
   | "Too simple to test" | "Simple code breaks. Test takes 30 seconds." |
   | "I'll test after" | "Tests passing immediately prove nothing." |
   | "Tests after achieve same goals" | "Tests-after = 'what does this do?' Tests-first = 'what should this do?'" |
   | "Already manually tested" | "Ad-hoc ≠ systematic. No record, can't re-run." |
   | "Deleting X hours is wasteful" | "Sunk cost fallacy. Keeping unverified code is technical debt." |
   | "Keep as reference, write tests first" | "You'll adapt it. That's testing after. Delete means delete." |
   | "Need to explore first" | "Fine. Throw away exploration, start with TDD." |
   | "Test hard = design unclear" | "Listen to test. Hard to test = hard to use." |
   | "TDD will slow me down" | "TDD faster than debugging. Pragmatic = test-first." |
   | "Manual test faster" | "Manual doesn't prove edge cases. You'll re-test every change." |
   | "Existing code has no tests" | "You're improving it. Add tests for existing code." |

5. **Red Flags** — a bulleted list of these 13 items verbatim, followed by the prescriptive conclusion line "All red flags mean: Delete code. Start over with TDD." (the conclusion is mandatory — without it the list is descriptive, not prescriptive):
   - Code before test
   - Test after implementation
   - Test passes immediately
   - Can't explain why test failed
   - Tests added "later"
   - Rationalizing "just this once"
   - "I already manually tested it"
   - "Tests after achieve the same purpose"
   - "It's about spirit not ritual"
   - "Keep as reference" or "adapt existing code"
   - "Already spent X hours, deleting is wasteful"
   - "TDD is dogmatic, I'm being pragmatic"
   - "This is different because..."

After editing source, mirror to runtime by byte-copy (reference files carry no path tokens):
`cp get-shit-done/references/tdd.md .claude/get-shit-done/references/tdd.md`
  </action>
  <verify>
grep -qi "no production code without a failing test" get-shit-done/references/tdd.md && \
grep -qi "too simple to test" get-shit-done/references/tdd.md && \
grep -qiE "agent.*prompt.*workflow|prompt.*workflow.*reference" get-shit-done/references/tdd.md && \
grep -qi "Delete code. Start over with TDD" get-shit-done/references/tdd.md && \
diff -q get-shit-done/references/tdd.md .claude/get-shit-done/references/tdd.md && echo PASS
  </verify>
  <acceptance_criteria>
    - `grep -qi "no production code without a failing test" get-shit-done/references/tdd.md` (Iron Law present, source)
    - `grep -qi "too simple to test" get-shit-done/references/tdd.md` (rationalization table present, source)
    - `grep -qiE "agent.*prompt.*workflow|prompt.*workflow.*reference" get-shit-done/references/tdd.md` (agent-change exemption present, source)
    - `grep -qi "Delete code. Start over with TDD" get-shit-done/references/tdd.md` (red-flags prescriptive conclusion present, source)
    - `diff -q get-shit-done/references/tdd.md .claude/get-shit-done/references/tdd.md` (runtime twin is byte-identical)
  </acceptance_criteria>
  <done>tdd.md (source + byte-identical runtime twin) contains the Iron Law, mandatory watch-it-fail, the 11-row rationalization table, the 13 red flags with the "Delete code. Start over with TDD." conclusion, and the agent/prompt/workflow/reference exemption.</done>
</task>

<task type="auto">
  <name>Task 2: Harden watch-it-fail enforcement in gsd-executor and execute-plan TDD blocks</name>
  <files>agents/gsd-executor.md, .claude/agents/gsd-executor.md, get-shit-done/workflows/execute-plan.md, .claude/get-shit-done/workflows/execute-plan.md</files>
  <read_first>
    - agents/gsd-executor.md (current state — `<tdd_execution>` block ~L216-227 and execution_flow step ~L87 "If tdd=\"true\", follow the TDD flow.")
    - get-shit-done/workflows/execute-plan.md (current state — `<tdd_plan_execution>` block ~L184-195, the plan-level orchestration)
    - .planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-RESEARCH.md (Gap 1 Edit 2 + Edit 4 — exact insertion text and anchors)
  </read_first>
  <action>
Locate edits by named block + quoted anchor text (line numbers may have drifted; read the file first).

**Edit A — `agents/gsd-executor.md`, `<tdd_execution>` block.** Find the RED step line containing "Run them — they should fail." Immediately after it, insert this watch-it-fail enforcement (verbatim):
> **STOP before continuing.** Verify the test output explicitly shows a failure. A test that passes immediately is wrong — delete the code and start over. See the Iron Law in `~/.claude/get-shit-done/references/tdd.md`.

At the bottom of the `<tdd_execution>` block, append a rationalization counter:
> If any rationalization arises ("already manually tested," "too simple to test," "keep as reference"), treat it as a red flag and apply the Iron Law. The full rationalization table and red-flags list are in `~/.claude/get-shit-done/references/tdd.md`.

**Edit B — `agents/gsd-executor.md`, execution_flow step.** Find the line `If tdd="true", follow the TDD flow.` (in the `execute_tasks` step, ~L87). Replace it with:
`If tdd="true", follow the TDD flow and enforce the Iron Law (watch-it-fail is mandatory — see ~/.claude/get-shit-done/references/tdd.md). Agent/prompt/workflow/reference edits are exempt from tdd="true".`

**Edit C — `get-shit-done/workflows/execute-plan.md`, `<tdd_plan_execution>` block.** Find the RED step of the plan-level TDD orchestration (the step that writes/runs the failing test). Insert the same watch-it-fail stop point + Iron Law reference:
> **STOP — verify the test visibly fails before writing any production code.** A test that passes immediately is wrong. See the Iron Law in `~/.claude/get-shit-done/references/tdd.md`.

**Mirror to runtime twins (token substitution, NOT cp — these files carry `~/.claude/` tokens):** apply the identical edits to `.claude/agents/gsd-executor.md` and `.claude/get-shit-done/workflows/execute-plan.md`, replacing each `~/.claude/` token with `/home/cleversol/gsd2/mine/.claude/`.
  </action>
  <verify>
grep -qi "STOP before continuing" agents/gsd-executor.md && \
grep -qi "STOP before continuing" .claude/agents/gsd-executor.md && \
grep -qiE "STOP.*verify the test visibly fails|verify the test visibly fails" get-shit-done/workflows/execute-plan.md && \
grep -qiE "STOP.*verify the test visibly fails|verify the test visibly fails" .claude/get-shit-done/workflows/execute-plan.md && \
grep -qi "exempt from tdd" agents/gsd-executor.md && echo PASS
  </verify>
  <acceptance_criteria>
    - `grep -qi "STOP before continuing" agents/gsd-executor.md` (executor RED-step watch-it-fail present, source)
    - `grep -qi "STOP before continuing" .claude/agents/gsd-executor.md` (executor runtime twin)
    - `grep -qiE "verify the test visibly fails" get-shit-done/workflows/execute-plan.md` (execute-plan watch-it-fail present, source)
    - `grep -qiE "verify the test visibly fails" .claude/get-shit-done/workflows/execute-plan.md` (execute-plan runtime twin)
    - `grep -qi "exempt from tdd" agents/gsd-executor.md` (exemption note in execution_flow, source)
    - Runtime twins use absolute `/home/cleversol/gsd2/mine/.claude/` path tokens, source uses `~/.claude/` (verify by reading the inserted lines in each twin)
  </acceptance_criteria>
  <done>The executor's `<tdd_execution>` block and execute-plan.md's `<tdd_plan_execution>` block both stop to verify the test fails before production code, reference the Iron Law in tdd.md, and the executor execution_flow notes the agent-edit exemption — in both source and runtime twins with correct path tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Add agent-change exemption clause to gsd-planner TDD Detection (all three copies)</name>
  <files>agents/gsd-planner.md, .claude/agents/gsd-planner.md, .claude/gsd-local-patches/agents/gsd-planner.md</files>
  <read_first>
    - agents/gsd-planner.md (current state — `## TDD Detection` at ~L122 in the `<task_design>` section)
    - .claude/gsd-local-patches/agents/gsd-planner.md (the diverged third copy — `## TDD Detection` at ~L120; apply ONLY this narrow edit, do not reconcile the rest)
    - .planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-RESEARCH.md (Gap 1 Edit 3 — exact clause text + the three-copy pitfall)
  </read_first>
  <action>
In the `## TDD Detection` section (after the existing sentence ending "...with a `<behavior>` block when appropriate."), insert this exemption clause verbatim:

> **Exempt from tdd="true":** changes to agent behavior via prompt edits, tool/reference edits, and workflow-only modifications where behavior cannot be unit-tested. Mark these `tdd="false"` explicitly so they are not force-tagged.

Apply this SAME edit to ALL THREE copies of gsd-planner.md (this is Pitfall 2 — the easiest miss):
1. `agents/gsd-planner.md` (source, committed)
2. `.claude/agents/gsd-planner.md` (runtime — no path tokens in this clause, so the text is identical)
3. `.claude/gsd-local-patches/agents/gsd-planner.md` (diverged third copy — insert the same clause after its own `## TDD Detection` sentence; do NOT touch its other divergence)
  </action>
  <verify>
grep -qi "Exempt from tdd" agents/gsd-planner.md && \
grep -qi "Exempt from tdd" .claude/agents/gsd-planner.md && \
grep -qi "Exempt from tdd" .claude/gsd-local-patches/agents/gsd-planner.md && echo PASS
  </verify>
  <acceptance_criteria>
    - `grep -qi "Exempt from tdd" agents/gsd-planner.md` (source)
    - `grep -qi "Exempt from tdd" .claude/agents/gsd-planner.md` (runtime twin)
    - `grep -qi "Exempt from tdd" .claude/gsd-local-patches/agents/gsd-planner.md` (local-patches third copy — Pitfall 2)
    - The clause names prompt edits, tool/reference edits, and workflow-only modifications (verify by reading)
  </acceptance_criteria>
  <done>All three gsd-planner.md copies carry the agent/prompt/workflow/reference exemption clause in `## TDD Detection`, so the planner does not force-tag GSD's own prompt-edit work as tdd="true".</done>
</task>

</tasks>

<verification>
Run the full Gap 1 grep suite:
```bash
grep -qi "no production code without a failing test" get-shit-done/references/tdd.md && \
grep -qi "too simple to test" get-shit-done/references/tdd.md && \
grep -qiE "agent.*prompt.*workflow|prompt.*workflow.*reference" get-shit-done/references/tdd.md && \
grep -qi "Delete code. Start over with TDD" get-shit-done/references/tdd.md && \
diff -q get-shit-done/references/tdd.md .claude/get-shit-done/references/tdd.md && \
grep -qi "STOP before continuing" agents/gsd-executor.md && \
grep -qi "STOP before continuing" .claude/agents/gsd-executor.md && \
grep -qiE "verify the test visibly fails" get-shit-done/workflows/execute-plan.md && \
grep -qiE "verify the test visibly fails" .claude/get-shit-done/workflows/execute-plan.md && \
grep -qi "Exempt from tdd" agents/gsd-planner.md && \
grep -qi "Exempt from tdd" .claude/agents/gsd-planner.md && \
grep -qi "Exempt from tdd" .claude/gsd-local-patches/agents/gsd-planner.md && echo "ALL GAP-1 PASS"
```
</verification>

<success_criteria>
- tdd.md carries the Iron Law, mandatory watch-it-fail, rationalization table, red flags + "Delete code" conclusion, and the agent-change exemption (source + byte-identical runtime).
- gsd-executor and execute-plan TDD blocks stop to verify the test fails before production code and point to the Iron Law (source + runtime twins, correct path tokens).
- All three gsd-planner.md copies exempt agent/prompt/workflow/reference edits from tdd="true".
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/06-skill-self-sufficiency-audit-and-port-superpo/06-01-SUMMARY.md`
</output>
