---
phase: 03-execution-detail-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/universal-anti-patterns.md
  - .claude/get-shit-done/references/universal-anti-patterns.md
  - agents/gsd-planner.md
  - .claude/agents/gsd-planner.md
autonomous: true
requirements: [GUIDE-01, GUIDE-02]
must_haves:
  truths:
    - "The planner can pull the universal-anti-patterns standard on-demand when making code-quality judgments (pointer lives in the gsd-planner agent definition, not forced into every run)"
    - "universal-anti-patterns.md folds the planner-antipatterns content as a ## Planner Anti-Patterns section (no third standalone file)"
    - "universal-anti-patterns.md contains a Python section covering anti-patterns, idioms, and typing conventions"
    - "Source tree and runtime tree copies of the doc and the agent file are byte-identical"
  artifacts:
    - ".claude/get-shit-done/references/universal-anti-patterns.md"
    - "get-shit-done/references/universal-anti-patterns.md"
  key_links:
    - "gsd-planner.md -> universal-anti-patterns.md (on-demand Read pointer; broken = planner improvises code-quality standard). Pointer MUST live in the agent definition, NOT plan-phase.md (text outside the constructed prompt never reaches the spawned agent)"
---

<objective>
Deliver the `universal-anti-patterns.md` reference doc (the "what good code looks like / what to avoid" standard) and wire an on-demand pointer to it into the gsd-planner agent, satisfying the anti-pattern half of GUIDE-01 plus the Python idioms/typing half of GUIDE-02.

Port the gsd-core source doc faithfully, FOLD the separate `planner-antipatterns.md` content in as a `## Planner Anti-Patterns` section (no third file), append a Python Anti-Patterns / idioms / typing-conventions section, and write the result into BOTH trees byte-identical. Then add an on-demand `<code_quality_reference>` block to `gsd-planner.md` (the agent definition — NOT plan-phase.md) in both trees, mirroring the on-demand idiom used for `tdd.md`. On-demand (not eager) keeps planning context lean per the project north-star.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5/phases/03-execution-detail-enrichment/03-CONTEXT.md
@.planning/v1.5/phases/03-execution-detail-enrichment/03-RESEARCH.md
@.planning/v1.5/phases/03-execution-detail-enrichment/03-VALIDATION.md

## Dual-tree rule (STATE.md, Phase 04-01 decision)
Every edit applies to BOTH trees, kept byte-identical (`diff -q` exits 0). NOTE the path asymmetry:
- Reference docs: source `get-shit-done/references/…` ↔ runtime `.claude/get-shit-done/references/…`
- Agent file: source `agents/gsd-planner.md` ↔ runtime `.claude/agents/gsd-planner.md` (the agent file is NOT under get-shit-done/)
Only the source tree is committed (runtime is gitignored); the runtime copy must still be written so the running install reflects the change.

## Source docs on disk (port these)
- `/home/cleversol/gsd2/core/gsd-core/references/universal-anti-patterns.md` (64 lines) — port verbatim, do not restructure existing sections.
- `/home/cleversol/gsd2/core/gsd-core/references/planner-antipatterns.md` (89 lines) — FOLD as a `## Planner Anti-Patterns` section; do NOT create a third file.

## Loading-idiom exemplar (the on-demand pattern to mirror) + PATH-TOKEN RULE
execute-plan.md references tdd.md on-demand ("Read … when …"). The gsd-planner agent has eager system-prompt blocks; the on-demand pointer goes in as its own block AFTER `</discovery_levels>` and BEFORE `<task_design>` (source agents/gsd-planner.md: `</discovery_levels>` at line 71, `<task_design>` at line 73).

PATH-TOKEN RULE: the two trees are NOT byte-identical for files containing a load path. install.js rewrites the source token `~/.claude/` into the resolved absolute path in the runtime tree (confirmed by `diff agents/gsd-planner.md .claude/agents/gsd-planner.md` and `tests/path-replacement.test.cjs`). So the pointer line differs per tree: SOURCE writes `Read ~/.claude/get-shit-done/references/universal-anti-patterns.md`; RUNTIME holds `Read /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md`. Do NOT assert `diff -q` between the two gsd-planner.md copies. The prose doc (Task 1) has no token and IS byte-identical (`diff -q` correct there).

## Why the agent file and NOT plan-phase.md
plan-phase.md constructs a prompt for the spawned gsd-planner agent. Any pointer added to plan-phase.md outside the constructed `<planning_context>` block never reaches the agent; adding it to the `<files_to_read>` block would make it EAGER (loaded every run), defeating the hybrid strategy. The agent definition is guaranteed in context on every spawn path (standard, --gaps, revision) without forcing the load. (RESEARCH.md Wiring Points + Common Pitfall 2.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author universal-anti-patterns.md (port + fold planner-antipatterns + Python section) in both trees</name>
  <files>get-shit-done/references/universal-anti-patterns.md, .claude/get-shit-done/references/universal-anti-patterns.md</files>
  <read_first>
    - /home/cleversol/gsd2/core/gsd-core/references/universal-anti-patterns.md (source doc to port verbatim)
    - /home/cleversol/gsd2/core/gsd-core/references/planner-antipatterns.md (to fold in as ## Planner Anti-Patterns)
    - /home/cleversol/gsd2/mine/.claude/get-shit-done/references/tdd.md (tone/format template for an on-demand planner reference doc)
    - .planning/v1.5/phases/03-execution-detail-enrichment/03-RESEARCH.md (Content Taxonomy → Doc 2 section outline; Anti-Pattern: creating a third standalone doc)
  </read_first>
  <action>
Create `/home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md`:

1. PORT the universal-anti-patterns source VERBATIM — keep all numbered rules (1-29) and their section headers exactly: Context Budget Rules, File Reading Rules, Subagent Rules, Questioning Anti-Patterns, State Management Anti-Patterns, Behavioral Rules, Error Recovery Rules, GSD-Specific Rules, and the iOS / Apple Platform Rules. Do not renumber or reorder.

2. FOLD the planner-antipatterns.md content as a single new section with the exact header `## Planner Anti-Patterns`, placed after the GSD-Specific Rules section (keep the iOS/Apple Platform Rules last). Include its four subsections: Checkpoint Anti-Patterns (bad: asking human to automate, bad: too many checkpoints, good: single verification, bad: mixing checkpoints with implementation), Specificity Examples (the too-vague-vs-just-right table + specificity test), Context Section Anti-Patterns (bad reflexive SUMMARY chaining vs good selective context), and Scope Reduction Anti-Patterns (the prohibited-language list). Do NOT create a separate planner-antipatterns.md file.

3. APPEND a Python section. The header MUST start with the literal `## Python` prefix (required for the GUIDE-02 grep) — use exactly:
```
## Python Anti-Patterns and Good Practices
```
Cover three labelled subsections (~40 lines total):
- **Anti-patterns**: bare `except`; modifying a list while iterating it; `range(len(...))` indexing instead of `enumerate`/`zip`; string concatenation in loops (use `str.join`); mutable default arguments.
- **Idioms** (what good Python looks like): comprehensions over imperative loops when readable; `with` for resource management; `enumerate()`/`zip()`; `dataclasses`/`NamedTuple` over raw dicts for structured data; `pathlib.Path` over `os.path`.
- **Typing conventions**: annotate all public signatures (params + return); use `Optional[X]` or `X | None` (3.10+); prefer `Sequence`/`Mapping` over `list`/`dict` in signatures; use `TypedDict` for structured dicts crossing module boundaries; `from __future__ import annotations` for forward refs. The words `typing`, `TypedDict`, and `Optional` MUST literally appear in this subsection.

Pure prose / minimal inline snippets; no runtime deps. Target total ~150-200 lines (on-demand, so slightly more headroom than the bug-patterns doc, but stay lean).

4. Write the IDENTICAL file to the runtime tree at `/home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md` (byte-for-byte).
  </action>
  <verify>
test -f /home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md \
&& test -f /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md \
&& diff -q /home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md \
&& grep -F '## Python' /home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md \
&& grep -F '## Planner Anti-Patterns' /home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md \
&& grep -iE 'typing|TypedDict|Optional' /home/cleversol/gsd2/mine/get-shit-done/references/universal-anti-patterns.md
  </verify>
  <acceptance_criteria>
    - `test -f get-shit-done/references/universal-anti-patterns.md` exits 0 (source tree)
    - `test -f .claude/get-shit-done/references/universal-anti-patterns.md` exits 0 (runtime tree)
    - `diff -q get-shit-done/references/universal-anti-patterns.md .claude/get-shit-done/references/universal-anti-patterns.md` exits 0 (byte-identical dual-tree)
    - `grep -F '## Python' get-shit-done/references/universal-anti-patterns.md` exits 0 (GUIDE-02 Python section)
    - `grep -F '## Planner Anti-Patterns' get-shit-done/references/universal-anti-patterns.md` exits 0 (planner-antipatterns folded in, no third file)
    - `grep -iE 'typing|TypedDict|Optional' get-shit-done/references/universal-anti-patterns.md` exits 0 (typing conventions present — NOTE: use proper ERE `|`, not `\|`; VALIDATION.md line 52's `\|` form is a bug that matches a literal pipe and fails)
    - No third file created: `test ! -f get-shit-done/references/planner-antipatterns.md` exits 0
  </acceptance_criteria>
  <done>Both trees contain a byte-identical universal-anti-patterns.md that ports rules 1-29 verbatim, folds planner-antipatterns as `## Planner Anti-Patterns`, and adds `## Python Anti-Patterns and Good Practices` with anti-patterns, idioms, and typing conventions. No standalone planner-antipatterns.md exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add on-demand pointer to universal-anti-patterns.md in gsd-planner.md (both trees)</name>
  <files>agents/gsd-planner.md, .claude/agents/gsd-planner.md</files>
  <read_first>
    - /home/cleversol/gsd2/mine/agents/gsd-planner.md (the agent definition being edited; `</discovery_levels>` at line 71, `<task_design>` at line 73)
    - /home/cleversol/gsd2/mine/.claude/agents/gsd-planner.md (runtime copy — must receive the identical edit)
    - .planning/v1.5/phases/03-execution-detail-enrichment/03-RESEARCH.md (Wiring Points → Anti-pattern doc — Planner on-demand pointer; Common Pitfall 2)
  </read_first>
  <action>
The two trees use DIFFERENT path forms (see context PATH-TOKEN RULE) — write the tilde token to source, the absolute path to runtime.

SOURCE `/home/cleversol/gsd2/mine/agents/gsd-planner.md`: insert this block AFTER the `</discovery_levels>` closing tag (line 71) and BEFORE the `<task_design>` opening tag (line 73), using the tilde token (matches the source's existing `~/.claude/` refs, e.g. its execution_context block):
```xml
<code_quality_reference>
When making code-quality judgments, choosing between implementation patterns, or flagging anti-patterns in plan tasks:
Read ~/.claude/get-shit-done/references/universal-anti-patterns.md

Skip if the phase is trivial glue/config with no design decisions.
</code_quality_reference>
```

RUNTIME `/home/cleversol/gsd2/mine/.claude/agents/gsd-planner.md`: insert the same block in the same position but with the install-expanded absolute path:
```xml
<code_quality_reference>
When making code-quality judgments, choosing between implementation patterns, or flagging anti-patterns in plan tasks:
Read /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md

Skip if the phase is trivial glue/config with no design decisions.
</code_quality_reference>
```

This is an ON-DEMAND "Read … when …" pointer, NOT an eager `@`-load — do not prefix with `@` and do not add it to any `<files_to_read>` list. Change nothing else. Do NOT touch plan-phase.md (see context: text outside the constructed prompt never reaches the agent). Only the source tree is committed; write the runtime copy too so the live install reflects the change.
  </action>
  <verify>
grep -F 'Read ~/.claude/get-shit-done/references/universal-anti-patterns.md' /home/cleversol/gsd2/mine/agents/gsd-planner.md \
&& grep -F 'Read /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md' /home/cleversol/gsd2/mine/.claude/agents/gsd-planner.md \
&& grep -F 'code_quality_reference' /home/cleversol/gsd2/mine/agents/gsd-planner.md \
&& ! grep -F 'universal-anti-patterns.md' /home/cleversol/gsd2/mine/get-shit-done/workflows/plan-phase.md
  </verify>
  <acceptance_criteria>
    - `grep -F 'Read ~/.claude/get-shit-done/references/universal-anti-patterns.md' agents/gsd-planner.md` exits 0 (source pointer uses tilde token)
    - `grep -F 'Read /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md' .claude/agents/gsd-planner.md` exits 0 (runtime pointer uses absolute path)
    - `grep -F 'code_quality_reference' agents/gsd-planner.md` exits 0 (the new block exists)
    - The pointer is on-demand, not eager: `grep -F '@~/.claude/get-shit-done/references/universal-anti-patterns.md' agents/gsd-planner.md` exits 1 (no `@`-prefixed eager load of this doc in source)
    - The pointer is NOT in plan-phase.md: `! grep -F 'universal-anti-patterns.md' get-shit-done/workflows/plan-phase.md` (exits 0 meaning grep found nothing)
    - Do NOT diff the two gsd-planner.md copies — they legitimately differ by path form (tilde vs absolute) per the PATH-TOKEN RULE
  </acceptance_criteria>
  <done>Source gsd-planner.md carries an on-demand `<code_quality_reference>` pointer with the tilde token and runtime gsd-planner.md carries the absolute-path equivalent, both between `</discovery_levels>` and `<task_design>`; the doc is not eager-loaded and plan-phase.md is untouched. The two copies differ only by the expected install path-token expansion.</done>
</task>

</tasks>

<verification>
Run from repo root (`/home/cleversol/gsd2/mine`):
```bash
test -f get-shit-done/references/universal-anti-patterns.md
test -f .claude/get-shit-done/references/universal-anti-patterns.md
diff -q get-shit-done/references/universal-anti-patterns.md .claude/get-shit-done/references/universal-anti-patterns.md
grep -F '## Python' get-shit-done/references/universal-anti-patterns.md
grep -F '## Planner Anti-Patterns' get-shit-done/references/universal-anti-patterns.md
grep -iE 'typing|TypedDict|Optional' get-shit-done/references/universal-anti-patterns.md
grep -F 'Read ~/.claude/get-shit-done/references/universal-anti-patterns.md' agents/gsd-planner.md
grep -F 'Read /home/cleversol/gsd2/mine/.claude/get-shit-done/references/universal-anti-patterns.md' .claude/agents/gsd-planner.md
test ! -f get-shit-done/references/planner-antipatterns.md
```
All must exit 0.
</verification>

<success_criteria>
- GUIDE-01 (anti-pattern half): universal-anti-patterns.md exists in both trees and is referenced on-demand by the planner via the gsd-planner agent definition.
- GUIDE-02 (Python idioms/typing half): the doc has a `## Python Anti-Patterns and Good Practices` section covering anti-patterns, idioms, and typing conventions.
- planner-antipatterns content folded in as `## Planner Anti-Patterns` — no third file.
- Dual-tree rule honored: the prose doc is byte-identical across trees; the gsd-planner.md pointer is present in both trees in each tree's own path form (tilde in source, absolute in runtime).
- On-demand, not eager: pointer lives in the agent definition, not in plan-phase.md or any `@`/`<files_to_read>` eager path.
</success_criteria>

## ASSUMPTIONS

- **[technical] Source tree uses the `~/.claude/` path token; install.js expands it to an absolute path in the runtime tree.** Confirmed by `diff` of source vs runtime gsd-planner.md and by `tests/path-replacement.test.cjs`. Files carrying a load path are therefore NOT byte-identical across trees; `diff -q` is intentionally dropped for gsd-planner.md (kept for the prose doc, which has no token).
- **[technical] Diverging from VALIDATION.md line 52's typing grep.** That line uses `grep -iE 'typing\\|TypedDict\\|Optional'` which under ERE matches a literal pipe and exits 1 (verified empirically). This plan uses the correct ERE form `grep -iE 'typing|TypedDict|Optional'` (RESEARCH.md line 330 intent).
- **[technical] Python idioms/typing content is MEDIUM-confidence community knowledge** (RESEARCH.md Sources → Secondary; PEP 484/526/544), not source-verified against an external reference. Accepted because the conventions are stable and widely recognized.

<output>
After completion, create `.planning/v1.5/phases/03-execution-detail-enrichment/03-02-SUMMARY.md`.
</output>
