---
phase: 09-skillopt-style-self-improving-skills
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/lesson.test.cjs
  - get-shit-done/bin/lib/lesson.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - TEACH-01
  - TEACH-03
  - TEACH-04
must_haves:
  truths:
    - "gsd-tools lesson append <json> writes exactly one JSONL line to .planning/lessons/lessons.jsonl and returns the assigned LSN-NNN id"
    - "gsd-tools lesson list filters by --agent and --disposition and tails with --last"
    - "gsd-tools lesson update <id> --disposition <d> [--commit <hash>] mutates the matching record in place"
    - "gsd-tools lesson bump-recurrence <id> increments the recurrence counter on the matching record"
    - "gsd-tools lesson attribute --agent <type> resolves an agent_type to its source prose file via a static table (gsd-executor -> agents/gsd-executor.md) and never starts with .claude/"
    - "gsd-tools lesson scan nominates ledger records with recurrence >= threshold and disposition != applied, and writes nothing"
  artifacts:
    - tests/lesson.test.cjs
    - get-shit-done/bin/lib/lesson.cjs
    - get-shit-done/bin/gsd-tools.cjs
  key_links:
    - "lesson.cjs reuses readTrace/filterTrace from trace.cjs for attribution telemetry pick (broken = attribution cannot read agent-trace.jsonl)"
    - "gsd-tools.cjs case 'lesson' dispatches to lesson.cjs handlers (broken = /teach workflow in Plan 02 cannot append/update records)"
---

<objective>
Build the lessons-ledger data layer: a JSONL store at `.planning/lessons/lessons.jsonl`, a `lib/lesson.cjs` module mirroring `lib/trace.cjs`, and a `gsd-tools lesson` subcommand. This is the substrate the `/teach` command (Plan 02) writes to and the auto-miner reads from. Includes the attribution helper (agent_type -> source file) as a unit-testable function, and a scoped `scan` (ledger-recurrence nominate-only) auto-miner. TDD: failing tests first (RED), then the module + dispatch (GREEN).

Why: the auto-miner's "seen 3x" recurrence count and the `/teach` gate's audit trail cannot exist without a persistent, queryable store. Attribution must be a pure function (not workflow prose) so it can be unit-tested per VALIDATION TEACH-01 ("proposes attribution from telemetry without writing anything").
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-CONTEXT.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-VALIDATION.md

<interfaces>
## Ledger record schema (JSONL, one object per line)
```json
{
  "id": "LSN-001",
  "ts": "ISO-8601",
  "description": "free-text failure description",
  "attributed_agent": "gsd-executor | ... | null",
  "attributed_file": "agents/gsd-executor.md | get-shit-done/references/... | null",
  "attribution_confirmed_by_user": true,
  "edit_summary": "one-line summary",
  "edit_type": "add | delete | replace",
  "lines_changed": 5,
  "disposition": "proposed | ratified | applied | rejected | reverted",
  "recurrence": 1,
  "commit": "git-hash | null",
  "session_id": "uuid | null",
  "source_failure": "verify-work-blocker | teach-manual | auto-miner-nomination | null"
}
```

## Attribution static table (agent_type -> source file)
gsd-executor -> agents/gsd-executor.md
gsd-planner -> agents/gsd-planner.md
gsd-verifier -> agents/gsd-verifier.md
gsd-plan-checker -> agents/gsd-plan-checker.md
gsd-phase-researcher -> agents/gsd-phase-researcher.md
gsd-debugger -> agents/gsd-debugger.md
gsd-fixer -> agents/gsd-fixer.md
(unknown / null agent) -> get-shit-done/references/common-bug-patterns.md

## Reused exports from get-shit-done/bin/lib/trace.cjs
readTrace(cwd) -> array of parsed agent-trace.jsonl records
filterTrace(records, { session, agent, confidence, last }) -> filtered/tailed array

## gsd-tools dispatch pattern (from case 'trace' at gsd-tools.cjs:723)
Subcommand parses flags by args.indexOf('--flag'), reads args[idx+1], calls module fn.
`raw` flag and `cwd` are already in scope in the dispatch switch.

## Test helpers (tests/helpers.cjs)
runGsdTools(argsArray, cwd) -> { success, output, error }
createTempProject() -> tmpDir with .planning/phases/
cleanup(tmpDir)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for lesson ledger CLI + attribution + scan</name>
  <files>tests/lesson.test.cjs</files>
  <read_first>
    - tests/parallel-gate.test.cjs (test structure: describe/test/beforeEach, fixture writers, runGsdTools usage)
    - tests/helpers.cjs (runGsdTools, createTempProject, cleanup signatures — lines 18-75)
    - get-shit-done/bin/lib/trace.cjs (the module shape lesson.cjs must mirror: readTrace, filterTrace, cmdTrace)
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md (Lessons Ledger Design section — schema + subcommand list; Auto-Miner First Version — Signal 1)
  </read_first>
  <action>
    Create `tests/lesson.test.cjs` using node:test (`const { test, describe, beforeEach, afterEach } = require('node:test')`, `assert`, `fs`, `path`, and `{ runGsdTools, createTempProject, cleanup } = require('./helpers.cjs')`). Each test creates a temp project, writes a fixture `.planning/lessons/lessons.jsonl` (and `.planning/telemetry/agent-trace.jsonl` where attribution is tested), runs `gsd-tools lesson ...` via runGsdTools, asserts on output/file state, then cleans up. Write these test groups (all RED — `lib/lesson.cjs` and the `lesson` dispatch case do not exist yet):

    1. `lesson append` — `runGsdTools(['lesson','append','{"description":"executor missed endpoint","disposition":"proposed"}'], tmp)` exits success; assert `.planning/lessons/lessons.jsonl` now has exactly 1 line; assert the appended record has an `id` matching `/^LSN-\d{3}$/` and a `ts` ISO string; assert the command output contains the assigned id. A second append assigns `LSN-002` (monotonic) and file has 2 lines.
    2. `lesson list` — seed 3 records (mixed agents/dispositions); `lesson list --agent gsd-executor` returns only executor rows; `lesson list --disposition applied` returns only applied rows; `lesson list --last 1` returns 1 row.
    3. `lesson update` — seed `LSN-001` disposition=proposed; `lesson update LSN-001 --disposition applied --commit abc1234` succeeds; re-read file and assert that record now has `disposition:"applied"` and `commit:"abc1234"`, and the line count is unchanged (in-place mutation, no append).
    4. `lesson bump-recurrence` — seed `LSN-001` recurrence=1; `lesson bump-recurrence LSN-001` succeeds; re-read and assert recurrence===2.
    5. `lesson attribute` — write a fixture `agent-trace.jsonl` with a recent `gsd-executor` record; `lesson attribute --agent gsd-executor` (or `lesson attribute --last 50` resolving most-recent execution agent) returns `attributed_file` === `agents/gsd-executor.md` and the output path does NOT start with `.claude/`; an unknown/`null` agent resolves to `get-shit-done/references/common-bug-patterns.md`. Assert the command wrote nothing (lessons.jsonl unchanged / absent).
    6. `lesson scan` — seed ledger with one record recurrence=3 disposition=proposed and one recurrence=1 disposition=applied; `lesson scan` output nominates only the recurrence>=3 non-applied record (contains its description), and does NOT nominate the applied one; assert lessons.jsonl is byte-identical before/after (scan writes nothing).

    Use the exact subcommand names: `append`, `list`, `update`, `bump-recurrence`, `attribute`, `scan`. Default recurrence threshold for scan = 3.
  </action>
  <verify>node scripts/run-tests.cjs lesson 2>&1 | tail -20 — tests RUN and FAIL (assertions/exec error), not "No test files found"</verify>
  <acceptance_criteria>
    - File `tests/lesson.test.cjs` exists and is discovered by the runner
    - `grep -c "runGsdTools(\['lesson'" tests/lesson.test.cjs` >= 6 (one per subcommand path)
    - `grep -q "LSN-" tests/lesson.test.cjs` (asserts on id format)
    - `grep -q "common-bug-patterns.md" tests/lesson.test.cjs` (attribution fallback asserted)
    - `grep -q "\.claude/" tests/lesson.test.cjs` AND the assertion checks attributed_file does NOT start with .claude/
    - `node scripts/run-tests.cjs lesson` exits non-zero (RED) because lib/lesson.cjs / dispatch absent
  </acceptance_criteria>
  <done>tests/lesson.test.cjs exists, is discovered, and fails because the implementation does not yet exist (genuine RED, not a missing-file skip).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — lib/lesson.cjs CRUD + gsd-tools lesson dispatch</name>
  <files>get-shit-done/bin/lib/lesson.cjs, get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/trace.cjs (mirror readTrace/filterTrace/cmdTrace + module.exports shape exactly)
    - get-shit-done/bin/gsd-tools.cjs lines 170-185 (require block — add `const lesson = require('./lib/lesson.cjs');`) and lines 723-736 (case 'trace' dispatch — model the case 'lesson' on it)
    - tests/lesson.test.cjs (the contract just written — read it to implement against the exact assertions)
  </read_first>
  <action>
    Create `get-shit-done/bin/lib/lesson.cjs` (CommonJS, `'use strict'`, `fs`/`path`/`crypto`). Implement and export:
    - `lessonsPath(cwd)` -> `path.join(cwd,'.planning','lessons','lessons.jsonl')`
    - `readLessons(cwd)` -> parse JSONL best-effort (skip malformed lines), mirroring `readTrace`. Returns `[]` if file absent.
    - `filterLessons(records, { agent, disposition, last })` -> filter by `attributed_agent` (startsWith) and exact `disposition`, then tail `last` (default 20). Mirror `filterTrace`.
    - `nextId(records)` -> `LSN-` + zero-padded(3) of `(max existing numeric suffix)+1`; first id `LSN-001`.
    - `cmdAppend(cwd, jsonString)` -> parse the json, fill defaults (`id` via nextId, `ts` new ISO, `recurrence` default 1, `disposition` default 'proposed'), ensure `.planning/lessons/` dir exists, `fs.appendFileSync` one `JSON.stringify(rec)+'\n'`, print the assigned id to stdout.
    - `cmdList(cwd, opts, raw)` -> filterLessons then print (raw: one JSON per line; else a plain-text table like trace.formatTable with columns id/ts/attributed_agent/disposition/recurrence).
    - `cmdUpdate(cwd, id, { disposition, commit })` -> read all lines, find record by id, mutate disposition and (if given) commit IN PLACE, rewrite the whole file (line count unchanged). Error to stderr + non-zero if id not found.
    - `cmdBumpRecurrence(cwd, id)` -> same read-mutate-rewrite, `recurrence = (recurrence||0)+1`.

    Then in `gsd-tools.cjs`: add the require near the other lib requires; add `case 'lesson': { ... }` modeled on case 'trace'. Sub-dispatch on `args[1]` (`append|list|update|bump-recurrence|attribute|scan`). Parse `--agent`/`--disposition`/`--last`/`--commit`/`--disposition` via `args.indexOf`. Route append/list/update/bump-recurrence to the lesson.cjs functions above. (attribute + scan land in Task 3.)
  </action>
  <verify>node scripts/run-tests.cjs lesson 2>&1 | tail -20 — append/list/update/bump-recurrence groups GREEN (attribute/scan may still be RED until Task 3)</verify>
  <acceptance_criteria>
    - `node -e "require('./get-shit-done/bin/lib/lesson.cjs')"` exits 0 (module loads)
    - `grep -q "module.exports" get-shit-done/bin/lib/lesson.cjs` and exports readLessons, filterLessons, nextId, cmdAppend, cmdList, cmdUpdate, cmdBumpRecurrence
    - `grep -q "require('./lib/lesson.cjs')" get-shit-done/bin/gsd-tools.cjs`
    - `grep -q "case 'lesson'" get-shit-done/bin/gsd-tools.cjs`
    - In a scratch temp dir: `gsd-tools lesson append '{"description":"x"}'` creates lessons.jsonl with 1 line whose id matches `LSN-001`
    - The `lesson append/list/update/bump-recurrence` test groups in `node scripts/run-tests.cjs lesson` pass
  </acceptance_criteria>
  <done>lessons.jsonl CRUD works end-to-end via the CLI; append/list/update/bump-recurrence test groups are GREEN. No edits to .claude/ runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — attribution function + scoped scan (ledger-recurrence-only) auto-miner</name>
  <files>get-shit-done/bin/lib/lesson.cjs, get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/trace.cjs (import readTrace + filterTrace — do NOT re-implement the JSONL reader)
    - get-shit-done/bin/lib/lesson.cjs (the module from Task 2 — extend it)
    - tests/lesson.test.cjs (the attribute + scan test groups — implement to their exact assertions)
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md (Pattern 3 Attribution Heuristic; Auto-Miner Signal 1 — ledger recurrence ONLY)
  </read_first>
  <action>
    Extend `get-shit-done/bin/lib/lesson.cjs`:
    - Add the static `AGENT_FILE_MAP` object with the exact entries from the interfaces block (gsd-executor -> agents/gsd-executor.md, ... , default/null -> get-shit-done/references/common-bug-patterns.md). All values are repo-root-relative; NONE start with `.claude/`.
    - `attributeFile(agentType)` -> returns `AGENT_FILE_MAP[agentType] || 'get-shit-done/references/common-bug-patterns.md'`. Add an assertion/guard: the returned path must NOT start with `.claude/` (Pitfall 3 guard) and must NOT contain `gsd-local-patches/` (Pitfall 6 guard).
    - `pickAttribution(cwd, { agent, last })` -> require `{ readTrace, filterTrace }` from `./trace.cjs`; if `agent` flag given, use it; else read telemetry (`filterTrace(readTrace(cwd), { last: last||50 })`), keep only execution agents (`['gsd-executor','gsd-planner','gsd-verifier']`), pick the most-recent. Return `{ attributed_agent, attributed_file: attributeFile(agentType) }`. Writes nothing.
    - `cmdAttribute(cwd, opts, raw)` -> call pickAttribution, print the `{attributed_agent, attributed_file}` (raw: JSON; else two lines). No file writes.
    - `cmdScan(cwd, opts)` -> read threshold from `.planning/config.json` key `teach.recurrence_threshold` (default 3); `readLessons(cwd).filter(l => (l.recurrence||0) >= threshold && l.disposition !== 'applied')`; print a nominations report: for each, a line `[{attributed_agent}] "{description}" — seen {recurrence}×` followed by `  → Run: /gsd2:teach "{description}"`. If none, print `No nominations (threshold={threshold}).`. SCOPED to ledger recurrence ONLY — do NOT scan VERIFICATION.md BLOCKERs or telemetry dips (deferred per scope guard; see ASSUMPTIONS in SUMMARY).

    Wire `attribute` and `scan` sub-cases into the `case 'lesson'` block in gsd-tools.cjs. Export attributeFile, pickAttribution, cmdAttribute, cmdScan.
  </action>
  <verify>node scripts/run-tests.cjs lesson 2>&1 | tail -20 — ALL lesson test groups GREEN; then npm test 2>&1 | tail -15 — full suite green (no regressions)</verify>
  <acceptance_criteria>
    - `grep -q "AGENT_FILE_MAP" get-shit-done/bin/lib/lesson.cjs`
    - `grep -q "common-bug-patterns.md" get-shit-done/bin/lib/lesson.cjs` (fallback)
    - `grep -q "gsd-local-patches" get-shit-done/bin/lib/lesson.cjs` (guard present)
    - attributeFile never returns a path starting with `.claude/` (assert in code + covered by test)
    - cmdScan reads `teach.recurrence_threshold` (grep `recurrence_threshold` in lesson.cjs)
    - cmdScan does NOT read any `*-VERIFICATION.md` (grep -L "VERIFICATION" lesson.cjs returns the file = absent) and does NOT call readTrace inside cmdScan (ledger-only)
    - `node scripts/run-tests.cjs lesson` ALL groups pass; `npm test` exits 0
  </acceptance_criteria>
  <done>Attribution resolves agent_type -> source file (never .claude/, never local-patches) as a unit-tested pure function; scan nominates ledger-recurrence records only and writes nothing; full suite green.</done>
</task>

</tasks>

<verification>
- `node scripts/run-tests.cjs lesson` — all lesson test groups green
- `npm test` — full suite green, no regressions
- `node get-shit-done/bin/gsd-tools.cjs lesson list` in repo root exits 0 (empty ledger ok)
- No file under `.claude/` was committed (source-only: `lib/lesson.cjs`, `gsd-tools.cjs`, `tests/lesson.test.cjs`)
</verification>

<success_criteria>
- TEACH-03: ledger lifecycle (append → list → update disposition → bump-recurrence) works via `gsd-tools lesson`
- TEACH-01 (data layer): attribution is a unit-tested function mapping agent_type → source file from telemetry, writing nothing
- TEACH-04: `lesson scan` nominates recurring (recurrence ≥ threshold, non-applied) lessons and edits nothing
- All implementation in `get-shit-done/` + `tests/` source; nothing in `.claude/` runtime committed
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-01-SUMMARY.md`. Record in it: the scoped-auto-miner ASSUMPTION (ledger-recurrence-only for v1; BLOCKER/telemetry signals deferred per scope guard, diverging from RESEARCH Pattern 6), and that the running session needs `npm run dev` before the new `lesson` subcommand is available in the `.claude/` runtime CLI.
</output>
