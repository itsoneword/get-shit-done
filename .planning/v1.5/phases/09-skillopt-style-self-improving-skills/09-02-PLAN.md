---
phase: 09-skillopt-style-self-improving-skills
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - get-shit-done/workflows/teach.md
  - commands/gsd2/teach.md
autonomous: false
requirements:
  - TEACH-01
  - TEACH-02
  - TEACH-05
must_haves:
  truths:
    - "/gsd2:teach \"<failure>\" reads telemetry, proposes a culprit source file, and asks the user to confirm or redirect before drafting any edit"
    - "An advisor-critic reflects on whether a skill was at fault and drafts a bounded edit (<=20 lines, one contiguous section) to ONE source file"
    - "The user sees the diff and ratifies [y/N] before anything touches get-shit-done/ or agents/ source — no auto-apply path"
    - "On ratify, the edit applies to the source path (never .claude/, never gsd-local-patches/) as a source-only commit, the applied ledger record is committed separately (so git revert of the source commit stays conflict-free), and the user is told to run npm run dev to propagate"
    - "/gsd2:teach scan runs the auto-miner nominations report only — proposes no edit"
    - "Reverting a bad lesson is documented: git revert <hash> + npm run dev"
  artifacts:
    - get-shit-done/workflows/teach.md
    - commands/gsd2/teach.md
  key_links:
    - "commands/gsd2/teach.md @-references ~/.claude/get-shit-done/workflows/teach.md (broken = slash command has no body)"
    - "teach.md workflow calls gsd-tools lesson append/update + attribute + scan from Plan 01 (broken = no ledger record, no attribution)"
    - "teach.md apply step asserts target path does NOT start with .claude/ (broken = edit lost at next npm run dev)"
---

<objective>
Ship the `/gsd2:teach` command: a standalone slash command (registration in `commands/gsd2/teach.md`) backed by an inline workflow (`get-shit-done/workflows/teach.md`). The workflow runs the online skill-evolution loop end-to-end: observe failure → attribute (via Plan 01's `lesson attribute`) → user confirms target → advisor-critic reflects + drafts a bounded edit → show diff → user ratifies [y/N] → apply to source + commit + record in ledger → tell user to `npm run dev`. `/gsd2:teach scan` runs the auto-miner nominations report only.

Runs INLINE (no Task spawn), uses the `advisor` tool inline for the reflect/critic step, mirroring `note.md`'s command+workflow pattern. This plan is prose-only (workflow + command registration) — it wires together the CLI substrate from Plan 01. NOT auto-applied at any point; the human ratification gate is the primary bloat guard, so this plan has a verification checkpoint.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-CONTEXT.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md
@.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-01-SUMMARY.md

<interfaces>
## Plan 01 CLI surface this workflow drives
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lesson attribute --agent <type>   # or --last 50; prints {attributed_agent, attributed_file}; writes nothing
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lesson append '<json>'             # returns LSN-NNN id
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lesson update LSN-001 --disposition applied --commit <hash>
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lesson bump-recurrence LSN-001
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lesson scan                        # nominations report (no edit)
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" trace --last 20 --raw             # telemetry for attribution context
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "<msg>" --files <src> .planning/lessons/lessons.jsonl
```

## Attribution static table (resolved by `lesson attribute`)
gsd-executor -> agents/gsd-executor.md ; gsd-planner -> agents/gsd-planner.md ; gsd-verifier -> agents/gsd-verifier.md ; (others per RESEARCH) ; unknown -> get-shit-done/references/common-bug-patterns.md
All targets are repo-root source. NEVER edit `.claude/` runtime or `gsd-local-patches/`.

## Bounded edit definition (the cap — concrete number, NOT just "bounded")
One contiguous section (delimited by a `##`/`###` header) within ONE file; one operation type (add | delete | replace); <= 20 lines changed (added + removed). If the advisor draft exceeds this, narrow scope or split into another lesson.

## Command+workflow pattern to mirror
commands/gsd2/note.md (frontmatter: name/description/argument-hint/allowed-tools; @-ref to workflow; <process> says run workflow end-to-end)
get-shit-done/workflows/note.md (numbered <step> process; subcommand parse table; constraints)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the teach workflow (the loop prose)</name>
  <files>get-shit-done/workflows/teach.md</files>
  <read_first>
    - get-shit-done/workflows/note.md (workflow structure: purpose, required_reading, numbered process steps, subcommand parse table, success_criteria)
    - get-shit-done/workflows/add-todo.md (a second workflow-structure exemplar)
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md (Pattern 1 the /teach loop; Pattern 5 apply+reversibility; Common Pitfalls 1-6 — these become explicit workflow guard lines)
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-01-SUMMARY.md (the exact lesson CLI surface Plan 01 shipped)
  </read_first>
  <action>
    Create `get-shit-done/workflows/teach.md` with a `<purpose>`, `<required_reading>`, a numbered `<process>`, and `<success_criteria>` (mirror note.md structure). Two paths driven by `$ARGUMENTS`:

    **Path A — `$ARGUMENTS` is `scan`:** run `gsd-tools lesson scan` and print its nominations report verbatim. Propose NO edit, write NO source file. End.

    **Path B — `$ARGUMENTS` is a failure description (default):** numbered steps:
    1. Init / telemetry — run `gsd-tools trace --last 20 --raw` for context.
    2. Attribution — run `gsd-tools lesson attribute --last 50` (or `--agent <type>` if the description names one). Present the proposed target file: `Looks like a {attributed_agent} issue — edit {attributed_file}? [y / redirect to <path>]`. The user confirms or names a different source file. State explicitly: the target MUST be repo-root source (`agents/...` or `get-shit-done/...`); NEVER `.claude/` runtime and NEVER `gsd-local-patches/`.
    3. Reflect (advisor-critic, inline) — invoke the `advisor` tool inline with the failure + the target file's current relevant section. Ask: "Was a skill genuinely at fault? What is the MINIMAL prose change that prevents recurrence?" The critic must also enforce the bound.
    4. Draft bounded edit — produce an add/delete/replace diff for ONE contiguous `##`/`###` section of the ONE target file, <= 20 lines changed. If the draft exceeds the bound, narrow it or split into a second lesson — do not proceed with an over-budget edit.
    5. Append proposed lesson — `gsd-tools lesson append '{"description":...,"attributed_agent":...,"attributed_file":...,"edit_type":...,"lines_changed":N,"disposition":"proposed","source_failure":"teach-manual"}'`; capture the returned `LSN-NNN`.
    6. Ratify gate — show the before/after diff inline and ask `Apply this edit to {attributed_file}? [y/N]`. NO auto-apply — even an advisor-passed draft waits for `y`. This is the only human round-trip and the primary bloat guard.
    7. Apply (on y) — ASSERT the resolved target path does NOT start with `.claude/` and does NOT contain `gsd-local-patches/` before writing. Use the `Edit` tool (surgical, not Write). Then `gsd-tools commit "teach({agent}): {summary}" --files {attributed_file} .planning/lessons/lessons.jsonl`; capture the commit hash; `gsd-tools lesson update {LSN} --disposition applied --commit {hash}`.
    8. Discard (on N) — `gsd-tools lesson update {LSN} --disposition rejected`; change no source file.
    9. Confirm — on apply: `Applied. Commit {hash}. Lesson {LSN}. Run \`npm run dev\` to propagate this edit to the current runtime.` Include the reversibility line: `To undo: git revert {hash} && npm run dev (lesson stays in ledger as disposition=reverted).`

    Encode the bound as the literal number **20 lines / one section** in the prose (not the word "bounded" alone). Document, in a constraints block, all six pitfalls as guard rules: (1) confirm attribution before drafting, (2) <=20 lines one section, (3) assert non-.claude/ source path, (4) never edit gsd-local-patches/, (5) always emit the npm run dev propagation line, (6) auto-miner nominates only — `/teach scan` proposes no edit.
  </action>
  <verify>test -f get-shit-done/workflows/teach.md && grep -q "20 lines" get-shit-done/workflows/teach.md && grep -q "npm run dev" get-shit-done/workflows/teach.md && grep -q "git revert" get-shit-done/workflows/teach.md && grep -q "y/N" get-shit-done/workflows/teach.md && grep -q "gsd-local-patches" get-shit-done/workflows/teach.md && grep -q "lesson attribute" get-shit-done/workflows/teach.md && echo PASS</verify>
  <acceptance_criteria>
    - File `get-shit-done/workflows/teach.md` exists with a numbered `<process>` and `<success_criteria>`
    - `grep -q "lesson attribute" get-shit-done/workflows/teach.md` AND `grep -q "lesson append" get-shit-done/workflows/teach.md` AND `grep -q "lesson update" get-shit-done/workflows/teach.md` AND `grep -q "lesson scan" get-shit-done/workflows/teach.md` (drives the Plan 01 CLI)
    - `grep -q "20 lines" get-shit-done/workflows/teach.md` (concrete bound, not just "bounded")
    - `grep -q "y/N" get-shit-done/workflows/teach.md` (ratify gate present)
    - `grep -q "npm run dev" get-shit-done/workflows/teach.md` (propagation step) AND `grep -q "git revert" get-shit-done/workflows/teach.md` (reversibility — TEACH-05)
    - `grep -q "gsd-local-patches" get-shit-done/workflows/teach.md` AND a guard asserting the target path does NOT start with `.claude/`
    - `grep -q "advisor" get-shit-done/workflows/teach.md` (inline critic step)
    - `grep -q "scan" get-shit-done/workflows/teach.md` with the no-edit nominations-only path
  </acceptance_criteria>
  <done>teach.md encodes the full loop: attribute → confirm → advisor reflect → bounded diff → [y/N] ratify → source-only apply + commit + ledger update + npm run dev, plus the scan-only path and all six pitfall guards. No source-prose edit is applied by this task itself.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register the /gsd2:teach slash command</name>
  <files>commands/gsd2/teach.md</files>
  <read_first>
    - commands/gsd2/note.md (the exact frontmatter shape: name, description, argument-hint, allowed-tools; @-ref execution_context; <context>$ARGUMENTS; <process> run workflow end-to-end)
    - get-shit-done/workflows/teach.md (the workflow this command @-references — written in Task 1)
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-RESEARCH.md (`/teach` Command Structure — Recommendation: standalone command, allowed-tools list, argument-hint "<failure description> | scan")
  </read_first>
  <action>
    Create `commands/gsd2/teach.md` mirroring `commands/gsd2/note.md` exactly in shape:
    - Frontmatter: `name: gsd2:teach`; `description: Teach GSD from a real failure. Proposes a bounded edit to the responsible artifact after human ratification.`; `argument-hint: "<failure description> | scan"`; `allowed-tools:` list = Read, Write, Edit, Bash, Grep, Glob.
    - `<execution_context>` @-refs `~/.claude/get-shit-done/workflows/teach.md`.
    - `<context>` block contains `$ARGUMENTS`.
    - `<process>` says: Execute the teach workflow from `@~/.claude/get-shit-done/workflows/teach.md` end-to-end. When `$ARGUMENTS` is `scan`, run the auto-miner nominations report only (no edit proposed).

    Use the `~/.claude/` path token in the @-reference (NOT a repo-root or `.claude/` absolute path) — this matches every other command file's runtime-path convention. Commit `commands/gsd2/teach.md` SOURCE only; `install.js` / `npm run dev` propagates it to `.claude/commands/gsd2/teach.md` (do NOT hand-copy or commit the runtime copy).
  </action>
  <verify>test -f commands/gsd2/teach.md && grep -q "name: gsd2:teach" commands/gsd2/teach.md && grep -q "argument-hint:" commands/gsd2/teach.md && grep -q "~/.claude/get-shit-done/workflows/teach.md" commands/gsd2/teach.md && grep -q '\$ARGUMENTS' commands/gsd2/teach.md && echo PASS</verify>
  <acceptance_criteria>
    - File `commands/gsd2/teach.md` exists
    - `grep -q "name: gsd2:teach" commands/gsd2/teach.md`
    - `grep -q 'argument-hint: "<failure description> | scan"' commands/gsd2/teach.md`
    - allowed-tools includes Read, Write, Edit, Bash, Grep, Glob (grep each)
    - `grep -q "@~/.claude/get-shit-done/workflows/teach.md" commands/gsd2/teach.md` (workflow @-ref with runtime token)
    - `grep -q '\$ARGUMENTS' commands/gsd2/teach.md`
    - No `.claude/commands/` file is committed (source-only)
  </acceptance_criteria>
  <done>`/gsd2:teach` is registered as a standalone command pointing at the teach workflow, using the `~/.claude/` runtime path token; only the source command file is committed.</done>
</task>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 3: Verify the ratify gate + source-only apply on a real failure</name>
  <files>get-shit-done/workflows/teach.md, commands/gsd2/teach.md</files>
  <read_first>
    - get-shit-done/workflows/teach.md
    - .planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-VALIDATION.md (Manual-Only Verifications table — SC2 no-auto-apply, SC3 real ratified edit)
  </read_first>
  <action>
    First run `npm run dev` so the new `lesson` subcommand + `teach` command + workflow are live in the `.claude/` runtime. Then the user verifies, against a REAL observed failure (this satisfies SC3, which is human-driven by design):

    1. Run `/gsd2:teach "<a real failure you observed>"`. Confirm the loop proposes an attribution target, lets you confirm/redirect, shows an advisor-critiqued bounded diff (<=20 lines, one section), and STOPS at `[y/N]` — nothing is written before you answer.
    2. NEGATIVE path (SC2): answer `N` once. Verify NO source file changed (`git status` clean for source) and the ledger record shows `disposition: rejected`.
    3. POSITIVE path (SC3): run again, answer `y`. Verify the APPLY commit touches ONLY the source file (`agents/...` or `get-shit-done/...`) — nothing under `.claude/`, and NOT the ledger (the ledger lands in a separate follow-up commit so `git revert` stays conflict-free). Verify the separate ledger commit records `disposition: applied` with the commit hash. Verify the confirmation printed the `npm run dev` propagation line.
    4. REVERSIBILITY (TEACH-05): `git revert <hash>` then `npm run dev`; confirm the source file is restored. (Optionally mark the ledger record `reverted`.)
    5. SCAN: run `/gsd2:teach scan`; confirm it prints nominations only and edits nothing.
  </action>
  <verify>Human confirms: (a) gate stops at [y/N] before any write; (b) N leaves source unchanged + ledger rejected; (c) y commits source-only + ledger applied + prints npm run dev; (d) git revert restores the file; (e) scan edits nothing. `git show --stat <hash>` lists only the source file + lessons.jsonl.</verify>
  <acceptance_criteria>
    - The apply commit's `git show --stat <hash>` lists ONLY a source path (`agents/` or `get-shit-done/`) — zero `.claude/` paths and NOT the ledger; the `applied` ledger record is committed in a separate follow-up commit (keeps `git revert <hash>` conflict-free)
    - Declining (N) leaves `git status` clean for source files and writes `disposition: rejected` to the ledger
    - The applied lesson record has `disposition: applied` and a non-null `commit`
    - `git revert <hash> && npm run dev` restores the source file (clean working tree after)
    - `/gsd2:teach scan` produces nominations text and changes no file
  </acceptance_criteria>
  <done>A real lesson lands as a committed, ratified, bounded, source-only edit to the correct GSD artifact; the no-auto-apply guarantee and git-reversibility are demonstrated; scan nominates without editing. (SC2 + SC3 satisfied.)</done>
</task>

</tasks>

<verification>
- `grep` acceptance criteria on `teach.md` and `commands/gsd2/teach.md` all pass (Tasks 1-2 automated)
- Checkpoint Task 3 confirms the behavioral guarantees that cannot be unit-tested (the ratification gate + source-only apply + reversibility)
- `npm test` still green (this plan adds no code paths, but run it to confirm no accidental regression)
- No `.claude/` runtime file committed
</verification>

<success_criteria>
- TEACH-01 (UX): `/gsd2:teach` proposes an attribution target from telemetry and asks the user to confirm before drafting an edit
- TEACH-02: ratified edits commit to `agents/` or `get-shit-done/` source ONLY (never `.claude/`), and each writes an `applied` ledger record with the commit hash
- TEACH-05: the loop is git-reversible (`git revert <hash> && npm run dev`), documented in the confirm output
- SC2 (no auto-apply) and SC3 (a real ratified bounded edit) demonstrated at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/09-skillopt-style-self-improving-skills/09-02-SUMMARY.md`. Record: the bounded-edit cap chosen (<=20 lines / one section — flag as planner ASSUMPTION, no published SkillOpt number); that the command + workflow are committed as SOURCE and require `npm run dev` to go live; and the LEARN-01 reconciliation note (Phase 9 materializes LEARN-01's intent under the TEACH- namespace — flag for the human whether to mark LEARN-01 satisfied).
</output>
