---
phase: 02-autonomous-technical-resolution
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements: [RSCH-01, RSCH-02, RSCH-03]
must_haves:
  truths:
    - "A LOW-confidence TECHNICAL/HYBRID question in discuss-phase runs the bounded resolution loop before any fallback to asking the human"
    - "MEDIUM confidence auto-decides with an override caveat instead of being presented as a question — the human is reached only on LOW-after-exhaustion or genuine preference"
    - "Before spawning research, the loop checks CONTEXT.md and skips questions already locked by a STRONG decision"
    - "When the loop resolves a question (HIGH/MEDIUM), the decision is recorded with a specialist-backed tag + confidence + source so it is not re-asked"
  artifacts:
    - "get-shit-done/workflows/discuss-phase.md (committed source) with a rewritten question_triage LOW branch"
  key_links:
    - "discuss-phase.md LOW branch → resolution loop → micro-research re-spawn (broken = LOW falls straight to ask-user, RSCH-02 defeated)"
    - "question_triage pre-check → CONTEXT.md STRONG decisions (broken = loop re-opens locked decisions, RSCH-03 defeated)"
    - "loop resolution → CONTEXT.md write-back with [STRONG/WEAK, specialist-backed] tag (broken = downstream re-asks, RSCH-03 defeated)"
---

<objective>
Wire the resolution loop into discuss-phase's `question_triage` block (committed source `get-shit-done/workflows/discuss-phase.md`, lines 274–317). Today the LOW-confidence branch falls straight to asking the human ("fall back to asking user"), and even HIGH/MEDIUM findings are presented back as questions ("Going with that unless you object?"). This plan changes three things per the locked decisions and 02-RESEARCH.md §Integration Mechanics Seam 1:

1. **LOW branch runs the loop** (re-research with critique) before any ask-user (RSCH-01, RSCH-02).
2. **MEDIUM auto-decides** with an override caveat — the core behavior change (Pitfall 5) — and HIGH presents as FYI, not a question (RSCH-02).
3. **Signal-strength pre-check** skips questions matching CONTEXT.md STRONG decisions; **write-back** records resolved decisions with specialist-backed tags (RSCH-03).

discuss-phase.md runs at the ORCHESTRATOR level (full tools incl. Task), so the loop's micro-research re-spawns execute correctly here — this is the safe wiring point per locked constraint #2.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-CONTEXT.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md
@get-shit-done/references/resolution-loop.md

<source_runtime_split>
CRITICAL (locked constraint #5): edit ONLY `get-shit-done/workflows/discuss-phase.md` (committed). `.claude/get-shit-done/workflows/discuss-phase.md` is gitignored — never edit it. After editing, the file MUST appear in `git status`.
</source_runtime_split>

<current_low_branch>
The block to replace is `get-shit-done/workflows/discuss-phase.md` lines 302–306 (the "Present based on confidence" list), preceded by the micro-research spawn (lines 290–300) and followed by the signal-strength block (307–316). Current text:

```
**Present based on confidence:**
- **HIGH** → recommendation: "For [domain], [recommendation] is standard — [reasoning]. Going with that unless you object?"
- **MEDIUM** → informed suggestion with options: "Research suggests [X] because [reasoning]. Alternative: [Y]. Preference?"
- **LOW** → fall back to asking user: "I looked into [topic] but it depends on your context. [findings]. Your thinking?"
```

The micro-research spawn that precedes it (keep, but it becomes iteration 1 of the loop):
```
**For TECHNICAL/HYBRID — spawn micro-research:**
Task(subagent_type="gsd-phase-researcher", prompt="<micro_research>...")
```
</current_low_branch>

<loop_contract>
@get-shit-done/references/resolution-loop.md
The canonical contract from Plan 01. This plan inlines the LOW-branch behavior into discuss-phase.md (grep tests assert on discuss-phase.md, so the branch prose must live there) AND @-references resolution-loop.md for the full DRY contract.
</loop_contract>
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Add the signal-strength pre-check before micro-research spawn</name>
  <read_first>
    - get-shit-done/workflows/discuss-phase.md lines 274–317 (the full question_triage block being modified)
    - get-shit-done/workflows/discuss-phase.md lines 49–62 (signal_strength_guide — the tag semantics)
    - get-shit-done/references/resolution-loop.md §Signal-strength pre-check (the contract)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Signal-Strength Honoring + Pitfall 2
  </read_first>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
In `get-shit-done/workflows/discuss-phase.md`, immediately BEFORE the "**For TECHNICAL/HYBRID — spawn micro-research:**" line (currently line 290), insert this pre-check block verbatim:

```
**Signal-strength pre-check (skip the loop for locked decisions):**
Before spawning any research, scan the phase CONTEXT.md `<decisions>` section. If the question's subject matches a decision tagged `[STRONG]`, `[STRONG, user-override]`, or `[STRONG, specialist-backed]`, do NOT spawn micro-research or the resolution loop — apply the existing decision directly and present it as settled (HIGH confidence, source: "CONTEXT.md locked decision"). Only proceed to research when no STRONG decision already covers the question.
```

This realizes RSCH-03's skip half: the loop never re-opens a STRONG decision (Pitfall 2). The existing line 314 "Skip micro-research when... prior phases decided it" stays; this adds the explicit STRONG-tag mechanism the test greps for.
  </action>
  <acceptance_criteria>
    - `git status --porcelain get-shit-done/workflows/discuss-phase.md` shows it modified (NOT .claude/)
    - `grep -c "Signal-strength pre-check" get-shit-done/workflows/discuss-phase.md` returns >= 1
    - `grep -c "skip the loop" get-shit-done/workflows/discuss-phase.md` returns >= 1
    - `grep -F "[STRONG, specialist-backed]" get-shit-done/workflows/discuss-phase.md` matches (the pre-check references the tag)
    - The pre-check text appears BEFORE the "spawn micro-research" line: `grep -n "Signal-strength pre-check\|spawn micro-research" get-shit-done/workflows/discuss-phase.md` shows pre-check on an earlier line number
  </acceptance_criteria>
  <verify>grep -c 'Signal-strength pre-check' get-shit-done/workflows/discuss-phase.md</verify>
  <done>The STRONG-decision pre-check is inserted ahead of the micro-research spawn in discuss-phase.md committed source; greps pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace the LOW branch with the resolution loop + MEDIUM auto-decide</name>
  <read_first>
    - get-shit-done/workflows/discuss-phase.md lines 290–316 (micro-research spawn + present-based-on-confidence + signal-strength block)
    - get-shit-done/references/resolution-loop.md §The loop + §Confidence → action + §Write-back (the contract to inline)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Integration Mechanics Seam 1 + §Code Examples (proposed loop extension) + Pitfall 5
  </read_first>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
In `get-shit-done/workflows/discuss-phase.md`, REPLACE the current "**Present based on confidence:**" list (lines 302–306) with the following. The micro-research spawn above it (lines 290–300) stays — it becomes iteration 1 of the loop.

```
**Resolve based on confidence (run the resolution loop — see `get-shit-done/references/resolution-loop.md`):**

The initial micro-research above is iteration 1. Act on its confidence:

- **HIGH** → decide autonomously, present as FYI (not a question): "I resolved [topic]: [recommendation]. Source: [source]." Continue — do not ask.
- **MEDIUM** → auto-decide WITH an override caveat: "Going with [X] — [reasoning]. You can override." Continue — do not turn this into a question. (MEDIUM does NOT fall through to ask-user; treating MEDIUM as LOW defeats the round-trip-reduction goal.)
- **LOW** → run the resolution loop before asking the human:
    - If micro-research budget remains (`budget_remaining > 0`):
        - Iteration 2 — broaden + re-research: re-spawn `gsd-phase-researcher` with a critique hint, e.g. "Previous pass returned LOW. Broaden scope: drop project-specific constraints, answer for the general domain; cross-check a different source type." Decrement budget.
        - If iteration 2 returns HIGH/MEDIUM → apply the HIGH/MEDIUM rule above (auto-decide) and record the decision (write-back below).
        - (Optional iteration 3 if budget allows: cross-check via an explicitly different source type than the first pass.)
    - Still LOW after the loop exhausts, OR budget already 0 at entry → THEN ask the human: "I looked into [topic] but signals are conflicting. [best-effort finding]. Your call?"

**Record resolved technical decisions (write-back — RSCH-03):**
When the loop resolves a question autonomously (HIGH or MEDIUM), append the decision to the phase CONTEXT.md `<decisions>` section so it is not re-asked downstream:
- HIGH → tag `[STRONG, specialist-backed]`
- MEDIUM → tag `[WEAK, specialist-backed]`
Include inline `confidence:` and `source:` and the marker `<!-- resolved inline by resolution loop -->`.
```

This realizes RSCH-01 (loop in the LOW branch), RSCH-02 (MEDIUM auto-decides; human reached only on LOW-after-exhaustion), and RSCH-03's write-back half. Leave the existing "**Signal strength for specialist-backed decisions:**" block (lines 307–316) intact below this — it still governs user-confirmed cases.
  </action>
  <acceptance_criteria>
    - `git status --porcelain get-shit-done/workflows/discuss-phase.md` shows it modified (NOT .claude/)
    - The old bare-ask LOW line is gone: `grep -c "fall back to asking user" get-shit-done/workflows/discuss-phase.md` returns 0
    - The LOW branch now runs the loop: `grep -cE "resolution loop|broaden \+ re-research|Iteration 2" get-shit-done/workflows/discuss-phase.md` returns >= 1
    - MEDIUM auto-decides: `grep -c "auto-decide" get-shit-done/workflows/discuss-phase.md` returns >= 1 AND the text states MEDIUM does not become a question (`grep -c "does NOT fall through to ask-user\|do not turn this into a question" get-shit-done/workflows/discuss-phase.md` returns >= 1)
    - Write-back present: `grep -F "[WEAK, specialist-backed]" get-shit-done/workflows/discuss-phase.md` matches AND `grep -c "resolved inline by resolution loop" get-shit-done/workflows/discuss-phase.md` returns >= 1
    - The reference is cited: `grep -c "resolution-loop.md" get-shit-done/workflows/discuss-phase.md` returns >= 1
    - `node --test tests/02-resolution-loop.test.cjs` — the RSCH-02 discuss-phase group and the RSCH-03 skip-logic group now PASS (were red before this plan)
  </acceptance_criteria>
  <verify>node --test tests/02-resolution-loop.test.cjs</verify>
  <done>LOW branch runs the loop and only asks the human after exhaustion; MEDIUM auto-decides; write-back tags present; the bare "fall back to asking user" line removed; Wave 0 test discuss-phase groups green.</done>
</task>
</tasks>

<verification>
- `node --test tests/02-resolution-loop.test.cjs` — RSCH-01, RSCH-02 (discuss-phase), and RSCH-03 (skip-logic) groups pass
- `grep -c "fall back to asking user" get-shit-done/workflows/discuss-phase.md` returns 0 (the eliminated round-trip)
- `git status` shows only `get-shit-done/workflows/discuss-phase.md` modified for this plan, never `.claude/`
- Manual dogfood (per 02-VALIDATION.md Manual-Only): a live discuss-phase run on a phase with a genuine technical unknown resolves it via the loop and records the decision, instead of asking — confirmed separately, not gated by the structural test
</verification>

<success_criteria>
- RSCH-01: the bounded resolution loop is present and runs in discuss-phase's LOW branch
- RSCH-02: MEDIUM auto-decides; the human is reached only when confidence stays LOW after the loop exhausts (or for PREFERENCE)
- RSCH-03: STRONG decisions are skipped via the pre-check; resolved decisions are written back with specialist-backed tags + confidence + source
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/02-autonomous-technical-resolution/02-02-SUMMARY.md`
</output>
