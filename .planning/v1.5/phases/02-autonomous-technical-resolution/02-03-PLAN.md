---
phase: 02-autonomous-technical-resolution
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-shit-done/workflows/plan-phase.md
  - agents/gsd-planner.md
autonomous: true
requirements: [RSCH-02, RSCH-03]
must_haves:
  truths:
    - "When the planner hits a mid-planning technical unknown it cannot resolve from RESEARCH.md, it surfaces a distinct return signal instead of guessing or forcing a human escalation"
    - "The plan-phase ORCHESTRATOR catches that signal, runs the resolution loop (Task-spawned research + self-critique), records the decision, and re-spawns the planner with the answer — all without bouncing to the human while confidence is resolvable"
    - "The loop's research never runs inside the gsd-planner subagent (which lacks Task/Agent/Skill); the planner only surfaces, the orchestrator resolves"
  artifacts:
    - "get-shit-done/workflows/plan-phase.md (committed source) with a new orchestrator resolution sub-branch in Handle Planner Return"
    - "agents/gsd-planner.md (committed source) with an open_question / TECHNICAL UNKNOWN surfacing mechanism in discovery + structured_returns"
  key_links:
    - "gsd-planner.md surfaces TECHNICAL UNKNOWN return → plan-phase.md Step 9 catches it (broken = signal emitted but never handled; planner stalls or guesses)"
    - "plan-phase.md resolution sub-branch → Task(gsd-phase-researcher) at orchestrator scope (broken = no research path; RSCH-02 inline gap unfilled)"
    - "orchestrator records decision to CONTEXT.md → re-spawns planner with answer (broken = planner re-asks the same unknown; RSCH-03 defeated)"
---

<objective>
Give plan-phase the inline technical-resolution path it lacks today (locked decision: this is the one verified, non-redundant gap). Per 02-RESEARCH.md §Integration Mechanics Seam 2 + §Open Questions 2 and locked constraint #2, this MUST be Option A (orchestrator-driven), NOT Option B (loop inside gsd-planner):

- `gsd-planner` tools = Read/Write/Bash/Glob/Grep/WebFetch/context7 — NO Task, NO Agent, NO Skill. It CANNOT spawn micro-research or `gsd-phase-researcher` full mode. A loop placed inside it (Option B) silently degrades to Context7/WebFetch guessing while structural grep still passes (Pitfall 4).
- Therefore: the planner only SURFACES a mid-planning technical unknown via a distinct return signal (`## TECHNICAL UNKNOWN` / `<open_question>`). The plan-phase ORCHESTRATOR — which has Task/Agent/Skill — catches it, runs the resolution loop, records the resolved decision to CONTEXT.md, and re-spawns the planner with the answer in context.

This is a NEW return signal, distinct from the existing `## PLANNING INCONCLUSIVE` (which escalates to the human via "Add context / Retry / Manual" — using that would defeat RSCH-02's autonomous-resolution goal). The new signal means "I can plan; resolve this one fact and re-spawn me."
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-CONTEXT.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md
@get-shit-done/references/resolution-loop.md

<source_runtime_split>
CRITICAL (locked constraint #5): edit ONLY `get-shit-done/workflows/plan-phase.md` and `agents/gsd-planner.md` (committed). The `.claude/` copies are gitignored — never edit them. After editing, both files MUST appear in `git status`.
</source_runtime_split>

<orchestrator_seam>
`get-shit-done/workflows/plan-phase.md` "## 9. Handle Planner Return" (around line 543) currently has three branches:
```
## 9. Handle Planner Return
- **`## PLANNING COMPLETE`:** ... → step 9.5 / 10 / 13
- **`## CHECKPOINT REACHED`:** Present to user, spawn continuation (step 12)
- **`## PLANNING INCONCLUSIVE`:** Show attempts, offer: Add context / Retry / Manual   ← escalates to HUMAN
```
The new `## TECHNICAL UNKNOWN` branch attaches here as a fourth case. The re-spawn mechanism (Task gsd-planner) at lines 534–541 and the continuation pattern (step 12) are the templates to reuse. Step 5 (Handle Research) already spawns `gsd-phase-researcher` via Task — reuse that invocation shape for the loop's research.
</orchestrator_seam>

<planner_seam>
`agents/gsd-planner.md`:
- `<discovery_levels>` (lines 56–72): Level 2-3 unknowns route to "discovery workflow" (DISCOVERY.md) — a separate spawn, not inline. The planner has no mechanism to surface a single mid-planning question.
- `<structured_returns>` (lines 410–469): defines `## PLANNING COMPLETE`, `## GAP CLOSURE`, `## REVISION COMPLETE`. A new `## TECHNICAL UNKNOWN` return shape is added here.
- `<step name="return_result">` (line 404): where the planner decides which structured return to emit.
</planner_seam>

<loop_contract>
@get-shit-done/references/resolution-loop.md
The canonical contract from Plan 01. The orchestrator branch inlines the loop invocation (grep tests assert on plan-phase.md) AND @-references resolution-loop.md.
</loop_contract>
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Add the TECHNICAL UNKNOWN surfacing mechanism to gsd-planner</name>
  <read_first>
    - agents/gsd-planner.md lines 56–72 (discovery_levels — where the planner decides to research)
    - agents/gsd-planner.md lines 404–469 (return_result step + structured_returns — where return shapes live)
    - get-shit-done/references/resolution-loop.md §Where the loop runs (the tool-boundary statement the planner must honor)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Integration Mechanics Seam 2 + Pitfall 4 + §Open Questions 2
  </read_first>
  <files>agents/gsd-planner.md</files>
  <action>
TWO edits to `agents/gsd-planner.md`. The planner SURFACES unknowns; it must NEVER spawn research itself (it has no Task/Agent/Skill tool).

Edit 1 — in `<discovery_levels>` (after line 72, just before `</discovery_levels>`), append:
```
**Mid-planning technical unknowns (surface, do NOT resolve here):** If a Level 2-3 technical unknown blocks a single planning decision and RESEARCH.md does not already answer it, do NOT guess and do NOT spawn a discovery workflow inline. You have no `Task`/`Agent`/`Skill` tool — you cannot run research. Instead, SURFACE the unknown to the orchestrator via the `## TECHNICAL UNKNOWN` return (see structured_returns). The orchestrator runs the resolution loop and re-spawns you with the answer. This keeps the loop's research at the orchestrator level, where its tools exist (see `get-shit-done/references/resolution-loop.md`).
```

Edit 2 — in `<structured_returns>` (before the closing `</structured_returns>` at line 469), add a new return shape:
```
## Technical Unknown (surface for orchestrator resolution)

Emit this INSTEAD of guessing when a single technical fact blocks planning and RESEARCH.md does not cover it. This is NOT `## PLANNING INCONCLUSIVE` (which escalates to the human) — this asks the orchestrator to resolve one fact autonomously and re-spawn you.

```markdown
## TECHNICAL UNKNOWN

**Question:** [the specific technical question blocking a planning decision]
**Why it blocks:** [which task/decision cannot be specified without this]
**Constraints:** [relevant project constraints the resolver should honor]
**What I tried:** [RESEARCH.md sections checked; codebase reads]
**Default if unresolved:** [the assumption you'd fall back to — typed `technical`]
```
```
Also update `<step name="return_result">` (line 404) to mention the new branch: change its body to "Return structured planning outcome to orchestrator. If a single technical fact blocks planning and RESEARCH.md does not cover it, emit `## TECHNICAL UNKNOWN` (not `## PLANNING INCONCLUSIVE`) so the orchestrator resolves it and re-spawns you."

DO NOT add any `Task(` call or research spawn anywhere in gsd-planner.md — the Wave 0 test asserts the planner does NOT spawn research (Pitfall-4 discriminator).
  </action>
  <acceptance_criteria>
    - `git status --porcelain agents/gsd-planner.md` shows it modified (NOT .claude/)
    - `grep -c "TECHNICAL UNKNOWN" agents/gsd-planner.md` returns >= 2 (discovery note + structured return)
    - `grep -c "open_question\|TECHNICAL UNKNOWN" agents/gsd-planner.md` returns >= 1
    - The planner explicitly states it cannot run research: `grep -c "no .Task./Agent./Skill. tool\|cannot run research\|You have no .Task." agents/gsd-planner.md` returns >= 1
    - NEGATIVE (Pitfall-4): the planner does NOT spawn research. `grep -c "Task(" agents/gsd-planner.md` returns 0 AND `grep -c "subagent_type=\"gsd-phase-researcher\"" agents/gsd-planner.md` returns 0
    - The new return is distinct from inconclusive: the text states it is NOT `## PLANNING INCONCLUSIVE` (`grep -c "NOT .## PLANNING INCONCLUSIVE\|not .## PLANNING INCONCLUSIVE" agents/gsd-planner.md` returns >= 1)
  </acceptance_criteria>
  <verify>grep -c 'TECHNICAL UNKNOWN' agents/gsd-planner.md</verify>
  <done>gsd-planner surfaces a distinct TECHNICAL UNKNOWN return, states it cannot run research itself, and contains zero Task-spawns; the Pitfall-4 negative test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the orchestrator resolution branch to plan-phase Handle Planner Return</name>
  <read_first>
    - get-shit-done/workflows/plan-phase.md lines 543–560 (Handle Planner Return + Preference Gap Check)
    - get-shit-done/workflows/plan-phase.md lines 242–282 (Step 5 gsd-phase-researcher Task spawn — the research invocation shape to reuse)
    - get-shit-done/workflows/plan-phase.md lines 534–541 (the gsd-planner Task re-spawn shape)
    - get-shit-done/references/resolution-loop.md §The loop + §Research primitives + §Write-back (the contract to inline)
    - agents/gsd-planner.md §structured_returns (the `## TECHNICAL UNKNOWN` shape this branch consumes — from Task 1)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Integration Mechanics Seam 2 + §Open Questions 2/3
  </read_first>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
In `get-shit-done/workflows/plan-phase.md`, add a FOURTH branch to "## 9. Handle Planner Return" (after the `## PLANNING INCONCLUSIVE` line ~547) and a new sub-step. Insert:

```
- **`## TECHNICAL UNKNOWN`:** The planner can plan but one technical fact blocks a decision. Do NOT escalate to the human (that is the INCONCLUSIVE path). Run the resolution loop at the orchestrator level → step 9.3.

## 9.3. Resolve Technical Unknown (orchestrator resolution loop)

The orchestrator (which has `Task`/`Agent`) runs the resolution loop — see `get-shit-done/references/resolution-loop.md`. The gsd-planner cannot do this itself (no `Task`/`Agent`/`Skill`).

**Signal-strength pre-check:** If the question matches a `[STRONG]` / `[STRONG, user-override]` / `[STRONG, specialist-backed]` decision in CONTEXT.md `<decisions>`, skip the loop — apply that decision and re-spawn the planner with it.

**Otherwise run the loop (max 2 iterations; budget-bounded):**
- Iteration 1 — light path: spawn micro-research.
```
Task(subagent_type="gsd-phase-researcher", prompt="
<micro_research>
QUESTION: {the planner's TECHNICAL UNKNOWN question}
CONSTRAINTS: {from the planner's return + CONTEXT.md}
PHASE GOAL: {from ROADMAP.md}
DOMAIN: {the technical domain}
</micro_research>
", description="Resolve plan-phase unknown: {short}")
```
- **HIGH/MEDIUM** → auto-decide (MEDIUM with override caveat); record + re-spawn planner.
- **LOW** → iteration 2: re-spawn with a critique hint (broaden / different source), OR escalate to the heavy path: `gsd-phase-researcher` full mode (orchestrator-spawned). Decrement budget.
- Still LOW after the loop exhausts → THEN surface to the human (this is the only path to the human).

**Record (RSCH-03 write-back):** Append the resolved decision to the phase CONTEXT.md `<decisions>` section — `[STRONG, specialist-backed]` if HIGH, `[WEAK, specialist-backed]` if MEDIUM — with inline `confidence:` + `source:` + the marker `<!-- resolved inline by resolution loop -->`. Downstream planners/verifiers read CONTEXT.md and will not re-ask.

**Re-spawn the planner** with the resolved answer in context (reuse the Step 8 `Task(subagent_type="gsd-planner", ...)` shape, adding the resolved decision to the prompt), then return to step 9.
```

The micro-research and full-mode spawns reuse the existing Step 5 invocation shape. DO place the `Task(` spawns in plan-phase.md (orchestrator) — that is the whole point of Option A.
  </action>
  <acceptance_criteria>
    - `git status --porcelain get-shit-done/workflows/plan-phase.md` shows it modified (NOT .claude/)
    - The new branch exists: `grep -c "## TECHNICAL UNKNOWN" get-shit-done/workflows/plan-phase.md` returns >= 1
    - It is distinct from the human-escalation path: `grep -c "Do NOT escalate to the human\|not the INCONCLUSIVE path\|that is the INCONCLUSIVE path" get-shit-done/workflows/plan-phase.md` returns >= 1
    - The orchestrator runs the loop with a research spawn: `grep -c "resolution loop\|resolution-loop.md" get-shit-done/workflows/plan-phase.md` returns >= 1 AND the new region contains a `Task(subagent_type="gsd-phase-researcher"` spawn (the inline research path that did not exist before)
    - Heavy path named: `grep -c "full mode" get-shit-done/workflows/plan-phase.md` returns >= 1
    - Write-back present: `grep -F "[STRONG, specialist-backed]" get-shit-done/workflows/plan-phase.md` matches AND `grep -c "resolved inline by resolution loop" get-shit-done/workflows/plan-phase.md` returns >= 1
    - Re-spawn present: the branch re-spawns the planner (`grep -c "re-spawn" get-shit-done/workflows/plan-phase.md` returns >= 1)
    - `node --test tests/02-resolution-loop.test.cjs` — the RSCH-02 plan-phase group (orchestrator loop present + planner surfaces + Pitfall-4 negative) and RSCH-03 group PASS
  </acceptance_criteria>
  <verify>node --test tests/02-resolution-loop.test.cjs</verify>
  <done>plan-phase.md has a TECHNICAL UNKNOWN branch (step 9.3) that runs the loop with orchestrator-level Task-spawned research, records the decision, and re-spawns the planner; distinct from INCONCLUSIVE; Wave 0 plan-phase + Pitfall-4 tests green.</done>
</task>
</tasks>

<verification>
- `node --test tests/02-resolution-loop.test.cjs` — RSCH-02 plan-phase group, RSCH-03 group, AND the Pitfall-4 negative assertion (gsd-planner does not spawn) all pass
- `grep -c "Task(" agents/gsd-planner.md` returns 0 (planner surfaces, never spawns)
- `grep -c "Task(subagent_type=\"gsd-phase-researcher\"" get-shit-done/workflows/plan-phase.md` returns >= 1 in the new region (orchestrator does the research)
- `git status` shows `get-shit-done/workflows/plan-phase.md` and `agents/gsd-planner.md` modified, never `.claude/`
- Manual dogfood (per 02-VALIDATION.md): a live plan-phase run hitting a technical unknown completes without stopping to ask the human, with the resolved decision recorded in CONTEXT.md — confirmed separately, not gated by the structural test
</verification>

<success_criteria>
- RSCH-02: plan-phase gains an inline technical-resolution path (absent today) at the orchestrator level; the human is reached only on LOW-after-exhaustion. The planner surfaces; the orchestrator resolves — honoring the tool boundary (locked constraint #2, Pitfall 4)
- RSCH-03: STRONG decisions are skipped; resolved decisions are written back to CONTEXT.md with specialist-backed tags so the re-spawned planner and downstream agents do not re-ask
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/02-autonomous-technical-resolution/02-03-SUMMARY.md`
</output>
