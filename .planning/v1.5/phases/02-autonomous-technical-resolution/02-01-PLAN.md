---
phase: 02-autonomous-technical-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/02-resolution-loop.test.cjs
  - get-shit-done/references/resolution-loop.md
autonomous: true
requirements: [RSCH-01]
must_haves:
  truths:
    - "A canonical resolution-loop contract exists describing research → self-critique → confidence verdict with a bounded iteration ceiling"
    - "The contract specifies MEDIUM confidence auto-decides (with override caveat); only LOW-after-exhaustion or genuine preference reaches the human"
    - "A structural test file exists that the two wiring plans (02, 03) run after each task to prove the loop prose is wired where its tools can execute"
  artifacts:
    - "get-shit-done/references/resolution-loop.md (committed source — the DRY loop contract)"
    - "tests/02-resolution-loop.test.cjs (Wave 0 structural test, runs against committed source files)"
  key_links:
    - "tests/02-resolution-loop.test.cjs reads committed get-shit-done/ and agents/ paths (NOT .claude/ runtime) — broken = tests validate the wrong copy"
    - "resolution-loop.md is @-referenced by the wiring plans for the DRY contract, but inline branch prose in the wiring files is what the grep tests assert — broken contract = drift between reference and wiring"
---

<objective>
Lay the foundation for the autonomous technical-resolution loop: (1) write the canonical loop contract as a committed reference file, and (2) write the Wave 0 structural test that the two downstream wiring plans (02 discuss-phase, 03 plan-phase) will run after every task. The contract is the single source of truth for the loop's shape (research → self-critique → confidence verdict, bounded iterations, MEDIUM auto-decides, signal-strength skip). The test is the feedback instrument — critically, it must DISCRIMINATE the locked tool-boundary constraint, not merely grep for prose presence (per 02-RESEARCH.md Pitfall 4: a loop placed inside gsd-planner passes naive grep but cannot actually run, because the planner has no Task/Skill tool).

This plan creates NO wiring — it produces the contract and the test that the wiring plans are graded against. Both downstream plans depend on this plan.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-CONTEXT.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md
@.planning/v1.5/phases/02-autonomous-technical-resolution/02-VALIDATION.md

<source_runtime_split>
CRITICAL (locked constraint #5): `.claude/` is GITIGNORED (install-time runtime mirror). ALL edits land in committed source: `get-shit-done/references/`, `tests/`. After editing, the file MUST appear in `git status`. Never edit `.claude/...`.
</source_runtime_split>

<loop_contract_facts>
From 02-RESEARCH.md §Architecture Patterns (the contract this plan codifies):

Resolution loop verdict shape (adapted from Phase 4 04-AGENT-SPEC.md verdict JSON):
```json
{
  "confidence": "HIGH | MEDIUM | LOW",
  "recommendation": "string",
  "reasoning": "string",
  "source": "string",
  "iterations_used": "number",
  "escalate": "boolean"
}
```

Loop structure (research §Recommended Loop Structure):
- max_iterations = 2 (ceiling; may be 3) — bounded, mirrors Phase 4 max-3 ceiling
- HIGH or MEDIUM → return immediately, decide autonomously
- LOW → critique (broaden query / alternate source) and re-research; decrement budget
- exhausted (budget 0 OR ceiling hit) → return LOW with escalate:true → ask human

Confidence → action threshold (research §Confidence Threshold for Auto-Decide):
- HIGH → decide autonomously, present as FYI ("I resolved X: [rec]. Source: [src]")
- MEDIUM → decide autonomously WITH caveat ("Going with X [reasoning]. You can override.") — THIS IS THE KEY BEHAVIOR CHANGE (Pitfall 5: MEDIUM must NOT be treated as LOW)
- LOW after exhaustion → surface to human

Signal-strength skip pre-check (research §Signal-Strength Honoring):
- Before spawning the loop, scan CONTEXT.md `<decisions>`. If question subject matches a `[STRONG]` / `[STRONG, user-override]` / `[STRONG, specialist-backed]` decision → apply directly, do NOT spawn loop.

Tool-boundary constraint (locked #2, research §Open Questions 1 & 2, Pitfall 4):
- The loop's research steps (light = micro_research_mode, heavy = gsd-phase-researcher full mode) require the Task/Agent tool.
- gsd-planner tools = Read/Write/Bash/Glob/Grep/WebFetch/context7 — NO Task, NO Agent, NO Skill.
- Therefore the loop's research MUST run at ORCHESTRATOR level (discuss-phase.md, plan-phase.md), never inside the gsd-planner subagent.
</loop_contract_facts>

<phase4_loop_reference>
@.planning/v1.4/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md
(The bounded-iteration verifier-loop shape to mirror: max iters, structured verdict, escalate-on-ceiling. Read the verdict shape and ceiling sections.)
</phase4_loop_reference>
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Write the canonical resolution-loop contract reference</name>
  <read_first>
    - get-shit-done/references/AGENTIC-PATTERNS.md (sibling reference — match doc style/heading conventions)
    - get-shit-done/references/verification-patterns.md (sibling reference — match style)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Architecture Patterns (the contract source — verdict shape, loop structure, confidence threshold, signal-strength honoring)
    - .planning/v1.4/phases/04-verification-harness-and-context-efficiency/04-AGENT-SPEC.md (the bounded-loop shape to mirror)
  </read_first>
  <files>get-shit-done/references/resolution-loop.md</files>
  <action>
Create `get-shit-done/references/resolution-loop.md` — the single source of truth for the autonomous technical-resolution loop. This is LLM-executed prose (a contract orchestrators @-reference and inline), not callable code. Write these sections with these EXACT load-bearing strings present:

# Resolution Loop — Autonomous Technical Resolution

## Purpose
One paragraph: when a TECHNICAL or HYBRID question arises in a GSD workflow, resolve it autonomously (research → self-critique → confidence verdict) instead of bouncing it to the human. The human is reached ONLY when confidence stays LOW after the loop exhausts, or the question is genuine preference/taste.

## Where the loop runs (tool-boundary constraint)
State explicitly: the loop's research steps require the `Task`/`Agent` tool and therefore MUST run at the **orchestrator** level (discuss-phase.md, plan-phase.md). They MUST NOT run inside the `gsd-planner` subagent, which has no `Task`/`Agent`/`Skill` tool (tools: Read, Write, Bash, Glob, Grep, WebFetch, context7). A loop placed inside the planner silently degrades to inline Context7/WebFetch guessing. Include the literal phrase: `orchestrator level` and the literal phrase: `gsd-planner has no Task`.

## Research primitives
- **Light path:** `gsd-phase-researcher` `micro_research_mode` (single-question inline, 15–30s, returns Recommendation/Reasoning/Confidence/Source/Caveat).
- **Heavy path:** orchestrator-spawned `gsd-phase-researcher` full mode (multi-source cross-verified). Include the literal phrase: `gsd-phase-researcher full mode`. NOTE explicitly: do NOT call `Skill(deep-research)` from any subagent — it is a native harness skill reachable only from the top-level orchestrator and not guaranteed on every install; use `gsd-phase-researcher` full mode as the portable heavy path.

## Signal-strength pre-check (RSCH-03)
Before spawning the loop, scan CONTEXT.md `<decisions>`. If the question subject matches a decision tagged `[STRONG]`, `[STRONG, user-override]`, or `[STRONG, specialist-backed]` → apply it directly (HIGH confidence, source "CONTEXT.md locked decision"), do NOT spawn the loop. Include the literal phrase: `skip the loop`.

## The loop (bounded iteration)
Document this structure verbatim:
```
resolution_loop(question, constraints, budget_remaining):
  max_iterations = 2   # ceiling; may be 3. Bounded — mirrors Phase 4 max-3 ceiling.
  iteration = 0
  while iteration < max_iterations and budget_remaining > 0:
    result = micro_research(question, constraints, critique_hint[iteration])
    if result.confidence in [HIGH, MEDIUM]:
      return verdict(result, iterations_used=iteration+1, escalate=false)
    iteration += 1; budget_remaining -= 1
    critique_hint[iteration] = derive_critique(result)  # broaden query / alternate source type
  return verdict(result, confidence=LOW, iterations_used=max_iterations, escalate=true)
```
Critique strategies: iteration 2 broadens (drop project-specific constraints, answer general domain); iteration 3 (if allowed) cross-checks via a different source type than the first pass.

## Verdict shape
```json
{
  "confidence": "HIGH | MEDIUM | LOW",
  "recommendation": "string",
  "reasoning": "string",
  "source": "string",
  "iterations_used": "number",
  "escalate": "boolean"
}
```

## Confidence → action (the behavior change)
- **HIGH** → decide autonomously, present as FYI.
- **MEDIUM** → decide autonomously WITH override caveat. Include the literal phrase: `MEDIUM` and the literal phrase: `auto-decide` (state MEDIUM auto-decides; it MUST NOT fall through to ask-user — that defeats the round-trip-reduction goal).
- **LOW after exhaustion** → surface to human (`escalate: true`).

## Write-back (RSCH-03)
When the loop resolves a question (HIGH or MEDIUM), record the decision in the phase CONTEXT.md `<decisions>` section so it is not re-asked downstream:
- HIGH → tag `[STRONG, specialist-backed]`
- MEDIUM → tag `[WEAK, specialist-backed]`
- Append inline `confidence:` and `source:` and a marker comment `<!-- resolved inline by resolution loop -->`.
Include the literal strings `[STRONG, specialist-backed]` and `[WEAK, specialist-backed]`.

## Budget
The loop's re-research iterations count against the existing discuss-phase `0-5 micro-research calls per session` budget. If budget is 0 at loop entry, skip to ask-user (immediate LOW escalation).
  </action>
  <acceptance_criteria>
    - `test -f get-shit-done/references/resolution-loop.md` succeeds
    - `git status --porcelain get-shit-done/references/resolution-loop.md` shows the file as added/modified (NOT under .claude/)
    - `grep -c "orchestrator level" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -c "gsd-planner has no Task" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -c "gsd-phase-researcher full mode" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -c "iterations_used" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -c "escalate" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -c "skip the loop" get-shit-done/references/resolution-loop.md` returns >= 1
    - `grep -F "[STRONG, specialist-backed]" get-shit-done/references/resolution-loop.md` matches
    - `grep -F "[WEAK, specialist-backed]" get-shit-done/references/resolution-loop.md` matches
    - `grep -cE "MEDIUM" get-shit-done/references/resolution-loop.md` returns >= 1 AND `grep -c "auto-decide" get-shit-done/references/resolution-loop.md` returns >= 1
  </acceptance_criteria>
  <verify>test -f get-shit-done/references/resolution-loop.md && grep -c iterations_used get-shit-done/references/resolution-loop.md</verify>
  <done>resolution-loop.md exists in committed source with all contract sections and load-bearing strings; appears in git status; not in .claude/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the Wave 0 structural test (discriminates the tool-boundary constraint)</name>
  <read_first>
    - tests/agent-spec-init.test.cjs (sibling test — match node:test cjs structure, describe/test shape, fs.readFileSync pattern)
    - tests/helpers.cjs (shared test helpers — reuse repo-root resolution if present)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-VALIDATION.md §Wave 0 Requirements (the assertions this test must encode)
    - .planning/v1.5/phases/02-autonomous-technical-resolution/02-RESEARCH.md §Validation Architecture + Pitfall 4 (why naive grep is insufficient — the negative assertions)
    - get-shit-done/references/resolution-loop.md (the contract this test cross-checks; created in Task 1)
  </read_first>
  <files>tests/02-resolution-loop.test.cjs</files>
  <action>
Create `tests/02-resolution-loop.test.cjs` using the built-in `node:test` runner (Jest is NOT a dependency — STATE.md confirms Phase 02 agent-spec used node:test). Resolve the repo root so paths point at COMMITTED source (`get-shit-done/...`, `agents/...`), NEVER `.claude/...`. Pattern after tests/agent-spec-init.test.cjs.

Read these files once at top: `get-shit-done/workflows/discuss-phase.md`, `get-shit-done/workflows/plan-phase.md`, `agents/gsd-planner.md`, `get-shit-done/references/resolution-loop.md`.

IMPORTANT — this test is the grading instrument for Plans 02 and 03, which do not exist yet. After ONLY Plan 01 runs, the RSCH-01 group (which targets resolution-loop.md) PASSES; the RSCH-02 and RSCH-03 groups (which target the WIRING files discuss-phase.md / plan-phase.md / gsd-planner.md) are EXPECTED to FAIL until Plans 02/03 run. That is correct Wave 0 behavior. To make the wiring groups genuinely discriminate, the RSCH-02 and RSCH-03 assertions MUST target the wiring files specifically — do NOT include resolution-loop.md in those OR-conditions, or the groups self-satisfy from Plan 01's contract file and stop catching missing wiring. Also DO NOT assert on CONTEXT.md write-back tags (those are manual/dogfood per VALIDATION.md — asserting them here authors a permanently-RED test).

Write these test groups:

describe('RSCH-01 — loop contract exists') [targets resolution-loop.md only]:
  - resolution-loop.md exists and contains 'iterations_used' and 'escalate' (verdict shape)
  - resolution-loop.md states MEDIUM auto-decides: matches /MEDIUM/ AND /auto-decide/
  - resolution-loop.md documents the bounded ceiling: matches /max_iterations/

describe('RSCH-02 — discuss-phase LOW branch runs the loop, not bare ask-user') [targets discuss-phase.md only]:
  - discuss-phase.md LOW branch references the resolution loop: discuss-phase.md matches /resolution.loop/i OR /self.critique/i OR /re.research/i
  - NEGATIVE/positional: the LOW branch is NOT a bare ask-user. Extract the question_triage block (between '<question_triage>' and '</question_triage>') and assert it contains a re-research/loop step (matches /broaden|re.research|iteration|resolution.loop/i) so LOW does not jump straight to "fall back to asking user" as the only action.

describe('RSCH-02 — plan-phase resolves inline at orchestrator, planner only surfaces') [targets plan-phase.md + gsd-planner.md]:
  - plan-phase.md contains an orchestrator-level resolution path: matches /resolution.loop/i AND a research spawn (matches /Task\\(/ near a gsd-phase-researcher reference). CRITICAL — this assertion is FILE-SCOPED: run the regex against the WHOLE plan-phase.md file text, NOT against a region bounded by the `## 9` heading. Rationale: Plan 03 places the research spawn in a new `## 9.3` numbered sub-step, which is architecturally part of the "Handle Planner Return" cluster (## 9.x sub-steps belong to ## 9) but falls OUTSIDE a naive next-`##`-heading region extractor. A heading-bounded extractor here would author a permanently-RED test even when the wiring is correct. So: whole-file match for `/resolution.loop/i` AND a `Task(`-near-`gsd-phase-researcher` spawn (e.g. /Task\\([\\s\\S]{0,200}gsd-phase-researcher/i OR /gsd-phase-researcher[\\s\\S]{0,200}Task\\(/i) anywhere in plan-phase.md.
  - plan-phase.md references a distinct technical-unknown return signal: matches /TECHNICAL UNKNOWN|open_question/
  - gsd-planner.md SURFACES the unknown: matches /open_question|TECHNICAL UNKNOWN/
  - NEGATIVE (the Pitfall-4 discriminator — CRITICAL): the resolution loop's research spawn is NOT inside gsd-planner. Assert gsd-planner.md does NOT contain a Task-spawn of research. Concretely: assert gsd-planner.md does NOT match /resolution.loop[\\s\\S]{0,400}Task\\(/i AND does NOT match /Task\\([\\s\\S]{0,200}gsd-phase-researcher/i (the planner has no Task tool; it must only SURFACE, never SPAWN). Verified baseline: gsd-planner.md currently contains zero `Task(` / `subagent_type` (checked 2026-06-04), so this negative holds before and after additive wiring. This is the assertion that catches a loop wrongly buried in the planner where its tools cannot run.

describe('RSCH-03 — signal-strength skip + STRONG-honoring present in WIRING files') [targets discuss-phase.md / plan-phase.md / gsd-planner.md — NOT resolution-loop.md]:
  - discuss-phase.md contains skip logic: matches /STRONG[\\s\\S]{0,80}skip|skip[\\s\\S]{0,80}STRONG|Signal-strength pre-check|skip the loop/i
  - the plan-phase orchestrator OR gsd-planner wiring references honoring STRONG decisions: (plan-phase.md OR gsd-planner.md) matches /\\[STRONG\\]|STRONG.*decision|signal-strength/i — explicitly EXCLUDE resolution-loop.md from this OR so it discriminates the wiring, not the contract.

Each test uses node:assert. Group with describe/test from node:test. The file must run with `node --test tests/02-resolution-loop.test.cjs`.
  </action>
  <acceptance_criteria>
    - `test -f tests/02-resolution-loop.test.cjs` succeeds
    - `git status --porcelain tests/02-resolution-loop.test.cjs` shows the file as added (NOT under .claude/)
    - `node -c tests/02-resolution-loop.test.cjs` (syntax check) exits 0
    - `node --test --test-name-pattern 'RSCH-01' tests/02-resolution-loop.test.cjs` PASSES and exits 0 (the only group that must be green at Wave 0 — Task 1 created resolution-loop.md). Running the full file (`node --test tests/02-resolution-loop.test.cjs`) intentionally exits NON-ZERO at Wave 0 because the RSCH-02 and RSCH-03 groups target the WIRING files and stay RED until Plans 02/03 run — that is correct, do NOT treat the full-file non-zero exit as a Task-2 failure.
    - `grep -c "gsd-phase-researcher" tests/02-resolution-loop.test.cjs` returns >= 1 (the negative Pitfall-4 assertion is present)
    - `grep -c "\.claude" tests/02-resolution-loop.test.cjs` returns 0 (test reads committed source only, never the runtime mirror)
    - The RSCH-03 group does NOT read resolution-loop.md for its assertions: `grep -nc "resolution-loop" tests/02-resolution-loop.test.cjs` shows resolution-loop references confined to the RSCH-01 group (manual read-check; the RSCH-03 assertions name discuss-phase.md / plan-phase.md / gsd-planner.md only)
    - The plan-phase RSCH-02 spawn assertion is file-scoped (whole-file match), NOT bounded by the `## 9` heading — so Plan 03's `## 9.3` sub-step placement of the spawn still satisfies it (manual read-check: the assertion matches against the full plan-phase.md text)
    - The file contains a NEGATIVE assertion that gsd-planner.md does NOT spawn research (grep for `doesNotMatch` referencing gsd-planner)
  </acceptance_criteria>
  <verify>node --test --test-name-pattern 'RSCH-01' tests/02-resolution-loop.test.cjs</verify>
  <done>tests/02-resolution-loop.test.cjs runs under node:test; the RSCH-01 group is green (verified in isolation — full-file run is intentionally red until Plans 02/03); RSCH-02/RSCH-03 groups target the WIRING files only (not resolution-loop.md); the plan-phase spawn assertion is file-scoped so Plan 03's `## 9.3` placement satisfies it; the Pitfall-4 negative assertion (planner does not spawn) is present; no .claude/ paths referenced.</done>
</task>
</tasks>

<verification>
- `node --test --test-name-pattern 'RSCH-01' tests/02-resolution-loop.test.cjs` passes and exits 0; the full-file `node --test tests/02-resolution-loop.test.cjs` runs without load errors but intentionally exits non-zero (RSCH-02/RSCH-03 red-pending until Plans 02/03)
- `git status` shows both new files under committed paths (get-shit-done/references/, tests/), none under .claude/
- resolution-loop.md contains every load-bearing string from Task 1 acceptance criteria
- The Wave 0 test references `agents/gsd-planner.md` and `get-shit-done/workflows/` source paths, never `.claude/`; the RSCH-03 group targets wiring files only; the plan-phase RSCH-02 spawn assertion is file-scoped (tolerates Plan 03's `## 9.3` sub-step placement)
</verification>

<success_criteria>
- The canonical loop contract (resolution-loop.md) exists in committed source with verdict shape, bounded-iteration structure, MEDIUM-auto-decides, signal-strength skip, write-back tags, and the orchestrator tool-boundary statement
- The Wave 0 structural test exists, runs under node:test, grades the two wiring plans, and crucially (a) includes the negative Pitfall-4 assertion (gsd-planner surfaces but does not spawn) so a loop wrongly buried in the planner FAILS, (b) targets the wiring files (not the contract file) in the RSCH-02/RSCH-03 groups so missing wiring is genuinely caught, and (c) scopes the plan-phase spawn assertion to the whole file so Plan 03's `## 9.3` sub-step placement keeps the test honestly green-able
- RSCH-01 satisfied: the loop contract is defined and composed from existing capability (no new agent)
</success_criteria>

<assumptions>
## ASSUMPTIONS

| # | Assumption | Type | Basis | If wrong |
|---|------------|------|-------|----------|
| 1 | Loop iteration ceiling = 2 (may be 3) | technical | 02-RESEARCH.md §Architecture Patterns ("max 2-3, planner decides"); mirrors Phase 4 max-3 | Executor bumps to 3 in contract — no structural change |
| 2 | Resolved decisions are written back to the phase CONTEXT.md `<decisions>` section (vs RESEARCH.md or a sidecar) | technical | 02-RESEARCH.md §Open Questions 3 recommendation — CONTEXT.md already read by downstream agents | Switch write-back target; all three plans reference the same target so change them together |
| 3 | The Wave 0 test reuses tests/helpers.cjs repo-root resolution if present, else resolves root relative to __dirname | technical | Established test convention in this repo | Inline path resolution; no behavior change |
| 4 | resolution-loop.md is the right home for the DRY contract (vs inlining everything in each workflow) | technical | Locked decision #3 explicitly authorizes "a new reference file get-shit-done/references/resolution-loop.md"; sibling references exist | Inline-only; grep tests target the workflow files regardless so still pass |
</assumptions>

<output>
After completion, create `.planning/v1.5/phases/02-autonomous-technical-resolution/02-01-SUMMARY.md`
</output>
</output>
