---
phase: 08-validated-example-corpus
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - get-shit-done/references/validated-examples/async-retry-backoff.md
  - get-shit-done/references/validated-examples/validation-layer-ts.md
  - get-shit-done/references/validated-examples/config-env-validation.md
autonomous: true
requirements:
  - SC1
  - SC3
must_haves:
  truths:
    - truth: "Three Node/TS entry files exist, curated from real reputation-vetted repos"
      verify:
        - cmd: "test -f get-shit-done/references/validated-examples/async-retry-backoff.md && test -f get-shit-done/references/validated-examples/validation-layer-ts.md && test -f get-shit-done/references/validated-examples/config-env-validation.md && echo OK"
          type: unit
          expect: /OK/
    - truth: "Every Node/TS entry carries the mandatory attribution + Phase-9-join front matter (pattern_id, counters, license, source_permalink)"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/async-retry-backoff.md get-shit-done/references/validated-examples/validation-layer-ts.md get-shit-done/references/validated-examples/config-env-validation.md; do grep -q 'pattern_id:' \"$f\" && grep -q 'counters:' \"$f\" && grep -q 'license:' \"$f\" && grep -q 'source_permalink:' \"$f\" || echo \"MISSING: $f\"; done"
          type: unit
    - truth: "Every entry has the four commentary sections including what-NOT-to-cargo-cult"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/async-retry-backoff.md get-shit-done/references/validated-examples/validation-layer-ts.md get-shit-done/references/validated-examples/config-env-validation.md; do grep -q '## What this solves' \"$f\" && grep -q '## Excerpt' \"$f\" && grep -q '## What NOT to cargo-cult' \"$f\" || echo \"MISSING-SECTION: $f\"; done"
          type: unit
    - truth: "Permalinks are pinned to a release tag, not HEAD or the default branch"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/async-retry-backoff.md get-shit-done/references/validated-examples/validation-layer-ts.md get-shit-done/references/validated-examples/config-env-validation.md; do grep 'source_permalink:' \"$f\" | grep -E '/blob/(main|master|HEAD)/' >/dev/null && echo \"UNPINNED: $f\"; done"
          type: unit
  artifacts:
    - get-shit-done/references/validated-examples/async-retry-backoff.md
    - get-shit-done/references/validated-examples/validation-layer-ts.md
    - get-shit-done/references/validated-examples/config-env-validation.md
  key_links:
    - "each entry counters value maps to a common-bug-patterns.md section header (broken = Phase 9 eval cannot bind entry to failure category)"
---

<objective>
Curate the three Node/TS seed entries from real, reputation-vetted repositories, each
following the _TEMPLATE.md contract from Plan 01. The executor MUST read the actual source
on GitHub and quote it accurately — do NOT synthesize code. INDEX rows are NOT written here
(Plan 04 owns INDEX.md) — this plan owns only its three entry files so it runs parallel
with Plan 02.

Covers ROADMAP SC1 (real-sourced commentary) and SC3 (counters: join key for Phase 9).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.5/phases/08-validated-example-corpus/08-CONTEXT.md
@.planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md
@.planning/reference/CODE-EXAMPLES.md
@get-shit-done/references/common-bug-patterns.md
@get-shit-done/references/validated-examples/_TEMPLATE.md
@get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Curate async-retry-backoff and config-env-validation entries</name>
  <read_first>
    - get-shit-done/references/validated-examples/_TEMPLATE.md (exact front-matter fields + section order)
    - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md (source bar + excerpt hygiene + licensing)
    - get-shit-done/references/common-bug-patterns.md (counters: `Async / Timing`, `Error Handling`, `Environment / Config` — verbatim headers)
    - .planning/reference/CODE-EXAMPLES.md (candidate Node repos: undici, Fastify, Node core)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q4: undici retry/backoff; fastify/node config-env init path)
  </read_first>
  <files>get-shit-done/references/validated-examples/async-retry-backoff.md, get-shit-done/references/validated-examples/config-env-validation.md</files>
  <action>
Author two entry files, each conforming exactly to _TEMPLATE.md.

(1) `async-retry-backoff.md` — `pattern_id: async-retry-backoff`, `language: javascript` (or
typescript if the source is TS). Source: `nodejs/undici` — the retry handler / backoff logic
(e.g. `lib/handler/retry-handler.js` or `lib/api/...` — the bounded-retry loop with backoff
delay and timeout/abort handling). LOCATE the real file + lines on a pinned release tag
(e.g. v6.x), record real `source_file`, `source_lines`, release-tag `source_permalink`,
`license: MIT`. Excerpt 15-25 lines showing the backoff computation + max-retry guard +
abort/timeout handling. `counters:` MUST include `Async / Timing` and `Error Handling`
(verbatim). Commentary: solves unbounded/naive retries + leaked timers; why good = bounded
attempts, computed backoff, respects abort signal; what NOT to cargo-cult = undici-specific
handler interface, protocol-specific status checks.

(2) `config-env-validation.md` — `pattern_id: config-env-validation`, `language: javascript`.
Source: a reputation-vetted Node repo's config/env init path (`fastify/fastify` env/options
parsing, or `nodejs/node` config reading) where env is read, validated as present + typed,
and fails fast with a clear message. LOCATE real file + lines on a pinned tag, record real
attribution, correct `license`. Excerpt 12-20 lines. `counters:` MUST include
`Environment / Config` and `Data Shape / API Contract` (verbatim). Commentary: solves missing
env var / silent undefined / hardcoded path; why good = fail-fast with message, typed coercion,
explicit defaults; what NOT to cargo-cult = framework-specific option schema.

Do NOT write INDEX rows. Do NOT paste excerpts outside these reference files. Quote real source.
  </action>
  <verify>for f in async-retry-backoff config-env-validation; do test -f "get-shit-done/references/validated-examples/$f.md" && grep -q 'counters:' "get-shit-done/references/validated-examples/$f.md" && grep -q '## What NOT to cargo-cult' "get-shit-done/references/validated-examples/$f.md" || echo "FAIL: $f"; done</verify>
  <acceptance_criteria>
    - Both files exist with all template front-matter fields present
    - async-retry-backoff `counters:` includes `Async / Timing` and `Error Handling`; config-env `counters:` includes `Environment / Config` and `Data Shape / API Contract` — each matching a `## ` header in common-bug-patterns.md
    - Each `source_permalink:` is release-tag-pinned (not main/master/HEAD)
    - Each file has all four sections and a fenced ~12-25 line `## Excerpt` block
    - `license:` matches the source repo (MIT for undici/fastify)
  </acceptance_criteria>
  <done>async-retry-backoff.md and config-env-validation.md exist, each with a real attributed excerpt, header-form counters, and full commentary.</done>
</task>

<task type="auto">
  <name>Task 2: Curate the Node/TS validation-layer (Zod) entry</name>
  <read_first>
    - get-shit-done/references/validated-examples/_TEMPLATE.md
    - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
    - get-shit-done/references/common-bug-patterns.md (counters: `Data Shape / API Contract`, `Type / Coercion` — verbatim headers)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q4: zod z.object().parse + .safeParse error shape)
  </read_first>
  <files>get-shit-done/references/validated-examples/validation-layer-ts.md</files>
  <action>
Author `validation-layer-ts.md` — `pattern_id: validation-layer-ts`, `language: typescript`.
Source: `colinhacks/zod`. Excerpt the schema-parse pattern: a `z.object({...}).parse()` /
`.safeParse()` usage showing typed output + the discriminated success/error result (parse at
the boundary, branch on `.success`). Prefer a real usage region in the repo (src or a
documented example file). LOCATE real file + lines on a pinned release tag, record real
`source_file`, `source_lines`, release-tag `source_permalink`, `license: MIT`. Excerpt 15-25
lines. `counters:` MUST include `Data Shape / API Contract` and `Type / Coercion` (verbatim).
Commentary: solves untyped boundary input + unsafe casts; why good = schema as single source
of truth, inferred static type, safeParse error branch; what NOT to cargo-cult = Zod-version
API surface, don't over-nest schemas.

Do NOT write INDEX rows.
  </action>
  <verify>test -f get-shit-done/references/validated-examples/validation-layer-ts.md && grep -q 'Type / Coercion' get-shit-done/references/validated-examples/validation-layer-ts.md && grep -q '## What NOT to cargo-cult' get-shit-done/references/validated-examples/validation-layer-ts.md</verify>
  <acceptance_criteria>
    - validation-layer-ts.md exists with all template front-matter fields
    - `counters:` includes `Data Shape / API Contract` and `Type / Coercion`, each matching a `## ` header in common-bug-patterns.md
    - `source_permalink:` is release-tag-pinned (not main/master/HEAD)
    - `license: MIT` present
    - all four commentary sections present; `## Excerpt` has a fenced ~15-25 line code block
  </acceptance_criteria>
  <done>validation-layer-ts.md exists with a real Zod excerpt, header-form counters, and full commentary.</done>
</task>
</tasks>

<verification>
- Three Node/TS entry files exist, each conforming to _TEMPLATE.md
- Every counters: value is an exact common-bug-patterns.md header (Phase 9 join intact)
- Every permalink is release-tag-pinned; every entry has all four commentary sections
- No INDEX edits in this plan (parallel-safe with Plan 02)
</verification>

<success_criteria>
The three Node/TS seed patterns (async-retry-backoff, validation-layer-ts, config-env-validation)
are curated from real reputation-vetted repos with short attributed excerpts and full commentary,
ready for Plan 04 to index.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/08-validated-example-corpus/08-03-SUMMARY.md`
</output>
