---
phase: 08-validated-example-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/validated-examples/INDEX.md
  - get-shit-done/references/validated-examples/_TEMPLATE.md
  - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
autonomous: true
requirements:
  - SC1
must_haves:
  truths:
    - truth: "The corpus directory and its three structure files exist (INDEX header, entry template, selection-criteria doc)"
      verify:
        - cmd: "test -f get-shit-done/references/validated-examples/INDEX.md && test -f get-shit-done/references/validated-examples/_TEMPLATE.md && test -f get-shit-done/references/validated-examples/SELECTION-CRITERIA.md && echo OK"
          type: unit
          expect: /OK/
    - truth: "The entry template declares the mandatory front-matter fields curation must fill"
      verify:
        - cmd: "grep -q 'pattern_id:' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -q 'counters:' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -q 'license:' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -q 'source_permalink:' get-shit-done/references/validated-examples/_TEMPLATE.md && echo OK"
          type: unit
          expect: /OK/
    - truth: "The template anchors counters: to the exact common-bug-patterns.md section headers (not slug form)"
      verify:
        - cmd: "grep -q 'common-bug-patterns.md' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -qE 'Async / Timing|Error Handling|Data Shape / API Contract' get-shit-done/references/validated-examples/_TEMPLATE.md && echo OK"
          type: unit
          expect: /OK/
    - truth: "INDEX.md is a slim 4-column table header with no code excerpts and no why-its-good prose"
      verify:
        - cmd: "grep -q 'pattern_id' get-shit-done/references/validated-examples/INDEX.md && grep -q '| language | file |' get-shit-done/references/validated-examples/INDEX.md && echo OK"
          type: unit
          expect: /OK/
  artifacts:
    - get-shit-done/references/validated-examples/INDEX.md
    - get-shit-done/references/validated-examples/_TEMPLATE.md
    - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
  key_links:
    - "_TEMPLATE.md counters: vocabulary maps to common-bug-patterns.md section headers (broken = Phase 9 eval join fails)"
---

<objective>
Establish the structural contract for the validated-example corpus: the per-entry file
template (front-matter schema + commentary sections), the slim INDEX.md schema, and a
selection-criteria doc. This is the interface the Wave 2 curation plans author against —
it must be written first so curators copy field names and section headings verbatim
instead of improvising. No real excerpts are curated here; this plan ships only the
empty scaffolding and the rules.

Covers ROADMAP SC1 (explicit selection criteria + pattern-indexed structure).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.5/phases/08-validated-example-corpus/08-CONTEXT.md
@.planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md
@.planning/reference/CODE-EXAMPLES.md
@get-shit-done/references/common-bug-patterns.md
@get-shit-done/references/tdd.md

<interfaces>
The canonical `counters:` vocabulary is the set of EXACT `## ` section-header titles in
get-shit-done/references/common-bug-patterns.md. As of this phase those headers are:

  Null / Undefined Access
  Off-by-One / Boundary
  Async / Timing
  State Management
  Import / Module
  Type / Coercion
  Environment / Config
  Data Shape / API Contract
  Regex / String
  Error Handling
  Scope / Closure
  Python-Specific Bugs

A `counters:` value MUST be one of these strings copied character-for-character. This is
the join key Phase 9 uses to bind an entry to the failure category it counters. Slug-form
values (e.g. `async-timing/missing-await`) are INVALID and break Phase 9 — note RESEARCH.md
Q3's example block uses slug form by mistake; the field-rationale table and CONTEXT.md
override it with the header-string rule. Use header strings.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write the entry-file template and selection-criteria doc</name>
  <read_first>
    - get-shit-done/references/common-bug-patterns.md (copy the EXACT `## ` header strings for the counters vocabulary block)
    - get-shit-done/references/tdd.md (mirror the existing Phase 3 reference-doc front-matter + section shape)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q3 entry format — use field NAMES from it, but counters in HEADER form per the field-rationale table, NOT the slug example block)
    - .planning/reference/CODE-EXAMPLES.md (selection criteria source: reputation-driven, handwritten, production-used, permissive enough to excerpt)
  </read_first>
  <files>get-shit-done/references/validated-examples/_TEMPLATE.md, get-shit-done/references/validated-examples/SELECTION-CRITERIA.md</files>
  <action>
Create the corpus directory and two files.

`_TEMPLATE.md` — the canonical per-entry shape curators copy. Front matter MUST declare these exact fields (with placeholder values + an inline comment per field):
```
--- (YAML front matter start; literal triple-dash in the real file)
pattern_id: <stable-kebab-id>            # stable key; matches INDEX row + filename
title: <human title>
language: <python|typescript|javascript>
source_repo: <https URL of repo>
source_file: <path/within/repo>
source_lines: "<start>-<end>"
source_permalink: <https permalink pinned to a RELEASE TAG, e.g. /blob/v6.19.2/...  NEVER /blob/main/ or HEAD>
license: <SPDX id, e.g. MIT, Apache-2.0, PSF-2.0>
counters:                                 # EXACT ## section-header titles from common-bug-patterns.md — NOT slugs
  - Async / Timing
  - Error Handling
--- (YAML front matter end; literal triple-dash in the real file)
```
NOTE: in the example above, the `--- (...literal triple-dash...)` markers are annotations for this plan only — in the actual _TEMPLATE.md file write BARE `---` delimiter lines (three hyphens, nothing else) around the YAML front matter, exactly like every other reference doc.

Below the front matter, four required commentary sections in this order:
`## What this solves` (one paragraph: the concrete constraint / invariant), `## Excerpt`
(a fenced code block, 10-30 lines, short targeted snippet — not the whole file),
`## Why it's good` (2-4 bullets), `## What NOT to cargo-cult` (2-3 bullets).

Immediately after the front matter include a literal comment block listing the FULL set of
valid `counters:` values (the 12 header strings copied verbatim from common-bug-patterns.md)
and the rule: "Each value MUST be one of these, copied character-for-character. No invented
sub-namespaces. No slug form. This is the Phase 9 join key."

`SELECTION-CRITERIA.md` — the curation rulebook, distilled from CODE-EXAMPLES.md + CONTEXT.md:
- Source bar: reputation-vetted (battle-tested, widely-used, human-maintained); low-usage/unproven repos disqualified.
- "Validated" means: excerpt from a reputation-vetted repo + curator commentary — NOT independently re-benchmarked by us.
- Licensing: short attributed excerpts for commentary/study = quotation, not vendoring. Always attribute precisely incl. license. NEVER paste excerpts into GSD runtime/executable code — they live only in references/ docs.
- Excerpt hygiene: 10-30 lines, minimal snippet showing the pattern without repo scaffolding; permalink pinned to a release tag.
- No synthetic examples: if the pattern can't be found in a reputation-vetted repo, it doesn't belong.
  </action>
  <verify>test -f get-shit-done/references/validated-examples/_TEMPLATE.md && test -f get-shit-done/references/validated-examples/SELECTION-CRITERIA.md && grep -q 'counters:' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -qE 'Async / Timing|Error Handling' get-shit-done/references/validated-examples/_TEMPLATE.md && grep -q 'release tag' get-shit-done/references/validated-examples/_TEMPLATE.md</verify>
  <acceptance_criteria>
    - get-shit-done/references/validated-examples/_TEMPLATE.md exists
    - _TEMPLATE.md front matter contains `pattern_id:`, `title:`, `language:`, `source_repo:`, `source_file:`, `source_lines:`, `source_permalink:`, `license:`, `counters:`
    - _TEMPLATE.md contains all four section headers: `## What this solves`, `## Excerpt`, `## Why it's good`, `## What NOT to cargo-cult`
    - _TEMPLATE.md contains the literal string `common-bug-patterns.md` and at least three exact header strings (`Async / Timing`, `Error Handling`, `Data Shape / API Contract`)
    - _TEMPLATE.md explicitly forbids slug-form and HEAD/main permalinks (grep for `release tag` and a no-slug instruction)
    - SELECTION-CRITERIA.md exists and contains `reputation`, `license`, and a `never paste`-into-runtime instruction
  </acceptance_criteria>
  <done>The template and selection-criteria doc exist; the template's counters vocabulary is locked to common-bug-patterns.md header strings; curators can copy the shape without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Write the slim INDEX.md schema header</name>
  <read_first>
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q2 + Q3 INDEX schema — slim 4-column, no code, no why-good prose)
    - get-shit-done/references/validated-examples/_TEMPLATE.md (just authored — pattern_id is the shared key between INDEX rows and entry files)
  </read_first>
  <files>get-shit-done/references/validated-examples/INDEX.md</files>
  <action>
Create `INDEX.md` with a title, a one-line usage instruction, and a strict 4-column markdown
table HEADER (no data rows yet — rows are populated in Plan 04 once entries exist). Exact shape:
```
# Validated Examples — Index

Read this to find which pattern file to load. Load only the specific file you need — never load all entries.

| pattern_id | constraint (one line) | language | file |
|------------|----------------------|----------|------|
```
Add a one-line comment under the table stating the INDEX must stay slim: only these four columns,
no code excerpts, no "why it's good" prose (those live in the per-pattern entry files). This guards
against the fat-INDEX anti-pattern that would re-create the eager-blob the user prohibited.
  </action>
  <verify>test -f get-shit-done/references/validated-examples/INDEX.md && grep -q '| pattern_id | constraint (one line) | language | file |' get-shit-done/references/validated-examples/INDEX.md</verify>
  <acceptance_criteria>
    - get-shit-done/references/validated-examples/INDEX.md exists
    - INDEX.md contains the exact 4-column header row `| pattern_id | constraint (one line) | language | file |`
    - INDEX.md contains the instruction `Load only the specific file you need` (anti-eager-blob)
    - INDEX.md contains no fenced code block (`grep -c '```' INDEX.md` returns 0)
  </acceptance_criteria>
  <done>INDEX.md exists with the slim 4-column schema header and the load-one-only instruction; data rows are deferred to Plan 04.</done>
</task>
</tasks>

<verification>
- All three structure files exist under get-shit-done/references/validated-examples/
- Template front matter declares every mandatory field; counters vocabulary is in header-string form
- INDEX is slim (4 columns, no code, no prose)
- No real excerpts curated yet (that's Wave 2) — this plan ships scaffolding only
</verification>

<success_criteria>
The corpus directory exists with a complete, unambiguous authoring contract: an entry template
that locks counters: to the canonical common-bug-patterns.md headers, a selection-criteria doc,
and a slim INDEX schema. Wave 2 curators can author entries by copying the template without
re-deriving field names or the counters vocabulary.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/08-validated-example-corpus/08-01-SUMMARY.md`
</output>
