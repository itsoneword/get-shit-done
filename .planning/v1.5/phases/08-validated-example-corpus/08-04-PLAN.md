---
phase: 08-validated-example-corpus
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - get-shit-done/references/validated-examples/INDEX.md
  - agents/gsd-planner.md
  - .claude/get-shit-done/references/validated-examples/INDEX.md
autonomous: true
requirements:
  - SC2
must_haves:
  truths:
    - truth: "INDEX.md lists one row per curated entry file, and every listed file exists"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/*.md; do b=$(basename \"$f\"); case \"$b\" in INDEX.md|_TEMPLATE.md|SELECTION-CRITERIA.md) continue;; esac; grep -q \"validated-examples/$b\" get-shit-done/references/validated-examples/INDEX.md || echo \"NOT-INDEXED: $b\"; done"
          type: unit
        - cmd: "grep -oE 'validated-examples/[a-z0-9-]+\\.md' get-shit-done/references/validated-examples/INDEX.md | sort -u | while read p; do test -f \"get-shit-done/references/$p\" || echo \"DANGLING: $p\"; done"
          type: unit
    - truth: "INDEX stays slim — no code fences leaked into it"
      verify:
        - cmd: "test \"$(grep -c '```' get-shit-done/references/validated-examples/INDEX.md)\" -eq 0 && echo OK"
          type: unit
          expect: /OK/
    - truth: "The planner loads the corpus on-demand via a pointer to INDEX.md inside the code_quality_reference section"
      verify:
        - cmd: "grep -q 'validated-examples/INDEX.md' agents/gsd-planner.md && grep -q 'Load only the pattern' agents/gsd-planner.md && echo OK"
          type: unit
          expect: /OK/
        - cmd: "awk '/<code_quality_reference>/{f=1} f&&/validated-examples\\/INDEX.md/{print \"OK\"; exit} /<\\/code_quality_reference>/{f=0}' agents/gsd-planner.md"
          type: unit
          expect: /OK/
    - truth: "The runtime copy of the corpus is propagated and matches source byte-for-byte"
      verify:
        - cmd: "test -d .claude/get-shit-done/references/validated-examples && test -f .claude/get-shit-done/references/validated-examples/INDEX.md && diff -rq get-shit-done/references/validated-examples .claude/get-shit-done/references/validated-examples && echo OK"
          type: integration
          expect: /OK/
  artifacts:
    - get-shit-done/references/validated-examples/INDEX.md
    - agents/gsd-planner.md
    - .claude/get-shit-done/references/validated-examples/INDEX.md
  key_links:
    - "gsd-planner.md code_quality_reference points to validated-examples/INDEX.md (broken = planner never sees the corpus; SC2 fails)"
    - "source validated-examples maps to the .claude runtime copy (broken = planner reads stale or absent corpus at runtime)"
---

<objective>
Wire the corpus into GSD: populate INDEX.md with one row per curated entry (assembled from the
front matter the Wave 2 plans wrote), add an on-demand pointer in agents/gsd-planner.md so the
planner reads INDEX.md then loads only the relevant pattern, and propagate the source tree to the
gitignored .claude/ runtime copy so the planner reads it at runtime. This is the integration plan
— it owns INDEX.md row population (deferred from Wave 2 to avoid the file-ownership collision that
would have serialized the two parallel curation plans).

Covers ROADMAP SC2 (corpus loaded into a GSD flow through the normal references mechanism).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md
@get-shit-done/references/validated-examples/INDEX.md
@agents/gsd-planner.md

<interfaces>
gsd-planner.md `<code_quality_reference>` section (currently lines 75-80):
```
<code_quality_reference>
When making code-quality judgments, choosing between implementation patterns, or flagging anti-patterns in plan tasks:
Read ~/.claude/get-shit-done/references/universal-anti-patterns.md

Skip if the phase is trivial glue/config with no design decisions.
</code_quality_reference>
```
The pointer is added INSIDE this section, before the "Skip if…" line — additive, mirroring the
existing on-demand tdd.md/anti-patterns pattern. Do not create a new section.

Runtime propagation convention (STATE.md Phase 3/4 decisions): source edits land in
get-shit-done/ and commands/; .claude/ is the gitignored runtime copy. install.js'
copyWithPathReplacement recurses into new subdirectories automatically, but the established
dev-loop convention is to `cp` source→runtime after writing for immediate byte-identity
(verified with `diff -q`). Commit SOURCE only — .claude/ is gitignored.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Populate INDEX rows and add the gsd-planner.md pointer</name>
  <read_first>
    - get-shit-done/references/validated-examples/INDEX.md (the slim 4-column schema header from Plan 01)
    - agents/gsd-planner.md (the exact code_quality_reference section to extend — read lines ~75-80)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q5 — the exact recommended pointer text)
    - all six entry files in get-shit-done/references/validated-examples/ (read each file's front matter to assemble the row: pattern_id, language, file path, and a one-line constraint from `## What this solves`)
  </read_first>
  <files>get-shit-done/references/validated-examples/INDEX.md, agents/gsd-planner.md</files>
  <action>
(1) INDEX rows: for EACH curated entry file (the six from Plans 02+03; skip INDEX/_TEMPLATE/
SELECTION-CRITERIA), append one table row to INDEX.md with: `pattern_id` (from front matter),
a one-line constraint (condense the entry's `## What this solves` to a single clause — no prose
paragraph), `language` (from front matter), and `file` = `validated-examples/<filename>.md`.
Keep it to exactly the four columns. Do NOT copy code or "why it's good" text into the INDEX.

(2) gsd-planner.md pointer: inside the existing `<code_quality_reference>` section, insert the
following block AFTER the universal-anti-patterns line and BEFORE the "Skip if…" line (exact text):
```
For pattern-specific validated examples from battle-tested real-world code:
Read ~/.claude/get-shit-done/references/validated-examples/INDEX.md — then load the specific pattern file(s) relevant to the task.
Load only the pattern(s) you need; never load all entries.
```
Do not alter the universal-anti-patterns line or the Skip line. Do not create a new section.

IMPORTANT — agent-file path-token rule: agents/gsd-planner.md is an AGENT source file. It uses
the `~/.claude/...` token (NOT an absolute path), and install.js rewrites that token to the
runtime absolute path on install (copyWithPathReplacement path substitution). So do NOT `cp`
gsd-planner.md into `.claude/` — agent source and runtime are intentionally NOT byte-identical,
and no `diff -q` is asserted for it. The pointer goes live at runtime via install.js substitution.
Only the corpus reference files are cp'd to runtime (Task 2) — never the agent file.
  </action>
  <verify>grep -q 'validated-examples/INDEX.md' agents/gsd-planner.md && grep -q 'Load only the pattern' agents/gsd-planner.md && for f in get-shit-done/references/validated-examples/*.md; do b=$(basename "$f"); case "$b" in INDEX.md|_TEMPLATE.md|SELECTION-CRITERIA.md) continue;; esac; grep -q "validated-examples/$b" get-shit-done/references/validated-examples/INDEX.md || echo "MISSING ROW: $b"; done</verify>
  <acceptance_criteria>
    - INDEX.md has one row per curated entry file; `grep -c '| validated-examples/' INDEX.md` equals the number of curated entry files (6)
    - Every INDEX file-path references an existing entry file (no dangling rows)
    - INDEX.md contains no fenced code block
    - agents/gsd-planner.md contains `validated-examples/INDEX.md` and `Load only the pattern(s) you need`
    - The pointer is inside `<code_quality_reference>...</code_quality_reference>` (awk window check passes)
    - The original `universal-anti-patterns.md` line and the `Skip if` line are still present in that section
  </acceptance_criteria>
  <done>INDEX.md lists every curated entry as a slim row; gsd-planner.md points the planner to the corpus on-demand inside the existing reference section.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate corpus to the runtime copy and verify byte-identity</name>
  <read_first>
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Pitfall 4 — runtime propagation; install recurses but dev-loop convention is cp + diff -q)
    - get-shit-done/references/validated-examples/INDEX.md (the source being propagated)
  </read_first>
  <files>.claude/get-shit-done/references/validated-examples/INDEX.md</files>
  <action>
Mirror the entire source corpus directory into the gitignored runtime copy so the planner reads
it at runtime. Run:
```
mkdir -p .claude/get-shit-done/references/validated-examples
cp -f get-shit-done/references/validated-examples/*.md .claude/get-shit-done/references/validated-examples/
```
Then verify byte-identity of every file with `diff -rq get-shit-done/references/validated-examples
.claude/get-shit-done/references/validated-examples` — it must report no differences. The runtime
copy is gitignored; do NOT git add it. (install.js would also propagate this on a real install via
copyWithPathReplacement recursion — the cp here gives immediate dev-loop parity, matching the
Phase 3/4 source↔runtime convention.)
  </action>
  <verify>test -d .claude/get-shit-done/references/validated-examples && test -f .claude/get-shit-done/references/validated-examples/INDEX.md && diff -rq get-shit-done/references/validated-examples .claude/get-shit-done/references/validated-examples</verify>
  <acceptance_criteria>
    - .claude/get-shit-done/references/validated-examples/ exists and contains INDEX.md, _TEMPLATE.md, SELECTION-CRITERIA.md, and all six entry files
    - `diff -rq get-shit-done/references/validated-examples .claude/get-shit-done/references/validated-examples` reports no differences
    - The runtime copy is NOT staged for commit (it is gitignored)
  </acceptance_criteria>
  <done>The runtime .claude/ copy of the corpus exists and is byte-identical to source; the planner's ~/.claude pointer resolves at runtime.</done>
</task>
</tasks>

<verification>
- INDEX.md lists every curated entry (one slim row each), no dangling rows, no code fences
- gsd-planner.md points to validated-examples/INDEX.md inside code_quality_reference, on-demand
- Runtime copy exists and is byte-identical to source
- Only source files (get-shit-done/, agents/) committed; .claude/ runtime not staged
</verification>

<success_criteria>
The corpus is fully wired into the GSD flow: the planner reads INDEX.md on-demand through the
normal references mechanism and loads only the pattern it needs, and the runtime copy is in place.
ROADMAP SC2 satisfied.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/08-validated-example-corpus/08-04-SUMMARY.md`
</output>
