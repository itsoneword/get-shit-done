---
phase: 08-validated-example-corpus
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - get-shit-done/references/validated-examples/error-propagation-python.md
  - get-shit-done/references/validated-examples/validation-layer-python.md
  - get-shit-done/references/validated-examples/python-resource-management.md
autonomous: true
requirements:
  - SC1
  - SC3
must_haves:
  truths:
    - truth: "Three Python entry files exist, curated from real reputation-vetted repos"
      verify:
        - cmd: "test -f get-shit-done/references/validated-examples/error-propagation-python.md && test -f get-shit-done/references/validated-examples/validation-layer-python.md && test -f get-shit-done/references/validated-examples/python-resource-management.md && echo OK"
          type: unit
          expect: /OK/
    - truth: "Every Python entry carries the mandatory attribution + Phase-9-join front matter (pattern_id, counters, license, source_permalink)"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/error-propagation-python.md get-shit-done/references/validated-examples/validation-layer-python.md get-shit-done/references/validated-examples/python-resource-management.md; do grep -q 'pattern_id:' \"$f\" && grep -q 'counters:' \"$f\" && grep -q 'license:' \"$f\" && grep -q 'source_permalink:' \"$f\" || echo \"MISSING: $f\"; done"
          type: unit
    - truth: "Every entry has the four commentary sections including what-NOT-to-cargo-cult"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/error-propagation-python.md get-shit-done/references/validated-examples/validation-layer-python.md get-shit-done/references/validated-examples/python-resource-management.md; do grep -q '## What this solves' \"$f\" && grep -q '## Excerpt' \"$f\" && grep -q '## What NOT to cargo-cult' \"$f\" || echo \"MISSING-SECTION: $f\"; done"
          type: unit
    - truth: "Permalinks are pinned to a release tag, not HEAD or the default branch"
      verify:
        - cmd: "for f in get-shit-done/references/validated-examples/error-propagation-python.md get-shit-done/references/validated-examples/validation-layer-python.md get-shit-done/references/validated-examples/python-resource-management.md; do grep 'source_permalink:' \"$f\" | grep -E '/blob/(main|master|HEAD)/' >/dev/null && echo \"UNPINNED: $f\"; done"
          type: unit
  artifacts:
    - get-shit-done/references/validated-examples/error-propagation-python.md
    - get-shit-done/references/validated-examples/validation-layer-python.md
    - get-shit-done/references/validated-examples/python-resource-management.md
  key_links:
    - "each entry counters value maps to a common-bug-patterns.md section header (broken = Phase 9 eval cannot bind entry to failure category)"
---

<objective>
Curate the three Python seed entries from real, reputation-vetted repositories, each
following the _TEMPLATE.md contract from Plan 01: short attributed excerpt + precise
attribution + commentary (what it solves / why good / what NOT to cargo-cult). The
executor MUST read the actual source on GitHub and quote it accurately — do NOT synthesize
code. INDEX rows are NOT written here (Plan 04 owns INDEX.md) — this plan owns only its
three entry files so it runs parallel with Plan 03.

Covers ROADMAP SC1 (real-sourced commentary) and SC3 (counters: join key for Phase 9).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.5/phases/08-validated-example-corpus/08-CONTEXT.md
@.planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md
@.planning/reference/CODE-EXAMPLES.md
@get-shit-done/references/common-bug-patterns.md
@get-shit-done/references/validated-examples/_TEMPLATE.md
@get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Curate error-propagation and Python resource-management entries</name>
  <read_first>
    - get-shit-done/references/validated-examples/_TEMPLATE.md (the exact front-matter fields + section order to fill)
    - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md (source bar + excerpt hygiene + licensing safeguards)
    - get-shit-done/references/common-bug-patterns.md (copy the EXACT `## ` header strings into counters: — `Error Handling`, `Python-Specific Bugs`)
    - .planning/reference/CODE-EXAMPLES.md (candidate Python repos: requests, CPython stdlib)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q4 seed table: requests/adapters.py for error wrapping; CPython contextlib.py for resource mgmt)
  </read_first>
  <files>get-shit-done/references/validated-examples/error-propagation-python.md, get-shit-done/references/validated-examples/python-resource-management.md</files>
  <action>
Author two entry files, each conforming exactly to _TEMPLATE.md.

(1) `error-propagation-python.md` — `pattern_id: error-propagation-python`, `language: python`.
Source: `psf/requests`, the connection-error wrapping in `requests/adapters.py` (the
`HTTPAdapter.send` except blocks that wrap urllib3 exceptions in requests' own exception
types and re-raise — preserving the original cause). LOCATE the actual lines on a pinned
release tag (e.g. v2.32.x), record the real `source_file`, `source_lines`, and a
release-tag `source_permalink`. Excerpt 10-20 lines: the except→wrap→raise chain.
`counters:` MUST include `Error Handling` and `Python-Specific Bugs` (verbatim headers).
Commentary: solves silent error swallowing / loss of original cause; why good = typed
exception hierarchy + `raise ... from`; what NOT to cargo-cult = requests-specific
exception class names, urllib3 coupling.

(2) `python-resource-management.md` — `pattern_id: python-resource-management`, `language: python`.
Source: `python/cpython` stdlib `Lib/contextlib.py` — the `@contextmanager` /
`_GeneratorContextManager` pattern (generator-based context manager with the `try/yield/finally`
cleanup guarantee). LOCATE actual lines on a pinned tag (e.g. v3.12.x), record real
file/lines/permalink. Excerpt 10-20 lines showing the yield + finally cleanup path.
`counters:` MUST include `Python-Specific Bugs` and `Error Handling` (verbatim).
Commentary: solves leaked resources / cleanup-skipped-on-exception; why good = deterministic
finally, single enter/exit contract; what NOT to cargo-cult = CPython-internal helper coupling,
don't reimplement contextlib by hand.

Do NOT write INDEX rows. Do NOT paste excerpts anywhere outside these reference files.
Quote the real source — if you cannot resolve the exact lines, pick the nearest faithful
region on the pinned tag and record its real range.
  </action>
  <verify>for f in error-propagation-python python-resource-management; do test -f "get-shit-done/references/validated-examples/$f.md" && grep -q 'pattern_id:' "get-shit-done/references/validated-examples/$f.md" && grep -q 'counters:' "get-shit-done/references/validated-examples/$f.md" && grep -q '## What NOT to cargo-cult' "get-shit-done/references/validated-examples/$f.md" || echo "FAIL: $f"; done</verify>
  <acceptance_criteria>
    - Both files exist with all template front-matter fields present (`pattern_id`, `language`, `source_repo`, `source_file`, `source_lines`, `source_permalink`, `license`, `counters`)
    - Each `counters:` value matches an exact `## ` header in common-bug-patterns.md (`grep -F "## <value>" get-shit-done/references/common-bug-patterns.md` succeeds for each)
    - Each `source_permalink:` contains `/blob/v` or a tag, not `/blob/main/` or `/blob/master/` or `/HEAD/`
    - Each file contains all four sections: `## What this solves`, `## Excerpt`, `## Why it's good`, `## What NOT to cargo-cult`
    - Each `## Excerpt` contains a fenced code block of roughly 10-30 lines
    - `license:` is a real SPDX id matching the source repo (Apache-2.0 for requests, PSF-2.0 for CPython)
  </acceptance_criteria>
  <done>error-propagation-python.md and python-resource-management.md exist, each with a real attributed excerpt, header-form counters, and full commentary.</done>
</task>

<task type="auto">
  <name>Task 2: Curate the Python validation-layer (Pydantic) entry</name>
  <read_first>
    - get-shit-done/references/validated-examples/_TEMPLATE.md
    - get-shit-done/references/validated-examples/SELECTION-CRITERIA.md
    - get-shit-done/references/common-bug-patterns.md (counters: `Data Shape / API Contract`, `Type / Coercion` — verbatim headers)
    - .planning/v1.5/phases/08-validated-example-corpus/08-RESEARCH.md (Q4: pydantic model_validate + ValidationError shape)
  </read_first>
  <files>get-shit-done/references/validated-examples/validation-layer-python.md</files>
  <action>
Author `validation-layer-python.md` — `pattern_id: validation-layer-python`, `language: python`.
Source: `pydantic/pydantic`. Excerpt the typed-parse → validation-error pattern: a
`model_validate` call path or a small `BaseModel` definition plus the `ValidationError`
raise/shape (the "parse, don't validate" discipline — untrusted input becomes a typed model
or raises a structured error). LOCATE real lines on a pinned release tag (e.g. v2.x),
record real `source_file`, `source_lines`, release-tag `source_permalink`, `license: MIT`.
Excerpt 15-25 lines. `counters:` MUST include `Data Shape / API Contract` and `Type / Coercion`
(verbatim headers). Commentary: solves untyped/unchecked input crossing a boundary; why good =
parse-to-type at the edge, structured error with field paths; what NOT to cargo-cult =
Pydantic-version-specific API (v1 vs v2), don't reach into private internals.

Do NOT write INDEX rows.
  </action>
  <verify>test -f get-shit-done/references/validated-examples/validation-layer-python.md && grep -q 'Data Shape / API Contract' get-shit-done/references/validated-examples/validation-layer-python.md && grep -q '## What NOT to cargo-cult' get-shit-done/references/validated-examples/validation-layer-python.md</verify>
  <acceptance_criteria>
    - validation-layer-python.md exists with all template front-matter fields
    - `counters:` includes `Data Shape / API Contract` and `Type / Coercion`, each matching a `## ` header in common-bug-patterns.md
    - `source_permalink:` is release-tag-pinned (not main/master/HEAD)
    - `license: MIT` present
    - all four commentary sections present; `## Excerpt` has a fenced ~15-25 line code block
  </acceptance_criteria>
  <done>validation-layer-python.md exists with a real Pydantic excerpt, header-form counters, and full commentary.</done>
</task>
</tasks>

<verification>
- Three Python entry files exist, each conforming to _TEMPLATE.md
- Every counters: value is an exact common-bug-patterns.md header (Phase 9 join intact)
- Every permalink is release-tag-pinned; every entry has all four commentary sections
- No INDEX edits in this plan (parallel-safe with Plan 03)
</verification>

<success_criteria>
The three Python seed patterns (error-propagation, validation-layer, resource-management) are
curated from real reputation-vetted repos with short attributed excerpts and full commentary,
ready for Plan 04 to index.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/08-validated-example-corpus/08-02-SUMMARY.md`
</output>
