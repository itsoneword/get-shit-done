---
phase: 07-parallel-multi-session-safety-planning-ergono
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/frontmatter.cjs
  - .claude/get-shit-done/bin/lib/frontmatter.cjs
  - get-shit-done/bin/lib/init.cjs
  - .claude/get-shit-done/bin/lib/init.cjs
  - get-shit-done/workflows/add-todo.md
  - .claude/get-shit-done/workflows/add-todo.md
  - tests/frontmatter.test.cjs
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "A new todo created via add-todo carries depends_on: [] and related_to: [] frontmatter fields"
    - "init todos parses depends_on/related_to off each pending todo and exposes them in the init JSON"
    - "A 'todo' schema exists in FRONTMATTER_SCHEMAS so the gate can validate todo edges"
  artifacts:
    - get-shit-done/bin/lib/frontmatter.cjs
    - get-shit-done/workflows/add-todo.md
  key_links:
    - "init.cjs cmdInitTodos → todo frontmatter depends_on/related_to (broken = gate in 07-05 cannot read quick-task edges)"
---

<objective>
Formalize `depends_on` and `related_to` on todo frontmatter so the parallel-safety gate (Plan 07-05) can reason about coupling between quick tasks, not just phases. This is SC3's substrate and a Wave-1 PRIMITIVE that the gate consumes — it MUST land before/with the gate (the gate reads these fields). Today there is no `todo` schema in `frontmatter.cjs` (only plan/summary/verification), so this is a clean additive schema.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md

<interfaces>
Source↔runtime mirror rule: every edit to `get-shit-done/` must also land byte-identically in `.claude/get-shit-done/` (write source, then `cp` to runtime, assert `diff -q`). Only source is committed.

FRONTMATTER_SCHEMAS (frontmatter.cjs ~L265): object keyed `plan`/`summary`/`verification`, each `{ required: [...] }`. Add a `todo` key.

cmdInitTodos (init.cjs ~L586): reads each pending todo .md, regex-extracts `created`/`title`/`area` via `content.match(/^created:\s*(.+)$/m)` etc., pushes `{file, created, title, area, path}` into `todos[]`. Extend the parse + the pushed object.

add-todo.md frontmatter template block (~L90–108): YAML written into new todo files. Add the two new fields with empty-array defaults.

Field-value convention (from RESEARCH §Scope 3): values are todo slugs (`260507-u0a`), phase refs (`phase:7`), or free text — parse loosely as a YAML list; default `[]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add todo schema to frontmatter.cjs + parse cases (RED first)</name>
  <read_first>
    - get-shit-done/bin/lib/frontmatter.cjs (FRONTMATTER_SCHEMAS ~L265, extractFrontmatter, the list-parsing logic)
    - tests/frontmatter.test.cjs (existing test structure/helpers to mirror)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md (§Scope Item 3 — schema + Validation Architecture SC3 cases)
  </read_first>
  <action>
    TDD: add failing tests to `tests/frontmatter.test.cjs` FIRST, watch fail, then implement.

    Tests to add:
    - A todo-shaped frontmatter string with `depends_on: [phase:6]` and `related_to: [260507-u0a]` parses via `extractFrontmatter` into arrays containing those values.
    - The `todo` key exists in FRONTMATTER_SCHEMAS with `depends_on` and `related_to` listed (assert via requiring the module or via a `frontmatter validate ... --schema todo` CLI call that does not error on a valid todo).
    - A todo with NO depends_on/related_to still validates (fields are optional / default empty).

    Then in `get-shit-done/bin/lib/frontmatter.cjs`: add to FRONTMATTER_SCHEMAS a `todo` entry. Todos have always carried `created`/`title`/`area`/`files`; make `required: ['created', 'title', 'area']` and treat `depends_on`/`related_to`/`files` as optional (so existing todos without the new fields still validate). Ensure `extractFrontmatter` returns `depends_on`/`related_to` as arrays when present.

    MIRROR: `cp get-shit-done/bin/lib/frontmatter.cjs .claude/get-shit-done/bin/lib/frontmatter.cjs`.
  </action>
  <verify>node --test tests/frontmatter.test.cjs 2>&1 | tail -5 && diff -q get-shit-done/bin/lib/frontmatter.cjs .claude/get-shit-done/bin/lib/frontmatter.cjs</verify>
  <acceptance_criteria>
    - `node --test tests/frontmatter.test.cjs` exits 0, `# fail 0`
    - `grep -E "todo:\s*\{" get-shit-done/bin/lib/frontmatter.cjs` returns 0 (todo schema key present)
    - `grep -A2 "todo:" get-shit-done/bin/lib/frontmatter.cjs | grep -E "depends_on|created"` shows the schema fields
    - New tests reference `depends_on` and `related_to`
    - `diff -q get-shit-done/bin/lib/frontmatter.cjs .claude/get-shit-done/bin/lib/frontmatter.cjs` passes
  </acceptance_criteria>
  <done>FRONTMATTER_SCHEMAS has a todo schema with depends_on/related_to; parsing returns them as arrays; both copies identical; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Parse depends_on/related_to in init.cjs cmdInitTodos + add fields to add-todo.md template</name>
  <read_first>
    - get-shit-done/bin/lib/init.cjs (cmdInitTodos L586–640 — the parse loop and pushed todo object)
    - get-shit-done/workflows/add-todo.md (frontmatter template block ~L90–108)
    - tests/init.test.cjs (if it covers cmdInitTodos, mirror its assertions)
  </read_first>
  <action>
    In `get-shit-done/bin/lib/init.cjs` `cmdInitTodos`: after the existing `areaMatch` extraction, add parsing for the two new fields. Since they are YAML lists, extract the block and split into an array; default `[]` when absent. Concretely, add helper inline:
    parse `depends_on` and `related_to` by matching the field then collecting subsequent `  - ` lines OR an inline `[...]` form. Push them into the todo object: change the pushed object to include `depends_on: <parsedArray>` and `related_to: <parsedArray>`. Keep existing fields unchanged.

    In `get-shit-done/workflows/add-todo.md` frontmatter template (~L90–108), add the two fields to the YAML written for new todos, with empty-array defaults and a one-line comment each:
    ```
    depends_on: []        # todo slugs or phase refs (phase:7) this must follow
    related_to: []        # todo slugs or phase refs that provide context
    ```

    MIRROR both: `cp get-shit-done/bin/lib/init.cjs .claude/get-shit-done/bin/lib/init.cjs` and `cp get-shit-done/workflows/add-todo.md .claude/get-shit-done/workflows/add-todo.md`.
  </action>
  <verify>node -e "const i=require('./get-shit-done/bin/lib/init.cjs'); console.log(typeof i.cmdInitTodos)" 2>/dev/null; grep -c "depends_on" get-shit-done/workflows/add-todo.md; diff -q get-shit-done/bin/lib/init.cjs .claude/get-shit-done/bin/lib/init.cjs && diff -q get-shit-done/workflows/add-todo.md .claude/get-shit-done/workflows/add-todo.md</verify>
  <acceptance_criteria>
    - `grep -q "depends_on" get-shit-done/bin/lib/init.cjs` returns 0 AND it is inside the cmdInitTodos pushed-object region (grep -n shows it between L586 and L645)
    - `grep -q "related_to" get-shit-done/bin/lib/init.cjs` returns 0
    - `grep -q "depends_on: \[\]" get-shit-done/workflows/add-todo.md` returns 0
    - `grep -q "related_to: \[\]" get-shit-done/workflows/add-todo.md` returns 0
    - `diff -q get-shit-done/bin/lib/init.cjs .claude/get-shit-done/bin/lib/init.cjs` passes
    - `diff -q get-shit-done/workflows/add-todo.md .claude/get-shit-done/workflows/add-todo.md` passes
    - Creating a temp pending todo with depends_on and running `init todos --raw` returns the field in its JSON (verify with a one-off node invocation against a temp .planning/todos/pending fixture, or extend init.test.cjs)
  </acceptance_criteria>
  <done>New todos get depends_on/related_to defaults; init todos surfaces them in JSON; both source+runtime copies identical for all three files.</done>
</task>

</tasks>

<verification>
- `node --test tests/frontmatter.test.cjs` green
- `diff -q` passes for frontmatter.cjs, init.cjs, add-todo.md (source↔runtime)
- A pending todo with `depends_on: [phase:6]` is surfaced by `init todos --raw` JSON
</verification>

<success_criteria>
SC3 substrate complete: todos carry `depends_on`/`related_to`, a `todo` frontmatter schema validates them, and `init todos` exposes the edges as JSON for the gate (Plan 07-05) to read.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-02-SUMMARY.md`
</output>
