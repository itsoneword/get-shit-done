---
phase: 07-parallel-multi-session-safety-planning-ergono
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/worktree.cjs
  - .claude/get-shit-done/bin/lib/worktree.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - .claude/get-shit-done/bin/gsd-tools.cjs
  - tests/worktree.test.cjs
  - tests/helpers.cjs
  - .gitignore
autonomous: true
requirements: [SC1]
must_haves:
  truths:
    - "git worktree add succeeds in this environment (Wave 0 smoke check passes)"
    - "A clean merge of two disjoint worktree branches auto-completes (returns clean:true)"
    - "A conflicting merge leaves a reviewable unmerged state and returns clean:false — the base file is NOT silently overwritten"
    - "Worktree dirs are created under .worktrees/ which is gitignored"
  artifacts:
    - get-shit-done/bin/lib/worktree.cjs
    - tests/worktree.test.cjs
  key_links:
    - "gsd-tools.cjs router `worktree` case → worktree.cjs cmd functions (broken = execute-phase cannot orchestrate isolation)"
    - "worktree merge → per-merge git exit-code check (broken = N-way merge silently overwrites)"
---

<objective>
Build the `gsd-tools worktree` CLI primitive that execute-phase (Plan 07-06) will call to isolate each parallel executor in its own linked git worktree and merge it back into the phase branch. This is the deterministic, CLI-resident isolation that the locked decision requires (NEVER agent prose). It consumes the Phase 6 git-worktree technique reference — it does NOT re-derive detection/fallback logic. Implements SC1's substrate: conflicts surface as reviewable merges instead of silent overwrites.

This plan builds ONLY the CLI helper + its tests. The execute-phase orchestration that calls it is Plan 07-06 (Wave 3). Phase 7's own plans run on the shared tree (we do not dogfood unbuilt isolation on itself).
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md
@get-shit-done/references/git-worktree.md

<interfaces>
Source↔runtime mirror rule (CRITICAL): every edit to `get-shit-done/` must also land byte-identically in `.claude/get-shit-done/`. Pattern from Phase 3/4: write source, then `cp` to runtime so `diff -q` passes. Only source is committed (runtime gitignored).

Router pattern (gsd-tools.cjs): lib modules are required at top (`const verify = require('./lib/verify.cjs')`), dispatched via `switch` `case` blocks (see `case 'validate'` ~L568). Add a NEW append-only `case 'worktree'` after an existing case; this is parallel-safe with other Wave-1 router additions (distinct label, additive — Phase 04/05 precedent).

Test helpers (tests/helpers.cjs): exports `runGsdTools(args, cwd)`, `createTempProject`, `cleanup`. There is NO `createTempGitRepo` helper yet — add one.

git merge exit-code semantics: exit 0 = clean merge; non-zero = conflict (leaves unmerged paths in the tree — do NOT `git merge --abort`, leave for human review).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 0): Smoke-test git worktree + prove writes-land-in-worktree + scaffold createTempGitRepo helper</name>
  <read_first>
    - get-shit-done/references/git-worktree.md (technique reference — Step 1b dir priority, ignore-check, sandbox fallback)
    - tests/helpers.cjs (existing exports: runGsdTools, createTempProject, cleanup — match their style)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    First, run a one-off smoke test to confirm `git worktree add` works in this environment (RESEARCH Open Question 3): in a temp dir, `git init`, make an initial commit, then `git worktree add <tmp>/wt -b smoke HEAD` and assert exit 0, then `git worktree remove <tmp>/wt`. If it fails with a sandbox/permission error, STOP and report — the rest of the phase's worktree path is unusable and must fall back to in-place.

    Then add a `createTempGitRepo()` helper to `tests/helpers.cjs` and export it. It must: create a unique temp dir (use `fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-wt-'))`), run `git init -q`, set `git config user.email`/`user.name` (commits need identity), write a seed file, `git add -A && git commit -q -m "init"`, and return `{ dir, cleanup }` where `cleanup` does `fs.rmSync(dir, { recursive: true, force: true })`. Mirror to `.claude/` is NOT required for test files (tests/ is not under get-shit-done/), so edit ONLY `tests/helpers.cjs`.

    THEN prove the LOAD-BEARING SC1 mechanism (advisor-flagged, the #1 thing execution must validate first): that a worktree's working dir is genuinely separate on disk so writes targeting it land in the worktree branch and NOT the main tree. In a temp git repo, `git worktree add <tmp>/wt -b probe HEAD`, then write a file via an ABSOLUTE path INTO <tmp>/wt (e.g. <tmp>/wt/probe.txt), `git -C <tmp>/wt add -A && git -C <tmp>/wt commit`, and assert: (a) the file exists on the `probe` branch, (b) the file does NOT exist on the main branch's HEAD tree (`git -C <maindir> show HEAD:probe.txt` fails). This confirms on-disk isolation is real. CRITICAL CAVEAT to record in the SUMMARY: this proves the DIRECTORY isolates; it does NOT prove a Task-spawned gsd-executor will target the worktree dir — executors use absolute-path tools and this environment resets subagent cwd between bash calls, so an executor writing to a REPO-ROOT absolute path (get-shit-done/...) lands in the MAIN tree, defeating isolation. If on-disk isolation works but executor-targeting cannot be made deterministic, the honest mechanism is the in-place fallback + human checkpoint (07-06 Task 3) — record this finding explicitly so 07-06 can rely on it.

    Add `tests/worktree.test.cjs` with one Wave-0 RED-acceptable smoke test: `test('git worktree add/remove works in this environment', ...)` that uses `createTempGitRepo()` and runs raw `git worktree add`/`remove`, asserting success. This test must PASS (it validates the environment, not unbuilt code).
  </action>
  <verify>node --test tests/worktree.test.cjs 2>&1 | grep -E "pass 1|# pass" </verify>
  <acceptance_criteria>
    - `grep -q "function createTempGitRepo" tests/helpers.cjs` returns 0
    - `grep -q "createTempGitRepo" tests/helpers.cjs` appears in the module.exports block (grep the exports object)
    - `tests/worktree.test.cjs` exists and contains `require('./helpers.cjs')` and `createTempGitRepo`
    - `node --test tests/worktree.test.cjs` reports the smoke test as passing (exit 0, `# fail 0`)
    - The smoke test actually invokes `git worktree add` (grep the test file for `worktree add`)
    - A worktree-write-isolation probe is present and PASSES: a file written into the worktree dir exists on the worktree branch but NOT on the main HEAD tree (test asserts `git show HEAD:<file>` fails on the main repo)
    - The SUMMARY records the executor-targeting caveat (absolute-path writes to repo-root land in main tree) and whether executor-into-worktree can be made deterministic or must rely on in-place fallback + checkpoint
  </acceptance_criteria>
  <done>git worktree confirmed working; on-disk write-isolation proven (file lands in worktree branch, not main tree); executor-targeting caveat recorded for 07-06; createTempGitRepo helper exported; test file green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement worktree.cjs lifecycle helper + RED tests</name>
  <read_first>
    - get-shit-done/references/git-worktree.md (Step 0 detect-existing, Step 1b dir priority, ignore-check, sandbox fallback — CONSUME, do not re-derive)
    - get-shit-done/bin/lib/verify.cjs (study output() / JSON-shape conventions used by sibling lib modules)
    - get-shit-done/bin/lib/core.cjs (shared helpers: output, error)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md (§Scope Item 1 — proposed API + Pitfall 2 per-merge clean check + merge-back code example)
  </read_first>
  <action>
    TDD: write the failing tests in `tests/worktree.test.cjs` FIRST, watch them fail, then implement.

    Tests to add (using `createTempGitRepo()`):
    - `worktree add` creates a linked worktree dir under `.worktrees/` and a branch — assert dir exists, `git worktree list` shows it.
    - `worktree merge` of two branches editing DISJOINT files → returns JSON `{clean:true}` and both files present after merge.
    - `worktree merge` of two branches editing the SAME line differently → returns JSON `{clean:false, conflict_files:[...]}`; assert `git status` shows unmerged paths AND the base file content is NOT equal to either branch's version alone (proves no silent overwrite). Per Pitfall 2: each `git merge` exit code is checked individually.
    - `worktree remove` removes the dir and deletes the branch.

    Then create `get-shit-done/bin/lib/worktree.cjs` exporting four functions matching the RESEARCH API:
    - `cmdWorktreeAdd(cwd, dir, branch, opts)` — runs Step 0 detect-existing (skip if already in linked worktree per git-worktree.md), runs ignore-check (`git check-ignore -q .worktrees`; if dir is project-local and not ignored, add `.worktrees/` to `.gitignore` and commit), then `git worktree add <dir> -b <branch> <base||HEAD>`. On sandbox/permission failure emit JSON `{ok:false, fallback:"in-place", reason:...}` (do NOT throw). Default dir `.worktrees/<branch-slug>`.
    - `cmdWorktreeMerge(cwd, branch, opts)` — `git merge <branch> --no-ff` into current/target branch; check exit code PER CALL; emit `{clean:bool, conflict_files:[]}` (parse `git diff --name-only --diff-filter=U` on conflict). NEVER `git merge --abort` on conflict.
    - `cmdWorktreeRemove(cwd, dir, opts)` — `git worktree remove <dir> [--force]` then `git branch -d/-D <branch>` (force on failure path).
    - `cmdWorktreePrune(cwd)` — `git worktree prune`.
    All emit JSON via the project's `output()` helper, unconditionally parseable (follow the Phase 04 cmdVerifyCommands precedent: always emit JSON regardless of --raw).

    Wire a new `case 'worktree'` into `get-shit-done/bin/gsd-tools.cjs` (require `worktree.cjs` at top with the other requires; add the case after the existing `case 'validate'` block) dispatching subcommands `add|merge|remove|prune`. Update the usage/help comment block listing subcommands.

    MIRROR: `cp get-shit-done/bin/lib/worktree.cjs .claude/get-shit-done/bin/lib/worktree.cjs` and `cp get-shit-done/bin/gsd-tools.cjs .claude/get-shit-done/bin/gsd-tools.cjs` after editing source.
  </action>
  <verify>node --test tests/worktree.test.cjs 2>&1 | tail -5 && diff -q get-shit-done/bin/lib/worktree.cjs .claude/get-shit-done/bin/lib/worktree.cjs && diff -q get-shit-done/bin/gsd-tools.cjs .claude/get-shit-done/bin/gsd-tools.cjs</verify>
  <acceptance_criteria>
    - `node --test tests/worktree.test.cjs` exits 0 with `# fail 0` and at least 4 passing tests
    - `grep -E "cmdWorktreeAdd|cmdWorktreeMerge|cmdWorktreeRemove|cmdWorktreePrune" get-shit-done/bin/lib/worktree.cjs` returns all four
    - `grep -q "case 'worktree'" get-shit-done/bin/gsd-tools.cjs` returns 0
    - `grep -q "conflict_files" get-shit-done/bin/lib/worktree.cjs` returns 0 (merge returns conflict file list)
    - `grep -q "merge --abort" get-shit-done/bin/lib/worktree.cjs` returns NON-zero (we must NOT abort on conflict)
    - `diff -q get-shit-done/bin/lib/worktree.cjs .claude/get-shit-done/bin/lib/worktree.cjs` passes (both copies identical)
    - `diff -q get-shit-done/bin/gsd-tools.cjs .claude/get-shit-done/bin/gsd-tools.cjs` passes
    - The conflict test asserts the base file is not silently overwritten to a single branch's version
  </acceptance_criteria>
  <done>`gsd-tools worktree add|merge|remove|prune` works, per-merge clean detection returns reviewable conflict state, both source and runtime copies identical, all tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ensure .worktrees/ is gitignored</name>
  <read_first>
    - .gitignore (current contents — check if .worktrees/ or .claude/get-shit-done already listed)
    - get-shit-done/references/git-worktree.md (Safety: Ignore Check section)
  </read_first>
  <action>
    Read `.gitignore`. If it does not already contain a `.worktrees/` entry, append `.worktrees/` on its own line. This is the project-root .gitignore (single copy — NOT a source↔runtime mirror file). Do this idempotently: only add if absent.
  </action>
  <verify>git check-ignore -q .worktrees && echo IGNORED</verify>
  <acceptance_criteria>
    - `grep -q "^\.worktrees/" .gitignore` returns 0
    - `git check-ignore -q .worktrees` exits 0 (path is ignored)
  </acceptance_criteria>
  <done>.worktrees/ is gitignored so worktree dirs never get committed.</done>
</task>

</tasks>

<verification>
- `node --test tests/worktree.test.cjs` green (all worktree lifecycle + conflict-detection tests pass)
- `node .claude/get-shit-done/bin/gsd-tools.cjs worktree prune` runs without error (smoke the runtime copy)
- `diff -q` passes for both worktree.cjs and gsd-tools.cjs (source↔runtime identical)
- `git check-ignore -q .worktrees` passes
</verification>

<success_criteria>
SC1 substrate complete: a deterministic CLI helper isolates work in a linked worktree and merges it back with per-merge clean detection, so a conflicting merge surfaces a reviewable unmerged state rather than a silent overwrite. (Full SC1 wiring lands in Plan 07-06.)
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-01-SUMMARY.md`
</output>
