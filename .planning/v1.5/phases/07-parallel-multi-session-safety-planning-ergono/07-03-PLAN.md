---
phase: 07-parallel-multi-session-safety-planning-ergono
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/verify.cjs
  - .claude/get-shit-done/bin/lib/verify.cjs
  - get-shit-done/workflows/health.md
  - .claude/get-shit-done/workflows/health.md
  - tests/verify-health.test.cjs
autonomous: true
requirements: [SC4]
must_haves:
  truths:
    - "/gsd2:health (validate health) reports a source↔runtime drift error when a get-shit-done file differs from its .claude/get-shit-done counterpart"
    - "The same single validate health invocation also reports settings.json hook/statusLine registration drift (an expected hook or statusLine entry missing/changed in .claude/settings.json)"
    - "The symmetry check does NOT report agents/ files as drift (PATH-TOKEN rule excludes them)"
    - "validate health --repair re-syncs source→runtime (excluding agents/); re-syncable settings.json parity drift is repaired too"
    - "The same check function is exported so execute-phase (07-06) can call it as a post-merge step"
  artifacts:
    - get-shit-done/bin/lib/verify.cjs
    - get-shit-done/workflows/health.md
  key_links:
    - "cmdValidateHealth → checkSourceRuntimeSymmetry (broken = drift goes unreported)"
    - "checkSourceRuntimeSymmetry → settings.json hook/statusLine parity sub-check (broken = a registered hook/statusLine can silently drift out of .claude/settings.json)"
    - "symmetry check → agents/ exclusion (broken = every run falsely reports drift)"
---

<objective>
Fold a source↔runtime symmetry-check into `/gsd2:health` (`validate health`, with `--repair`) so a single invocation reports drift between `get-shit-done/` source and `.claude/get-shit-done/` runtime. Per the locked CONTEXT "Doctor symmetry-check shape" decision, this check has TWO named parts surfaced in the SAME `validate health` run: (1) the `get-shit-done/` ↔ `.claude/get-shit-done/` file-tree diff, and (2) settings.json hook/statusLine registration parity (every hook name / statusLine entry that should be present in `.claude/settings.json` is present; drift is flagged). Export the check function so execute-phase (Plan 07-06) can reuse it as a post-merge drift step. NO new command — this absorbs ex-backlog-999.1. Implements SC4. Per the PATH-TOKEN rule, `agents/` files legitimately differ (source uses `~/.claude/` token, runtime uses absolute path) and MUST be excluded from the file-tree diff.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md

<interfaces>
Source↔runtime mirror rule: every edit to `get-shit-done/` lands byte-identically in `.claude/get-shit-done/` (write source, cp to runtime, assert diff -q). Only source committed.

cmdValidateHealth (verify.cjs L764): builds `errors`/`warnings`/`info`/`repairs` arrays via `addIssue(severity, code, message, fix, repairable)`, runs numbered Check blocks (Check 1, Check 2...), then outputs `{status, errors, warnings, info, repairable_count}`. `options.repair` triggers repair actions. New check inserts as another Check block; new repair pushes to `repairs`.

Router (gsd-tools.cjs ~L568): `case 'validate'` → `validate health [--repair]` → `cmdValidateHealth(cwd, {repair}, raw)`. No router change needed (check folds into existing function). Optionally accept `--symmetry-only` if 07-06 wants a fast path — add by reading `args.includes('--symmetry-only')` in the validate case and passing through options; OPTIONAL, flag if added.

PATH-TOKEN RULE (STATE.md Phase 3): source agent files use `~/.claude/`, runtime uses absolute path — `diff -q` legitimately differs for `agents/`. Exclude `agents/` paths from the file-tree symmetry diff.

settings.json parity (CONTEXT "Doctor symmetry-check shape" + ASSUMPTIONS A2): the locked decision is `diff -rq get-shit-done .claude/get-shit-done` PLUS settings.json hook/statusLine registration parity. The parity sub-check reads `.claude/settings.json` and asserts the expected hook registrations (the `hooks` object entries the framework installs) and statusLine entry are present; missing/changed → flag as drift in the SAME `validate health` output. Treat the currently-installed-framework set as the expectation source (whatever the framework's install/sync writes — do NOT invent new hooks here). This is the file-tree diff's sibling concern, not a separate command.

Diff scope (ASSUMPTION, see 07-ASSUMPTIONS A2): `get-shit-done/` ↔ `.claude/get-shit-done/` for the file-tree half, plus `.claude/settings.json` hook/statusLine parity for the settings half. NOT `commands/`. Per CONTEXT decision + RESEARCH Q1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checkSourceRuntimeSymmetry (file-tree diff + settings.json parity) to verify.cjs + RED tests</name>
  <read_first>
    - get-shit-done/bin/lib/verify.cjs (cmdValidateHealth L764+ — Check block structure, addIssue, repairs array, exports block ~L1092)
    - tests/verify-health.test.cjs (existing test scaffolding: createTempProject, writeMinimal* helpers, the 8-check coverage style)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md (§Scope Item 4 + Pitfall 3 + Validation SC4 cases)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md (Doctor symmetry-check shape — the file-tree diff AND settings.json parity are BOTH the locked check)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-ASSUMPTIONS.md (A2 — settings.json parity is in-scope, commands/ is out)
  </read_first>
  <action>
    TDD: add failing tests to `tests/verify-health.test.cjs` FIRST, watch fail, then implement.

    Tests to add (build a temp project with both `get-shit-done/` and `.claude/get-shit-done/` subtrees AND a `.claude/settings.json`):
    - Identical trees + settings.json with all expected hook/statusLine entries → no symmetry error.
    - `get-shit-done/foo.cjs` present but `.claude/get-shit-done/foo.cjs` missing OR differing content → `validate health` returns an error whose message/code identifies source↔runtime drift (assert on a stable code like `E-DRIFT` or message substring `source-runtime` — pick one and use it consistently).
    - An `agents/` file differing between the two trees → NO drift error (PATH-TOKEN exclusion).
    - settings.json parity: a `.claude/settings.json` MISSING an expected hook registration (or its statusLine entry) → `validate health` reports a settings parity drift error in the SAME invocation (assert on a stable code/substring like `settings`/`E-SETTINGS-DRIFT`). A settings.json with all expected entries → no parity error.
    - `validate health --repair` on a drifted tree → after repair, the runtime file matches source AND a repair entry was recorded; agents/ files are NOT touched by repair; a re-syncable settings.json parity drift is repaired (or, if a given parity item is not safely re-syncable, it is reported but left for the human — document which in the check).

    Then implement `checkSourceRuntimeSymmetry(cwd)` in `get-shit-done/bin/lib/verify.cjs` with TWO named sub-checks in ONE function:
    - SUB-CHECK A (file-tree): compare `get-shit-done/` vs `.claude/get-shit-done/` recursively. Use a deterministic file-walk (node fs, not shelling to `diff` — keeps it testable and avoids agent-prose). For each file under source (excluding any path containing `/agents/` or starting `agents/`), check the runtime counterpart exists and has identical bytes. Collect mismatches.
    - SUB-CHECK B (settings.json parity): read `.claude/settings.json`; assert every expected hook registration (the framework-installed `hooks` entries) and the `statusLine` entry are present. Collect any missing/changed registration as a settings-parity mismatch. Reference `settings.json` and the `hooks`/`statusLine` keys explicitly in the code so the parity intent is grep-verifiable. Source the expectation from whatever the framework install/sync already writes — do not invent new hooks.
    - Return BOTH mismatch sets in the same shape as other checks (so the caller pushes `addIssue('error', '<code>', 'source↔runtime drift: <file>' | 'settings.json parity drift: <hook/statusLine>', 'run /gsd2:health --repair', <repairable>)` per mismatch, and a repair action that `cp`s source→runtime for file mismatches and re-syncs re-syncable settings entries).
    - Skip cleanly (info, not error) if `.claude/get-shit-done/` does not exist (e.g., source checkout without install). Likewise skip the settings sub-check cleanly (info) if `.claude/settings.json` is absent.
    - Call it as a new Check block inside `cmdValidateHealth` (insert before the final output()) — BOTH sub-checks surface through this one Check block / one `validate health` invocation.
    - Repair: when `options.repair`, copy each drifted source file over its runtime counterpart (still excluding agents/), and re-sync re-syncable settings.json parity drift.
    - EXPORT `checkSourceRuntimeSymmetry` from the module (add to the exports object ~L1092) so Plan 07-06 can require and call it directly.

    MIRROR: `cp get-shit-done/bin/lib/verify.cjs .claude/get-shit-done/bin/lib/verify.cjs`.
  </action>
  <verify>node --test tests/verify-health.test.cjs 2>&1 | tail -5 && diff -q get-shit-done/bin/lib/verify.cjs .claude/get-shit-done/bin/lib/verify.cjs</verify>
  <acceptance_criteria>
    - `node --test tests/verify-health.test.cjs` exits 0, `# fail 0`
    - `grep -q "function checkSourceRuntimeSymmetry" get-shit-done/bin/lib/verify.cjs` returns 0
    - `grep -q "checkSourceRuntimeSymmetry" get-shit-done/bin/lib/verify.cjs` appears in the module.exports object
    - The symmetry code excludes agents/: `grep -E "agents" get-shit-done/bin/lib/verify.cjs` shows an exclusion in the symmetry function
    - The settings.json parity sub-check exists: `grep -E "settings\.json" get-shit-done/bin/lib/verify.cjs` AND `grep -E "hooks|statusLine" get-shit-done/bin/lib/verify.cjs` both appear inside/around the symmetry function (the parity sub-check references settings.json and the hooks/statusLine keys)
    - A test asserts agents/ drift is NOT reported
    - A test asserts a non-agent file drift IS reported with a stable code/substring
    - A test asserts a missing settings.json hook/statusLine registration IS reported in the same `validate health` invocation
    - `diff -q get-shit-done/bin/lib/verify.cjs .claude/get-shit-done/bin/lib/verify.cjs` passes
  </acceptance_criteria>
  <done>validate health reports BOTH source↔runtime file-tree drift (excluding agents/) AND settings.json hook/statusLine parity drift in one invocation, --repair re-syncs the re-syncable drift, the check is exported for reuse, both copies identical, tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document symmetry-check (file-tree + settings.json parity) in health.md workflow</name>
  <read_first>
    - get-shit-done/workflows/health.md (current structure — where checks are described, how --repair is documented)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md (Doctor symmetry-check shape decision — file-tree diff + settings.json parity, no /gsd2:doctor name)
  </read_first>
  <action>
    In `get-shit-done/workflows/health.md`, add a short section documenting the new check. Document BOTH parts of the locked decision: (1) it compares `get-shit-done/` source vs `.claude/get-shit-done/` runtime and reports any file-tree drift as an error (excluding `agents/` per the PATH-TOKEN rule), and (2) it checks settings.json hook/statusLine registration parity in the SAME invocation. Note both are repaired with `--repair` where re-syncable. Explicitly note this is the structural symmetry-check folded in here (NOT a new `/gsd2:doctor` command — that name stays reserved for the future semantic stale-decision healer). Keep it concise and consistent with the existing check descriptions' tone.

    MIRROR: `cp get-shit-done/workflows/health.md .claude/get-shit-done/workflows/health.md`.
  </action>
  <verify>grep -iE "source.?runtime|symmetry|drift" get-shit-done/workflows/health.md && grep -iE "settings\.json|hook|statusline" get-shit-done/workflows/health.md && diff -q get-shit-done/workflows/health.md .claude/get-shit-done/workflows/health.md</verify>
  <acceptance_criteria>
    - `grep -iE "symmetry|source.?runtime drift" get-shit-done/workflows/health.md` returns 0
    - `grep -iE "settings\.json" get-shit-done/workflows/health.md` returns 0 (settings parity documented)
    - `grep -i "agents" get-shit-done/workflows/health.md` shows the PATH-TOKEN exclusion is documented
    - `grep -i "doctor" get-shit-done/workflows/health.md` either absent OR explicitly states the name is reserved (no claim of a new /gsd2:doctor command)
    - `diff -q get-shit-done/workflows/health.md .claude/get-shit-done/workflows/health.md` passes
  </acceptance_criteria>
  <done>health.md documents the symmetry-check's two parts (file-tree diff + settings.json parity), its agents/ exclusion, and --repair; both copies identical.</done>
</task>

</tasks>

<verification>
- `node --test tests/verify-health.test.cjs` green
- `node .claude/get-shit-done/bin/gsd-tools.cjs validate health` runs and (on a synced tree) reports no drift
- `diff -q` passes for verify.cjs and health.md
</verification>

<success_criteria>
SC4 complete: `/gsd2:health` reports source↔runtime file-tree drift AND settings.json hook/statusLine parity drift in one invocation, excludes agents/ correctly, repairs the re-syncable drift with --repair, and exports the check for execute-phase post-merge reuse (07-06).
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-03-SUMMARY.md`
</output>
