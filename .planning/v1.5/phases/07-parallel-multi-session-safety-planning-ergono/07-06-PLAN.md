---
phase: 07-parallel-multi-session-safety-planning-ergono
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-03", "07-05"]
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - .claude/get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/quick.md
  - .claude/get-shit-done/workflows/quick.md
  - get-shit-done/workflows/discuss-phase.md
  - .claude/get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/plan-phase.md
  - .claude/get-shit-done/workflows/plan-phase.md
autonomous: false
requirements: [SC1, SC2, SC4]
user_setup: []
must_haves:
  truths:
    - "execute-phase isolates each parallel executor in a worktree via gsd-tools worktree add, then merges back per-merge clean-check after the wave"
    - "execute-phase runs the source↔runtime symmetry-check as a post-merge step"
    - "execute-phase refuses to start when an axis-B-coupled DIFFERENT phase is concurrently executing; warns on axis-A overlap"
    - "/gsd2:quick auto-creates a worktree when it detects a concurrent phase running, falls back in-place otherwise"
    - "discuss-phase and plan-phase HARD-refuse running on a phase that is depends_on-coupled to a concurrently-discussed/planned phase"
  artifacts:
    - get-shit-done/workflows/execute-phase.md
    - get-shit-done/workflows/quick.md
  key_links:
    - "execute_waves → gsd-tools worktree add/merge (broken = no isolation, silent overwrites return)"
    - "worktree merge → validate health symmetry post-merge (broken = drift after merge unreported)"
    - "discuss/plan/execute start → gsd-tools parallel-safe (broken = dependent phases run in parallel, axis-B violated)"
---

<objective>
Wire the CLI primitives (built in Waves 1–3) into the workflows — the SINGLE integration owner of all four workflow files (deliberately one plan so the `execute-phase.md` collision the three pieces would otherwise cause is avoided). This delivers the end-to-end behavior of SC1 (worktree-isolated execute + merge), SC2 (gate fires in discuss/plan/execute), and SC4 (post-merge symmetry-check). All isolation/gate logic is DETERMINISTIC CLI calls (`gsd-tools worktree …`, `gsd-tools parallel-safe …`, `gsd-tools validate health …`) — the workflow prose only orchestrates the calls, never re-implements the logic.

Has a checkpoint (autonomous: false): a human verifies the end-to-end worktree+merge+gate flow.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md
@get-shit-done/references/git-worktree.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-01-PLAN.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-03-PLAN.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-05-PLAN.md

<interfaces>
Source↔runtime mirror rule: every workflow .md edit lands in BOTH `get-shit-done/workflows/` and `.claude/get-shit-done/workflows/` (write source, cp, diff -q). Only source committed.

CLI primitives available (built in 07-01/07-03/07-05 across Waves 1–3 — call them, do NOT re-implement):
- `gsd-tools worktree add <dir> <branch> [--base <ref>]` → JSON {ok|fallback}. add before spawning each parallel executor.
- `gsd-tools worktree merge <branch> [--target <ref>]` → JSON {clean, conflict_files}. PER-MERGE check after wave (RESEARCH Pitfall 2: each merge can conflict even if prior was clean).
- `gsd-tools worktree remove <dir> [--force]` → cleanup on success AND failure.
- `gsd-tools worktree prune` → leftover cleanup from crashed runs.
- `gsd-tools validate health` (+ exported checkSourceRuntimeSymmetry) → post-merge drift step.
- `gsd-tools parallel-safe <A> <B> --raw` → JSON {safe, decision: refuse|warn|greenlight, ...}.

execute-phase.md insertion points (verified line/step map):
- `validate_phase` (L135): add `gsd-tools worktree prune` to clear crash leftovers.
- NEW `parallel_safety_check` step (before/inside `handle_branching` L123): read STATE.md status; if a DIFFERENT phase is executing (Pitfall 4: skip when proposed == currently-executing), call `parallel-safe <other> <this>` → refuse on axis-B, warn on axis-A. Authoritative "is something running" signal = `git worktree list` (Pitfall 1 — STATE.md status is stale-prone; use both).
- `execute_waves` (L176) step 2: wrap each parallel executor spawn with `worktree add` before; in worktree mode REMOVE the `--no-verify` parallel_execution instruction from the executor prompt (ASSUMPTION 3 — kept only in in-place fallback). After the wave (step 3 / before aggregate_results), `worktree merge` each branch sequentially with per-merge clean check; on conflict PAUSE + surface `git diff` and await human; `worktree remove` each on success/failure.
- After `aggregate_results` (L494): NEW post-merge step calling `gsd-tools validate health` (symmetry) to catch drift introduced by the merge.

quick.md (L54 `branch` step, L41 `init`): add worktree auto-detection — `git worktree list --porcelain` count of linked worktrees > 0 (RESEARCH Worktree Detection example) OR STATE.md status executing → create a worktree for the quick task; else in-place (current behavior). Deterministic via the CLI, falls back in-place on sandbox failure.

discuss-phase.md (`build_understanding` L125) and plan-phase.md (start, after init/identify_phase — file has no <step name= markers; insert near the phase-identification logic): before discussing/planning phase N, if another phase discuss/plan is active and `parallel-safe <other> N` returns axis-B → HARD REFUSE (axis B is unrecoverable for discuss/plan). This restates the roadmap success criterion "refuses parallel discussion of dependent phases."

EXECUTOR-TARGETING CAVEAT (from 07-01 Wave 0 — read 07-01-SUMMARY.md): on-disk worktree isolation is real, BUT a Task-spawned executor writing to a REPO-ROOT absolute path (e.g. get-shit-done/...) lands in the MAIN tree, not the worktree — defeating isolation. When wiring `execute_waves`, the executor MUST be pointed at the worktree dir as its working directory (and any paths it writes must resolve under that dir), OR the honest mechanism is the in-place fallback + human checkpoint. Read the 07-01 SUMMARY's recorded finding to know which path Wave 0 proved deterministic before wiring worktree mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire worktree isolation + merge + post-merge symmetry into execute-phase.md</name>
  <read_first>
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-01-SUMMARY.md (Wave 0 executor-targeting caveat — READ FIRST: tells you whether executor-into-worktree is deterministic or must rely on in-place fallback + checkpoint; gate the worktree-mode wiring on this finding)
    - get-shit-done/workflows/execute-phase.md (validate_phase L135, handle_branching L123, execute_waves L176–351 incl. the --no-verify parallel_execution block L214–220, aggregate_results L494)
    - get-shit-done/references/git-worktree.md (Step 0 detect-existing, sandbox fallback — referenced, not re-derived)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-01-PLAN.md (worktree CLI API)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-03-PLAN.md (validate health symmetry)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md (§Scope 1 insert points + Pitfalls 1/2)
  </read_first>
  <action>
    Edit `get-shit-done/workflows/execute-phase.md`:
    1. In `validate_phase`: add a `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" worktree prune` call (clears leftover worktrees from a crashed prior run).
    2. In `execute_waves` step 2: before spawning each parallel executor, add a deterministic `worktree add ".worktrees/${PHASE}-${PLAN_ID}" "worktree/${PHASE}-${PLAN_ID}"` call; pass the worktree dir to the executor as its working directory in the prompt. Per the 07-01 Wave 0 executor-targeting caveat (see read_first SUMMARY), explicitly instruct the executor to treat the worktree dir as its root and write relative to it — an absolute repo-root path would land in the main tree and defeat isolation. If the Wave 0 finding records that executor-into-worktree could NOT be made deterministic, follow its recommended honest mechanism (in-place fallback + human checkpoint) instead of claiming isolation. If `worktree add` returns `fallback:"in-place"` (sandbox), run the current shared-tree behavior. In worktree mode, REMOVE the `<parallel_execution>` `--no-verify` instruction (each worktree has no contention — ASSUMPTION 3); keep that block ONLY for the in-place fallback path (gate it behind "if in-place fallback").
    3. After the wave completes (between step 3 wait-for-agents and `aggregate_results`): add a merge-back sub-step that loops over completed worktree branches, calls `worktree merge` PER BRANCH, checks `clean`; on `clean:false` PAUSE, print `git diff` / conflict_files, and wait for human resolution (do NOT auto-abort); then `worktree remove` each branch (force on failure). Emphasize per-merge clean check (Pitfall 2).
    4. After `aggregate_results`: add a `post_merge_drift_check` note/step calling `gsd-tools validate health` and surfacing any source↔runtime drift the merge introduced.
    Keep all logic as CLI calls + concise orchestration prose; do not inline bash that re-implements worktree/merge logic.

    MIRROR: `cp get-shit-done/workflows/execute-phase.md .claude/get-shit-done/workflows/execute-phase.md`.
  </action>
  <verify>grep -nE "worktree prune|worktree add|worktree merge|worktree remove|validate health" get-shit-done/workflows/execute-phase.md && diff -q get-shit-done/workflows/execute-phase.md .claude/get-shit-done/workflows/execute-phase.md</verify>
  <acceptance_criteria>
    - `grep -q "worktree prune" get-shit-done/workflows/execute-phase.md` returns 0 (in validate_phase region)
    - `grep -q "worktree add" get-shit-done/workflows/execute-phase.md` returns 0 (in execute_waves region)
    - `grep -q "worktree merge" get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -iE "per-merge|each merge|per merge" get-shit-done/workflows/execute-phase.md` returns 0 (Pitfall 2 reflected)
    - `grep -q "validate health" get-shit-done/workflows/execute-phase.md` returns 0 (post-merge step)
    - The worktree-mode wiring reflects the 07-01 executor-targeting caveat (executor pointed at the worktree dir / writes relative to it, OR the in-place fallback is the stated honest mechanism)
    - The `--no-verify` parallel_execution block is now conditional on the in-place fallback (grep shows it gated, not unconditional)
    - `diff -q get-shit-done/workflows/execute-phase.md .claude/get-shit-done/workflows/execute-phase.md` passes
  </acceptance_criteria>
  <done>execute-phase deterministically adds a worktree per parallel executor (pointing the executor at the worktree dir per the Wave 0 caveat), merges back with per-merge clean detection (conflict → reviewable pause), runs post-merge symmetry-check, falls back in-place on sandbox; both copies identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire parallel-safety gate into execute-phase, discuss-phase, plan-phase; auto-worktree into quick.md</name>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (handle_branching L123 — insert parallel_safety_check before it)
    - get-shit-done/workflows/discuss-phase.md (build_understanding L125)
    - get-shit-done/workflows/plan-phase.md (phase-identification region — no <step name= markers; read the opening to find the right spot)
    - get-shit-done/workflows/quick.md (init L41, branch L54)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-05-PLAN.md (parallel-safe CLI contract)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md (§Scope 2 invocation surface + Pitfalls 1/4)
  </read_first>
  <action>
    `execute-phase.md`: add a `parallel_safety_check` step before `handle_branching`. Determine "is a DIFFERENT phase running" via `git worktree list --porcelain` (authoritative — Pitfall 1) AND STATE.md status. If a different phase P is running, call `gsd-tools parallel-safe P <this-phase> --raw`; on `decision:"refuse"` (axis B) STOP with the gate's reason; on `decision:"warn"` (axis A) print the warning + overlap_files and continue (worktrees make it reviewable). If proposed phase == currently-executing phase, SKIP the gate (Pitfall 4 — re-entry for next wave is not a new parallel set).

    `discuss-phase.md` (build_understanding) and `plan-phase.md` (phase-identification region): before discussing/planning phase N, if another phase's discuss/plan is concurrently active, call `parallel-safe <other> N --raw`; on axis-B coupling → HARD REFUSE (discuss/plan decision coupling is unrecoverable — worktrees don't help). State this restates the roadmap criterion. axis-A overlap for discuss/plan is low-risk (separate phase folders) — warn only.

    `quick.md` (init or branch step): add deterministic worktree auto-detection — if `git worktree list --porcelain | grep "^worktree " | grep -v <main> | wc -l` > 0 (a phase is running) OR STATE.md status == executing, create a worktree for the quick task (`gsd-tools worktree add`), running the task in it and merging back at finish; else fall back to the current in-place branch behavior. On sandbox failure, fall back in-place with a warning. This is the highest-value case (quick-fix while phase runs → axis A dominates).

    MIRROR all four: `cp` each edited workflow to its `.claude/get-shit-done/workflows/` counterpart.
  </action>
  <verify>grep -lE "parallel-safe" get-shit-done/workflows/execute-phase.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/plan-phase.md && grep -E "worktree add|worktree list" get-shit-done/workflows/quick.md && for f in execute-phase discuss-phase plan-phase quick; do diff -q get-shit-done/workflows/$f.md .claude/get-shit-done/workflows/$f.md || echo "DRIFT $f"; done</verify>
  <acceptance_criteria>
    - `grep -q "parallel-safe" get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -q "parallel-safe" get-shit-done/workflows/discuss-phase.md` returns 0
    - `grep -q "parallel-safe" get-shit-done/workflows/plan-phase.md` returns 0
    - discuss-phase and plan-phase prose contain an explicit HARD REFUSE on axis-B coupling (grep -iE "hard.?refuse|refuse" each file)
    - execute-phase parallel_safety_check skips when proposed == currently-executing (Pitfall 4 — grep for the same-phase skip condition)
    - `grep -qE "worktree list|worktree add" get-shit-done/workflows/quick.md` returns 0
    - quick.md contains the in-place fallback condition (grep -i "in-place" or "fall back")
    - `diff -q` passes for all four workflow files (source↔runtime)
  </acceptance_criteria>
  <done>The gate fires deterministically at execute/discuss/plan start (refuse axis-B, warn axis-A, skip same-phase re-entry); /gsd2:quick auto-worktrees under concurrent execution and falls back in-place otherwise; all four copies identical.</done>
</task>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 3: Human verifies end-to-end worktree + merge + gate flow</name>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (the wired worktree/gate/symmetry steps)
    - .planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-VALIDATION.md (Manual-Only Verifications row)
  </read_first>
  <action>
    Present the wired flow for human verification. Provide these exact checks for the user to run:
    1. `node .claude/get-shit-done/bin/gsd-tools.cjs worktree prune` — runs clean.
    2. `node .claude/get-shit-done/bin/gsd-tools.cjs parallel-safe 6 7 --raw` — returns decision "refuse" (Phase 7 depends_on Phase 6).
    3. `node .claude/get-shit-done/bin/gsd-tools.cjs validate health` — reports no source↔runtime drift on the synced tree.
    4. Confirm (read-through) that execute-phase.md's execute_waves wraps the executor spawn with `worktree add` and merges back with a per-merge clean check, and that quick.md auto-worktrees under a running phase.
    Summarize what the user should observe and ask them to confirm the end-to-end ergonomics (a quick-fix in a parallel session surfaces a reviewable merge, never a silent overwrite — the manual-only row in 07-VALIDATION.md).
  </action>
  <verify>node .claude/get-shit-done/bin/gsd-tools.cjs parallel-safe 6 7 --raw && node .claude/get-shit-done/bin/gsd-tools.cjs validate health 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - `parallel-safe 6 7 --raw` JSON shows `decision` of "refuse" or "warn" (Phase 7↔6 coupling detected)
    - `validate health` reports no source↔runtime drift (or only expected agents/ differences excluded)
    - `worktree prune` exits 0
    - User confirms the end-to-end flow behaves as described
  </acceptance_criteria>
  <done>Human has confirmed worktree isolation, per-merge clean detection, the gate refusal on coupled phases, and the symmetry-check all work end to end.</done>
</task>

</tasks>

<verification>
- `grep parallel-safe` present in execute-phase, discuss-phase, plan-phase
- `grep worktree` present in execute-phase and quick
- `grep "validate health"` present in execute-phase (post-merge)
- `diff -q` passes for all four workflow files (source↔runtime)
- CLI smokes: `parallel-safe 6 7` → refuse; `validate health` → no drift; `worktree prune` → clean
</verification>

<success_criteria>
SC1, SC2, SC4 end-to-end: execute-phase isolates+merges with reviewable conflicts and a post-merge symmetry-check; the gate refuses axis-B and warns axis-A at execute/discuss/plan; /gsd2:quick auto-worktrees under concurrent execution. All deterministic via CLI, never agent prose.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-06-SUMMARY.md`
</output>
