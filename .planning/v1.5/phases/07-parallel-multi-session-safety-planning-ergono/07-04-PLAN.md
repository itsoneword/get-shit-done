---
phase: 07-parallel-multi-session-safety-planning-ergono
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - get-shit-done/bin/lib/phase.cjs
  - .claude/get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - .claude/get-shit-done/bin/gsd-tools.cjs
  - .claude/commands/gsd2/add-backlog.md
  - .claude/commands/gsd2/review-backlog.md
  - .planning/ROADMAP.md
  - tests/roadmap.test.cjs
autonomous: true
requirements: [SC5]
must_haves:
  truths:
    - "gsd-tools phase next-backlog-id returns B1 when no backlog items exist, B2 when B1 exists, etc."
    - "add-backlog allocates a B-prefixed ID (not 999.x); review-backlog globs B* dirs"
    - "Existing 999.2 backlog dir is migrated to B1; the empty 999.1 dir is removed"
    - "roadmap.cjs phase parser does NOT parse B-prefixed IDs as phases (phase-space separation)"
  artifacts:
    - get-shit-done/bin/lib/phase.cjs
    - .claude/commands/gsd2/add-backlog.md
  key_links:
    - "add-backlog → phase next-backlog-id (broken = backlog reverts to 999.x)"
    - "B-prefix IDs ∉ roadmap phase regex (broken = backlog items pollute phase list)"
---

<objective>
Replace the `999.x` backlog ID scheme — which conflates "unsequenced backlog" with "phase number" — with non-phase `B1, B2, …` IDs that live outside the phase-number space. Add a `phase next-backlog-id` allocator, repoint the backlog command files, migrate the existing `999.x` directories + ROADMAP entries, and confirm the roadmap phase parser ignores B-prefixed IDs. Implements SC5.

NOTE (ASSUMPTION, see 07-ASSUMPTIONS): the backlog command files (`add-backlog.md`, `review-backlog.md`) exist ONLY in `.claude/commands/gsd2/` — there is NO `get-shit-done/commands/gsd2/` source for them (verified). So the source↔runtime mirror rule does NOT apply to those two files; they are edited in place in `.claude/`. The CLI/lib changes (phase.cjs, gsd-tools.cjs) DO follow the mirror rule.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-CONTEXT.md
@.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-RESEARCH.md

<interfaces>
Source↔runtime mirror rule applies to phase.cjs and gsd-tools.cjs (write source, cp to runtime, diff -q). Does NOT apply to the backlog command .md files (runtime-only — no source copy exists).

cmdPhaseNextDecimal (phase.cjs ~L115–146): scans phase dirs for `999.N` decimals, returns `{found, base_phase, next, existing}` and the bare `next` string. Model the new allocator on this.

Router (gsd-tools.cjs `case 'phase'` L490): dispatches `next-decimal|add|insert|remove|complete`. Add `next-backlog-id` branch + update the help string at L515. NOTE: this plan depends_on 07-01 (also edits gsd-tools.cjs) so the worktree case already exists — append your case after it.

roadmap.cjs L107 phase regex: `/#{2,4}\s*Phase\s+(\d+[A-Z]?(?:\.\d+)*)\s*:\s*([^\n]+)/gi`. It requires a leading DIGIT after "Phase " — `B1` will NOT match (RESEARCH confirmed). No regex change needed; backlog headings must NOT use the `Phase N:` form.

Existing 999.x dirs (verified):
- `.planning/v1.5/phases/999.1-doctor-source-runtime-symmetry-check/` — folded into Phase 7; REMOVE (its content is now this phase).
- `.planning/v1.5/phases/999.2-terse-output-default-verbose-opt-in/` — has accumulated context; MIGRATE to B1.

Backlog command refs to repoint:
- `.claude/commands/gsd2/add-backlog.md` L26: `phase next-decimal 999 --raw` → `phase next-backlog-id --raw`; L3/L12/L73/L76: 999.x prose → B-prefix prose.
- `.claude/commands/gsd2/review-backlog.md` L19: `ls -d .planning/**/phases/999*` → `ls -d .planning/**/phases/B[0-9]*`; L11/L22/L34: 999.x prose/glob → B*.
- `.planning/ROADMAP.md` `## Backlog`: `### Phase 999.2: ...` → `### B1: Terse output default + verbose opt-in`; remove the `### Phase 999.1:` folded-entry line (it's now Phase 7).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase next-backlog-id allocator + RED tests; confirm B-IDs not parsed as phases</name>
  <read_first>
    - get-shit-done/bin/lib/phase.cjs (cmdPhaseNextDecimal L115–146 — model the allocator on it)
    - get-shit-done/bin/lib/roadmap.cjs (L107 phase regex — confirm it won't match B1)
    - get-shit-done/bin/gsd-tools.cjs (case 'phase' L490–515 — router + help string)
    - tests/roadmap.test.cjs (test scaffolding for roadmap parsing + CLI invocation)
  </read_first>
  <action>
    TDD: add failing tests FIRST (split between tests/roadmap.test.cjs for the parser-ignores-B assertion and the allocator), watch fail, then implement.

    Tests to add:
    - `next-backlog-id` with no existing B* backlog dirs/entries returns `B1`.
    - With a `B1-...` dir present, returns `B2`.
    - roadmap parse: a ROADMAP with a `## Backlog` section containing `### B1: Some Item` does NOT include `B1` in the parsed phases list (assert `phases.find(p => p.number === 'B1')` is undefined). This proves phase-space separation; NO regex change should be needed.

    Then implement `cmdPhaseNextBacklogId(cwd, raw)` in `get-shit-done/bin/lib/phase.cjs`: scan the phases dir (use the same phasesDir resolution as the decimal allocator) for dirs matching `^B(\d+)-`, take max N, return `B<max+1>` (or `B1` if none). Also scan ROADMAP.md `## Backlog` for `### B(\d+):` headings to be safe and union the max. Output `{next, existing}` and the bare `next` string (mirror cmdPhaseNextDecimal's output() shape).

    Wire `next-backlog-id` into the `case 'phase'` router branch and append it to the help string (`Available: next-decimal, next-backlog-id, add, insert, remove, complete`).

    MIRROR: `cp get-shit-done/bin/lib/phase.cjs .claude/get-shit-done/bin/lib/phase.cjs` and `cp get-shit-done/bin/gsd-tools.cjs .claude/get-shit-done/bin/gsd-tools.cjs`.
  </action>
  <verify>node --test tests/roadmap.test.cjs 2>&1 | tail -5 && node get-shit-done/bin/gsd-tools.cjs phase next-backlog-id --raw && diff -q get-shit-done/bin/lib/phase.cjs .claude/get-shit-done/bin/lib/phase.cjs && diff -q get-shit-done/bin/gsd-tools.cjs .claude/get-shit-done/bin/gsd-tools.cjs</verify>
  <acceptance_criteria>
    - `node --test tests/roadmap.test.cjs` exits 0, `# fail 0`
    - `grep -q "cmdPhaseNextBacklogId" get-shit-done/bin/lib/phase.cjs` returns 0
    - `grep -q "next-backlog-id" get-shit-done/bin/gsd-tools.cjs` returns 0
    - `node get-shit-done/bin/gsd-tools.cjs phase next-backlog-id --raw` prints a `B`-prefixed ID
    - A test asserts a `### B1:` backlog heading is NOT in the parsed phases array
    - `roadmap.cjs` L107 regex is UNCHANGED (no edit to phase.cjs/roadmap.cjs regex required — verify via git diff that roadmap.cjs has no regex change)
    - `diff -q` passes for both phase.cjs and gsd-tools.cjs
  </acceptance_criteria>
  <done>next-backlog-id returns sequential B-IDs; roadmap parser ignores B-prefixed headings; both CLI copies identical; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repoint backlog command files + migrate existing 999.x dirs and ROADMAP entries</name>
  <read_first>
    - .claude/commands/gsd2/add-backlog.md (full file — 999.x refs at L3/L12/L26/L73/L76)
    - .claude/commands/gsd2/review-backlog.md (full file — 999* glob L19, prose L11/L22/L34)
    - .planning/ROADMAP.md (## Backlog section ~L147–160)
  </read_first>
  <action>
    Edit `.claude/commands/gsd2/add-backlog.md` (runtime-only, no source mirror):
    - L26: replace `phase next-decimal 999 --raw` with `phase next-backlog-id --raw`.
    - L3 description, L12, L73, L76: replace "999.x numbering" / "999.x" prose with "B-prefixed (B1, B2…) numbering" describing IDs outside the phase-number space, allocated by `next-backlog-id`.

    Edit `.claude/commands/gsd2/review-backlog.md` (runtime-only):
    - L19: replace `ls -d .planning/**/phases/999* 2>/dev/null` with `ls -d .planning/**/phases/B[0-9]* 2>/dev/null`.
    - L11/L22/L34: replace 999.x prose/glob with B* equivalents (read all backlog items = `B*` dirs; promote = rename `B{n}-slug` → `{new_num}-slug`).

    Migrate directories:
    - `git rm -r .planning/v1.5/phases/999.1-doctor-source-runtime-symmetry-check/` (folded into Phase 7 — remove). If it contains tracked files, use git rm; if untracked/empty, `rm -rf`.
    - `git mv .planning/v1.5/phases/999.2-terse-output-default-verbose-opt-in .planning/v1.5/phases/B1-terse-output-default-verbose-opt-in` (preserve its accumulated content).

    Edit `.planning/ROADMAP.md` `## Backlog`:
    - Remove the `### Phase 999.1: Doctor — source↔runtime symmetry check → FOLDED INTO PHASE 7` block (it is now Phase 7, no longer backlog).
    - Rename `### Phase 999.2: Terse output default + verbose opt-in (BACKLOG)` → `### B1: Terse output default + verbose opt-in (BACKLOG)`. Update any inline `999.2` references in that block to `B1`.
  </action>
  <verify>grep -rn "999" .claude/commands/gsd2/add-backlog.md .claude/commands/gsd2/review-backlog.md; ls -d .planning/v1.5/phases/B1-* 2>/dev/null; test ! -d .planning/v1.5/phases/999.1-doctor-source-runtime-symmetry-check && echo "999.1 removed"; grep -n "999" .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `grep -c "999" .claude/commands/gsd2/add-backlog.md` returns 0
    - `grep -c "999" .claude/commands/gsd2/review-backlog.md` returns 0
    - `grep -q "next-backlog-id" .claude/commands/gsd2/add-backlog.md` returns 0
    - `grep -q "B\[0-9\]" .claude/commands/gsd2/review-backlog.md` returns 0
    - `test -d .planning/v1.5/phases/B1-terse-output-default-verbose-opt-in` succeeds
    - `test ! -d .planning/v1.5/phases/999.1-doctor-source-runtime-symmetry-check` succeeds
    - `test ! -d .planning/v1.5/phases/999.2-terse-output-default-verbose-opt-in` succeeds
    - `grep -c "999" .planning/ROADMAP.md` returns 0 (no 999.x references remain in ROADMAP backlog)
    - `grep -q "### B1:" .planning/ROADMAP.md` returns 0
  </acceptance_criteria>
  <done>Backlog commands allocate/glob B-IDs; 999.1 removed, 999.2 migrated to B1; ROADMAP backlog uses B-prefixed headings; no 999 references remain.</done>
</task>

</tasks>

<verification>
- `node --test tests/roadmap.test.cjs` green
- `node get-shit-done/bin/gsd-tools.cjs phase next-backlog-id --raw` returns a B-ID
- No `999` references in backlog command files or ROADMAP backlog
- B1 dir exists, both 999.x dirs gone
- `diff -q` passes for phase.cjs and gsd-tools.cjs
</verification>

<success_criteria>
SC5 complete: the backlog ID scheme uses `B1, B2, …` outside the phase-number space; the roadmap phase parser ignores them; existing backlog migrated; allocator + commands repointed.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/07-parallel-multi-session-safety-planning-ergono/07-04-SUMMARY.md`
</output>
