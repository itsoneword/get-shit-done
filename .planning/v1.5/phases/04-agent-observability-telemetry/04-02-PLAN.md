---
phase: 04-agent-observability-telemetry
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - hooks/gsd2-agent-trace.js
  - scripts/build-hooks.js
  - bin/install.js
  - .planning/config.json
  - .gitignore
files_owned:
  - hooks/
  - scripts/build-hooks.js
  - bin/install.js
  - .planning/config.json
  - .gitignore
autonomous: false
requirements: [OBS-01, OBS-02]
user_setup: []
must_haves:
  truths:
    - "After a GSD run that spawns a gsd-* subagent, .planning/telemetry/agent-trace.jsonl contains one JSONL record per spawn with ts_return, agent_type, description, desc_hash, confidence, seq."
    - "The hook logs ONLY gsd-* subagent spawns (subagent_type starting with gsd-); non-gsd spawns produce no record."
    - "A hook failure never blocks the tool — every error path exits 0 silently."
    - "config.hooks.agent_trace === false disables the hook; default (key absent or true) keeps it on."
    - "PostToolUse and PostToolUseFailure entries with matcher Task|Agent are registered by install.js; the failure path writes event: agent.error."
  artifacts:
    - "hooks/gsd2-agent-trace.js (full record-writing body)"
    - "hooks/dist/gsd2-agent-trace.js (produced by build-hooks.js)"
    - "bin/install.js registration for PostToolUse + PostToolUseFailure Task|Agent"
    - ".planning/config.json hooks.agent_trace key"
    - ".gitignore entry for .planning/telemetry/"
  key_links:
    - "hook → .planning/telemetry/agent-trace.jsonl append (broken = silent empty log, the Pitfall-1 failure mode)."
    - "scripts/build-hooks.js HOOKS_TO_COPY includes gsd2-agent-trace.js (broken = runtime never gets the hook)."
    - "bin/install.js PostToolUse/PostToolUseFailure entries reference gsd2-agent-trace.js (broken = hook never fires)."
---

<objective>
Complete the telemetry hook: take the Plan-01 skeleton (which already exports scrapeConfidence + extractReturnText and guards stdin) and flesh out its stdin reader to write one JSONL record per gsd-* subagent spawn. Wire it into the build pipeline, the installer, the config gate, and .gitignore.

This is the OBS-01 core (spawn logging) + the OBS-02 emit side (confidence captured from return text). The reader (OBS-02's "optional reader") is Plan 03.

DEPENDS ON Plan 01: use the confirmed tool_name string and tool_response field name recorded in 04-01-SUMMARY. If Plan 01 confirmed the live envelope uses a field other than `tool_response`, read that field instead — the SUMMARY is the source of truth over the proposed schema here.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-CONTEXT.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-01-SUMMARY.md
@hooks/gsd2-context-monitor.js
@hooks/gsd2-agent-trace.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out the hook's stdin reader to write one JSONL record per gsd-* spawn</name>
  <files>hooks/gsd2-agent-trace.js</files>
  <read_first>
    - hooks/gsd2-agent-trace.js (the Plan-01 skeleton — DO NOT remove the module.exports or the require.main guard; extend the body inside the require.main block)
    - hooks/gsd2-context-monitor.js (verbatim idioms: 10s stdin timeout, config-gate read, .planning/STATE.md existence guard, try/catch → silent process.exit(0))
    - 04-01-SUMMARY.md (the CONFIRMED tool_name and tool_response field name — use these, not the proposed names, if they differ)
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md (Pattern 1 full hook body; Q3 correlation; Q4 guard; record schema in Code Examples)
  </read_first>
  <action>
    Inside the existing `if (require.main === module)` block of hooks/gsd2-agent-trace.js, implement the full reader (mirroring gsd2-context-monitor.js scaffold). On stdin end, inside a try/catch that silently exits 0 on ANY error:

    1. Parse the stdin JSON: `const data = JSON.parse(input)`. Extract `session_id`, `cwd`, `tool_input`, `tool_response`, and `hook_event_name` (use the CONFIRMED field names from 04-01-SUMMARY if they differ from these).
    2. Bail guards (each → process.exit(0)):
       - `if (!session_id || !cwd) process.exit(0)`
       - gsd-* filter (this is BOTH the OBS-01 filter and the subagent-context guard, per RESEARCH Q4):
         `const agentType = tool_input?.subagent_type || ''; if (!agentType.startsWith('gsd-')) process.exit(0);`
       - config gate (mirror context-monitor):
         read `path.join(cwd,'.planning','config.json')`; `if (cfg.hooks?.agent_trace === false) process.exit(0);` (default-on: absent/true means enabled)
       - GSD-project guard: `if (!fs.existsSync(path.join(cwd,'.planning','STATE.md'))) process.exit(0);`
    3. Compute fields:
       - `const returnText = extractReturnText(tool_response);`
       - `const confidence = scrapeConfidence(returnText);` (null is the common case — most gsd-* spawns emit no verdict)
       - `const desc = tool_input?.description || '';`
       - `const descHash = crypto.createHash('sha256').update(desc).digest('hex').slice(0,8);`
       - event type: `const event = hook_event_name === 'PostToolUseFailure' ? 'agent.error' : 'agent.return';`
    4. Ensure dir + derive per-session seq:
       - `const telemetryDir = path.join(cwd,'.planning','telemetry'); fs.mkdirSync(telemetryDir,{recursive:true});`
       - `const logPath = path.join(telemetryDir,'agent-trace.jsonl');`
       - seq = count of EXISTING lines for THIS session only (not all lines):
         ```js
         let seq = 0;
         if (fs.existsSync(logPath)) {
           const existing = fs.readFileSync(logPath,'utf8');
           seq = (existing.match(new RegExp('"session_id":"' + session_id + '"','g')) || []).length;
         }
         ```
    5. Build and append the record (exact schema):
       ```js
       const record = {
         event,
         session_id,
         seq,
         ts_return: new Date().toISOString(),
         agent_type: agentType,
         description: desc,
         desc_hash: descHash,
         confidence,
         duration_ms: null
       };
       fs.appendFileSync(logPath, JSON.stringify(record) + '\n');
       ```
       duration_ms stays null unless 04-01-SUMMARY confirmed a structured field (e.g. totalDurationMs) in the envelope — if confirmed, parse it opportunistically: `record.duration_ms = data.tool_response?.totalDurationMs ?? null` using the confirmed field path.
    6. `process.exit(0)` at the end. The whole body is wrapped in try/catch → silent exit 0.

    Do NOT touch module.exports or the require.main guard from Plan 01 — the scraper unit tests must keep passing.
  </action>
  <verify>node --test test/agent-trace-scraper.test.js && node scripts/build-hooks.js 2>&1 | grep -q gsd2-agent-trace || echo "build wiring added in Task 2"</verify>
  <acceptance_criteria>
    - hooks/gsd2-agent-trace.js contains the literal string "agent.error" and the literal string "agent.return"
    - hooks/gsd2-agent-trace.js contains "if (!agentType.startsWith('gsd-')) process.exit(0)" (or semantically identical gsd- prefix guard)
    - hooks/gsd2-agent-trace.js contains "cfg.hooks?.agent_trace === false" and "fs.appendFileSync"
    - hooks/gsd2-agent-trace.js contains "module.exports = { scrapeConfidence, extractReturnText }" (Plan 01 export preserved)
    - `node --test test/agent-trace-scraper.test.js` still exits 0 (scraper unchanged)
    - Simulated stdin produces a record: `printf '{"session_id":"s1","cwd":"%s","hook_event_name":"PostToolUse","tool_input":{"subagent_type":"gsd-planner","description":"Plan X"},"tool_response":[{"type":"text","text":"**Confidence:** HIGH"}]}' "$PWD" | node hooks/gsd2-agent-trace.js` then `grep -q '"agent_type":"gsd-planner"' .planning/telemetry/agent-trace.jsonl && grep -q '"confidence":"HIGH"' .planning/telemetry/agent-trace.jsonl` exits 0
    - Non-gsd spawn writes nothing: `printf '{"session_id":"s2","cwd":"%s","hook_event_name":"PostToolUse","tool_input":{"subagent_type":"general-purpose","description":"x"},"tool_response":[]}' "$PWD" | node hooks/gsd2-agent-trace.js` produces NO new line containing "s2" in the log
  </acceptance_criteria>
  <done>The hook appends a correct JSONL record for gsd-* spawns, skips non-gsd spawns, honors the config gate, and never throws past its try/catch. Scraper tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the hook into the build pipeline, installer, config gate, and gitignore</name>
  <files>scripts/build-hooks.js, bin/install.js, .planning/config.json, .gitignore</files>
  <read_first>
    - scripts/build-hooks.js (the HOOKS_TO_COPY array — explicit list, NOT a glob)
    - bin/install.js (lines ~3139-3235: the contextMonitorCommand/promptGuard pattern for declaring a command + pushing a PostToolUse/PreToolUse entry; and line ~2214 the uninstall gsdHooks array)
    - .planning/config.json (the hooks.* block to extend)
    - .gitignore (confirm .claude/ already ignored; add telemetry)
  </read_first>
  <action>
    Four wiring edits (source files — .claude/ runtime is gitignored and regenerated by build/install):

    1. **scripts/build-hooks.js** — add `'gsd2-agent-trace.js'` to the `HOOKS_TO_COPY` array (after `'gsd2-read-guard.js'`). Without this the hook never reaches hooks/dist/ (Pitfall 4).

    2. **bin/install.js** — mirror the gsd2-context-monitor wiring block (~lines 3139-3207):
       - Declare the command near the other hook commands (~line 3148):
         ```js
         const agentTraceCommand = isGlobal
           ? buildHookCommand(targetDir, 'gsd2-agent-trace.js')
           : 'node ' + dirName + '/hooks/gsd2-agent-trace.js';
         ```
       - Inside the `if (!isOpencode)` block, after the read-injection-scanner registration (~line 3219), register the PostToolUse Task|Agent entry (idempotent, mirror the hasContextMonitorHook pattern):
         ```js
         const hasAgentTrace = settings.hooks[postToolEvent].some(entry =>
           entry.hooks && entry.hooks.some(h => h.command && h.command.includes('gsd2-agent-trace'))
         );
         if (!hasAgentTrace) {
           settings.hooks[postToolEvent].push({
             matcher: 'Task|Agent',
             hooks: [ { type: 'command', command: agentTraceCommand } ]
           });
           console.log(`  ${green}✓${reset} Configured agent trace hook`);
         }
         ```
       - Add a NET-NEW PostToolUseFailure block (no existing block — this structure does not exist yet). Use a matching event name (Gemini/Antigravity may not support it — guard with the same isOpencode scope and only add for the standard postToolEvent === 'PostToolUse' case):
         ```js
         if (postToolEvent === 'PostToolUse') {
           if (!settings.hooks['PostToolUseFailure']) settings.hooks['PostToolUseFailure'] = [];
           const hasAgentTraceFail = settings.hooks['PostToolUseFailure'].some(entry =>
             entry.hooks && entry.hooks.some(h => h.command && h.command.includes('gsd2-agent-trace'))
           );
           if (!hasAgentTraceFail) {
             settings.hooks['PostToolUseFailure'].push({
               matcher: 'Task|Agent',
               hooks: [ { type: 'command', command: agentTraceCommand } ]
             });
             console.log(`  ${green}✓${reset} Configured agent trace failure hook`);
           }
         }
         ```
       - Add `'gsd2-agent-trace.js'` to the uninstall `gsdHooks` array at ~line 2214 so uninstall removes it.

    3. **.planning/config.json** — add `"agent_trace": true` to the existing `hooks` object (alongside prompt_guard/read_injection_scanner/read_guard). Default-on per CONTEXT decision.

    4. **.gitignore** — add a line `.planning/telemetry/` so the append-only JSONL log is never committed (Pitfall 7). (.claude/ is already ignored — confirm, do not duplicate.)

    Then run the build and confirm the runtime copy is produced.
  </action>
  <verify>node scripts/build-hooks.js && test -f hooks/dist/gsd2-agent-trace.js && grep -q '"agent_trace"' .planning/config.json && grep -q 'gsd2-agent-trace' bin/install.js && grep -q '.planning/telemetry/' .gitignore</verify>
  <acceptance_criteria>
    - scripts/build-hooks.js HOOKS_TO_COPY array contains 'gsd2-agent-trace.js' (grep -q "gsd2-agent-trace.js" scripts/build-hooks.js)
    - `node scripts/build-hooks.js` exits 0 and creates hooks/dist/gsd2-agent-trace.js (test -f exits 0)
    - bin/install.js contains the literal string "matcher: 'Task|Agent'" AND "PostToolUseFailure" AND "gsd2-agent-trace"
    - bin/install.js uninstall gsdHooks array (line ~2214 region) contains 'gsd2-agent-trace.js'
    - .planning/config.json parses and `node -e "process.exit(JSON.parse(require('fs').readFileSync('.planning/config.json')).hooks.agent_trace===true?0:1)"` exits 0
    - .gitignore contains the line ".planning/telemetry/"
  </acceptance_criteria>
  <done>The hook builds into dist, installs as PostToolUse + PostToolUseFailure Task|Agent entries, is config-gated default-on, removed on uninstall, and the telemetry dir is gitignored.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Confirm the hook fires end-to-end in live Claude Code</name>
  <files>.claude/settings.json (runtime, regenerated), .planning/telemetry/agent-trace.jsonl</files>
  <read_first>
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md (Test Commands table — "Hook fires on spawn", "Config gate")
    - 04-01-SUMMARY.md (confirmed tool_name — verifies the Task|Agent matcher actually matches)
  </read_first>
  <action>
    WHY HUMAN-VERIFY: hook execution requires the live Claude Code runtime and a real subagent spawn; it cannot be reproduced in node:test, and the executor subagent cannot trigger a parent-firing spawn from inside itself (RESEARCH Q4 + issue #34692). Automation is done (Tasks 1-2); this confirms the wiring lands in the live runtime.

    Human/orchestrator steps:
    1. Propagate source → runtime: re-run install (`node bin/install.js --claude --local`) OR cp the built hook (`cp hooks/dist/gsd2-agent-trace.js .claude/hooks/gsd2-agent-trace.js`) and confirm `.claude/settings.json` now has a PostToolUse entry with `matcher: "Task|Agent"` and command `gsd2-agent-trace.js`, plus a PostToolUseFailure entry.
    2. Run a GSD command that spawns a gsd-* subagent (e.g. a phase command spawning gsd-planner or gsd-phase-researcher).
    3. Confirm a line was appended: `cat .planning/telemetry/agent-trace.jsonl` shows a record with the spawned agent_type, a ts_return, and a desc_hash.
    4. Confidence path: if the spawned agent was a resolution/micro_research agent that returns a verdict, confirm `confidence` is HIGH/MEDIUM/LOW (not null). For non-verdict agents (gsd-planner), confidence: null is correct.
    5. Config gate: set `.planning/config.json` hooks.agent_trace to false, trigger another spawn, confirm NO new line is appended; restore to true.
    6. Non-blocking: confirm the GSD run completed normally (the hook never interrupted it).
  </action>
  <verify>test -f .planning/telemetry/agent-trace.jsonl && tail -1 .planning/telemetry/agent-trace.jsonl | grep -q '"agent_type":"gsd-'</verify>
  <acceptance_criteria>
    - .claude/settings.json contains a PostToolUse entry with "matcher": "Task|Agent" and command referencing gsd2-agent-trace.js
    - .planning/telemetry/agent-trace.jsonl exists and its last line is a valid JSON record with agent_type starting "gsd-" and a non-empty ts_return
    - The SUMMARY records: which agent was spawned, whether confidence was scraped (and to what value), and confirmation the run was not blocked
    - Config-gate verified: with hooks.agent_trace=false no new line appended (recorded in SUMMARY)
  </acceptance_criteria>
  <done>A real gsd-* spawn produces exactly one correct JSONL record in the live runtime; the config gate disables it; the run is never blocked.</done>
</task>

</tasks>

<verification>
- `node --test test/agent-trace-scraper.test.js` exits 0 (scraper still green after the body was added).
- `node scripts/build-hooks.js` produces hooks/dist/gsd2-agent-trace.js.
- Simulated stdin (gsd-planner) appends a record; non-gsd spawn appends nothing.
- Live spawn (Task 3) appends one record; config gate disables; run not blocked.
- Zero workflow/agent .md prompt files modified.
</verification>

<success_criteria>
- OBS-01: every gsd-* spawn produces a structured, timestamped JSONL record (type, description, desc_hash) with zero prompt-file changes.
- OBS-02 (emit side): the record carries the scraped confidence verdict when present; PostToolUseFailure captures crashed spawns as agent.error.
- Best-effort/non-blocking: all error paths exit 0; the hook self-disables outside GSD projects and when config-gated off.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/04-agent-observability-telemetry/04-02-SUMMARY.md`.
</output>
