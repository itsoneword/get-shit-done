---
phase: 04-agent-observability-telemetry
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/fixtures/agent-trace/agent-result-fixture.json
  - test/fixtures/agent-trace/hook-stdin-envelope.json
  - test/agent-trace-scraper.test.js
  - hooks/gsd2-agent-trace.js
autonomous: false
requirements: [OBS-01, OBS-02]
user_setup: []
must_haves:
  truths:
    - "A test fixture exists holding the real shape of a gsd-* subagent return (content array of {type,text} blocks, agentType, totalDurationMs)."
    - "scrapeConfidence() extracts HIGH/MEDIUM/LOW from both the prose form (**Confidence:** HIGH) and the JSON form (\"confidence\": \"HIGH\"), and returns null when absent."
    - "extractReturnText() flattens a content-array tool_response into a single string."
    - "The runtime hook stdin envelope field names (tool_name value, tool_response shape) are empirically confirmed and recorded."
  artifacts:
    - "test/fixtures/agent-trace/agent-result-fixture.json"
    - "test/fixtures/agent-trace/hook-stdin-envelope.json"
    - "test/agent-trace-scraper.test.js"
    - "hooks/gsd2-agent-trace.js (skeleton exporting scrapeConfidence + extractReturnText)"
  key_links:
    - "test/agent-trace-scraper.test.js imports scrapeConfidence/extractReturnText from hooks/gsd2-agent-trace.js (broken = the scraper logic is untestable and downstream Plan 02 ships unverified)."
---

<objective>
Resolve the two empirical unknowns that gate the entire phase BEFORE any scraper/parse logic is trusted, and build the unit-test foundation the hook is verified against.

The research (04-RESEARCH.md) and validation strategy (04-VALIDATION.md Wave 0) flag two things that cannot be assumed:
1. The runtime spawn tool surfaces as `Agent` in the transcript but `Task` in docs/settings — the hook matcher must be empirically confirmed.
2. The hook stdin `tool_response` shape is uncertain (content-array vs a `result` string) — this directly gates whether confidence scraping works.

This plan produces (a) a transcript-seeded fixture of a real gsd-* return so the scraper logic is testable now, (b) a human-action step that captures the live hook stdin envelope to confirm field names, and (c) the scraper/extract functions with passing unit tests. The full hook + wiring is Plan 02; the reader is Plan 03.

Confirmed evidence (already gathered from the real transcript `~/.claude/projects/-home-cleversol-gsd2-mine/c5609700-*.jsonl`, line 185-186): the spawn fires as `tool_use name="Agent"` with `input.{description, subagent_type, model, prompt}`; the result is `toolUseResult.content` = an array of `{type:"text", text:"..."}` blocks, with top-level `agentType: "gsd-planner"` and `totalDurationMs: 742811`. The OPEN question is whether the HOOK STDIN ENVELOPE uses the same field names (`tool_response` vs `toolUseResult`, `content` array vs `result` string) — that is what the live echo confirms.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-CONTEXT.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md
@.planning/v1.5/phases/04-agent-observability-telemetry/04-VALIDATION.md
@hooks/gsd2-context-monitor.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed the transcript-derived result fixture and write scraper/extract unit tests (RED)</name>
  <files>test/fixtures/agent-trace/agent-result-fixture.json, test/agent-trace-scraper.test.js</files>
  <read_first>
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md (Q2 confidence regex + corrected form; "Pattern 1" extractReturnText/scrapeConfidence reference; Code Examples)
    - hooks/gsd2-context-monitor.js (the hook scaffold this phase mirrors — note it runs an stdin IIFE on load and exports nothing)
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-VALIDATION.md (Wave 0 Requirements — both literal strings + null case)
  </read_first>
  <action>
    Create the test-fixture directory and a fixture representing a REAL gsd-* subagent return, derived from the confirmed transcript evidence.

    Write `test/fixtures/agent-trace/agent-result-fixture.json` modeling the content-array return shape (this is the confirmed transcript shape):
    ```json
    {
      "status": "completed",
      "agentType": "gsd-planner",
      "totalDurationMs": 742811,
      "content": [
        { "type": "text", "text": "## PLANNING COMPLETE\n\n**Phase:** 01-security-hooks\n\n**Confidence:** HIGH\n" }
      ]
    }
    ```
    Also add a second fixture variant inside the same file (as a JSON object with keys `prose`, `json_form`, `absent`, `array_blocks`) capturing each scraper case:
    - `prose`: the string `"**Confidence:** HIGH"`
    - `json_form`: the string `"{\"confidence\": \"MEDIUM\", \"reasoning\": \"...\"}"`
    - `absent`: the string `"## PLANNING COMPLETE\nNo verdict here."`
    - `array_blocks`: an array `[{"type":"text","text":"prefix "},{"type":"text","text":"**Confidence:** LOW"}]`

    Then write `test/agent-trace-scraper.test.js` using the built-in node:test runner (NOT Jest — Jest is not a project dependency, see STATE.md Phase 02 decision). The test imports from the hook:
    ```js
    const { test } = require('node:test');
    const assert = require('node:assert');
    const { scrapeConfidence, extractReturnText } = require('../hooks/gsd2-agent-trace.js');
    ```
    Assert exactly:
    - `scrapeConfidence('**Confidence:** HIGH')` === `'HIGH'`  (prose form WITH the space after `:**` — the corrected-regex case)
    - `scrapeConfidence('"confidence": "MEDIUM"')` === `'MEDIUM'`  (JSON form)
    - `scrapeConfidence('## PLANNING COMPLETE\nNo verdict here.')` === `null`  (absent → null)
    - `scrapeConfidence('')` === `null` and `scrapeConfidence(null)` === `null`
    - `extractReturnText([{type:'text',text:'a'},{type:'text',text:'b'}])` === `'a\nb'`  (content-array flatten)
    - `extractReturnText({result:'hello'})` === `'hello'`  (result-string fallback candidate)
    - `extractReturnText('plain')` === `'plain'`

    This test will FAIL initially (RED) because hooks/gsd2-agent-trace.js does not yet exist / does not export — that is expected and correct for Wave 0.
  </action>
  <verify>node --test test/agent-trace-scraper.test.js ; echo "expected RED until Task 2"</verify>
  <acceptance_criteria>
    - test/fixtures/agent-trace/agent-result-fixture.json exists and is valid JSON (node -e "JSON.parse(require('fs').readFileSync('test/fixtures/agent-trace/agent-result-fixture.json','utf8'))" exits 0)
    - test/agent-trace-scraper.test.js exists and contains the literal string "require('../hooks/gsd2-agent-trace.js')"
    - test/agent-trace-scraper.test.js contains the literal assertion strings "**Confidence:** HIGH" and "\"confidence\": \"MEDIUM\""
    - Running `node --test test/agent-trace-scraper.test.js` executes (does not hang) and reports failures referencing the missing module — it MUST NOT hang waiting on stdin
  </acceptance_criteria>
  <done>Fixture + RED scraper tests exist; the test file imports the two named functions from the hook path; running the test does not hang.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scrapeConfidence + extractReturnText in the hook skeleton (GREEN)</name>
  <files>hooks/gsd2-agent-trace.js</files>
  <read_first>
    - test/agent-trace-scraper.test.js (the contract from Task 1)
    - hooks/gsd2-context-monitor.js (stdin IIFE scaffold to mirror; note it runs on load — you MUST guard the reader so importing the file in node:test does NOT hang on stdin)
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md (corrected regex; Pitfall 3)
  </read_first>
  <action>
    Create `hooks/gsd2-agent-trace.js` as a skeleton that (a) exports the two pure functions for testing and (b) only runs the stdin reader when executed directly. The full record-writing body lands in Plan 02; this plan ships the pure functions + the import guard.

    Implement EXACTLY this corrected regex (verbatim from RESEARCH.md Q2 — the previous version missed the prose space and is an anti-pattern):
    ```js
    function scrapeConfidence(text) {
      if (!text) return null;
      const m = String(text).match(/confidence\s*["']?\s*[:=]\s*\**\s*["']?(HIGH|MEDIUM|LOW)/i);
      return m ? m[1].toUpperCase() : null;
    }
    ```
    Implement extractReturnText leading with the content-array candidate (confirmed transcript shape), then the `result` string fallback, then stringify:
    ```js
    function extractReturnText(toolResponse) {
      if (Array.isArray(toolResponse)) {
        return toolResponse
          .filter(b => b && b.type === 'text')
          .map(b => b.text || '')
          .join('\n');
      }
      if (toolResponse && typeof toolResponse.result === 'string') return toolResponse.result;
      return typeof toolResponse === 'string' ? toolResponse : JSON.stringify(toolResponse || '');
    }
    ```
    CRITICAL — guard the stdin reader and export the functions so node:test can import without hanging:
    ```js
    module.exports = { scrapeConfidence, extractReturnText };

    if (require.main === module) {
      // stdin reader IIFE (full body added in Plan 02) — for now, a minimal pass-through:
      let input = '';
      const stdinTimeout = setTimeout(() => process.exit(0), 10000);
      process.stdin.setEncoding('utf8');
      process.stdin.on('data', c => input += c);
      process.stdin.on('end', () => { clearTimeout(stdinTimeout); process.exit(0); });
    }
    ```
    Keep the file pure Node.js (require fs/path/crypto only as needed) — no external deps (SEC-04 constraint).
  </action>
  <verify>node --test test/agent-trace-scraper.test.js</verify>
  <acceptance_criteria>
    - hooks/gsd2-agent-trace.js exists and contains the literal regex `/confidence\s*["']?\s*[:=]\s*\**\s*["']?(HIGH|MEDIUM|LOW)/i`
    - hooks/gsd2-agent-trace.js contains the literal string "module.exports = { scrapeConfidence, extractReturnText }"
    - hooks/gsd2-agent-trace.js contains the literal string "if (require.main === module)"
    - `node --test test/agent-trace-scraper.test.js` exits 0 (all assertions pass — GREEN)
    - `node -e "require('./hooks/gsd2-agent-trace.js')"` exits 0 within 1 second (does NOT hang on stdin — proves the import guard works)
  </acceptance_criteria>
  <done>Both functions pass all Task-1 assertions; the hook can be imported without triggering the stdin reader.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Capture the live hook stdin envelope to confirm tool_name + tool_response field names</name>
  <files>test/fixtures/agent-trace/hook-stdin-envelope.json, .claude/settings.json (temporary), hooks/gsd2-agent-trace-debug.js (temporary)</files>
  <read_first>
    - .planning/v1.5/phases/04-agent-observability-telemetry/04-RESEARCH.md (Validation Architecture → Wave-0 Gate, both exit criteria)
    - .claude/settings.json (the PostToolUse array shape to temporarily extend)
  </read_first>
  <action>
    WHY HUMAN-ACTION: hooks only fire in the live Claude Code runtime, and a spawn-firing hook cannot be triggered from inside a spawned executor subagent (the executor lacks the Task/Agent tool, and subagent tool calls do not fire parent hooks — RESEARCH.md Q4 + issue #34692). The orchestrator/human must trigger one real gsd-* spawn.

    Steps for the human (or orchestrator at the top level):
    1. Create a throwaway debug hook `hooks/gsd2-agent-trace-debug.js` containing exactly:
       ```js
       process.stdin.on('data', d => require('fs').appendFileSync('/tmp/gsd-hook-debug.json', d));
       process.stdin.on('end', () => process.exit(0));
       ```
       Mirror it to runtime: `cp hooks/gsd2-agent-trace-debug.js .claude/hooks/gsd2-agent-trace-debug.js`
    2. Temporarily add a PostToolUse entry with NO matcher (fires on every tool) to `.claude/settings.json`:
       `{ "hooks": [ { "type": "command", "command": "node .claude/hooks/gsd2-agent-trace-debug.js" } ] }`
    3. Run a GSD command that spawns a gsd-* subagent (e.g. trigger any phase command that spawns gsd-planner/gsd-phase-researcher).
    4. Inspect `/tmp/gsd-hook-debug.json` and RECORD in the SUMMARY:
       - the value of `tool_name` (is it `"Task"`, `"Agent"`, or does the field appear under another key?)
       - the structure of `tool_response` (array of {type,text} blocks? a `result` string? does it match the transcript `toolUseResult.content` shape?)
       - whether `hook_event_name` is present in the payload
       - whether `session_id` and `cwd` are present
    5. Save the captured raw JSON (or a representative redacted slice) to `test/fixtures/agent-trace/hook-stdin-envelope.json`.
    6. CLEAN UP: remove the temporary PostToolUse entry from `.claude/settings.json`, delete `hooks/gsd2-agent-trace-debug.js` and `.claude/hooks/gsd2-agent-trace-debug.js`, and delete `/tmp/gsd-hook-debug.json`.

    DECISION RECORDED FOR PLAN 02: confirm whether `matcher: "Task|Agent"` is correct (keep alternation if either/both names appear), and confirm whether extractReturnText's array-first ordering matches the live envelope. If the live envelope differs from the transcript shape, note the corrected field name so Plan 02's extractReturnText reads the right key.
  </action>
  <verify>test -f test/fixtures/agent-trace/hook-stdin-envelope.json && echo "envelope fixture captured"</verify>
  <acceptance_criteria>
    - test/fixtures/agent-trace/hook-stdin-envelope.json exists and is valid JSON (or contains the recorded raw payload)
    - The SUMMARY for this plan records: the observed tool_name string, the tool_response shape, and presence/absence of hook_event_name + session_id + cwd
    - No debug hook remains: `test ! -f hooks/gsd2-agent-trace-debug.js` AND the temporary no-matcher PostToolUse entry is removed from .claude/settings.json
  </acceptance_criteria>
  <done>The live envelope field names are confirmed and recorded; Plan 02 can read tool_response with the correct key and set the matcher with confidence. Debug artifacts removed.</done>
</task>

</tasks>

<verification>
- `node --test test/agent-trace-scraper.test.js` exits 0 (scraper + extract proven against both confidence formats and the content-array shape).
- The hook can be required without hanging: `node -e "require('./hooks/gsd2-agent-trace.js')"` exits 0 fast.
- The live envelope fixture exists and the SUMMARY records the confirmed tool_name + tool_response shape.
- No debug hook or temporary settings entry remains.
</verification>

<success_criteria>
- Both empirical unknowns (tool_name string, tool_response shape) are resolved and recorded — Plan 02 builds on confirmed facts, not assumptions.
- scrapeConfidence and extractReturnText are implemented, exported, and pass unit tests covering prose, JSON, absent, and array-block cases.
- Zero workflow/agent .md prompt files modified.
</success_criteria>

<output>
After completion, create `.planning/v1.5/phases/04-agent-observability-telemetry/04-01-SUMMARY.md`. MUST record the confirmed tool_name string, the tool_response field name/shape, and whether the matcher should remain "Task|Agent".
</output>

## ASSUMPTIONS

> Phase-level assumptions surfaced during planning. Applies to all three Phase 4 plans.

| # | Type | Assumption | Basis / Resolution |
|---|------|-----------|--------------------|
| A1 | technical (evidence-backed) | The hook matcher is `"Task\|Agent"`, NOT the `"Task"` literally named in OBS-01. | OBS-01 wording says `matcher: Task`, but the real transcript (`c5609700-*.jsonl` L185) shows the spawn tool fires as `name: "Agent"` while docs/settings use `Task`. RESEARCH.md Q1 recommends alternation (`Edit\|Write` precedent already in settings.json). This is a requirement-wording divergence resolved by evidence — the mechanism OBS-01 intends is unchanged. Wave-0 Task 3 empirically confirms the live string and may tighten the matcher. |
| A2 | technical (evidence-backed) | The hook stdin `tool_response` is a content-array of `{type,text}` blocks (extractReturnText leads with the array candidate), with a `result`-string fallback. | Transcript `toolUseResult.content` is a content array (L186), but that is the TRANSCRIPT field; the live HOOK ENVELOPE field name is confirmed in Wave-0 Task 3. extractReturnText is written tolerant (array → result-string → stringify) so it degrades safely if the live shape differs. |
| A3 | technical | A single `PostToolUse(Task\|Agent)` record per spawn (collapsing the roadmap's proposed `agent.spawn`+`agent.return` two-event schema into one `agent.return`), plus `PostToolUseFailure` → `agent.error` for crashed spawns. No PreToolUse. | RESEARCH.md Q1: PostToolUse fires once at return carrying BOTH spawn input and return text; `ts_return` suffices for ordering; PreToolUse adds no value. Roadmap two-event schema was explicitly marked "proposed, pending feasibility" in CONTEXT.md open questions. |
| A4 | preference (inherited, not re-litigated) | duration_ms logged as `null` initially. | The transcript shows `totalDurationMs` as a structured field, but whether it survives into the hook envelope is a Wave-0 finding; Plan 02 populates it opportunistically only if confirmed. Non-load-bearing for OBS-01/02. |
| A5 | technical | Wave-0 fixture capture (Task 3) and the live e2e confirmation (Plan 02 Task 3) are checkpoints, NOT auto tasks. | A spawned `gsd-executor` subagent lacks the Task/Agent tool and cannot fire parent hooks (RESEARCH Q4 + issue #34692), so it physically cannot trigger a hook-firing spawn from inside itself. These must run at the orchestrator/human level — Plans 01 and 02 are therefore `autonomous: false`. |

**Invariant self-check (verified against these plans):** No task edits any workflow or agent `.md` prompt file. All observability lives in `hooks/*.js`, `scripts/build-hooks.js`, `bin/install.js`, `.planning/config.json`, `.gitignore`, and `get-shit-done/bin/` (CLI) — code/config only. ✓
