---
phase: 01-security-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-statusline.js
  - hooks/gsd-check-update.js
  - hooks/gsd-context-monitor.js
  - hooks/gsd-workflow-guard.js
  - hooks/dist/gsd-statusline.js
  - hooks/dist/gsd-check-update.js
  - hooks/dist/gsd-context-monitor.js
  - hooks/dist/gsd-workflow-guard.js
  - scripts/build-hooks.js
  - bin/install.js
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Running `npm run build:hooks` regenerates hooks/dist/ with gsd2-* filenames and zero syntax errors"
    - "A fresh `node bin/install.js --claude --local` registers gsd2-* hook commands in settings.json (SessionStart gsd2-check-update, PostToolUse gsd2-context-monitor, statusLine gsd2-statusline)"
    - "An upgrade over a settings.json that already has gsd-* registrations removes the stale gsd-* entries and the stale gsd-statusline.js statusLine command"
    - "No source or dist file under hooks/ is named gsd-*.js after the rename"
  artifacts:
    - hooks/gsd2-statusline.js
    - hooks/gsd2-check-update.js
    - hooks/gsd2-context-monitor.js
    - hooks/gsd2-workflow-guard.js
    - hooks/dist/gsd2-statusline.js
    - hooks/dist/gsd2-check-update.js
    - hooks/dist/gsd2-context-monitor.js
    - hooks/dist/gsd2-workflow-guard.js
  key_links:
    - "scripts/build-hooks.js HOOKS_TO_COPY -> hooks/gsd2-*.js (broken = build copies nothing / skips with warning)"
    - "bin/install.js command builders -> hooks/gsd2-*.js (broken = settings.json points at deleted files)"
    - "bin/install.js orphanedHookPatterns -> old gsd-*.js names (broken = upgrades leave duplicate/orphaned registrations)"
---

<objective>
Rename all 4 existing GSD hooks from the `gsd-*` to the `gsd2-*` filename convention, end to end: the source files in `hooks/`, the built copies in `hooks/dist/`, the `HOOKS_TO_COPY` array in `scripts/build-hooks.js`, and every reference in `bin/install.js` (command builders, registration idempotency checks, uninstall list, statusline-migration). Add the OLD `gsd-*` filenames to upgrade-migration so existing users' `settings.json` registrations and statusLine command are cleaned and re-pointed at the new names.

Why: the fork's hooks currently share filenames with gsd-core (`gsd-*`), so they collide in a shared `~/.claude/hooks/`. `gsd2-*` makes ownership unambiguous and collision-safe. This rename must land FIRST so Plan 02 can author the 3 new security hooks directly into an already-`gsd2` world (purely additive, no re-touch). This plan ships no new hooks — it is a pure rename + migration. SEC-01 (gsd2-* convention) and SEC-02 (build + install wiring) are advanced by this plan; the new hooks themselves arrive in Plan 02.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5/phases/01-security-hooks/01-CONTEXT.md
@scripts/build-hooks.js
@hooks/gsd-workflow-guard.js

## Key facts established during planning (do not re-derive)

- `hooks/dist/` IS committed to git (`.gitignore` line `# hooks/dist/` is commented out). Both `hooks/*.js` and `hooks/dist/*.js` are tracked. Commit BOTH source and dist renames.
- `.claude/` runtime is gitignored — commit only source (`hooks/`, `hooks/dist/`, `scripts/`, `bin/`). `install.js` propagates to runtime; do NOT manually edit `.claude/hooks/`.
- `gsd-workflow-guard.js` is built into dist (it is in HOOKS_TO_COPY) but is NOT registered in `bin/install.js` settings.json anywhere. This is pre-existing. Rename the file + its HOOKS_TO_COPY entry only. Do NOT add a registration block for it to "fix" the gap — that is out of scope.
- There is NO PreToolUse registration block in install.js today. Do not touch PreToolUse in this plan (Plan 02 creates it).
- The `{{GSD_VERSION}}` stamp lives in each hook's `// gsd-hook-version:` line. build-hooks.js replaces it during copy. Renaming files must NOT alter that line — it must survive verbatim.

## Exact install.js reference inventory (verified line numbers, may shift as you edit)

| Line(s) | What | gsd- name(s) present |
|---------|------|----------------------|
| 1936 | `cleanupOrphanedHooks` `orphanedHookPatterns` array | (add old names here) |
| 1976-1981 | statusLine migration regex (`hooks/statusline.js` -> `gsd-statusline.js`) | gsd-statusline.js (target) |
| 2200 | uninstall `gsdHooks` array | gsd-statusline.js, gsd-check-update.js, gsd-check-update.sh, gsd-context-monitor.js |
| 2239 | uninstall statusLine cleanup `.includes('gsd-statusline')` | gsd-statusline |
| 2252 | uninstall SessionStart cleanup `.includes('gsd-check-update') / gsd-statusline` | both |
| 2275 | uninstall PostToolUse cleanup `.includes('gsd-context-monitor')` | gsd-context-monitor |
| 3119-3120 | statuslineCommand builder | gsd-statusline.js |
| 3122-3123 | updateCheckCommand builder | gsd-check-update.js |
| 3125-3126 | contextMonitorCommand builder | gsd-context-monitor.js |
| 3148-3149 | SessionStart idempotency `.includes('gsd-check-update')` | gsd-check-update |
| 3169-3170 | PostToolUse idempotency `.includes('gsd-context-monitor')` | gsd-context-monitor |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the 4 hook source + dist files and update build-hooks.js</name>
  <files>hooks/gsd2-statusline.js, hooks/gsd2-check-update.js, hooks/gsd2-context-monitor.js, hooks/gsd2-workflow-guard.js, hooks/dist/*, scripts/build-hooks.js</files>
  <read_first>
    - hooks/gsd-workflow-guard.js (confirm the `// gsd-hook-version: {{GSD_VERSION}}` line shape that must survive)
    - scripts/build-hooks.js (HOOKS_TO_COPY array lines 17-22)
    - Run `git ls-files hooks/` to see the exact tracked files
  </read_first>
  <action>
  Git-rename all 8 tracked hook files (4 source + 4 dist) from gsd-* to gsd2-*. Use `git mv` so history is preserved:
    git mv hooks/gsd-statusline.js hooks/gsd2-statusline.js
    git mv hooks/gsd-check-update.js hooks/gsd2-check-update.js
    git mv hooks/gsd-context-monitor.js hooks/gsd2-context-monitor.js
    git mv hooks/gsd-workflow-guard.js hooks/gsd2-workflow-guard.js
    git mv hooks/dist/gsd-statusline.js hooks/dist/gsd2-statusline.js
    git mv hooks/dist/gsd-check-update.js hooks/dist/gsd2-check-update.js
    git mv hooks/dist/gsd-context-monitor.js hooks/dist/gsd2-context-monitor.js
    git mv hooks/dist/gsd-workflow-guard.js hooks/dist/gsd2-workflow-guard.js
  Do NOT edit the body of these files in this task except: grep each renamed source file for any self-reference to its OWN old `gsd-*` filename in comments/strings and update to `gsd2-*` (e.g. a header comment that names the file). Do NOT touch the `// gsd-hook-version: {{GSD_VERSION}}` line — it must remain exactly `// gsd-hook-version: {{GSD_VERSION}}` (or the already-stamped version in dist).
  Then update scripts/build-hooks.js HOOKS_TO_COPY (lines 17-22) so all four entries read the gsd2-* names:
    const HOOKS_TO_COPY = [
      'gsd2-check-update.js',
      'gsd2-context-monitor.js',
      'gsd2-statusline.js',
      'gsd2-workflow-guard.js'
    ];
  </action>
  <verify>
  `ls hooks/*.js hooks/dist/*.js | grep -c 'gsd-'` returns 0 (zero remaining gsd-* files).
  `grep -c 'gsd2-' scripts/build-hooks.js` returns >= 4.
  `grep -c "gsd-" scripts/build-hooks.js` returns 0 (no stale gsd- entries in HOOKS_TO_COPY or comments).
  `npm run build:hooks` exits 0 and prints "Build complete." with no SyntaxError lines.
  </verify>
  <acceptance_criteria>
    - hooks/gsd2-statusline.js, hooks/gsd2-check-update.js, hooks/gsd2-context-monitor.js, hooks/gsd2-workflow-guard.js all exist
    - hooks/dist/gsd2-statusline.js, hooks/dist/gsd2-check-update.js, hooks/dist/gsd2-context-monitor.js, hooks/dist/gsd2-workflow-guard.js all exist
    - No file matching hooks/gsd-*.js or hooks/dist/gsd-*.js exists
    - scripts/build-hooks.js HOOKS_TO_COPY contains 'gsd2-check-update.js', 'gsd2-context-monitor.js', 'gsd2-statusline.js', 'gsd2-workflow-guard.js'
    - `npm run build:hooks` exits 0
    - Each hooks/gsd2-*.js source still contains the literal string `{{GSD_VERSION}}` on its `// gsd-hook-version:` line (grep `grep -l '{{GSD_VERSION}}' hooks/gsd2-*.js` lists all 4)
  </acceptance_criteria>
  <done>All 4 hooks renamed in source and dist with git history preserved; build-hooks.js HOOKS_TO_COPY references only gsd2-* names; npm run build:hooks succeeds; the version stamp placeholder survives.</done>
</task>

<task type="auto">
  <name>Task 2: Update all bin/install.js references and add upgrade migration for old gsd-* names</name>
  <files>bin/install.js</files>
  <read_first>
    - bin/install.js lines 1915-1985 (cleanupOrphanedFiles, cleanupOrphanedHooks, orphanedHookPatterns, statusLine migration regex)
    - bin/install.js lines 2195-2285 (uninstall path: gsdHooks array + statusLine/SessionStart/PostToolUse cleanup)
    - bin/install.js lines 3113-3187 (command builders + SessionStart/PostToolUse registration idempotency checks)
    - The reference inventory table in this plan's <context> block
  </read_first>
  <action>
  Replace every `gsd-` hook filename reference in bin/install.js with its `gsd2-` equivalent, AND add the old names to upgrade migration so existing installs are cleaned. Specific edits:

  1. Command builders (~lines 3119-3126): change `'gsd-statusline.js'` -> `'gsd2-statusline.js'`, `'gsd-check-update.js'` -> `'gsd2-check-update.js'`, `'gsd-context-monitor.js'` -> `'gsd2-context-monitor.js'` in both the buildHookCommand call AND the local `'node ' + dirName + '/hooks/gsd-*.js'` fallback string.

  2. Registration idempotency checks: `.includes('gsd-check-update')` (~line 3149) -> `.includes('gsd2-check-update')`; `.includes('gsd-context-monitor')` (~line 3170) -> `.includes('gsd2-context-monitor')`.

  3. orphanedHookPatterns array (~line 1934-1940): ADD the four OLD source-relative names so upgrades drop their stale registrations from settings.json. Append these entries (keep existing entries):
       'gsd-statusline.js',  // Renamed to gsd2-statusline.js in v1.5
       'gsd-check-update.js',  // Renamed to gsd2-check-update.js in v1.5
       'gsd-context-monitor.js',  // Renamed to gsd2-context-monitor.js in v1.5
       'gsd-workflow-guard.js',  // Renamed to gsd2-workflow-guard.js in v1.5
     CAUTION: these patterns are matched with `.includes()` against the full command string. `gsd-statusline.js` will NOT accidentally match `gsd2-statusline.js` (the substring 'gsd-statusline.js' is not present in a 'gsd2-statusline.js' path). Confirm by inspection.

  4. statusLine command upgrade migration (~lines 1975-1981): the existing block only rewrites pre-v1.9 `hooks/statusline.js` -> `gsd-statusline.js`. statusLine is stored in `settings.statusLine.command`, NOT in `settings.hooks`, so cleanupOrphanedHooks's filter does NOT touch it and handleStatusline SKIPS replacement on upgrade (existing statusline detected). Add a SECOND rewrite block immediately after the existing one, mirroring its shape, to re-point a stale gsd- statusline:
       if (settings.statusLine && settings.statusLine.command &&
           /hooks[\/\\]gsd-statusline\.js/.test(settings.statusLine.command)) {
         settings.statusLine.command = settings.statusLine.command.replace(
           /hooks([\/\\])gsd-statusline\.js/,
           'hooks$1gsd2-statusline.js'
         );
         console.log(`  ${green}✓${reset} Updated statusline path (gsd-statusline.js → gsd2-statusline.js)`);
       }
     (Use the same green/reset console pattern as the surrounding code; the checkmark char matches the file's existing style.)

  5. Uninstall path (~lines 2200, 2239, 2252, 2275): update the gsdHooks array and the three `.includes(...)` cleanup checks to gsd2-* names so uninstall removes the current hooks. For maximum safety on machines that still have old files, ALSO keep the old gsd-* names in the uninstall gsdHooks file-removal array (so uninstall after a partial upgrade still removes stale files). Concretely: gsdHooks at ~2200 becomes the union, e.g. ['gsd2-statusline.js','gsd2-check-update.js','gsd2-context-monitor.js','gsd-statusline.js','gsd-check-update.js','gsd-check-update.sh','gsd-context-monitor.js']. For the `.includes()` cleanup checks at 2239/2252/2275, match BOTH old and new: e.g. `(h.command.includes('gsd-context-monitor') || h.command.includes('gsd2-context-monitor'))`.

  Do NOT add any PreToolUse handling, and do NOT add a registration for workflow-guard — both are out of scope for this plan.
  </action>
  <verify>
  `grep -n "gsd-statusline.js\|gsd-check-update.js\|gsd-context-monitor.js\|gsd-workflow-guard.js" bin/install.js` shows the old names ONLY inside orphanedHookPatterns, the statusLine migration regex, and the uninstall arrays/checks (NOT in command builders or registration idempotency checks).
  `grep -c "gsd2-statusline.js\|gsd2-check-update.js\|gsd2-context-monitor.js" bin/install.js` returns >= 6 (3 command builders x2 forms minimum).
  `node -c bin/install.js` (or `node --check bin/install.js`) exits 0 (no syntax error).
  </verify>
  <acceptance_criteria>
    - bin/install.js command builders reference 'gsd2-statusline.js', 'gsd2-check-update.js', 'gsd2-context-monitor.js' (both buildHookCommand and fallback string forms)
    - bin/install.js SessionStart idempotency check uses `.includes('gsd2-check-update')`
    - bin/install.js PostToolUse idempotency check uses `.includes('gsd2-context-monitor')`
    - bin/install.js orphanedHookPatterns contains 'gsd-statusline.js', 'gsd-check-update.js', 'gsd-context-monitor.js', 'gsd-workflow-guard.js'
    - bin/install.js contains a statusLine migration block matching /hooks[\/\\]gsd-statusline\.js/ that rewrites to gsd2-statusline.js
    - `node --check bin/install.js` exits 0
  </acceptance_criteria>
  <done>Every install.js command builder and registration check points at gsd2-* names; old gsd-* names are added to orphan cleanup and statusLine migration so upgrades self-heal; the uninstall path removes both old and new files; install.js parses without error.</done>
</task>

<task type="auto">
  <name>Task 3: Verify fresh-install and upgrade-migration behaviour with throwaway settings.json</name>
  <files>bin/install.js (no edit — verification harness only; temp scratch dirs)</files>
  <read_first>
    - bin/install.js lines 3113-3187 (the function that writes settings.hooks)
    - bin/install.js cleanupOrphanedHooks (~1933) and the statusLine migration (~1975)
  </read_first>
  <action>
  Run two automated simulations in a temp directory to prove both the fresh-install and the upgrade paths. Do NOT modify the user's real ~/.claude. Use a scratch target dir.

  FRESH INSTALL simulation:
    Create a temp dir: `TMP=$(mktemp -d)`. LOCAL install writes to `process.cwd()/.claude` (see install.js ~lines 2001-2003), so the reliable temp-target is to run the install with cwd set to $TMP — do NOT hunt for a --config-dir flag and do NOT `require()` install.js (it runs main() on load with side effects):
      (cd "$TMP" && node /home/cleversol/gsd2/mine/bin/install.js --claude --local)
    The `--claude` runtime flag skips the interactive promptRuntime, so this runs non-interactively. After install, read `$TMP/.claude/settings.json` and assert:
      - settings.statusLine.command contains 'gsd2-statusline.js'
      - SessionStart entry command contains 'gsd2-check-update.js'
      - PostToolUse entry command contains 'gsd2-context-monitor.js'
      - no command anywhere contains the substring 'gsd-statusline.js' / 'gsd-check-update.js' / 'gsd-context-monitor.js'

  UPGRADE simulation (this is the bug-catching path — a fresh install will NOT exercise it):
    In a second temp target, pre-seed a settings.json that mimics an OLD install BEFORE running install:
      {
        "statusLine": { "type": "command", "command": "node /old/hooks/gsd-statusline.js" },
        "hooks": {
          "SessionStart": [ { "hooks": [ { "type": "command", "command": "node /old/hooks/gsd-check-update.js" } ] } ],
          "PostToolUse": [ { "hooks": [ { "type": "command", "command": "node /old/hooks/gsd-context-monitor.js" } ] } ]
        }
      }
    Pre-seed it at `$TMP2/.claude/settings.json`, then run `(cd "$TMP2" && node /home/cleversol/gsd2/mine/bin/install.js --claude --local)`. CONFIRM it does NOT hang: handleStatusline sees the pre-seeded statusLine and, because the run is non-interactive (--claude flag), takes the "already configured, skipping" branch rather than prompting — but the statusLine MIGRATION block (1975+) still rewrites the command in place. Then re-read settings.json and assert:
      - statusLine.command now contains 'gsd2-statusline.js' (migration re-pointed it)
      - statusLine.command does NOT contain 'gsd-statusline.js'
      - SessionStart no longer contains a command with 'gsd-check-update.js' (orphan cleanup removed it) and DOES contain 'gsd2-check-update.js'
      - PostToolUse no longer contains 'gsd-context-monitor.js' and DOES contain 'gsd2-context-monitor.js'

  Document in the SUMMARY the exact install invocation used and the full settings.json from both simulations.
  </action>
  <verify>
  Both simulations complete and every assertion above holds. Capture the final settings.json contents from each simulation in the task output / SUMMARY.
  Clean up temp dirs after (`rm -rf "$TMP"`).
  </verify>
  <acceptance_criteria>
    - Fresh-install settings.json: statusLine + SessionStart + PostToolUse all reference gsd2-* and zero gsd-* hook filenames
    - Upgrade settings.json: pre-seeded gsd-* statusLine.command rewritten to gsd2-statusline.js
    - Upgrade settings.json: pre-seeded gsd-* SessionStart/PostToolUse registrations removed; gsd2-* registrations present
    - No assertion failed; temp dirs removed
  </acceptance_criteria>
  <done>Empirical proof that a fresh install registers gsd2-* hooks and an upgrade over a gsd-* settings.json removes the stale registrations AND re-points the statusLine command — the riskiest part of the rename is verified, not assumed.</done>
</task>

</tasks>

<verification>
- `npm run build:hooks` exits 0 and hooks/dist/ contains only gsd2-*.js files
- `node --check bin/install.js` exits 0
- `ls hooks/*.js hooks/dist/*.js | grep gsd- | grep -v gsd2-` returns nothing
- Fresh-install and upgrade simulations from Task 3 both pass all assertions
- `grep -rn "gsd-statusline\|gsd-check-update\|gsd-context-monitor\|gsd-workflow-guard" bin/ scripts/ hooks/ | grep -v gsd2-` shows old names ONLY in install.js migration/uninstall/orphan-cleanup contexts (intentional back-compat), nowhere else
</verification>

<success_criteria>
- All 7 references-classes in install.js (command builders, idempotency checks, orphan patterns, statusLine migration, uninstall array, uninstall checks) updated
- The 4 hooks exist as gsd2-* in both hooks/ and hooks/dist/ with git history preserved and {{GSD_VERSION}} stamp intact
- Upgrade migration removes stale gsd-* registrations and re-points statusLine — proven by Task 3 upgrade simulation
- No new hooks added (those are Plan 02); no PreToolUse block added; workflow-guard registration gap left as-is
</success_criteria>


## ASSUMPTIONS

- **Local install targets `process.cwd()/.claude`** (install.js ~2001-2003). Task 3 sets cwd to a temp dir rather than using any config-dir flag. If a future install.js refactor changes the local-target resolution, Task 3 invocation must be revisited.
- **`--claude --local` runs non-interactively** (the runtime flag skips promptRuntime; pre-seeded statusLine hits the non-interactive "already configured" skip in handleStatusline). If the install grows a mandatory prompt, the upgrade sim would hang and must pass an answer on stdin.
- **`hooks/dist/` is committed** (the `.gitignore` line is commented out), so both source and dist renames are committed artifacts. If dist is later gitignored, the dist git mv becomes a runtime-only regeneration.
- **`gsd-statusline.js` substring does not collide with `gsd2-statusline.js`** under `.includes()` matching, so adding old names to orphanedHookPatterns will not accidentally strip new registrations. Verified by inspection during planning.
- **gsd2-workflow-guard remains unregistered in settings.json** — this is the pre-existing state, intentionally preserved. Wiring it up is out of scope for this phase.

<output>
After completion, create `.planning/v1.5/phases/01-security-hooks/01-01-SUMMARY.md` documenting: exact files renamed, every install.js edit location, how the install was invoked in Task 3, and the final settings.json from both simulations.
</output>
