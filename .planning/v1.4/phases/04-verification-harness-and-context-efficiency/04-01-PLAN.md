---
phase: 04-verification-harness-and-context-efficiency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-shit-done/bin/lib/init.cjs
  - .claude/get-shit-done/bin/lib/roadmap.cjs
  - .claude/get-shit-done/bin/lib/core.cjs
  - .claude/get-shit-done/bin/lib/phase.cjs
  - .claude/commands/gsd2/progress.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - truth: "init progress accepts --scoped flag and returns phases trimmed to current ±1 / next ±1 (≤4 entries)"
      artifacts:
        - path: .claude/get-shit-done/bin/lib/init.cjs
          provides: cmdInitProgress with --scoped support
          contains: "--scoped"
      verify:
        - cmd: "node .claude/get-shit-done/bin/gsd-tools.cjs init progress --scoped --raw | jq '.phases | length'"
          expect: "3"
          type: integration
    - truth: "roadmap analyze accepts --scoped flag and returns trimmed phase slice"
      artifacts:
        - path: .claude/get-shit-done/bin/lib/roadmap.cjs
          provides: cmdRoadmapAnalyze with --scoped support
          contains: "--scoped"
      verify:
        - cmd: "node .claude/get-shit-done/bin/gsd-tools.cjs roadmap analyze --scoped --raw | jq '.phases | length'"
          expect: "3"
          type: integration
    - truth: "newly generated phase slugs are capped at ≤45 chars and contain no trailing hyphens"
      artifacts:
        - path: .claude/get-shit-done/bin/lib/core.cjs
          provides: generateSlugInternal with length cap
          contains: "slice(0, 45)"
      verify: []  # Static check via artifacts.contains "slice(0, 45)" above. Dynamic verification skipped: harness parser does not unescape YAML \" so embedded-quote `node -e` cmds fail. Tracked as harness tech debt.
    - truth: "progress.md is no longer @-included in the gsd2 command file (Read-tool injection only)"
      artifacts:
        - path: .claude/commands/gsd2/progress.md
          provides: command stub without redundant @-include of workflow
      verify:
        - cmd: "grep -cE '@.*workflows/progress\\.md' .claude/commands/gsd2/progress.md"
          expect: "0"
          type: unit
  artifacts:
    - path: .claude/get-shit-done/bin/lib/init.cjs
      provides: cmdInitProgress
      contains: "--scoped"
    - path: .claude/get-shit-done/bin/lib/roadmap.cjs
      provides: cmdRoadmapAnalyze
      contains: "--scoped"
    - path: .claude/get-shit-done/bin/lib/core.cjs
      provides: generateSlugInternal
      contains: "slice(0, 45)"
    - path: .claude/commands/gsd2/progress.md
      provides: gsd2:progress command without double include
  key_links:
    - from: .claude/commands/gsd2/progress.md
      to: .claude/get-shit-done/workflows/progress.md
      via: Read-tool injection (single path; no @-include)
      pattern: "Read.*progress\\.md"
    - from: .claude/get-shit-done/workflows/progress.md
      to: .claude/get-shit-done/bin/lib/init.cjs
      via: "init progress --scoped" CLI call
      pattern: "init progress --scoped"
---

<objective>
Cut roughly 13k tokens off every `/gsd2:progress` invocation by applying three independent, mechanical fixes — data scoping in `init progress` and `roadmap analyze`, a 45-char cap on new phase slugs, and removing the duplicate @-include of `progress.md` from the command file. No agentic logic; pure tooling cleanup.
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-CONTEXT.md
@.planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md
</context>

<interfaces>
**Existing function locations (from RESEARCH.md §5):**
- `cmdInitProgress` — `.claude/get-shit-done/bin/lib/init.cjs:799` (returns full `phases[]` at line ~923)
- `cmdRoadmapAnalyze` — `.claude/get-shit-done/bin/lib/roadmap.cjs:93` (returns full `phases[]` at line ~212)
- `generateSlugInternal` — `.claude/get-shit-done/bin/lib/core.cjs:704` (currently `text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '')`)
- `cmdPhaseAdd` calls slug helper — `.claude/get-shit-done/bin/lib/phase.cjs:324`

**CLI plumbing for new flags:** `gsd-tools.cjs` parses `process.argv` and forwards through to subcommand functions. Add `--scoped` to the argv parsing path used by `init progress` and `roadmap analyze` and pipe a boolean kwarg into the subcommand.

**Current progress.md command file (`.claude/commands/gsd2/progress.md`):**
- Line 18: `@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/progress.md`
- Line 22: prose instruction to execute the workflow from the same path
The Read-tool injection from the prose instruction overlaps with the @-include — remove the @-include line.
</interfaces>

<tasks>

<task type="auto" verify_after="true">
  <name>Task 1: Add --scoped flag to init progress and roadmap analyze (data-scoping fix)</name>
  <files>.claude/get-shit-done/bin/lib/init.cjs, .claude/get-shit-done/bin/lib/roadmap.cjs, .claude/get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - .claude/get-shit-done/bin/lib/init.cjs (cmdInitProgress around line 799; phase array assembly around line 923)
    - .claude/get-shit-done/bin/lib/roadmap.cjs (cmdRoadmapAnalyze around line 93; phase array around line 212)
    - .claude/get-shit-done/bin/gsd-tools.cjs (argv parsing for `init progress` and `roadmap analyze`)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md (§5)
  </read_first>
  <action>
1. In `.claude/get-shit-done/bin/gsd-tools.cjs`, extend the argv parser for the `init progress` and `roadmap analyze` subcommand paths to detect a `--scoped` flag and pass `{ scoped: true }` (or equivalent positional argument) to `cmdInitProgress` / `cmdRoadmapAnalyze`. Do NOT change behavior when the flag is absent.
2. In `.claude/get-shit-done/bin/lib/init.cjs::cmdInitProgress`, accept the new `scoped` boolean. When `scoped === true` AND the function would normally return a full `phases[]` array, replace it with the trimmed slice `[current_index - 1, current_index, current_index + 1, current_index + 2]` filtered to in-bounds indices (clamp at 0 and `phases.length`). Keep `current_phase` and `next_phase` scalars unchanged. The result must contain at most 4 phase entries.
3. In `.claude/get-shit-done/bin/lib/roadmap.cjs::cmdRoadmapAnalyze`, accept the same `scoped` boolean and apply the same trimming logic to the returned `phases[]` array. Preserve all other top-level fields (totals, status, etc.).
4. No changes to default (un-scoped) behavior — existing callers must not break.
  </action>
  <verify>
node .claude/get-shit-done/bin/gsd-tools.cjs init progress --scoped --raw | jq '.phases | length' &&
node .claude/get-shit-done/bin/gsd-tools.cjs roadmap analyze --scoped --raw | jq '.phases | length' &&
node .claude/get-shit-done/bin/gsd-tools.cjs init progress --raw | jq '.phases | length'
  </verify>
  <acceptance_criteria>
    - `grep -q "scoped" .claude/get-shit-done/bin/lib/init.cjs` returns 0 (success)
    - `grep -q "scoped" .claude/get-shit-done/bin/lib/roadmap.cjs` returns 0
    - `node .claude/get-shit-done/bin/gsd-tools.cjs init progress --scoped --raw | jq '.phases | length'` returns an integer ≤ 4
    - `node .claude/get-shit-done/bin/gsd-tools.cjs roadmap analyze --scoped --raw | jq '.phases | length'` returns an integer ≤ 4
    - `node .claude/get-shit-done/bin/gsd-tools.cjs init progress --raw | jq '.phases | length'` (no flag) still returns the full project phase count (regression check)
    - `node .claude/get-shit-done/bin/gsd-tools.cjs init progress --scoped --raw | jq -e '.current_phase'` exits 0 (scalar preserved)
  </acceptance_criteria>
  <done>Both subcommands accept `--scoped`, return ≤4 phase entries when scoped, and preserve full output when un-scoped.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 2: Cap new phase slugs at 45 chars in generateSlugInternal</name>
  <files>.claude/get-shit-done/bin/lib/core.cjs</files>
  <read_first>
    - .claude/get-shit-done/bin/lib/core.cjs (generateSlugInternal at line 704)
    - .claude/get-shit-done/bin/lib/phase.cjs (cmdPhaseAdd around line 311; generateSlugInternal call at line 324)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md (§5 — exact slice value 45)
  </read_first>
  <action>
In `.claude/get-shit-done/bin/lib/core.cjs::generateSlugInternal` (line 704), append a length cap and trailing-hyphen strip after the existing transformation. Replace:

```js
return text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '');
```

with:

```js
const raw = text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '');
return raw.slice(0, 45).replace(/-+$/, '');
```

Do NOT touch any other call site. Do NOT rename existing phase directories on disk — the cap applies to NEW slugs only.
  </action>
  <verify>
node -e "const {generateSlugInternal}=require('./.claude/get-shit-done/bin/lib/core.cjs'); const s=generateSlugInternal('a really long phase title that exceeds the cap and keeps going on and on for many more characters'); console.log('len=',s.length,'tail=',s.slice(-1),'slug=',s)"
  </verify>
  <acceptance_criteria>
    - `grep -q "slice(0, 45)" .claude/get-shit-done/bin/lib/core.cjs` returns 0
    - `grep -q "replace(/-+\\$/" .claude/get-shit-done/bin/lib/core.cjs` returns 0
    - Calling `generateSlugInternal` with a 200-char string returns a slug whose `.length <= 45`
    - The returned slug does not end with `-` (strip-trailing-hyphen step is applied AFTER the slice)
    - `generateSlugInternal('short title')` still returns `'short-title'` (no behavior change for short inputs)
    - No other source files under `.claude/get-shit-done/bin/lib/` are modified by this task (`git diff --stat .claude/get-shit-done/bin/lib/` shows only `core.cjs`)
  </acceptance_criteria>
  <done>generateSlugInternal returns ≤45-char slugs with no trailing hyphens; phase.cjs is unchanged.</done>
</task>

<task type="auto" verify_after="true">
  <name>Task 3: Remove duplicate progress.md @-include from gsd2 command file</name>
  <files>.claude/commands/gsd2/progress.md</files>
  <read_first>
    - .claude/commands/gsd2/progress.md (current contents — locate the `@.../workflows/progress.md` @-include line and the prose instruction that already references the same path)
    - .claude/get-shit-done/workflows/progress.md (top of file — confirms `<required_reading>` block instructs the agent to read referenced files, so Read-tool injection covers the load path)
    - .planning/phases/04-verification-harness-and-context-efficiency/04-RESEARCH.md (§5 — double @-include analysis)
  </read_first>
  <action>
In `.claude/commands/gsd2/progress.md`, delete the line that begins with `@` and points to `.claude/get-shit-done/workflows/progress.md` (currently around line 18). Keep the prose instruction that tells the agent to execute the workflow at that path — the Read tool will inject the workflow contents once on first read, eliminating the duplicate.

Do NOT remove the prose execution instruction. Do NOT edit `.claude/get-shit-done/workflows/progress.md` itself.

After the edit: the command file must contain ZERO `@`-prefixed references to `workflows/progress.md`, but must still contain the prose path reference (string `workflows/progress.md` without leading `@` is fine).
  </action>
  <verify>
grep -cE '@.*workflows/progress\.md' .claude/commands/gsd2/progress.md && grep -c 'workflows/progress.md' .claude/commands/gsd2/progress.md
  </verify>
  <acceptance_criteria>
    - `grep -cE '@.*workflows/progress\.md' .claude/commands/gsd2/progress.md` returns `0`
    - `grep -c 'workflows/progress.md' .claude/commands/gsd2/progress.md` returns ≥1 (prose reference preserved)
    - `git diff --stat .claude/commands/gsd2/` shows ONLY `progress.md` modified
    - No changes to `.claude/get-shit-done/workflows/progress.md` (`git diff --stat .claude/get-shit-done/workflows/progress.md` is empty)
    - File still parses as valid markdown (no broken frontmatter — line count delta is exactly -1 line)
  </acceptance_criteria>
  <done>The command file no longer @-includes the workflow; only the Read-tool path remains.</done>
</task>

</tasks>

<verification>
1. All three subcommands work: `init progress --scoped`, `roadmap analyze --scoped`, plus un-scoped (regression).
2. Slug cap unit check: programmatic call to `generateSlugInternal` with a long string returns ≤45 chars.
3. Double-include eliminated: `grep -cE '@.*workflows/progress\.md' .claude/commands/gsd2/progress.md` is `0`.
4. Whole-suite sanity: `node .claude/get-shit-done/bin/gsd-tools.cjs init progress --raw` (un-scoped) still produces valid JSON.
</verification>

<success_criteria>
- Token-payload measurement target (CONTEXT §"Total expected savings: ~13k tokens"): `node .claude/get-shit-done/bin/gsd-tools.cjs roadmap analyze --raw | wc -c` (un-scoped) minus the `--scoped` variant should produce a multi-kilobyte difference (qualitative — actual ratio depends on phase count).
- Backward compatibility: every existing caller of `init progress` / `roadmap analyze` continues to work without code changes (no callers updated in this plan).
- New phase directories created after this plan ships have slug names ≤45 chars; existing directories are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-and-context-efficiency/04-01-SUMMARY.md` describing:
- The three fixes applied (file:line citations)
- Token-savings measurement (before/after byte counts of `roadmap analyze --raw`)
- Confirmation that no existing callers were modified (only the producers/consumers of the new flag)
</output>
