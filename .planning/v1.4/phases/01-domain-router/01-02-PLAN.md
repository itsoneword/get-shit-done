---
phase: 01-domain-router
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - get-shit-done/workflows/plan-phase.md
autonomous: true
requirements:
  - DRTR-04
user_setup: []
must_haves:
  truths:
    - "Plan-phase step 5.6 reads 'Detected domain:' from CONTEXT.md instead of running keyword grep on ROADMAP phase section"
    - "When CONTEXT.md exists with domain field, plan-phase does NOT ask the user about UI design contract if domain was already classified"
    - "When CONTEXT.md does NOT exist (backward compat), plan-phase falls back to the original keyword grep + yes/no gate behavior"
    - "Agentic domain is recognized but no-ops (Phase 2 hook point with commented stub)"
  artifacts:
    - get-shit-done/workflows/plan-phase.md
  key_links:
    - "plan-phase step 5.6 grep for '**Detected domain:**' in CONTEXT.md -> UI-SPEC artifact check (broken = ignores domain classification, falls back to keyword grep)"
    - "plan-phase backward compat branch -> original keyword grep (broken = old phases without CONTEXT.md lose UI-SPEC gate)"
---

<objective>
Replace the hardcoded UI keyword grep + yes/no gate in plan-phase.md step 5.6 with a domain-aware artifact existence check. When CONTEXT.md exists (from discuss-phase router in Plan 01), plan-phase reads the `Detected domain:` field and checks for the corresponding spec artifact (UI-SPEC.md for UI domain). When CONTEXT.md does not exist (backward compatibility), it falls back to the original keyword grep behavior. This eliminates double-classification (discuss-phase + plan-phase both asking about domain) and creates the hook point for Phase 2's AGENT-SPEC.
</objective>

<execution_context>
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsoneword/Downloads/devProjects/GSD/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-router/01-CONTEXT.md
@.planning/phases/01-domain-router/01-RESEARCH.md
@.planning/phases/01-domain-router/01-01-PLAN.md
</context>

<interfaces>
Plan 01 establishes this CONTEXT.md `<domain>` format:

```markdown
<domain>
## Phase Boundary

[prose description]

**Detected domain:** UI | Agentic | Generic | UI+Agentic
**Evidence:** [comma-separated signals]
**Confirmed by user:** yes | overridden to [domain]

</domain>
```

The grep pattern to extract domain is: `grep "^\*\*Detected domain:\*\*" "${CONTEXT_PATH}"`
This returns a line like: `**Detected domain:** UI` or `**Detected domain:** UI+Agentic`

Current plan-phase step 5.6 (to be replaced) lives between step 5.5 (Validation Strategy Gate) and step 6 (Check Existing Plans). It:
1. Reads `workflow.ui_phase` and `workflow.ui_safety_gate` from config
2. Greps ROADMAP phase section for UI keywords
3. If keywords found and no UI-SPEC.md exists, asks user via AskUserQuestion
4. Offers "Generate UI-SPEC first", "Continue without UI-SPEC", "Not a frontend phase"
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Rewrite plan-phase step 5.6 from keyword grep to domain-aware artifact check</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <read_first>
    - get-shit-done/workflows/plan-phase.md (full file -- understand step 5.6 current implementation and surrounding context)
    - .planning/phases/01-domain-router/01-RESEARCH.md (Plan-Phase Step 5.6 Changes section with replacement logic)
    - .planning/phases/01-domain-router/01-CONTEXT.md (locked decision: plan-phase keeps lightweight guard, file-existence check not full classifier)
  </read_first>
  <action>
Replace the entire content of step 5.6 ("## 5.6. UI Design Contract Gate") in `get-shit-done/workflows/plan-phase.md`. Keep the heading and the skip condition for config. Replace everything after the config check.

New step 5.6 content:

```markdown
## 5.6. UI Design Contract Gate

> Skip if `workflow.ui_phase` is explicitly `false` AND `workflow.ui_safety_gate` is explicitly `false` in `.planning/config.json`. If keys are absent, treat as enabled.

```bash
UI_PHASE_CFG=$(node "~/.claude/get-shit-done/bin/gsd-tools.cjs" config-get workflow.ui_phase 2>/dev/null || echo "true")
UI_GATE_CFG=$(node "~/.claude/get-shit-done/bin/gsd-tools.cjs" config-get workflow.ui_safety_gate 2>/dev/null || echo "true")
```

**If both are `false`:** Skip to step 6.

**Domain-aware classification (preferred path):**

Check if CONTEXT.md exists and has a domain classification from discuss-phase:

```bash
DETECTED_DOMAIN=""
if [[ -n "$CONTEXT_PATH" ]] && [[ -f "$CONTEXT_PATH" ]]; then
  DETECTED_DOMAIN=$(grep "^\*\*Detected domain:\*\*" "${CONTEXT_PATH}" 2>/dev/null | sed 's/.*\*\* //' | tr -d '\r')
fi
```

**If `DETECTED_DOMAIN` is non-empty (CONTEXT.md has domain classification):**

Use the domain classification from discuss-phase. No keyword grep needed.

- **If `DETECTED_DOMAIN` contains "UI"** (matches "UI" or "UI+Agentic"):
  ```bash
  UI_SPEC_FILE=$(ls "${PHASE_DIR}"/*-UI-SPEC.md 2>/dev/null | head -1)
  ```
  - **If UI-SPEC.md found:** Set `UI_SPEC_PATH=$UI_SPEC_FILE`. Display: `Using UI design contract: ${UI_SPEC_PATH}`
  - **If UI-SPEC.md missing AND `UI_GATE_CFG` is `true`:**
    Use AskUserQuestion:
    - header: "UI Design Contract"
    - question: "Phase {N} is classified as {DETECTED_DOMAIN} but has no UI-SPEC.md. Generate a design contract before planning?"
    - options:
      - "Generate UI-SPEC first" -> Display: "Run `/gsd2:ui-phase {N}` then re-run `/gsd2:plan-phase {N}`". Exit workflow.
      - "Continue without UI-SPEC" -> Continue to step 6.

- **If `DETECTED_DOMAIN` contains "Agentic"** (matches "Agentic" or "UI+Agentic"):
  ```bash
  AGENT_SPEC_FILE=$(ls "${PHASE_DIR}"/*-AGENT-SPEC.md 2>/dev/null | head -1)
  ```
  - **If AGENT-SPEC.md found:** Set `AGENT_SPEC_PATH=$AGENT_SPEC_FILE`. Display: `Using agent spec: ${AGENT_SPEC_PATH}`
  - **If AGENT-SPEC.md missing:** Skip silently. (AGENT-SPEC workflow is delivered in Phase 2.)

- **If `DETECTED_DOMAIN` is "Generic":** Skip silently to step 6.

**Fallback path (no CONTEXT.md or no domain field -- backward compatibility):**

**If `DETECTED_DOMAIN` is empty (no CONTEXT.md or no domain classification):**

Fall back to keyword grep on ROADMAP phase section (original behavior):

```bash
PHASE_SECTION=$(node "~/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap get-phase "${PHASE}" 2>/dev/null)
echo "$PHASE_SECTION" | grep -iE "UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget" > /dev/null 2>&1
HAS_UI=$?
```

**If `HAS_UI` is 0 (frontend indicators found):**

Check for existing UI-SPEC:
```bash
UI_SPEC_FILE=$(ls "${PHASE_DIR}"/*-UI-SPEC.md 2>/dev/null | head -1)
```

**If UI-SPEC.md found:** Set `UI_SPEC_PATH=$UI_SPEC_FILE`. Display: `Using UI design contract: ${UI_SPEC_PATH}`

**If UI-SPEC.md missing AND `UI_GATE_CFG` is `true`:**

Use AskUserQuestion:
- header: "UI Design Contract"
- question: "Phase {N} has frontend indicators but no UI-SPEC.md. Generate a design contract before planning?"
- options:
  - "Generate UI-SPEC first" -> Display: "Run `/gsd2:ui-phase {N}` then re-run `/gsd2:plan-phase {N}`". Exit workflow.
  - "Continue without UI-SPEC" -> Continue to step 6.
  - "Not a frontend phase" -> Continue to step 6.

**If `HAS_UI` is 1 (no frontend indicators):** Skip silently to step 6.
```

Key differences from old step 5.6:
1. Primary path reads `**Detected domain:**` from CONTEXT.md (no keyword grep)
2. Agentic domain check added (no-op stub for Phase 2's AGENT-SPEC)
3. Original keyword grep only runs as fallback when no CONTEXT.md domain field exists
4. The "Not a frontend phase" option only appears in fallback path (domain-aware path already knows the domain)
  </action>
  <verify>
Run these checks against the modified file:

```bash
# Domain-aware path exists
grep -c "Detected domain" get-shit-done/workflows/plan-phase.md

# CONTEXT_PATH grep pattern present
grep -c 'grep.*Detected domain.*CONTEXT_PATH' get-shit-done/workflows/plan-phase.md

# Agentic check stub present
grep -c "AGENT-SPEC" get-shit-done/workflows/plan-phase.md

# Fallback path preserved
grep -c "Fallback path" get-shit-done/workflows/plan-phase.md

# Original keyword grep still exists in fallback
grep -c "frontend indicators" get-shit-done/workflows/plan-phase.md

# Config check preserved
grep -c "UI_PHASE_CFG" get-shit-done/workflows/plan-phase.md
grep -c "UI_GATE_CFG" get-shit-done/workflows/plan-phase.md

# Step 6 reference preserved
grep -c "step 6" get-shit-done/workflows/plan-phase.md
```

All commands should return >= 1.
  </verify>
  <acceptance_criteria>
- `get-shit-done/workflows/plan-phase.md` step 5.6 contains `grep "^\*\*Detected domain:\*\*" "${CONTEXT_PATH}"` for primary domain detection
- Step 5.6 contains `DETECTED_DOMAIN` variable that is parsed from CONTEXT.md
- Step 5.6 checks `DETECTED_DOMAIN` contains "UI" for UI-SPEC gate
- Step 5.6 checks `DETECTED_DOMAIN` contains "Agentic" for AGENT-SPEC stub (with "Phase 2" comment)
- Step 5.6 contains `AGENT_SPEC_FILE=$(ls "${PHASE_DIR}"/*-AGENT-SPEC.md` check
- Step 5.6 contains fallback path with original `grep -iE "UI|interface|frontend|component|layout|page|screen|view|form|dashboard|widget"` behavior
- Fallback only triggers when `DETECTED_DOMAIN` is empty (no CONTEXT.md or missing field)
- Config check for `workflow.ui_phase` and `workflow.ui_safety_gate` is preserved at the top
- The heading `## 5.6. UI Design Contract Gate` is preserved
- Step 6 reference ("Skip to step 6" / "Continue to step 6") is preserved
- No other steps in plan-phase.md are modified
  </acceptance_criteria>
</task>
</tasks>

<verification>
After task completion:
1. `grep -c "Detected domain" get-shit-done/workflows/plan-phase.md` returns >= 2 (domain-aware path + CONTEXT.md grep)
2. `grep -c "AGENT-SPEC" get-shit-done/workflows/plan-phase.md` returns >= 1
3. `grep -c "Fallback path\|backward compat" get-shit-done/workflows/plan-phase.md` returns >= 1
4. `grep -c "frontend indicators" get-shit-done/workflows/plan-phase.md` returns >= 1 (fallback preserved)
5. Step 5.6 heading preserved: `grep "## 5.6" get-shit-done/workflows/plan-phase.md`
6. No other step headings (5.5, 6, 7, etc.) removed or reordered
</verification>

<success_criteria>
- plan-phase step 5.6 reads domain from CONTEXT.md when available (no keyword grep in primary path)
- Backward compatibility preserved: phases without CONTEXT.md still get the original keyword grep + yes/no gate
- Agentic domain stub exists as hook point for Phase 2
- No double-prompting: if discuss-phase already classified domain and user confirmed, plan-phase does not re-ask
- Config keys `workflow.ui_phase` and `workflow.ui_safety_gate` still respected
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-router/01-02-SUMMARY.md`
</output>
