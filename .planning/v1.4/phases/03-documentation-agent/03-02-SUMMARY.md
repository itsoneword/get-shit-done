---
phase: 03-documentation-agent
plan: 02
subsystem: commands
tags: [command-stub, gsd2, document]
requires:
  - commands/gsd2/map-codebase.md (reference pattern)
provides:
  - /gsd2:document command stub (source + runtime)
affects:
  - workflows/document.md (referenced but not created here — Plan 03 lands it)
  - install.js (propagates commands/gsd2/*.md to runtimes — no code change needed)
tech-stack:
  added: []
  patterns:
    - Dual-copy command stub (source in commands/gsd2/, runtime in .claude/commands/gsd2/)
    - Workflow delegation via <execution_context> @-reference
key-files:
  created:
    - commands/gsd2/document.md
    - .claude/commands/gsd2/document.md (gitignored runtime copy)
  modified: []
decisions:
  - "Runtime copy lives under .claude/ which is gitignored; commit only the source — install.js is the propagation mechanism"
  - "Rejection text for --phase N lives in <context> section, not argument-hint, so the hint stays a pure valid-flag list"
metrics:
  duration: "~1m"
  completed: "2026-04-17T20:37:40Z"
  tasks: 1
  files_created: 2
  files_modified: 0
---

# Phase 03 Plan 02: /gsd2:document Command Stub Summary

Created the `/gsd2:document` command entry point in both source (`commands/gsd2/document.md`) and runtime (`.claude/commands/gsd2/document.md`) trees — frontmatter declares the four locked flags (`--full`, `--subsystem <name>`, `--yes`, `--skip-docs`), lists the seven required tools (Read, Bash, Glob, Grep, Write, Edit, Task), and hands off to `workflows/document.md` (created by Plan 03) via absolute-path `<execution_context>` @-reference. The stub is self-consistent and deployable independently of Plan 03; it will fail cleanly (workflow missing) rather than with malformed-command errors until Plan 03 lands.

## What Shipped

- **Command stub** with full `<objective>`, `<execution_context>`, `<context>`, `<when_to_use>`, `<process>`, and `<success_criteria>` sections.
- **Explicit `--phase N` rejection** documented in `<context>` with the exact error message the workflow must emit — reinforces that the documentation agent is scoped by subsystem, not by phase.
- **Dual-copy invariant** maintained via `cp` (no drift between source and runtime).

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1    | Create /gsd2:document command stub in source and runtime locations | `b823b85` | `commands/gsd2/document.md` (+ runtime copy via cp) |

## Verification

All plan-specified checks passed:
- Both files exist and are byte-identical (`diff` empty)
- Frontmatter `name: gsd2:document` present exactly once
- `argument-hint` contains all four flags; `--phase` absent from the hint line
- All seven tools listed as `- Tool` under `allowed-tools:`
- `<execution_context>` points to the absolute runtime workflow path
- All six required section tags present
- `--phase N is not supported` rejection text present in `<context>`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `.claude/` is gitignored — adjusted commit strategy**
- **Found during:** Task 1 commit
- **Issue:** `git add .claude/commands/gsd2/document.md` was rejected because `.claude/` is in `.gitignore`. The plan implied committing both copies, but the runtime copy is install-artifact territory.
- **Fix:** Committed only the source (`commands/gsd2/document.md`); the runtime copy exists on disk (created via `cp`) and will be regenerated by `install.js` from the committed source. Dual-copy invariant is preserved at runtime; version control tracks only the source of truth.
- **Files modified:** commit contains `commands/gsd2/document.md` only
- **Commit:** `b823b85`

No other deviations — the plan executed as written.

## Follow-ups for Plan 03

Plan 03 must create `.claude/get-shit-done/workflows/document.md` for the `<execution_context>` @-reference to resolve. Running `/gsd2:document` before Plan 03 lands will fail with a missing-workflow error — that's expected and is the handoff mechanism.

## Self-Check: PASSED

- `commands/gsd2/document.md` — FOUND
- `.claude/commands/gsd2/document.md` — FOUND (gitignored, byte-identical to source)
- Commit `b823b85` — FOUND in `git log`
