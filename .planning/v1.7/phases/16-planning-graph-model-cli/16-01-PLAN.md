---
phase: 16-planning-graph-model-cli
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/roadmap.cjs
  - get-shit-done/bin/lib/graph.cjs
  - tests/roadmap.test.cjs
  - tests/graph.test.cjs
autonomous: true
requirements: [GRAPH-01, GRAPH-02]
sidecar_impact: []
must_haves:
  truths:
    - "gsd-tools roadmap analyze still emits the existing `depends_on` raw-string field unchanged for every phase (zero behavior change to existing callers)"
    - "gsd-tools roadmap analyze additionally emits a `depends_on_parsed` array of node-id strings (e.g. [\"phase:11\",\"phase:12\"]) per phase, empty array when the prose says Nothing/first phase"
    - "roadmap.cjs exports pure helpers `parsePhaseSections(content)` and `parseDependsOnPhaseRefs(rawDependsOn)`; graph.cjs requires and calls them directly instead of duplicating either regex (LOCKED reuse rule)"
    - "buildGraph(cwd) returns { nodes, edges } where nodes include every phase (type phase) and every plan (type plan) found under the current milestone's phases dir"
    - "buildGraph(cwd) edges include phase-to-phase depends_on (from ROADMAP, via roadmap.cjs's shared parser), plan-to-plan depends_on (from PLAN frontmatter depends_on, both cross-phase 'NN-NN' and bare same-phase 'NN' forms), and undirected plan-to-plan depends_on from files_modified overlap"
    - "npm test passes with zero regressions in any existing roadmap.cjs/parallel-gate.cjs/phase.cjs test file"
  artifacts:
    - get-shit-done/bin/lib/roadmap.cjs
    - get-shit-done/bin/lib/graph.cjs
    - tests/roadmap.test.cjs
    - tests/graph.test.cjs
  key_links:
    - "roadmap.cjs cmdRoadmapAnalyze depends_on_parsed array -> graph.cjs phase depends_on edge builder (broken = phase->phase depends_on edges never appear in the graph model)"
    - "PLAN frontmatter depends_on (bare 'NN' same-phase form) -> resolvePlanDepRef combining with the plan's own phase number (broken = same-phase plan dependency edges silently vanish, e.g. 04-02/04-03 depends_on: [01])"
    - "roadmap.cjs exported parsePhaseSections/parseDependsOnPhaseRefs -> graph.cjs require('./roadmap.cjs') call site (broken = graph.cjs re-implements/duplicates ROADMAP regex parsing, violating the LOCKED reuse rule and risking silent drift between the two copies)"
---

<objective>
Build the foundation half of Phase 16: (1) `roadmap.cjs` parses phase `**Depends on**:` prose into a structured, node-id-form array (GRAPH-01) additively — no existing field is removed or changed; and (2) a new pure module `get-shit-done/bin/lib/graph.cjs` builds the first slice of the normalized `{nodes, edges}` planning-graph model (GRAPH-02, part A): phase nodes, plan nodes, phase-to-phase `depends_on` edges (fed by the new parsed array), plan-to-plan `depends_on` edges (from PLAN frontmatter `depends_on`), and undirected plan-to-plan `depends_on` edges from `files_modified` overlap. This is read-only, additive-only work — no existing consumer of `roadmap.cjs` changes behavior. Plan 16-02 completes the model (SUMMARY requires/provides/affects, PLAN key_links, requirement traceability, todo edges) and wires the CLI.

Why: `templates/summary.md` promises transitive-closure context selection and nothing computes it; waves are guessed, not derived; cycle detection is an LLM eyeball instruction. This plan lays the parsing/node/edge groundwork those later phases (17-19) need, without touching any execution behavior yet.
</objective>

<execution_context>
@/home/cleversol/gsd2/mine/.claude/get-shit-done/workflows/execute-plan.md
@/home/cleversol/gsd2/mine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.7/phases/16-planning-graph-model-cli/16-CONTEXT.md
@.planning/todos/pending/2026-07-04-add-graph-cjs-planning-graph-layer-normalizing-existing-edges.md
@get-shit-done/bin/lib/roadmap.cjs
@get-shit-done/bin/lib/frontmatter.cjs
@get-shit-done/bin/lib/core.cjs
@tests/helpers.cjs
@tests/roadmap.test.cjs

<interfaces>
## GRAPH-01 — roadmap.cjs change (additive only, EXPORTED for reuse by graph.cjs)

Location: `get-shit-done/bin/lib/roadmap.cjs`. This plan extracts TWO pure, exported helpers so `graph.cjs` never re-implements ROADMAP parsing (LOCKED CONTEXT.md: "REUSE existing readers; do NOT re-implement parsing" — nothing keeps a hand-copied regex in sync with the original, so there must be exactly one copy).

### `parsePhaseSections(content)` — extracted phase-heading + depends_on-prose scan
Refactor `cmdRoadmapAnalyze`'s existing inline phase-heading loop (the `phasePattern` match, section-slice, and `dependsMatch` logic already there, around the existing line):
```js
const dependsMatch = section.match(/\*\*Depends on(?::\*\*|\*\*:)\s*([^\n]+)/i);
const depends_on = dependsMatch ? dependsMatch[1].trim() : null;
```
into a standalone pure function that BOTH `cmdRoadmapAnalyze` and `graph.cjs` call:
```js
function parsePhaseSections(content) {
  const phasePattern = /#{2,4}\s*Phase\s+(\d+[A-Z]?(?:\.\d+)*)\s*:\s*([^\n]+)/gi;
  const sections = [];
  let match;
  while ((match = phasePattern.exec(content)) !== null) {
    const phaseNum = match[1];
    const phaseName = match[2].replace(/\(INSERTED\)/i, '').trim();
    const sectionStart = match.index;
    const restOfContent = content.slice(sectionStart);
    const nextHeader = restOfContent.match(/\n#{2,4}\s+Phase\s+\d/i);
    const sectionEnd = nextHeader ? sectionStart + nextHeader.index : content.length;
    const section = content.slice(sectionStart, sectionEnd);
    const dependsMatch = section.match(/\*\*Depends on(?::\*\*|\*\*:)\s*([^\n]+)/i);
    const depends_on = dependsMatch ? dependsMatch[1].trim() : null;
    sections.push({ number: phaseNum, name: phaseName, section, depends_on });
  }
  return sections;
}
```
`cmdRoadmapAnalyze` calls `parsePhaseSections(content)` ONCE and iterates its result for the goal/sequence_after/disk-status/checkbox/`depends_on_parsed` enrichment it already does per phase — this is a pure refactor of existing inline logic; same fields, same values, same order, zero behavior change.

### `parseDependsOnPhaseRefs(rawDependsOn)` — depends_on prose -> node-id array
```js
function parseDependsOnPhaseRefs(rawDependsOn) {
  if (!rawDependsOn) return [];
  const matches = [...rawDependsOn.matchAll(/Phase\s+(\d+(\.\d+)?)/g)];
  return matches.map(m => `phase:${m[1]}`);
}
```
This is the exact `/Phase\s+(\d+(\.\d+)?)/g` pattern already used by `overnight.md` (workflows/overnight.md:143) — reuse it verbatim, do not invent a new pattern. "Nothing (first phase)", "Nothing (first v1.7 phase)", null, or any string with zero "Phase N" matches → `[]`.

Example: `"Phase 11 (ledger.cjs and mailbox.cjs must exist...); Phase 12 (mailbox must exist...)"` → `["phase:11", "phase:12"]`.

`cmdRoadmapAnalyze` adds `depends_on_parsed: parseDependsOnPhaseRefs(section.depends_on)` to each `phases.push({...})` object, alongside the existing (unchanged) `depends_on` field.

**BOTH `parsePhaseSections` and `parseDependsOnPhaseRefs` MUST be added to `roadmap.cjs`'s `module.exports`** (alongside the existing `cmdRoadmapGetPhase`, `cmdRoadmapAnalyze`, `cmdRoadmapUpdatePlanProgress`) — `graph.cjs` `require`s and calls them directly; it never duplicates either regex.

## GRAPH-02 (Part A) — graph.cjs node/edge model, this plan's slice

File: `get-shit-done/bin/lib/graph.cjs` (new, CommonJS, `'use strict'`). Pure module — no network, no process.exit in any exported function except the `cmd*` wrappers added in Plan 16-02 (this plan does NOT add CLI dispatch — that's 16-02 Task 2).

Exports this plan must add:
- `buildGraph(cwd)` → `{ nodes: Node[], edges: Edge[] }`
- `refToNodeId(str)` — pure helper, exported for direct unit testing
- `resolvePlanDepRef(item, currentPhaseNum)` — pure helper, exported for direct unit testing

### Node shape
`{ id, type }` only — no extra descriptive fields (keeps dedup/equality trivial). `type` ∈ `phase | plan` for this plan's slice (`requirement | todo | artifact` land in Plan 16-02).

Node id conventions (LOCKED, from 16-CONTEXT.md):
- `phase:<N>` — N is the phase number string as it appears in ROADMAP (e.g. `phase:16`, `phase:72.1`)
- `plan:<N>-<PP>` — N is the phase number, PP is the two-digit plan number (e.g. `plan:16-01`)

### Edge shape
`{ from, to, type, source }` — `type` ∈ `depends_on | provides | affects | satisfies | wires`. `source` names the encoding the edge came from (debuggability), always one of the exact strings used below.

### refToNodeId(str) — phase/plan reference resolver
Resolves a free-form string that MAY start with a phase or plan reference into a node id, or `null` if unresolvable. Used for `Phase N` prose and bare `NN-NN`/`NN-NN-slug` forms (Plan 16-02 reuses this for SUMMARY requires/affects and the REQUIREMENTS.md traceability table). **Not used for todo references** — todo slugs are date-prefixed and this pattern misparses them (see Plan 16-02's Source 4).
```js
function refToNodeId(str) {
  if (typeof str !== 'string') return null;
  const m = str.trim().match(/^(?:Phase\s+)?(\d+[A-Z]?(?:\.\d+)*)(?:-(\d{2}))?/i);
  if (!m) return null;
  return m[2] ? `plan:${m[1]}-${m[2]}` : `phase:${m[1]}`;
}
```
Test cases (must pass): `refToNodeId("Phase 16")` → `"phase:16"`; `refToNodeId("05-01")` → `"plan:05-01"`; `refToNodeId("07-05-parallel-safety-gate")` → `"plan:07-05"`; `refToNodeId("existing")` → `null`; `refToNodeId("add-backlog")` → `null`; `refToNodeId("future agentic phases")` → `null`.

### resolvePlanDepRef(item, currentPhaseNum) — PLAN depends_on resolver
PLAN frontmatter `depends_on` arrays contain EITHER a cross-phase `"NN-NN"` string OR a bare same-phase `"NN"` string (real example: `.planning/v1.5/phases/04-agent-observability-telemetry/04-02-PLAN.md` has `depends_on: [01]`, meaning "this phase's own plan 01", i.e. `plan:04-01`).
```js
function resolvePlanDepRef(item, currentPhaseNum) {
  if (typeof item !== 'string' && typeof item !== 'number') return null;
  const s = String(item).trim();
  const full = s.match(/^(\d+[A-Z]?(?:\.\d+)*)-(\d+)$/);
  if (full) return `plan:${full[1]}-${full[2].padStart(2, '0')}`;
  const bare = s.match(/^(\d+)$/);
  if (bare) return `plan:${currentPhaseNum}-${bare[1].padStart(2, '0')}`;
  return null;
}
```
Test cases: `resolvePlanDepRef("05-01", "05")` → `"plan:05-01"`; `resolvePlanDepRef("01", "04")` → `"plan:04-01"`; `resolvePlanDepRef(1, "04")` → `"plan:04-01"` (numeric frontmatter values are legal YAML, extractFrontmatter may hand back a string already — handle both).

### buildGraph(cwd) — this plan's slice
1. Read `planningPaths(cwd).roadmap`, run its content through `extractCurrentMilestone` (same as roadmap.cjs does) to scope to the active milestone, then call `require('./roadmap.cjs').parsePhaseSections(content)` — do NOT re-implement the phase-heading regex. Add one `phase:<N>` node per section returned.
2. Read every `*-PLAN.md` under `phasesDir(cwd)` (from `core.cjs`, via `planningPaths(cwd).phases` or `require('./core.cjs').phasesDir`). For each PLAN.md: `extractFrontmatter` (from `frontmatter.cjs`) gives `phase` (slug like `16-planning-graph-model-cli`), `plan` (e.g. `"01"`), `depends_on` (array), `files_modified` (array). Extract the phase number from the `phase` slug via `/^(\d+[A-Z]?(?:\.\d+)*)/`. Add one `plan:<N>-<PP>` node per PLAN.md (PP = `String(fm.plan).padStart(2,'0')`).
3. Phase depends_on edges: for each section returned by `parsePhaseSections`, call `require('./roadmap.cjs').parseDependsOnPhaseRefs(section.depends_on)` (the SAME exported helper `roadmap.cjs` uses internally for `depends_on_parsed` — do not duplicate the regex), and for each returned id add edge `{ from: 'phase:<this>', to: '<parsed id>', type: 'depends_on', source: 'roadmap_depends_on' }`.
4. Plan depends_on edges: for each PLAN.md, for each item in `fm.depends_on`, `target = resolvePlanDepRef(item, thisPlanPhaseNum)`; if non-null, add edge `{ from: 'plan:<N>-<PP>', to: target, type: 'depends_on', source: 'plan_depends_on' }`.
5. files_modified overlap edges: for every unordered pair of distinct plan nodes (P1, P2) whose `files_modified` arrays share at least one path, add ONE edge (order the pair by ascending string comparison of their ids so `P1.id < P2.id`, preventing a mirrored duplicate): `{ from: P1.id, to: P2.id, type: 'depends_on', source: 'files_modified' }`.

### Node/edge dedup + determinism (apply now, both plans reuse)
- Nodes: keyed by `id` in a `Map`; if an id is added twice, keep the first (no error, no re-add).
- Edges: keyed by the composite string `` `${from}|${to}|${type}|${source}` `` in a `Set`; exact duplicates are dropped, but the SAME `{from,to,type}` triple from a DIFFERENT `source` is kept (provenance matters).
- Final output arrays are SORTED for determinism: `nodes` ascending by `id` (plain string `<`); `edges` ascending by `from`, then `to`, then `type`, then `source`.

### Reuse note (do NOT re-implement parsing you don't have to)
- Use `roadmap.cjs`'s exported `parsePhaseSections(content)` and `parseDependsOnPhaseRefs(rawDependsOn)` for ALL phase-heading and depends_on-prose parsing — `graph.cjs` MUST `require('./roadmap.cjs')` and call these directly. Do NOT copy the phase-heading regex or the `**Depends on**:` regex into `graph.cjs` — there must be no duplicated `Depends on` regex literal in graph.cjs (grep-verifiable).
- Use `frontmatter.cjs`'s `extractFrontmatter` for all PLAN.md reads — do not hand-roll YAML parsing.
- Use `core.cjs`'s `phasesDir(cwd)` (or `planningPaths(cwd).phases`) to locate the phase directory list — do not hand-roll milestone-partition detection.
- Scope: this plan's `buildGraph` operates on the CURRENT milestone only (whatever `phasesDir(cwd)` / `extractCurrentMilestone` resolve to) — archived milestones are out of scope for Phase 16 (see `## ASSUMPTIONS`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for roadmap.cjs depends_on_parsed/exports and graph.cjs buildGraph (Part A)</name>
  <files>tests/roadmap.test.cjs, tests/graph.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/roadmap.cjs (cmdRoadmapAnalyze, the dependsMatch regex at ~line 125, and module.exports at the bottom)
    - get-shit-done/workflows/overnight.md:143 (the exact `/Phase\s+(\d+(\.\d+)?)/g` pattern being reused)
    - get-shit-done/bin/lib/frontmatter.cjs (extractFrontmatter — the parser graph.cjs will call)
    - get-shit-done/bin/lib/core.cjs (phasesDir, planningPaths — locate phase dirs)
    - tests/roadmap.test.cjs (existing describe/test structure to extend, not replace)
    - tests/helpers.cjs (createTempProject, cleanup, runGsdTools)
  </read_first>
  <action>
Add tests to `tests/roadmap.test.cjs` (extend the existing `describe('roadmap analyze command', ...)` block or add a new sibling describe — do not delete existing tests):
- ROADMAP.md with a phase whose `**Depends on**:` reads `"Phase 11 (ledger.cjs and mailbox.cjs must exist before the evaluator can write verdicts); Phase 12 (mailbox must exist to receive unresolved positions)"` → `roadmap analyze --raw` JSON's matching phase entry has BOTH `depends_on` (unchanged raw string, non-null) AND `depends_on_parsed: ["phase:11", "phase:12"]`.
- A phase whose `**Depends on**:` reads `"Nothing (first phase)"` → `depends_on_parsed: []`.
- A phase with no `**Depends on**:` line at all → `depends_on` is `null` AND `depends_on_parsed` is `[]` (not undefined, not missing).
- A phase whose depends_on mentions a decimal phase (`"Phase 72.1"`) → `depends_on_parsed` includes `"phase:72.1"`.
- `require('../get-shit-done/bin/lib/roadmap.cjs')` exposes `parsePhaseSections` and `parseDependsOnPhaseRefs` as functions (`typeof === 'function'`) — this is the RED assertion driving Task 2's export refactor.
- `parsePhaseSections(content)` called directly on a small two-phase-heading fixture string returns an array of `{number, name, section, depends_on}` objects, one per heading, with `depends_on` populated only for the phase whose section actually has a `**Depends on**:` line.
- Assert the EXISTING tests in this file still pass unmodified (regression guard — do not weaken any existing assertion).

Create `tests/graph.test.cjs` (new file, modeled on `tests/roadmap.test.cjs` structure: `node:test`, `require('./helpers.cjs')`, `createTempProject`/`cleanup` in `beforeEach`/`afterEach`). Import directly from the library (unit-test the pure functions, do not shell out via `runGsdTools` for this plan — there is no CLI dispatch yet, that lands in Plan 16-02):
```js
const { buildGraph, refToNodeId, resolvePlanDepRef } = require('../get-shit-done/bin/lib/graph.cjs');
```
Cover, as FAILING tests (graph.cjs does not exist yet):

`refToNodeId`:
- `refToNodeId("Phase 16")` → `"phase:16"`
- `refToNodeId("05-01")` → `"plan:05-01"`
- `refToNodeId("07-05-parallel-safety-gate")` → `"plan:07-05"`
- `refToNodeId("existing")` → `null`
- `refToNodeId("future agentic phases")` → `null`

`resolvePlanDepRef`:
- `resolvePlanDepRef("05-01", "05")` → `"plan:05-01"`
- `resolvePlanDepRef("01", "04")` → `"plan:04-01"`
- `resolvePlanDepRef("bogus", "04")` → `null`

`buildGraph(cwd)` against a synthetic `.planning/` tree built with `createTempProject()` + `fs.writeFileSync`:
- Write a `ROADMAP.md` with two phase sections: `### Phase 1: Foo` (no Depends on) and `### Phase 2: Bar` (`**Depends on**: Phase 1`).
- Write `.planning/phases/01-foo/01-01-PLAN.md` with frontmatter `phase: 01-foo`, `plan: "01"`, `depends_on: []`, `files_modified: [a.cjs, b.cjs]`.
- Write `.planning/phases/02-bar/02-01-PLAN.md` with frontmatter `phase: 02-bar`, `plan: "01"`, `depends_on: ["01-01"]`, `files_modified: [b.cjs, c.cjs]` (note: shares `b.cjs` with 01-01 — must produce a files_modified edge).
- Write `.planning/phases/02-bar/02-02-PLAN.md` with frontmatter `phase: 02-bar`, `plan: "02"`, `depends_on: [01]` (bare same-phase form — must resolve to `plan:02-01`), `files_modified: [d.cjs]`.
- Assert `nodes` contains `{id:'phase:1',type:'phase'}`, `{id:'phase:2',type:'phase'}`, `{id:'plan:01-01',type:'plan'}`, `{id:'plan:02-01',type:'plan'}`, `{id:'plan:02-02',type:'plan'}`.
- Assert `edges` contains `{from:'phase:2',to:'phase:1',type:'depends_on',source:'roadmap_depends_on'}`.
- Assert `edges` contains `{from:'plan:02-01',to:'plan:01-01',type:'depends_on',source:'plan_depends_on'}`.
- Assert `edges` contains `{from:'plan:02-02',to:'plan:02-01',type:'depends_on',source:'plan_depends_on'}` (bare-form resolution).
- Assert `edges` contains exactly one edge with `source:'files_modified'` between `plan:01-01` and `plan:02-01` (ordered so `from < to` lexicographically), and NOT a second mirrored copy.
- Assert `nodes` is sorted ascending by `id` and `edges` is sorted by `(from,to,type,source)` (determinism check — e.g. compare `JSON.stringify` of two `buildGraph` calls on the same fixture are byte-identical).

Do NOT implement `graph.cjs` in this task — the require must fail (module not found) or the tests must fail on missing exports.
  </action>
  <verify>node --test tests/roadmap.test.cjs 2>&1 | tail -10; node --test tests/graph.test.cjs 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - File tests/graph.test.cjs exists
    - `grep -c "depends_on_parsed" tests/roadmap.test.cjs` returns >= 3
    - `grep -q "parsePhaseSections" tests/roadmap.test.cjs` and `grep -q "parseDependsOnPhaseRefs" tests/roadmap.test.cjs` (both true — the export contract is tested directly, not only through cmdRoadmapAnalyze output)
    - tests/graph.test.cjs contains "refToNodeId", "resolvePlanDepRef", "buildGraph"
    - tests/graph.test.cjs contains "files_modified" and "plan_depends_on" and "roadmap_depends_on"
    - Running `node --test tests/graph.test.cjs` FAILS (RED — Cannot find module '../get-shit-done/bin/lib/graph.cjs' or assertion failures, not a syntax error)
    - Running `node --test tests/roadmap.test.cjs` FAILS on the new depends_on_parsed/export assertions (RED) while any pre-existing assertions in the file are unaffected by the failure mode (no syntax errors)
  </acceptance_criteria>
  <done>tests/graph.test.cjs and the extended tests/roadmap.test.cjs cover GRAPH-01 (including the roadmap.cjs export contract) and buildGraph Part A behavior and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement roadmap.cjs depends_on_parsed/exports and graph.cjs buildGraph Part A (GREEN)</name>
  <files>get-shit-done/bin/lib/roadmap.cjs, get-shit-done/bin/lib/graph.cjs</files>
  <read_first>
    - tests/roadmap.test.cjs (the RED contract just written — implement to pass)
    - tests/graph.test.cjs (the RED contract just written — implement to pass)
    - get-shit-done/bin/lib/roadmap.cjs (cmdRoadmapAnalyze — exact insertion point, and module.exports at the bottom)
    - get-shit-done/bin/lib/frontmatter.cjs (extractFrontmatter signature/return shape)
    - get-shit-done/bin/lib/core.cjs (phasesDir(cwd), planningPaths(cwd) signatures)
  </read_first>
  <action>
1. In `get-shit-done/bin/lib/roadmap.cjs`:
   - Extract `parsePhaseSections(content)` as a standalone pure function (exact code in `<interfaces>` above) by refactoring `cmdRoadmapAnalyze`'s inline phase-heading/section-slice/`dependsMatch` loop to call it instead — same fields, same values, same order, zero behavior change to `cmdRoadmapAnalyze`'s output.
   - Add `parseDependsOnPhaseRefs(rawDependsOn)` (exact code in `<interfaces>` above).
   - In `cmdRoadmapAnalyze`, call `parsePhaseSections(content)` once and iterate its result; for each section add `depends_on_parsed: parseDependsOnPhaseRefs(section.depends_on)` alongside the existing (untouched) `depends_on` field on the `phases.push({...})` object.
   - Add `parsePhaseSections` and `parseDependsOnPhaseRefs` to `roadmap.cjs`'s `module.exports` (alongside the existing `cmdRoadmapGetPhase`, `cmdRoadmapAnalyze`, `cmdRoadmapUpdatePlanProgress`). Do not rename, remove, or alter the existing `depends_on` field or any other field in the pushed object.

2. Create `get-shit-done/bin/lib/graph.cjs`:
```js
'use strict';
const fs = require('fs');
const path = require('path');
const { extractFrontmatter } = require('./frontmatter.cjs');
const { phasesDir, planningPaths, extractCurrentMilestone } = require('./core.cjs');
const { parsePhaseSections, parseDependsOnPhaseRefs } = require('./roadmap.cjs');
```
Implement `refToNodeId`, `resolvePlanDepRef` exactly per the `<interfaces>` block. Implement `buildGraph(cwd)`:
- Read `planningPaths(cwd).roadmap`, run its content through `extractCurrentMilestone` (same as roadmap.cjs does) to scope to the active milestone, then call `parsePhaseSections(content)` — do NOT re-implement the phase-heading regex.
- For each section returned: add a `phase:<N>` node; call `parseDependsOnPhaseRefs(section.depends_on)` (the SAME exported helper `roadmap.cjs` uses for `depends_on_parsed` — do not duplicate the regex) and add a `roadmap_depends_on` edge per returned id.
- Enumerate PLAN.md files under `phasesDir(cwd)` (read each phase subdirectory, filter files ending `-PLAN.md` or exactly `PLAN.md`), `extractFrontmatter` each, derive the plan's own node id from `fm.phase` (leading number) + `fm.plan` (zero-padded), and build plan nodes + `plan_depends_on` edges (via `resolvePlanDepRef`) + `files_modified` overlap edges (pairwise, per the `<interfaces>` spec — collect `{id, files: Set}` per plan first, then do one O(n²) pairwise comparison over the plan list, which is fine at this scale).
- Apply the node/edge dedup + sort rules from `<interfaces>` before returning `{ nodes, edges }`.

Export `{ buildGraph, refToNodeId, resolvePlanDepRef }`. This plan does NOT export any `cmd*` CLI handler yet (Plan 16-02 adds `cmdGraphAnalyze`/`cmdGraphExport`) and does NOT touch `gsd-tools.cjs`.
  </action>
  <verify>node --test tests/roadmap.test.cjs 2>&1 | tail -10; node --test tests/graph.test.cjs 2>&1 | tail -15; npm test 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - `node -e "require('./get-shit-done/bin/lib/graph.cjs')"` exits 0
    - `grep -q "depends_on_parsed" get-shit-done/bin/lib/roadmap.cjs` (field added)
    - `grep -q "depends_on:" get-shit-done/bin/lib/roadmap.cjs` (existing field untouched)
    - `grep -q "parsePhaseSections\|parseDependsOnPhaseRefs" get-shit-done/bin/lib/roadmap.cjs` in its `module.exports` block (both helpers exported)
    - `grep -q "require('./roadmap" get-shit-done/bin/lib/graph.cjs` (graph.cjs requires roadmap.cjs's shared helpers, not a duplicate implementation)
    - `grep -qE "parsePhaseSections|parseDependsOnPhaseRefs" get-shit-done/bin/lib/graph.cjs` (graph.cjs calls the shared helper(s))
    - `! grep -q "Depends on" get-shit-done/bin/lib/graph.cjs` (no duplicated `**Depends on**:` regex literal in graph.cjs — confirms zero-drift reuse per the LOCKED rule)
    - graph.cjs exports contain `buildGraph`, `refToNodeId`, `resolvePlanDepRef`: `node -e "const g=require('./get-shit-done/bin/lib/graph.cjs'); if(typeof g.buildGraph!=='function'||typeof g.refToNodeId!=='function'||typeof g.resolvePlanDepRef!=='function') process.exit(1)"` exits 0
    - `node --test tests/graph.test.cjs` exits 0 (GREEN)
    - `node --test tests/roadmap.test.cjs` exits 0 (GREEN, including pre-existing tests)
    - `npm test` exits 0 (full suite, zero regressions — GRAPH-01's zero-behavior-change invariant)
  </acceptance_criteria>
  <done>roadmap.cjs additively exposes depends_on_parsed and exports parsePhaseSections/parseDependsOnPhaseRefs; graph.cjs requires and calls those exports directly (no duplicated regex), producing phase/plan nodes and depends_on edges from all three Part-A sources (roadmap depends_on, plan depends_on, files_modified overlap); full test suite green.</done>
</task>

</tasks>

<verification>
- `npm test` exits 0 (full suite, including the new/extended tests/roadmap.test.cjs and tests/graph.test.cjs)
- `node -e "require('./get-shit-done/bin/lib/graph.cjs')"` loads without throwing
- Manual sanity: `node -e "const {buildGraph}=require('./get-shit-done/bin/lib/graph.cjs'); const g=buildGraph(process.cwd()); console.log(g.nodes.length, g.edges.length)"` run from the repo root prints two positive integers (this repo's own ROADMAP/PLAN tree produces a non-empty model)
</verification>

<success_criteria>
- GRAPH-01: `roadmap.cjs` exposes `depends_on_parsed` as a structured array additively (and exports `parsePhaseSections`/`parseDependsOnPhaseRefs` as reusable pure helpers); the raw `depends_on` string field is unchanged for every existing caller
- GRAPH-02 (Part A): `graph.cjs` `buildGraph` produces phase and plan nodes, and `depends_on` edges from phase depends_on prose (via roadmap.cjs's shared parser, not a duplicate), PLAN depends_on frontmatter (both cross-phase and bare same-phase forms), and files_modified overlap
- Zero behavior change: `npm test` passes with no regressions in any existing roadmap.cjs-dependent test
</success_criteria>

<output>
After completion, create `.planning/v1.7/phases/16-planning-graph-model-cli/16-01-SUMMARY.md`
</output>

## ASSUMPTIONS

Discretionary decisions made during planning (within 16-CONTEXT.md's "Claude's Discretion" grant — none override a LOCKED decision):

1. **`wave` frontmatter is descriptive-only in Phase 16**: PLAN frontmatter's `wave: N` field is NOT converted into a graph edge or a node property in this phase. Only `depends_on` (PLAN frontmatter array + ROADMAP phase prose) produces `depends_on` edges. This is LOCKED-consistent: the CONTEXT.md edge-mapping table lists "Plan `depends_on` + `wave:N` frontmatter" as the row's source description, but the produced edge is `plan→plan depends_on` — `wave` is context for the reader, not a second edge source. ROADMAP/REQUIREMENTS.md phrase the source as "plan depends_on/wave," but only `depends_on` is consumed; there is no wave-derived edge and no `wave` field on any node. A future phase can derive wave-implied ordering edges if that's ever needed (see Phase 19's computed-waves goal in 16-CONTEXT.md's Deferred Ideas).
